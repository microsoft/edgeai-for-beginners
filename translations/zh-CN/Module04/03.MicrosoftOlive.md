# 第3节：Microsoft Olive 优化套件

## 目录
1. [简介](#简介)
2. [什么是 Microsoft Olive？](#什么是-microsoft-olive？)
3. [安装](#安装)
4. [快速入门指南](#快速入门指南)
5. [示例：将 Qwen3 转换为 ONNX INT4](#示例：将-qwen3-转换为-onnx-int4)
6. [高级用法](#高级用法)
7. [Olive 配方仓库](#olive-配方仓库)
8. [最佳实践](#最佳实践)
9. [故障排除](#故障排除)
10. [附加资源](#附加资源)

## 简介

Microsoft Olive 是一款强大且易于使用的硬件感知模型优化工具包，简化了在不同硬件平台上部署机器学习模型的优化过程。无论您是针对 CPU、GPU 还是专用 AI 加速器，Olive 都能帮助您在保持模型准确度的同时，实现最佳性能。

## 什么是 Microsoft Olive？

Olive 是一款易于使用的硬件感知模型优化工具，整合了行业领先的模型压缩、优化和编译技术。它与 ONNX Runtime 配合使用，作为端到端 (E2E) 推理优化解决方案。

### 主要功能

- <strong>硬件感知优化</strong>：自动选择针对目标硬件的最佳优化技术
- **40+ 内置优化组件**：涵盖模型压缩、量化、图优化等
- **简易 CLI 界面**：针对常见优化任务的简洁命令
- <strong>多框架支持</strong>：兼容 PyTorch、Hugging Face 模型和 ONNX
- <strong>流行模型支持</strong>：Olive 可开箱即用地自动优化如 Llama、Phi、Qwen、Gemma 等流行模型架构

### 优势

- <strong>缩短开发时间</strong>：无需手动尝试各种优化技术
- <strong>性能提升</strong>：显著加速（某些情况下可达 6 倍）
- <strong>跨平台部署</strong>：优化后的模型可在不同硬件和操作系统上运行
- <strong>保持准确度</strong>：优化在提升性能的同时保存模型质量

## 安装

### 先决条件

- Python 3.8 或更高版本
- pip 包管理器
- 推荐使用虚拟环境

### 基本安装

创建并激活虚拟环境：

```bash
# 创建虚拟环境
python -m venv olive-env

# 激活虚拟环境
# 在 Windows 上：
olive-env\Scripts\activate
# 在 macOS/Linux 上：
source olive-env/bin/activate
```

使用自动优化功能安装 Olive：

```bash
pip install olive-ai[auto-opt]
pip install transformers onnxruntime-genai
```

### 可选依赖

Olive 提供多个可选依赖以支持更多功能：

```bash
# 用于 Azure ML 集成
pip install olive-ai[azureml]

# 用于 DirectML（Windows GPU 加速）
pip install olive-ai[directml]

# 用于 CPU 优化
pip install olive-ai[cpu]

# 用于所有功能
pip install olive-ai[all]
```

### 验证安装

```bash
olive --help
```

如果成功，您将看到 Olive CLI 帮助信息。

## 快速入门指南

### 您的首次优化

让我们使用 Olive 的自动优化功能优化一个小型语言模型：

```bash
olive auto-opt \
  --model_name_or_path HuggingFaceTB/SmolLM2-135M-Instruct \
  --output_path models/smolm2-optimized \
  --device cpu \
  --provider CPUExecutionProvider \
  --use_ort_genai \
  --precision int4 \
  --log_level 1
```

### 此命令的作用

优化过程包括：从本地缓存获取模型，捕获 ONNX 图并将权重存储在 ONNX 数据文件中，优化 ONNX 图，使用 RTN 方法将模型量化为 int4。

### 命令参数说明

- `--model_name_or_path`：Hugging Face 模型标识符或本地路径
- `--output_path`：优化后模型保存目录
- `--device`：目标设备（cpu、gpu）
- `--provider`：执行提供程序（CPUExecutionProvider、CUDAExecutionProvider、DmlExecutionProvider）
- `--use_ort_genai`：使用 ONNX Runtime Generate AI 推理
- `--precision`：量化精度（int4、int8、fp16）
- `--log_level`：日志详细级别（0=最简、1=详细）

## 示例：将 Qwen3 转换为 ONNX INT4

基于 [lokinfey/Qwen3-8B-ONNX-INT4-CPU](https://huggingface.co/lokinfey/Qwen3-8B-ONNX-INT4-CPU) 提供的 Hugging Face 示例，以下是如何优化 Qwen3 模型：

### 步骤1：下载模型（可选）

为减少下载时间，仅缓存必要文件：

```bash
huggingface-cli download Qwen/Qwen2.5-0.5B-Instruct *.json *.safetensors *.txt
```

### 步骤2：优化 Qwen3 模型

```bash
olive auto-opt \
  --model_name_or_path Qwen/Qwen2.5-0.5B-Instruct \
  --output_path models/qwen3-onnx-int4 \
  --device cpu \
  --provider CPUExecutionProvider \
  --use_ort_genai \
  --precision int4 \
  --log_level 1
```

### 步骤3：测试优化后模型

创建一个简单的 Python 脚本来测试您的优化模型：

```python
import onnxruntime_genai as og

# 加载优化后的模型
model = og.Model('models/qwen3-onnx-int4')
tokenizer = og.Tokenizer(model)

# 创建聊天模板
chat_template = '<|im_start|>user\n{input}<|im_end|>\n<|im_start|>assistant\n'

# 生成文本
prompt = "What is machine learning?"
input_tokens = tokenizer.encode(chat_template.format(input=prompt))

params = og.GeneratorParams(model)
params.set_search_options(max_length=200)
params.input_ids = input_tokens

generator = og.Generator(model, params)

print("Generated response:")
while not generator.is_done():
    generator.compute_logits()
    generator.generate_next_token()
    
    new_token = generator.get_next_tokens()[0]
    print(tokenizer.decode([new_token]), end='', flush=True)

print()
```

### 输出结构

优化后，您的输出目录将包含：

```
models/qwen3-onnx-int4/
├── model.onnx              # Optimized ONNX model
├── model.onnx.data         # Model weights
├── genai_config.json       # Generation configuration
├── tokenizer.json          # Tokenizer files
├── tokenizer_config.json
└── special_tokens_map.json
```

## 高级用法

### 配置文件

对于更复杂的优化工作流，您可以使用 JSON 配置文件：

```json
{
  "input_model": {
    "type": "PyTorchModel",
    "config": {
      "hf_config": {
        "model_name": "Qwen/Qwen2.5-0.5B-Instruct",
        "task": "text-generation"
      }
    }
  },
  "systems": {
    "local_system": {
      "type": "LocalSystem",
      "config": {
        "accelerators": [
          {
            "device": "cpu",
            "execution_providers": ["CPUExecutionProvider"]
          }
        ]
      }
    }
  },
  "evaluators": {
    "common_evaluator": {
      "metrics": [
        {
          "name": "latency",
          "type": "latency",
          "sub_types": [{"name": "avg"}]
        }
      ]
    }
  },
  "passes": {
    "conversion": {
      "type": "ModelBuilder",
      "config": {
        "precision": "int4"
      }
    },
    "optimization": {
      "type": "OrtTransformersOptimization",
      "config": {
        "model_type": "gpt2"
      }
    }
  },
  "engine": {
    "search_strategy": {
      "execution_order": "joint",
      "search_algorithm": "tpe"
    },
    "evaluator": "common_evaluator",
    "host": "local_system",
    "target": "local_system",
    "cache_dir": "cache",
    "output_dir": "models/optimized"
  }
}
```

使用配置运行：

```bash
olive run --config config.json
```

### GPU 优化

针对 CUDA GPU 优化：

```bash
olive auto-opt \
  --model_name_or_path Qwen/Qwen2.5-0.5B-Instruct \
  --output_path models/qwen3-gpu-int4 \
  --device gpu \
  --provider CUDAExecutionProvider \
  --use_ort_genai \
  --precision int4 \
  --log_level 1
```

针对 DirectML（Windows）：

```bash
olive auto-opt \
  --model_name_or_path Qwen/Qwen2.5-0.5B-Instruct \
  --output_path models/qwen3-directml-int4 \
  --device gpu \
  --provider DmlExecutionProvider \
  --use_ort_genai \
  --precision int4 \
  --log_level 1
```

### 使用 Olive 微调

Olive 还支持模型微调：

```bash
olive finetune \
  --model_name_or_path meta-llama/Llama-3.2-1B-Instruct \
  --data_name microsoft/dolly-15k \
  --text_template "### Question: {instruction}\n### Answer: {response}" \
  --max_steps 100 \
  --output_path models/llama-finetuned
```

## 最佳实践

### 1. 模型选择
- 初期测试选择较小模型（如 0.5B-7B 参数量）
- 确保 Olive 支持您的目标模型架构

### 2. 硬件考虑
- 优化目标应与部署硬件匹配
- 如有 CUDA 兼容硬件，可使用 GPU 优化
- Windows集成显卡可考虑 DirectML

### 3. 精度选择
- **INT4**：极致压缩，轻微准确度损失
- **INT8**：大小和准确度的良好平衡
- **FP16**：准确度损失最小，适度缩减模型体积

### 4. 测试与验证
- 使用特定用例测试优化模型
- 对比性能指标（延迟、吞吐量、准确度）
- 使用有代表性的输入数据进行评估

### 5. 迭代优化
- 使用自动优化快速获得结果
- 使用配置文件实现细粒度控制
- 尝试不同的优化流程

## 故障排除

### 常见问题

#### 1. 安装问题
```bash
# 如果您遇到依赖冲突：
pip install --upgrade pip
pip install olive-ai[auto-opt] --force-reinstall
```

#### 2. CUDA/GPU 问题
```bash
# 验证CUDA安装：
nvidia-smi

# 安装正确的ONNX Runtime GPU包：
pip install onnxruntime-gpu
```

#### 3. 内存问题
- 优化时使用更小批次大小
- 尝试使用更高精度的量化（先用 int8 再试 int4）
- 确保有足够磁盘空间缓存模型

#### 4. 模型加载错误
- 验证模型路径和访问权限
- 检查模型是否需要 `trust_remote_code=True`
- 确保所有必需模型文件已下载完成

### 获取帮助

- <strong>文档</strong>：[microsoft.github.io/Olive](https://microsoft.github.io/Olive/)
- **GitHub 问题追踪**：[github.com/microsoft/Olive/issues](https://github.com/microsoft/Olive/issues)
- <strong>示例</strong>：[microsoft.github.io/Olive/examples.html](https://microsoft.github.io/Olive/examples.html)

## Olive 配方仓库

### Olive 配方介绍

[microsoft/olive-recipes](https://github.com/microsoft/olive-recipes) 仓库是主 Olive 工具包的补充，提供了丰富的针对热门 AI 模型的现成优化配方。该仓库作为实际参考，支持优化公开模型和构建专有模型优化工作流。

### 主要功能

- **100+ 预构建配方**：热门模型的现成优化配置
- <strong>多架构支持</strong>：涵盖变换器模型、视觉模型和多模态架构
- <strong>硬件特定优化</strong>：针对 CPU、GPU 和专用加速器量身定制
- <strong>流行模型家族</strong>：包括 Phi、Llama、Qwen、Gemma、Mistral 等

### 支持的模型家族

仓库包含以下模型的优化配方：

#### 语言模型
- **Microsoft Phi**：Phi-3-mini、Phi-3.5-mini、Phi-4-mini、Phi-4-reasoning
- **Meta Llama**：Llama-2-7b、Llama-3.1-8B、Llama-3.2-1B/3B
- **阿里巴巴 Qwen**：Qwen1.5-7B、Qwen2-7B、Qwen2.5 系列（0.5B 到 14B）
- **谷歌 Gemma**：各种 Gemma 模型配置
- **Mistral AI**：Mistral-7B 系列
- **DeepSeek**：R1-Distill 系列模型

#### 视觉与多模态模型
- **Stable Diffusion**：v1.4、XL-base-1.0
- **CLIP 模型**：多种 CLIP-ViT 配置
- **ResNet**：ResNet-50 优化
- <strong>视觉变换器</strong>：ViT-base-patch16-224

#### 专用模型
- **Whisper**：OpenAI Whisper-large-v3
- **BERT**：基础版及多语言版
- <strong>句子变换器</strong>：all-MiniLM-L6-v2

### 使用 Olive 配方

#### 方法1：克隆特定配方

```bash
# 克隆 recipes 仓库
git clone https://github.com/microsoft/olive-recipes.git
cd olive-recipes

# 导航到特定的模型配方
cd microsoft-Phi-4-mini-instruct

# 运行优化
olive run --config olive_config.json
```

#### 方法2：使用配方作为模板

```bash
# 复制您的模型的配方配置
cp olive-recipes/microsoft-Phi-3-mini-4k-instruct/olive_config.json ./my_config.json

# 根据您的需求修改配置
# 更新模型路径、优化参数等

# 使用您的自定义配置运行
olive run --config my_config.json
```

### 配方结构

每个配方目录通常包含：

```
model-name/
├── olive_config.json       # Main optimization configuration
├── requirements.txt        # Python dependencies
├── README.md              # Model-specific instructions
├── user_script.py         # Custom preprocessing/evaluation scripts
└── sample_data/           # Sample input data for testing
```

### 示例：使用 Phi-4-mini 配方

以 Phi-4-mini 配方为例：

```bash
# 克隆仓库
git clone https://github.com/microsoft/olive-recipes.git
cd olive-recipes/microsoft-Phi-4-mini-instruct

# 安装依赖
pip install -r requirements.txt

# 运行优化
olive run --config olive_config.json
```

配置文件通常包括：

```json
{
  "input_model": {
    "type": "PyTorchModel",
    "config": {
      "hf_config": {
        "model_name": "microsoft/Phi-4-mini-instruct",
        "task": "text-generation",
        "trust_remote_code": true
      }
    }
  },
  "systems": {
    "local_system": {
      "type": "LocalSystem",
      "config": {
        "accelerators": [
          {
            "device": "cpu",
            "execution_providers": ["CPUExecutionProvider"]
          }
        ]
      }
    }
  },
  "passes": {
    "convert": {
      "type": "ModelBuilder",
      "config": {
        "precision": "int4"
      }
    }
  }
}
```

### 自定义配方

#### 修改目标硬件

通过更新 `systems` 部分更改目标硬件：

```json
{
  "systems": {
    "gpu_system": {
      "type": "LocalSystem",
      "config": {
        "accelerators": [
          {
            "device": "gpu",
            "execution_providers": ["CUDAExecutionProvider"]
          }
        ]
      }
    }
  }
}
```

#### 调整优化参数

修改 `passes` 部分，实现不同优化级别：

```json
{
  "passes": {
    "convert": {
      "type": "ModelBuilder",
      "config": {
        "precision": "int8",           // Change from int4 to int8
        "use_ort_genai": true,
        "use_dynamo_exporter": true
      }
    },
    "optimize": {
      "type": "OrtTransformersOptimization",
      "config": {
        "optimization_level": "all"
      }
    }
  }
}
```

### 创建您自己的配方

1. <strong>从相似模型开始</strong>：寻找与您架构相近的配方
2. <strong>更新模型配置</strong>：更改配置中的模型名称/路径
3. <strong>调整参数</strong>：根据需要修改优化参数
4. <strong>测试与验证</strong>：运行优化并验证结果
5. <strong>贡献回社区</strong>：考虑将您的配方提交到仓库

### 使用配方的优势

#### 1. <strong>经过验证的配置</strong>
- 针对特定模型测试过的优化设置
- 避免寻找最佳参数的试错过程

#### 2. <strong>硬件特定调优</strong>
- 针对不同执行提供程序预优化
- CPU、GPU 和 NPU 目标的现成配置

#### 3. <strong>全面覆盖</strong>
- 支持最流行的开源模型
- 随新模型发布保持定期更新

#### 4. <strong>社区贡献</strong>
- 与 AI 社区协同开发
- 共享知识和最佳实践

### 贡献 Olive 配方

如果您优化了仓库中未覆盖的模型：

1. **Fork 仓库**：创建您自己的 olive-recipes fork
2. <strong>创建配方目录</strong>：为您的模型添加新目录
3. <strong>包含配置</strong>：添加 olive_config.json 及相关文件
4. <strong>文档说明</strong>：提供清晰的 README 和使用说明
5. **提交 Pull Request**：贡献回社区

### 性能基准

许多配方包含性能基准，显示：
- <strong>延迟改善</strong>：相对于基线通常提升 2-6 倍速度
- <strong>内存减少</strong>：量化后内存使用降低 50-75%
- <strong>准确度保持</strong>：保留 95-99% 准确率

### 与 AI 工具包集成

配方可无缝配合以下工具使用：
- **VS Code AI Toolkit**：直接集成模型优化
- **Azure 机器学习**：云端优化工作流
- **ONNX Runtime**：优化推理部署

## 附加资源

### 官方链接
- **GitHub 仓库**：[github.com/microsoft/Olive](https://github.com/microsoft/Olive)
- **Olive 配方仓库**：[github.com/microsoft/olive-recipes](https://github.com/microsoft/olive-recipes)
- **ONNX Runtime 文档**：[onnxruntime.ai/docs/performance/olive.html](https://onnxruntime.ai/docs/performance/olive.html)
- **Hugging Face 示例**：[huggingface.co/lokinfey/Qwen3-8B-ONNX-INT4-CPU](https://huggingface.co/lokinfey/Qwen3-8B-ONNX-INT4-CPU)

### 社区示例
- **Jupyter 笔记本**：Olive GitHub 仓库中提供 — https://github.com/microsoft/Olive/tree/main/examples
- **VS Code 扩展**：VS Code 的 AI Toolkit 概述 — https://learn.microsoft.com/azure/ai-toolkit/overview
- <strong>博客文章</strong>：Microsoft 开源博客 — https://opensource.microsoft.com/blog/

### 相关工具
- **ONNX Runtime**：高性能推理引擎 — https://onnxruntime.ai/
- **Hugging Face Transformers**：众多兼容模型来源 — https://huggingface.co/docs/transformers/index
- **Azure 机器学习**：云端优化工作流 — https://learn.microsoft.com/azure/machine-learning/


## ➡️ 接下来的内容

- [04: OpenVINO 工具包优化套件](./04.openvino.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**免责声明**：
本文件由 AI 翻译服务 [Co-op Translator](https://github.com/Azure/co-op-translator) 翻译完成。尽管我们力求准确，但请注意，自动翻译可能包含错误或不准确之处。原始语言版文件应视为权威来源。对于重要信息，建议使用专业人工翻译。我们对因使用本翻译而产生的任何误解或误释不承担责任。
<!-- CO-OP TRANSLATOR DISCLAIMER END -->