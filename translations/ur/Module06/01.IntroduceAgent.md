# AI ایجنٹس اور چھوٹے زبان کے ماڈلز: ایک جامع رہنمائی

## تعارف

اس ٹیوٹوریل میں، ہم AI ایجنٹس اور چھوٹے زبان کے ماڈلز (SLMs) اور ان کی جدید نفاذ کی حکمت عملیوں کو ایج کمپیوٹنگ ماحول کے لیے دریافت کریں گے۔ ہم ایجنٹک AI کے بنیادی تصورات، SLM کے اصلاحی تکنیک، وسائل محدود آلات کے لیے عملی تعیناتی کی حکمت عملی اور Microsoft Agent Framework کے ذریعے پروڈکشن-ریڈی ایجنٹ سسٹمز بنانے پر بات کریں گے۔

مصنوعی ذہانت کا منظرنامہ 2025 میں ایک بنیادی تبدیلی کا سامنا کر رہا ہے۔ جہاں 2023 چیٹ بوٹس کا سال تھا اور 2024 میں کوپائلٹس کا عروج آیا، 2025 کا سال AI ایجنٹس کا ہے — ذہین نظام جو سوچتے، تجزیہ کرتے، منصوبہ بندی کرتے، اوزار استعمال کرتے اور کم از کم انسانی مداخلت کے ساتھ کام انجام دیتے ہیں، جو بڑھتے ہوئے مؤثر چھوٹے زبان کے ماڈلز سے طاقت حاصل کرتے ہیں۔ Microsoft Agent Framework ایسے ذہین نظاموں کو آف لائن ایج پر مبنی صلاحیتیں فراہم کرنے والا ایک نمایاں حل کے طور پر ابھرتا ہے۔

## سیکھنے کے مقاصد

اس ٹیوٹوریل کے اختتام تک، آپ قادر ہوں گے:

- 🤖 AI ایجنٹس اور ایجنٹک سسٹمز کے بنیادی تصورات کو سمجھنا
- 🔬 ایجنٹک ایپلیکیشنز میں چھوٹے زبان کے ماڈلز کے بڑے زبان کے ماڈلز پر فوائد کی شناخت
- 🚀 ایج کمپیوٹنگ کے ماحول کے لیے جدید SLM تعیناتی کی حکمت عملی سیکھنا
- 📱 حقیقی دنیا کی ایپلیکیشنز کے لیے عملی SLM پر مبنی ایجنٹس کا نفاذ
- 🏗️ Microsoft Agent Framework کے استعمال سے پروڈکشن-ریڈی ایجنٹس کی تعمیر
- 🌐 مقامی LLM اور SLM انضمام کے ساتھ آف لائن ایج-بیسڈ ایجنٹس کی تعیناتی
- 🔧 ایج تعیناتی کے لیے Microsoft Agent Framework کو Foundry Local کے ساتھ انٹیگریٹ کرنا

## AI ایجنٹس کو سمجھنا: بنیادیں اور درجہ بندی

### تعریف اور بنیادی تصورات

مصنوعی ذہانت (AI) ایجنٹ سے مراد ایک ایسا نظام یا پروگرام ہے جو صارف یا کسی دوسرے نظام کی جانب سے خود مختاری سے کام انجام دینے کے قابل ہوتا ہے، جس کا اپنا ورک فلو ڈیزائن کرتا ہے اور دستیاب آلات کا استعمال کرتا ہے۔ روایتی AI جو صرف آپ کے سوالات کے جواب دیتا ہے کے برخلاف، ایک ایجنٹ خود مختار طور پر مقصد حاصل کرنے کے لیے کام کر سکتا ہے۔

### ایجنٹ کی درجہ بندی کا فریم ورک

ایجنٹ کی حدود کو سمجھنا مختلف کمپیوٹنگ منظرناموں کے لیے مناسب ایجنٹ کی قسم منتخب کرنے میں مدد دیتا ہے:

- **🔬 سادہ ردعمل ایجنٹس**: اصول پر مبنی نظام جو فوری مشاہدات پر ردعمل دیتے ہیں (تھرماسٹیٹس، بنیادی آٹومیشن)
- **📱 ماڈل-بیسڈ ایجنٹس**: وہ نظام جو اندرونی حالت اور یادداشت رکھتے ہیں (روبوٹ ویکیوم، نیویگیشن سسٹمز)
- **⚖️ مقصد-بنیاد ایجنٹس**: وہ نظام جو اہداف حاصل کرنے کے لیے منصوبہ بندی اور سلسلہ وار کام انجام دیتے ہیں (راستہ منصوبہ ساز، ٹاسک شیڈیولرز)
- **🧠 سیکھنے والے ایجنٹس**: موافقت پذیر نظام جو وقت کے ساتھ کارکردگی بہتر کرتے ہیں (تجاویز دینے والے نظام، ذاتی معاونین)

### AI ایجنٹس کے کلیدی فوائد

AI ایجنٹس کئی بنیادی فوائد پیش کرتے ہیں جو انہیں ایج کمپیوٹنگ ایپلیکیشنز کے لیے مثالی بناتے ہیں:

**عملی خود مختاری**: ایجنٹس مستقل انسانی نگرانی کے بغیر آزادانہ کام انجام دیتے ہیں، جو انہیں حقیقی وقت کی ایپلیکیشنز کے لیے مثالی بناتا ہے۔ وہ کم نگرانی کے باوجود لچکدار رویہ قائم رکھتے ہیں، جس سے محدود وسائل والے آلات پر تعیناتی ممکن ہو جاتی ہے اور آپریٹنگ اوور ہیڈ کم ہوتا ہے۔

**تعیناتی کی لچک**: یہ نظام آن-ڈیوائس AI صلاحیتیں انٹرنیٹ کنیکٹیویٹی کے بغیر فراہم کرتے ہیں، مقامی پراسیسنگ کے ذریعے پرائیویسی اور سیکیورٹی کو بہتر بناتے ہیں، مخصوص ڈومین ایپلیکیشنز کے لیے حسب ضرورت ہو سکتے ہیں، اور مختلف ایج کمپیوٹنگ ماحول کے لیے موزوں ہوتے ہیں۔

**قیمت میں کفایت**: ایجنٹ سسٹمز کلاؤڈ پر مبنی حل کی نسبت کم لاگت میں تعینات کیے جا سکتے ہیں، جن کی آپریٹنگ لاگت اور بینڈوڈتھ کی ضروریات ایج ایپلیکیشنز کے لیے کم ہیں۔

## جدید چھوٹے زبان کے ماڈل کی حکمت عملیاں

### SLM (چھوٹے زبان کا ماڈل) کی بنیادی باتیں

ایک چھوٹا زبان کا ماڈل (SLM) ایسا زبان ماڈل ہے جو عام صارف کی الیکٹرانک ڈیوائس پر فٹ ہو سکتا ہے اور ایسے تاخیر کے ساتھ انفرنس کر سکتا ہے جو ایک صارف کی ایجنٹک درخواستوں کو عملی طور پر سرور کر سکے۔ عملی طور پر، SLM عام طور پر 10 ارب سے کم پیرامیٹرز کے ماڈلز ہوتے ہیں۔

**فارمیٹ کی دریافت کی خصوصیات**: SLM مختلف کوانٹائزیشن لیولز، کراس پلیٹ فارم مطابقت، حقیقی وقت کی کارکردگی کی بہتری، اور ایج تعیناتی صلاحیتیں فراہم کرتے ہیں۔ صارفین مقامی پراسیسنگ کے ذریعے بہتر پرائیویسی اور براؤزر پر مبنی تعیناتی کے لیے WebGPU سپورٹ تک رسائی حاصل کر سکتے ہیں۔

**کوانٹائزیشن لیول کلیکشنز**: مشہور SLM فارمیٹس میں Q4_K_M شامل ہے جو موبائل ایپلیکیشنز میں متوازن کمپریشن فراہم کرتا ہے، Q5_K_S سیریز جو ایج تعیناتی کے لیے معیار پر زور دیتا ہے، Q8_0 جو طاقتور ایج ڈیوائسز پر اصل کے قریب درستگی فراہم کرتا ہے، اور تجرباتی فارمیٹس جیسے Q2_K جو انتہائی کم وسائل کے مناظرات کے لیے ہیں۔

### GGUF (General GGML Universal Format) SLM تعیناتی کے لیے

GGUF کوانٹائزڈ SLMs کو CPU اور ایج ڈیوائسز پر تعینات کرنے کے لیے بنیادی فارمیٹ کے طور پر کام کرتا ہے، خاص طور پر ایجنٹک ایپلیکیشنز کے لیے بہتر بنایا گیا:

**ایجنٹ-بہترین خصوصیات**: یہ فارمیٹ SLM تبادلوں اور تعیناتی کے لیے مکمل وسائل فراہم کرتا ہے، جس میں ٹول کالنگ، ساختی آؤٹ پٹ جنریشن، اور متعدد دور کی بات چیت کی بہتری شامل ہے۔ کراس پلیٹ فارم مطابقت مختلف ایج ڈیوائسز پر مستقل ایجنٹ رویہ کو یقینی بناتی ہے۔

**کارکردگی کی بہتری**: GGUF ایجنٹ ورک فلو کے لیے موثر یادداشت کے استعمال کی اجازت دیتا ہے، کثیر ایجنٹ نظاموں کے لیے متحرک ماڈل لوڈنگ کی حمایت کرتا ہے، اور حقیقی وقت کے ایجنٹ تعاملات کے لیے بہتر انفرنس فراہم کرتا ہے۔

### ایج-بہترین SLM فریم ورکس

#### ایجنٹس کے لیے Llama.cpp کی اصلاح

Llama.cpp ایجنٹک SLM تعیناتی کے لیے خاص طور پر بہتر کردہ جدید کوانٹائزیشن تکنیکیں فراہم کرتا ہے:

**ایجنٹ-مخصوص کوانٹائزیشن**: فریم ورک Q4_0 (موبائل ایجنٹ تعیناتی کے لیے مثالی، 75% سائز کی کمی کے ساتھ)، Q5_1 (ایج انفرنس ایجنٹس کے لیے معیار-کمپریشن کا توازن)، اور Q8_0 (پروڈکشن ایجنٹ سسٹمز کے لیے اصل کے قریب معیار) کی حمایت کرتا ہے۔ جدید فارمیٹس انتہائی ایج مناظرات کے لیے انتہائی کمپریسڈ ایجنٹس فراہم کرتے ہیں۔

**نفاذ کے فوائد**: SIMD ایکسلریشن کے ساتھ CPU-آپٹیمائزڈ انفرنس میموری موثر ایجنٹ اجرا فراہم کرتا ہے۔ x86، ARM، اور Apple Silicon آرکیٹیکچرز پر کراس پلیٹ فارم مطابقت عالمی ایجنٹ تعیناتی کی صلاحیتیں فراہم کرتی ہے۔

#### SLM ایجنٹس کے لیے Apple MLX فریم ورک

Apple MLX خاص طور پر Apple Silicon ڈیوائسز پر SLM پر مبنی ایجنٹس کے لیے مقامی اصلاح فراہم کرتا ہے:

**Apple Silicon ایجنٹ اصلاح**: فریم ورک یونیفائیڈ میموری آرکیٹیکچر کو Metal Performance Shaders انٹیگریشن، ایجنٹ انفرنس کے لیے آٹومیٹک مکسڈ پریسیژن، اور کثیر ایجنٹ سسٹمز کے لیے بہتر میموری بینڈوتھ کے ساتھ استعمال کرتا ہے۔ SLM ایجنٹس M-سیریز چپس پر شاندار کارکردگی دکھاتے ہیں۔

**توسیعی خصوصیات**: ایجنٹ-مخصوص اصلاحات کے ساتھ Python اور Swift API سپورٹ، ایجنٹ سیکھنے کے لیے خودکار تفریق، اور Apple ڈیولپمنٹ ٹولز کے ساتھ بغیر رکاوٹ انضمام جامع ایجنٹ ترقیاتی ماحول فراہم کرتے ہیں۔

#### کراس-پلیٹ فارم SLM ایجنٹس کے لیے ONNX رن ٹائم

ONNX رن ٹائم ایک عالمی انفرنس انجن فراہم کرتا ہے جو مختلف ہارڈویئر پلیٹ فارمز اور آپریٹنگ سسٹمز پر SLM ایجنٹس کو مسلسل چلانے کے قابل بناتا ہے:

**عالمی تعیناتی**: ONNX رن ٹائم Windows، Linux، macOS، iOS، اور Android پلیٹ فارمز پر SLM ایجنٹس کے مستحکم رویہ کو یقینی بناتا ہے۔ یہ کراس پلیٹ فارم مطابقت ڈویلپرز کو ایک بار کوڈ لکھ کر ہر جگہ تعینات کرنے کی سہولت دیتی ہے، جو کثیر پلیٹ فارم ایپلیکیشنز کے لیے ترقی اور دیکھ بھال کی لاگت کم کرتی ہے۔

**ہارڈویئر ایکسلریشن کے اختیارات**: فریم ورک مختلف ہارڈویئر کنفیگریشنز کے لیے بہتر چلانے والے فراہم کرتا ہے، جن میں CPU (Intel, AMD, ARM)، GPU (NVIDIA CUDA, AMD ROCm)، اور خصوصی ایکسلریٹرز (Intel VPU, Qualcomm NPU) شامل ہیں۔ SLM ایجنٹس کوڈ میں تبدیلی کے بغیر بہترین دستیاب ہارڈویئر سے خودکار فائدہ اٹھا سکتے ہیں۔

**پروڈکشن-ریڈی خصوصیات**: ONNX رن ٹائم پروڈکشن ایجنٹ تعیناتی کے لیے ضروری انٹرپرائز گریڈ خصوصیات پیش کرتا ہے، جن میں تیز انفرنس کے لیے گراف کی اصلاح، محدود وسائل والے ماحول کے لیے میموری مینجمنٹ، اور کارکردگی کے تجزیہ کے لیے جامع پروفائلنگ ٹولز شامل ہیں۔ فریم ورک Python اور C++ APIs دونوں کی حمایت کرتا ہے تاکہ آسان انضمام ممکن ہو۔


## ایجنٹک سسٹمز میں SLM بمقابلہ LLM: جدید موازنہ

### ایجنٹ ایپلیکیشنز میں SLM کے فوائد

**عملی کارکردگی**: SLM ایجنٹ کاموں کے لیے LLM کے مقابلے میں 10-30 گنا قیمت کی بچت فراہم کرتے ہیں، جس سے پیمانے پر حقیقی وقت کے ایجنٹ جوابات ممکن ہوتے ہیں۔ یہ کم کمپیوٹیشنل پیچیدگی کی وجہ سے تیز انفرنس وقت فراہم کرتے ہیں، جو انہیں انٹرایکٹو ایجنٹ ایپلیکیشنز کے لیے مثالی بناتے ہیں۔

**ایج تعیناتی کی صلاحیتیں**: SLM ایج ڈیوائس پر بغیر انٹرنیٹ کے منحصر ایجنٹ اجرا کی اجازت دیتے ہیں، مقامی ایجنٹ پراسیسنگ کے ذریعے بہتر پرائیویسی، اور مخصوص ڈومین ایجنٹ ایپلیکیشنز کے لیے حسب ضرورت فراہم کرتے ہیں جو مختلف ایج کمپیوٹنگ ماحول کے لیے موزوں ہیں۔

**ایجنٹ-مخصوص اصلاح**: SLM ٹول کالنگ، ساختہ آؤٹ پٹ جنریشن، اور معمول کے فیصلہ سازی کے ورک فلو میں مہارت رکھتے ہیں، جن کا معمول کے ایجنٹ کاموں میں 70-80% حصہ ہوتا ہے۔

### ایجنٹ سسٹمز میں کب SLMs بمقابلہ LLMs استعمال کریں

**SLMs کے لیے بہترین**:
- **دوہراتے ہوئے ایجنٹ کام**: ڈیٹا انٹری، فارم بھرنا، معمول کی API کالز
- **اوزار انضمام**: ڈیٹا بیس سوالات، فائل آپریشنز، نظام کے تعاملات
- **ساخت شدہ ورک فلو**: پیشگی طے شدہ ایجنٹ عمل کی پیروی کرنا
- **ڈومین-مخصوص ایجنٹس**: کسٹمر سروس، شیڈولنگ، بنیادی تجزیہ
- **مقامی پراسیسنگ**: پرائیویسی حساس ایجنٹ آپریشنز

**LLMs کے لیے بہتر**:
- **پیچیدہ استدلال**: نئے مسئلے کا حل، حکمت عملی کی منصوبہ بندی
- **کھلے اختتام کی بات چیت**: عمومی بات چیت، تخلیقی مباحثے
- **وسیع علمی کام**: تحقیق جس کے لیے وسیع عمومی معلومات درکار ہوں
- **نیا منظر نامے**: بالکل نئے ایجنٹ مناظرات کا سامنا

### ہائبرڈ ایجنٹ فن تعمیر

بہترین طریقہ کار مختلف ایجنٹک سسٹمز میں SLMs اور LLMs کو جوڑتا ہے:

**سمارٹ ایجنٹ آرکیسٹریشن**:
1. **SLM کو بنیادی کے طور پر استعمال کرنا**: معمول کے 70-80% ایجنٹ کام مقامی طور پر سنبھالنا
2. **زیادہ ضرورت پر LLM استعمال کرنا**: پیچیدہ سوالات کو کلاؤڈ پر مبنی بڑے ماڈلز کی طرف بھیجنا
3. **مخصوص SLMs**: مختلف ایجنٹ ڈومینز کے لیے مختلف چھوٹے ماڈلز
4. **قیمت کی بہتر تنظیم**: ذہین روٹنگ کے ذریعے مہنگی LLM کالز کو کم سے کم کرنا

## پروڈکشن SLM ایجنٹ تعیناتی کی حکمت عملیاں

### Foundry Local: انٹرپرائز گریڈ ایج AI رن ٹائم

Foundry Local (https://github.com/microsoft/foundry-local) پروڈکشن ایج ماحول میں چھوٹے زبان کے ماڈلز کی تعیناتی کے لیے Microsoft کا فلیگ شپ حل ہے۔ یہ SLM-طاقتور ایجنٹس کے لیے مکمل رن ٹائم ماحول فراہم کرتا ہے جس میں انٹرپرائز گریڈ خصوصیات اور بغیر رکاوٹ انضمام کی صلاحیتیں شامل ہیں۔

**بنیادی فن تعمیر اور خصوصیات**:
- **OpenAI-مطابق API**: OpenAI SDK اور ایجنٹ فریم ورک انٹیگریشن کے ساتھ مکمل مطابقت
- **خودکار ہارڈویئر اصلاح**: دستیاب ہارڈویئر (CUDA GPU, Qualcomm NPU, CPU) کی بنیاد پر ماڈل کے ورژن کا ذہین انتخاب
- **ماڈل مینجمنٹ**: SLM ماڈلز کی خودکار ڈاؤن لوڈنگ، کیشنگ، اور لائف سائیکل مینجمنٹ
- **سروس ڈسکوری**: ایجنٹ فریم ورکس کے لیے زیرو-کنفیگریشن سروس کی شناخت
- **وسائل کی اصلاح**: ایج تعیناتی کے لیے ذہین میموری مینجمنٹ اور توانائی کی بچت

#### تنصیب اور سیٹ اپ

**کراس-پلیٹ فارم تنصیب**:
```bash
# ونڈوز (تجویز کردہ)
winget install Microsoft.FoundryLocal

# میک او ایس
brew tap microsoft/foundrylocal
brew install foundrylocal

# لینکس (دستی تنصیب)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**ایجنٹ ترقی کے لیے فوری آغاز**:
```bash
# خودکار ماڈل لوڈنگ کے ساتھ سروس شروع کریں
foundry model run phi-4-mini

# سروس کی صورتحال اور اینڈپوائنٹ کی تصدیق کریں
foundry service status

# دستیاب ماڈلز کی فہرست بنائیں
foundry model ls

# API اینڈپوائنٹ کا ٹیسٹ کریں
curl http://localhost:<port>/v1/models
```

#### ایجنٹ فریم ورک انٹیگریشن

**Foundry Local SDK انٹیگریشن**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Foundry لوکل کو خودکار سروس مینجمنٹ کے ساتھ شروع کریں
manager = FoundryLocalManager("phi-4-mini")

# لوکل انفرنس کے لیے OpenAI کلائنٹ کو ترتیب دیں
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # لوکل استعمال کے لیے خودکار طور پر تیار کیا گیا
)

# Foundry لوکل بیک اینڈ کے ساتھ ایجنٹ بنائیں
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**خودکار ماڈل انتخاب اور ہارڈویئر کی اصلاح**:
```python
# فانڈری لوکل خود بخود بہترین ماڈل ویرینٹ منتخب کرتا ہے
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB، انتہائی تیز
    "general_conversation": "phi-4-mini",       # 2.4GB، متوازن
    "complex_reasoning": "phi-4",               # 7GB، اعلیٰ صلاحیت
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB، کوڈ کے لیے بہتر بنایا گیا
}

# فانڈری لوکل ہارڈویئر کی شناخت اور کوانٹائزیشن سنبھالتا ہے
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # نتائج کی مثالیں:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (CPU کے لیے بہتر بنایا گیا)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (GPU کے ذریعے تیز کیا گیا)
```

#### پروڈکشن تعیناتی کے نمونے

**سنگل-ایجنٹ پروڈکشن سیٹ اپ**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # پیداواری آلات شامل کریں
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# پیداواری استعمال
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**کثیر-ایجنٹ پروڈکشن آرکیسٹریشن**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # ہلکا پھلکا روٹنگ ایجنٹ
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # کسٹمر سروس ایجنٹ
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # تکنیکی معاونت ایجنٹ
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # درخواست کو مناسب ایجنٹ تک پہنچائیں
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # روٹنگ کی بنیاد پر ہدف ایجنٹ کا تعین کریں
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # ماہر ایجنٹ کے ساتھ عمل کریں
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# پیداوار کی تعیناتی
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### انٹرپرائز خصوصیات اور مانیٹرنگ

**صحت مانیٹرنگ اور مطالعہ**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# پیداواری نگرانی کا قیام
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# مسلسل نگرانی
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # ہر 30 سیکنڈ میں میٹرکس جمع کریں
```

**وسائل کا انتظام اور خود کار پیمائش**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # اگر میموری کا استعمال زیادہ ہو تو کم کریں
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # اگر طلب زیادہ ہو اور وسائل دستیاب ہوں تو بڑھائیں
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 منٹ غیر فعال
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### جدید ترتیب اور اصلاح

**حسب ضرورت ماڈل ترتیب**:
```python
# پیداواری کے لیے ایڈوانسڈ فاؤنڈری لوکل کنفیگریشن
from foundry_local import FoundryLocalManager, ModelConfig

# مخصوص استعمال کے معاملات کے لیے حسب ضرورت کنفیگریشن
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # مخصوص کوانٹائزیشن کی سطح
    context_length=4096,    # پیچیدہ ایجنٹس کے لیے توسیعی سیاق و سباق
    batch_size=1,          # واحد صارف کے ایجنٹس کے لیے بہتر بنایا گیا
    threads=4,             # سی پی یو تھریڈ کی اصلاح
    gpu_layers=32,         # جی پی یو تیز رفتاری کی تہیں
    memory_lock=True,      # مسلسل کارکردگی کے لیے ماڈل کو میموری میں لاک کریں
    numa=True              # کثیر ساکٹ سسٹمز کے لیے نیوما کی اصلاح
)

manager = FoundryLocalManager(config=config)
```

**پروڈکشن تعیناتی کے چیک لسٹ**:

✅ **سروس ترتیب**:
- مخصوص استعمال کے لیے مناسب ماڈل عرفیات ترتیب دیں
- وسائل کی حدود اور مانیٹرنگ کے حد بندی مقرر کریں
- صحت چیکس اور میٹرکس جمع کرنا فعال کریں
- خودکار ری اسٹارٹ اور فیل اوور کی ترتیب دیں

✅ **سیکیورٹی ترتیب**:
- صرف مقامی API رسائی فعال کریں (کوئی بیرونی رسائی نہیں)
- مناسب API کی کلید مینجمنٹ ترتیب دیں
- ایجنٹ تعاملات کے لیے آڈٹ لاگنگ نافذ کریں
- پروڈکشن استعمال کے لیے ریٹ لمیٹنگ نافذ کریں

✅ **کارکردگی کی اصلاح**:
- متوقع بوجھ کے تحت ماڈل کی کارکردگی کا ٹیسٹ کریں
- مناسب کوانٹائزیشن لیول منتخب کریں
- ماڈل کیشنگ اور وارمنگ حکمت عملیاں ترتیب دیں
- میموری اور CPU استعمال کے نمونے مانیٹر کریں

✅ **انٹیگریشن ٹیسٹنگ**:
- ایجنٹ فریم ورک انٹیگریشن کا ٹیسٹ کریں
- آف لائن آپریشن صلاحیتوں کی تصدیق کریں
- فیل اوور اور بازیابی کے منظرناموں کا ٹیسٹ کریں
- مکمل ایجنٹ ورک فلو کی توثیق کریں

### Ollama: آسان SLM ایجنٹ تعیناتی

### Ollama: کمیونٹی-مرکز SLM ایجنٹ تعیناتی

Ollama SLM ایجنٹ تعیناتی کے لیے کمیونٹی پر مبنی طریقہ فراہم کرتا ہے، جس کا زور سادگی، وسیع ماڈل ماحولیاتی نظام، اور ڈویلپر کے موافق ورک فلو پر ہے۔ جہاں Foundry Local انٹرپرائز گریڈ خصوصیات پر توجہ دیتا ہے، Ollama تیز پروٹوٹائپنگ، کمیونٹی ماڈل رسائی، اور آسان تعیناتی مناظرات میں نمایاں ہے۔

**بنیادی فن تعمیر اور خصوصیات**:
- **OpenAI-مطابق API**: بغیر رکاوٹ ایجنٹ فریم ورک انٹیگریشن کے لیے مکمل REST API مطابقت
- **وسیع ماڈل لائبریری**: سیکڑوں کمیونٹی شراکت کردہ اور سرکاری ماڈلز تک رسائی
- **سادہ ماڈل مینجمنٹ**: ایک کمانڈ میں ماڈل کی تنصیب اور تبدیلی
- **کراس-پلیٹ فارم سپورٹ**: Windows، macOS، اور Linux پر مقامی سپورٹ
- **وسائل کی بہتر تنظیم**: خودکار کوانٹائزیشن اور ہارڈویئر کی شناخت

#### تنصیب اور سیٹ اپ

**کراس-پلیٹ فارم تنصیب**:
```bash
# ونڈوز
winget install Ollama.Ollama

# میک او ایس
brew install ollama

# لینکس
curl -fsSL https://ollama.com/install.sh | sh

# ڈوکر کی تعیناتی
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**ایجنٹ ترقی کے لیے فوری آغاز**:
```bash
# اولاما سروس شروع کریں
ollama serve

# ایجنٹ کی ترقی کے لیے ماڈلز کھینچیں اور چلائیں
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # مائیکروسافٹ فی-3.5 منی
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # کوئن2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # للما 3.2 1B

# ماڈل کی دستیابی کا ٹیسٹ کریں
ollama list

# اے پی آئی اینڈپوائنٹ کا ٹیسٹ کریں
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### ایجنٹ فریم ورک انٹیگریشن

**Microsoft Agent Framework کے ساتھ Ollama**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama اصلی API کلید کی ضرورت نہیں ہے
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# ایجنٹ کی ترقی کے لیے Ollama کی ابتدا کریں
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# ایجنٹ کو Ollama بیک اینڈ کے ساتھ ترتیب دیں
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Ollama کے ساتھ ملٹی-ماڈل ایجنٹ سیٹ اپ**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # اگر دستیاب نہ ہو ماڈل کو حاصل کریں
            self._pull_model(model_name)
            
            # ہر ماڈل کے لیے OpenAI کلائنٹ بنائیں
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# استعمال کی مثال
manager = OllamaMultiModelManager()

# مختلف کاموں کے لیے مناسب ایجنٹس حاصل کریں
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### پروڈکشن تعیناتی کے نمونے

**Ollama کے ساتھ پروڈکشن سروس**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # یقینی بنائیں کہ ماڈل دستیاب ہے
                self._ensure_model_ready(model_name)
                
                # پروڈکشن ایجنٹ بنائیں
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # ایجنٹ کی قسم کی بنیاد پر پروڈکشن آلات شامل کریں
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # چیک کریں کہ ماڈل موجود ہے
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # ڈیٹا بیس تلاش کی نقل کریں
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # میٹرکس کو اپ ڈیٹ کریں
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # پروڈکشن میں، یہ ایک مناسب لاگنگ سسٹم میں لکھے گا
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # اولاما سروس کی صحت چیک کریں
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # ماڈل کی دستیابی چیک کریں
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# پروڈکشن ڈیپلائمنٹ کی مثال
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# درخواستوں کو عمل میں لائیں
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### انٹرپرائز خصوصیات اور مانیٹرنگ

**Ollama مانیٹرنگ اور مشاہدہ**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # سروس کی صحت چیک کریں
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # ماڈل کی معلومات حاصل کریں
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # مختصر خلاصہ
                    }
                
                # استدلال کی کارکردگی کو آزمائیں
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # صرف آخری 100 میٹرکس اندراجات رکھیں
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # ٹوکنز کا اندازہ لگائیں (تقریباً)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # اگر دستیاب ہو تو وقت کی معلومات حاصل کریں
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # نینو سیکنڈز کو سیکنڈز میں تبدیل کریں
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # جواب کے وقت کو چیک کریں
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # سروس کی حالت چیک کریں
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # اوسط نکالیں
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# پیداواری نگرانی کا سیٹ اپ
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # ہر 10 منٹ بعد کارکردگی کا خلاصہ لاگ کریں
            if int(time.time()) % 600 == 0:  # ہر 10 منٹ
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # ہر 30 سیکنڈ میں چیک کریں

# نگرانی شروع کریں
# asyncio.create_task(monitoring_loop())
```

#### جدید ترتیب اور اصلاح

**Ollama کے ساتھ حسب ضرورت ماڈل مینجمنٹ**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # تیز ردعمل کے لئے ہلکے پھلکے ماڈلز
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # عام استعمال کے لئے متوازن ماڈلز
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # مخصوص کاموں کے لئے مخصوص ماڈلز
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # اعلیٰ صلاحیت کے ماڈلز
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # پل ماڈل
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # عام طور پر یہ اصل ہارڈویئر کی خصوصیات چیک کرے گا
        # ڈیمو کے مقاصد کے لئے، ہم ہارڈویئر کا سراغ لگانے کا تقلیب کریں گے
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# پروڈکشن ماڈل کی ترتیب
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Ollama کے لیے پروڈکشن تعیناتی کا چیک لسٹ**:

✅ **سروس ترتیب**:
- مناسب سسٹم انضمام کے ساتھ Ollama سروس انسٹال کریں
- مخصوص ایجنٹ استعمال کے کیسز کے لیے ماڈلز ترتیب دیں
- مناسب اسٹارٹ اپ اسکرپٹس اور سروس مینجمنٹ قائم کریں
- ماڈل لوڈنگ اور API دستیابی کا ٹیسٹ کریں

✅ **ماڈل مینجمنٹ**:
- مطلوبہ ماڈلز حاصل کریں اور سالمیت کی تصدیق کریں
- ماڈل اپ ڈیٹ اور گردش کے عمل قائم کریں
- ماڈل کیشنگ اور اسٹوریج کی بہتر تنظیم کریں
- متوقع بوجھ کے تحت ماڈل کی کارکردگی کا ٹیسٹ کریں

✅ **سیکیورٹی ترتیب**:
- مقامی رسائی کے لیے فائر وال رولز ترتیب دیں
- API رسائی کنٹرولز اور ریٹ لمیٹنگ قائم کریں
- ایجنٹ تعاملات کی آڈٹ لاگنگ نافذ کریں
- ماڈل اسٹوریج اور رسائی کو محفوظ بنائیں

✅ **کارکردگی کی اصلاح**:
- متوقع استعمال کے کیسز کے لیے ماڈلز کی بینچ مارکنگ کریں
- مناسب ہارڈویئر ایکسلریشن منتخب کریں
- ماڈل وارمنگ اور کیشنگ حکمت عملیاں ترتیب دیں
- وسائل کے استعمال اور کارکردگی کے میٹرکس مانیٹر کریں

✅ **انٹیگریشن ٹیسٹنگ**:
- Microsoft Agent Framework انٹیگریشن کا ٹیسٹ کریں
- آف لائن آپریشن صلاحیتوں کی تصدیق کریں

- ٹیسٹ فال اوور منظرنامے اور خرابی کا ہینڈلنگ
- اینڈ ٹو اینڈ ایجنٹ ورک فلو کی تصدیق کریں

**Foundry Local کے ساتھ موازنہ**:

| خصوصیت | Foundry Local | Ollama |
|---------|---------------|--------|
| **ہدف استعمال کا کیس** | انٹرپرائز پیداوار | ترقی اور کمیونٹی |
| **ماڈل ایکوسسٹم** | مائیکروسافٹ کی منتخب کردہ | وسیع کمیونٹی |
| **ہارڈویئر موافقت** | خودکار (CUDA/NPU/CPU) | دستی ترتیب |
| **انٹرپرائز خصوصیات** | بلٹ ان مانیٹرنگ، سیکیورٹی | کمیونٹی کے اوزار |
| **توسع کی پیچیدگی** | آسان (winget انسٹال) | آسان (curl انسٹال) |
| **API مطابقت** | OpenAI + ایکسٹینشنز | OpenAI اسٹینڈرڈ |
| **سپورٹ** | مائیکروسافٹ آفیشل | کمیونٹی کی زیر قیادت |
| **سب سے بہتر** | پیداوار ایجنٹس | پروٹوٹائپنگ، تحقیق |

**Ollama کب منتخب کریں**:
- **ترقی اور پروٹوٹائپنگ**: مختلف ماڈلز کے ساتھ تیز تجربات
- **کمیونٹی ماڈلز**: جدید کمیونٹی کی جانب سے فراہم کردہ ماڈلز تک رسائی
- **تعلیمی استعمال**: AI ایجنٹ کی ترقی سیکھنا اور پڑھانا
- **تحقیقی منصوبے**: تعلیمی تحقیق کے لیے متنوع ماڈل کی رسائی
- **حسب ضرورت ماڈلز**: اپنی مرضی کے مطابق تیار کردہ ماڈلز بنانا اور آزمائش کرنا

### VLLM: اعلی کارکردگی کا SLM ایجنٹ انفرنس

VLLM (بہت بڑے زبان ماڈل انفرنس) اعلی تھروپٹ اور میموری مؤثر انفرنس انجن فراہم کرتا ہے جو خاص طور پر بڑے پیمانے پر پیداوار SLM تعیناتیوں کے لیے بہتر کیا گیا ہے۔ جب کہ Foundry Local آسانی کے لیے مرکوز ہے اور Ollama کمیونٹی ماڈلز پر زور دیتا ہے، VLLM اعلی کارکردگی کے مناظر میں شاندار ہے جہاں زیادہ سے زیادہ تھروپٹ اور مؤثر وسائل کے استعمال کی ضرورت ہو۔

**مرکزی ساخت اور خصوصیات**:
- **PagedAttention**: موثر اٹینشن کمپیوٹیشن کے لیے انقلابی میموری مینجمنٹ
- **Dynamic Batching**: بہتر تھروپٹ کے لیے ذہین درخواست کی بیچنگ
- **GPU Optimization**: جدید CUDA کرنلز اور ٹینسر پیرالیل ازم کی حمایت
- **OpenAI Compatibility**: بغیر رکاوٹ انضمام کے لیے مکمل API مطابقت
- **Speculative Decoding**: جدید انفرنس تیز کرنے کی تکنیکیں
- **Quantization Support**: میموری کی بچت کے لیے INT4، INT8، اور FP16 کوانٹائزیشن

#### انسٹالیشن اور سیٹ اپ

**انسٹالیشن کے اختیارات**:
```bash
# معیاری تنصیب
pip install vllm

# ایجنٹ فریم ورک کے اضافی انحصار کے ساتھ
pip install vllm[agent] openai

# پیداوار کے لیے ڈاکر تعیناتی
docker pull vllm/vllm-openai:latest

# تازہ ترین خصوصیات کے لیے ماخذ سے
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**ایجنٹ ڈیولپمنٹ کے لیے فوری آغاز**:
```bash
# SLM ماڈل کے ساتھ VLLM سرور شروع کریں
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# متبادل: ہلکے ایجنٹس کے لیے Qwen2.5 کے ساتھ شروع کریں
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# API اینڈپوائنٹ کا امتحان کریں
curl http://localhost:8000/v1/models

# چیٹ مکمل کرنے کا امتحان کریں
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### ایجنٹ فریم ورک انضمام

**VLLM مائیکروسافٹ ایجنٹ فریم ورک کے ساتھ**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # ایجنٹس کے لیے لاگنگ کم کریں
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # سرور کے شروع ہونے کا انتظار کریں
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# اعلی کارکردگی والے ایجنٹس کے لیے VLLM کو شروع کریں
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # ایجنٹ کو VLLM بیک اینڈ کے ساتھ ترتیب دیں
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM کو API کلید کی ضرورت نہیں ہے
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**ہائی تھروپٹ ملٹی ایجنٹ سیٹ اپ**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # بلاکنگ سے بچنے کے لیے تھریڈ میں سرور شروع کریں
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # ایجنٹ بنائیں
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # زیادہ تھرو پٹ کے لیے کلائنٹ پول بنائیں
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # متوازی کام کے لیے ہر ماڈل کے لیے 5 کلائنٹس
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # چیک کریں کہ ترجیحی ایجنٹ دستیاب ہے اور اوورلوڈ نہیں ہے
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # ہر ایجنٹ پر زیادہ سے زیادہ 10 متوازی
            return preferred_category
        
        # کم سے کم بوجھ والا دستیاب ایجنٹ استعمال کریں
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # ڈیفالٹ بیک اپ
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # بہترین ایجنٹ کا تعین کریں
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # پروسیسنگ ٹاسک بنائیں
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # تمام درخواستیں متوازی پروسیس کریں
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # نتائج کو فارمیٹ کریں
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# اعلی تھرو پٹ کے استعمال کی مثال
throughput_manager = VLLMHighThroughputManager()

# تمام ماڈلز کو شروع کریں
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# بیچ درخواستوں کو پروسیس کریں
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### پیداواری تعیناتی کے پیٹرنز

**انٹرپرائز VLLM پیداواری سروس**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # ورکرز کی پروسیسنگ شروع کریں
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # زیادہ سے زیادہ 4 ورکرز
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # اگر مخصوص ہو تو کوانٹسائزیشن شامل کریں
        if config.quantization:
            # یہ مینیجر کے شروع کرنے کے کمانڈ میں شامل کیا جائے گا
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # پروڈکشن ایجنٹ بنائیں
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # پروڈکشن ٹولز شامل کریں
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # پروڈکشن ڈیٹا بیس لک اپ
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # درخواست کو قطار میں شامل کریں
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # پروسیسنگ کے انتظار کریں (ٹائم آوٹ کے ساتھ)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # قطار سے درخواست حاصل کریں
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # مناسب ایجنٹ منتخب کریں
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # درخواست پر عمل کریں
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # میٹرکس کو اپ ڈیٹ کریں
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # یہ سادہ کیا گیا ہے - پروڈکشن میں آپ مستقبل کو صحیح طریقے سے ٹریک کریں گے
        await asyncio.sleep(0.1)  # پلیس ہولڈر
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # سادگی کے لیے واحد ورکر
        )
        server = uvicorn.Server(config)
        await server.serve()

# پروڈکشن ڈپلائمنٹ کی مثال
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# پروڈکشن سروس کو شروع اور انیشیئلائز کریں
# production_service.initialize_production_environment() کا انتظار کریں
# production_service.start_production_server() کا انتظار کریں
```

#### انٹرپرائز خصوصیات اور مانیٹرنگ

**جدید VLLM کارکردگی کی مانیٹرنگ**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # GPU کی نگرانی کے لیے NVIDIA ML کو شروع کریں
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # ایرر میٹرکس بنائیں
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # API کے ذریعہ VLLM مخصوص میٹرکس حاصل کریں
        vllm_stats = await self._get_vllm_stats(manager)
        
        # سسٹم میٹرکس حاصل کریں
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # اگر دستیاب ہو GPU میٹرکس حاصل کریں
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # سادگی کے لیے پہلے GPU کو فرض کریں
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # تاخیر کی پیمائش کے لیے ٹیسٹ انفرنس
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # VLLM کے ذریعے ظاہر کرنے کی ضرورت ہوگی
                "active_requests": 1  # اندازہ
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # ہر مثال کا تجزیہ کریں
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # زیادہ تاخیر کی سفارش
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # زیادہ GPU استعمال کی سفارش
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # کم استعمال کی سفارش
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# جدید نگرانی کا سیٹ اپ
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # ضرورت کے مطابق دیگر مینیجرز شامل کریں
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # میٹرکس جمع کریں
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # صرف آخری 1000 اندراجات رکھیں
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # ہر 5 منٹ بعد سفارشات تیار کریں
            if len(monitoring.metrics_history) % 10 == 0:  # ہر 10ویں جمع (اگر ہر 30 سیکنڈ میں ایک بار جمع کر رہے ہوں تو 5 منٹ)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # ہر گھنٹے ایک کارکردگی رپورٹ تیار کریں
            if len(monitoring.metrics_history) % 120 == 0:  # ہر 120ویں جمع (1 گھنٹہ)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # ہر 30 سیکنڈ میں میٹرکس جمع کریں

# جدید نگرانی شروع کریں
# asyncio.create_task(advanced_monitoring_loop())
```

#### جدید ترتیب اور موافقت

**پیداواری VLLM ترتیب کے سانچے**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # اختیاری پیرامیٹرز شامل کریں
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# استعمال کی مثالیں
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**VLLM کے لیے پیداواری تعیناتی چیک لسٹ**:

✅ **ہارڈویئر موافقت**:
- ملٹی-GPU سیٹ اپ کے لیے ٹینسر پیرالیل ازم ترتیب دیں
- میموری کی بچت کے لیے کوانٹائزیشن (AWQ/GPTQ) فعال کریں
- مثالی GPU میموری کے استعمال کو سیٹ کریں (85-95%)
- تھروپٹ کے لیے مناسب بیچ سائز سیٹ کریں

✅ **کارکردگی کی ٹیوننگ**:
- بار بار پوچھ گچھ کے لیے پری فکس کیشنگ فعال کریں
- طولانی سٹرنگز کے لیے چنکڈ پریفل سیٹ کریں
- تیز انفرنس کے لیے اسپیکیولیٹیو ڈیکوڈنگ سیٹ اپ کریں
- ہارڈویئر کی بنیاد پر max_num_seqs بہتر کریں

✅ **پیداواری خصوصیات**:
- صحت کی مانیٹرنگ اور میٹرکس جمع کرنا سیٹ کریں
- خودکار ری اسٹارٹ اور فال اوور ترتیب دیں
- درخواست قطار سازی اور لوڈ بیلنسنگ نافذ کریں
- مکمل لاگنگ اور الرٹنگ ترتیب دیں

✅ **سیکیورٹی اور اعتماد**:
- فائر وال رولز اور رسائی کنٹرول ترتیب دیں
- API ریٹ لمٹنگ اور توثیق سیٹ کریں
- نرم بندش اور صفائی کا نفاذ کریں
- بیک اپ اور ڈیزاسٹر ریکوری ترتیب دیں

✅ **انضمام کی جانچ**:
- مائیکروسافٹ ایجنٹ فریم ورک انضمام کی جانچ کریں
- ہائی تھروپٹ منظرناموں کی توثیق کریں
- فال اوور اور بازیابی کے طریقہ کار کی جانچ کریں
- لوڈ کے تحت کارکردگی کا موازنہ کریں

**دیگر حل کے ساتھ موازنہ**:

| خصوصیت | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **ہدف استعمال کا کیس** | اعلی تھروپٹ پیداوار | انٹرپرائز آسانی | ترقی اور کمیونٹی |
| **کارکردگی** | زیادہ سے زیادہ تھروپٹ | متوازن | اچھا |
| **میموری کی بچت** | PagedAttention کی بہتری | خودکار بہتر سازی | معیاری |
| **سیٹ اپ کی پیچیدگی** | زیادہ (بہت سارے پیرامیٹرز) | کم (خودکار) | کم (آسان) |
| **وسعت پذیری** | شاندار (ٹینسر/پائپ لائن پیرالیل) | اچھی | محدود |
| **کوانٹائزیشن** | جدید (AWQ، GPTQ، FP8) | خودکار | معیاری GGUF |
| **انٹرپرائز خصوصیات** | حسب ضرورت نفاذ کی ضرورت | بلٹ ان | کمیونٹی کے اوزار |
| **سب سے بہتر** | بڑے پیمانے پر پیداوار ایجنٹس | انٹرپرائز پیداوار | ترقی |

**VLLM کب منتخب کریں**:
- **ہائی تھروپٹ کی ضرورت**: سیکنڈ میں سینکڑوں درخواستوں کی پراسیسنگ
- **بڑے پیمانے کی تعیناتیاں**: ملٹی-GPU، ملٹی نوڈ تعیناتیاں
- **کارکردگی میں شدت**: پیمانے پر سیکنڈ سے بھی کم ردعمل کا وقت
- **جدید بہتری**: حسب ضرورت کوانٹائزیشن اور بیچنگ کی ضرورت
- **وسائل کی مؤثریت**: مہنگے GPU ہارڈویئر کا زیادہ سے زیادہ استعمال


## حقیقی دنیا کے SLM ایجنٹ ایپلیکیشنز

### کسٹمر سروس SLM ایجنٹس
- **SLM صلاحیتیں**: اکاؤنٹ تلاش، پاس ورڈ ری سیٹ، آرڈر کی حالت چیک
- **لاگت کے فوائد**: LLM ایجنٹس کے مقابلے میں انفرنس لاگت میں 10 گنا کمی
- **کارکردگی**: معمول کی سوالات کے لیے تیز ردعمل اور مستقل معیار

### کاروباری عمل SLM ایجنٹس
- **انوائس پراسیسنگ ایجنٹس**: ڈیٹا نکالنا، معلومات کی تصدیق، منظوری کے لیے روٹ کرنا
- **ای میل مینجمنٹ ایجنٹس**: خودکار طور پر درجہ بندی، ترجیح دینا، جواب مسودہ تیار کرنا
- **شیڈولنگ ایجنٹس**: میٹنگز کو مربوط کرنا، کیلنڈرز کا انتظام، یاد دہانیاں بھیجنا

### ذاتی SLM ڈیجیٹل اسسٹنٹس
- **ٹاسک مینجمنٹ ایجنٹس**: مؤثر طریقے سے کام کی فہرست بنانا، اپ ڈیٹ کرنا اور منظم کرنا
- **معلومات اکٹھا کرنے والے ایجنٹس**: موضوعات پر تحقیق، مقامی طور پر خلاصہ کرنا
- **رابطہ کاری ایجنٹس**: ای میل، پیغامات، سوشل میڈیا پوسٹس کو نجی طور پر مسودہ کریں

### تجارتی اور مالیاتی SLM ایجنٹس
- **مارکیٹ مانیٹرنگ ایجنٹس**: قیمتوں کو ٹریک کرنا، اصل وقت میں رجحانات کی شناخت
- **رپورٹ جنریشن ایجنٹس**: روزانہ/ہفتہ وار خلاصے خودکار پیدا کرنا
- **خطرے کا اندازہ لگانے والے ایجنٹس**: مقامی ڈیٹا کی بنیاد پر پورٹ فولیو پوزیشنز کا جائزہ لینا

### صحت کی مدد SLM ایجنٹس
- **مریض شیڈولنگ ایجنٹس**: اپائنٹمنٹس کو مربوط کرنا، خودکار یاد دہانیاں بھیجنا
- **دستاویزات ایجنٹس**: طبی خلاصے، رپورٹس مقامی طور پر تیار کرنا
- **نسخہ بندی کے مینجمنٹ ایجنٹس**: ری فلز کو ٹریک کرنا، تعاملات کو نجی طور پر چیک کرنا

## مائیکروسافٹ ایجنٹ فریم ورک: پیداواری طور پر تیار ایجنٹ کی ترقی

### جائزہ اور ساخت

مائیکروسافٹ ایجنٹ فریم ورک ایک جامع، انٹرپرائز گریڈ پلیٹ فارم مہیا کرتا ہے جو AI ایجنٹس کی تعمیر، تعیناتی، اور انتظام کے لیے ہے جو کلاؤڈ اور آف لائن ایج دونوں ماحول میں کام کر سکتے ہیں۔ یہ فریم ورک خاص طور پر سمال لینگویج ماڈلز اور ایج کمپیوٹنگ مناظر کے لیے ڈیزائن کیا گیا ہے، جس سے یہ پرائیویسی کی حساس اور وسائل محدود تعیناتیوں کے لیے مثالی ہے۔

**مرکزی فریم ورک اجزاء**:
- **ایجنٹ رن ٹائم**: ایج ڈیوائسز کے لیے ہلکا پھلکا عمل درآمد کا ماحول
- **ٹول انٹیگریشن سسٹم**: بیرونی سروسز اور API کے لیے قابل توسیع پلگ ان ساخت
- **اسٹیٹ مینجمنٹ**: سیشنز کے دوران ایجنٹ کی مستقل یادداشت اور سیاق و سباق کا انتظام
- **سیکیورٹی پرت**: انٹرپرائز تعیناتی کے لیے بلٹ ان سیکیورٹی کنٹرولز
- **آرکیسٹریشن انجن**: ملٹی ایجنٹ ہم آہنگی اور ورک فلو کا انتظام

### ایج تعیناتی کے کلیدی خصوصیات

**آف لائن-فرسٹ ساخت**: مائیکروسافٹ ایجنٹ فریم ورک آف لائن-فرسٹ اصولوں کے ساتھ ڈیزائن کیا گیا ہے، جو ایجنٹس کو مستقل انٹرنیٹ کنیکٹوٹی کے بغیر مؤثر طریقے سے کام کرنے کے قابل بناتا ہے۔ اس میں مقامی ماڈل انفرنس، کیش شدہ نالج بیسز، آف لائن ٹول نفاذ، اور کلاؤڈ سروسز دستیاب نہ ہونے پر نرم کمی شامل ہے۔

**وسائل کی موافقت**: فریم ورک ذہین وسائل کا انتظام فراہم کرتا ہے جس میں SLMs کے لیے خودکار میموری آپٹیمائزیشن، ایج ڈیوائسز کے لیے CPU/GPU لوڈ بیلنسنگ، دستیاب وسائل کی بنیاد پر ماڈل کا انتخاب، اور موبائل تعیناتی کے لیے پاور ایفیشنٹ انفرنس پیٹرنز شامل ہیں۔

**سیکیورٹی اور پرائیویسی**: انٹرپرائز گریڈ سیکیورٹی خصوصیات میں مقامی ڈیٹا پراسیسنگ شامل ہے تاکہ پرائیویسی برقرار رہے، انکرپٹڈ ایجنٹ کمیونیکیشن چینلز، ایجنٹ صلاحیتوں کے لیے رول بیسڈ رسائی کنٹرولز، اور کمپلائنس ضروریات کے لیے آڈٹ لاگنگ شامل ہے۔

### Foundry Local کے ساتھ انضمام

مائیکروسافٹ ایجنٹ فریم ورک بغیر کسی رکاوٹ کے Foundry Local کے ساتھ انٹیگریٹ ہوتا ہے تاکہ مکمل ایج AI حل فراہم کیا جا سکے:

**خودکار ماڈل کی دریافت**: فریم ورک خود بخود Foundry Local انسٹینسز کو ڈھونڈتا اور کنیکٹ کرتا ہے، دستیاب SLM ماڈلز کی دریافت کرتا ہے، اور ایجنٹ کی ضروریات اور ہارڈویئر صلاحیتوں کی بنیاد پر بہترین ماڈلز منتخب کرتا ہے۔

**حرکی ماڈل لوڈنگ**: ایجنٹس مختلف کاموں کے لیے متحرک طور پر مختلف SLM لوڈ کر سکتے ہیں، جو ملٹی-ماڈل ایجنٹ سسٹمز کو ممکن بناتا ہے جہاں مختلف ماڈلز مختلف قسم کی درخواستیں سنبھالتے ہیں، اور دستیابی اور کارکردگی کی بنیاد پر خودکار فال اوور ممکن ہوتا ہے۔

**کارکردگی کی موافقت**: مربوط کیشنگ میکانزمز ماڈل لوڈنگ کے اوقات کو کم کرتے ہیں، کنکشن پولنگ Foundry Local کو API کالز کو بہتر بناتا ہے، اور ذہین بیچنگ متعدد ایجنٹ درخواستوں کے لیے تھروپٹ کو بہتر بناتی ہے۔

### مائیکروسافٹ ایجنٹ فریم ورک کے ساتھ ایجنٹس بنانا

#### ایجنٹ کی تعریف اور ترتیب

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# ایجنٹ کو فاؤنڈری لوکل انٹیگریشن کے ساتھ ترتیب دیں
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# فاؤنڈری لوکل کنکشن کو ابتدائی کریں
foundry = FoundryLocalManager("phi-4-mini")

# ایجنٹ کی مثال بنائیں
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### ایج منظرناموں کے لیے ٹول انٹیگریشن

```python
# آف لائن آپریشن کے لئے آلات کی تعریف کریں
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # مقامی ڈیٹا بیس کی تلاش - آف لائن کام کرتی ہے
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # مقامی ٹکٹ کی تخلیق، آن لائن ہونے پر ہم آہنگی کے ساتھ
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # کیلنڈر انضمام کے ساتھ مقامی شیڈولنگ
    return local_calendar.schedule(customer_id, preferred_time)
```

#### ملٹی ایجنٹ آرکیسٹریشن

```python
from microsoft_agent_framework import AgentOrchestrator

# مختلف شعبوں کے لیے مخصوص ایجنٹس بنائیں
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # آسان کاموں کے لیے ہلکا پھلکا
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # پیچیدہ مسائل کے لیے زیادہ قابل
        specialized_for="technical_support"
    )
)

# متعدد ایجنٹس کو منظم کریں
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# ارادے کی بنیاد پر درخواستوں کو راستہ دیں
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### جدید ایج تعیناتی کے نمونے

#### درجہ بندی کا ایجنٹ ساخت

**مقامی ایجنٹ کلسٹرز**: ایج ڈیوائسز پر متعدد مخصوص SLM ایجنٹس تعینات کریں، ہر ایک مخصوص کاموں کے لیے بہترین بنایا گیا۔ آسان روٹنگ اور شیڈولنگ کے لیے Qwen2.5-0.5B جیسے ہلکے ماڈلز استعمال کریں، کسٹمر سروس اور دستاویزات کے لیے Phi-4-Mini جیسے درمیانے ماڈلز، اور جب وسائل دستیاب ہوں تو پیچیدہ استدلال کے لیے بڑے ماڈلز استعمال کریں۔

**ایج سے کلاؤڈ ہم آہنگی**: ذہین اسکیلیشن نمونے نافذ کریں جہاں مقامی ایجنٹس معمول کے کام سرانجام دیں، کلاؤڈ ایجنٹس کنیکٹوٹی ہونے پر پیچیدہ استدلال فراہم کریں، اور ایج اور کلاؤڈ پراسیسنگ کے درمیان بغیر رکاوٹ ہینڈ آف تسلسل برقرار رکھے۔

#### تعیناتی کی ترتیبیں

**واحد ڈیوائس تعیناتی**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**توزیع شدہ ایج تعیناتی**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### ایج ایجنٹس کے لیے کارکردگی کی موافقت

#### ماڈل انتخاب کی حکمت عملیاں

**ٹاسک پر مبنی ماڈل اسائنمنٹ**: مائیکروسافٹ ایجنٹ فریم ورک کام کی پیچیدگی اور ضروریات کی بنیاد پر ذہین ماڈل کا انتخاب ممکن بناتا ہے:

- **سادہ کام** (سوالات اور جوابات، روٹنگ): Qwen2.5-0.5B (500MB، <100ms ردعمل)
- **درمیانے درجے کے کام** (کسٹمر سروس، شیڈولنگ): Phi-4-Mini (2.4GB، 200-500ms ردعمل)
- **پیچیدہ کام** (تکنیکی تجزیہ، منصوبہ بندی): Phi-4 (7GB، وسائل کی دستیابی پر 1-3 سیکنڈ ردعمل)

**حرکی ماڈل سوئچنگ**: ایجنٹس موجودہ سسٹم لوڈ، کام کی پیچیدگی کا جائزہ، صارف کی ترجیحات، اور دستیاب ہارڈویئر وسائل کی بنیاد پر ماڈلز کے درمیان سوئچ کر سکتے ہیں۔

#### میموری اور وسائل کا انتظام

```python
# کنارے کی تعیناتی کے لیے وسائل کی پابندیاں ترتیب دیں
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### انٹرپرائز انضمام کے نمونے

#### سیکیورٹی اور تعمیل

**مقامی ڈیٹا پراسیسنگ**: تمام ایجنٹ پراسیسنگ مقامی سطح پر ہوتی ہے، اس بات کو یقینی بناتے ہوئے کہ حساس ڈیٹا کبھی ایج ڈیوائس سے باہر نہ جائے۔ اس میں کسٹمر معلومات کی حفاظت، صحت کی دیکھ بھال کے ایجنٹس کے لیے HIPAA تعمیل، بینکنگ ایجنٹس کے لیے مالیاتی ڈیٹا کی سیکیورٹی، اور یورپی تعیناتیوں کے لیے GDPR تعمیل شامل ہے۔

**رسائی کنٹرول**: کردار پر مبنی اجازتیں کنٹرول کرتی ہیں کہ ایجنٹس کون سے اوزار تک رسائی حاصل کر سکتے ہیں، ایجنٹ تعاملات کے لیے صارف تصدیق، اور تمام ایجنٹ کارروائیوں اور فیصلوں کے لیے آڈٹ ٹریلز۔

#### نگرانی اور مشاہدہ

```python
from microsoft_agent_framework import AgentMonitor

# ایج ایجنٹس کے لیے نگرانی ترتیب دیں
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # آف لائن آپریشن کے لیے میٹرکس مقامی طور پر ذخیرہ کریں
)

agent.add_monitor(monitor)
```

### حقیقی دنیا کی نفاذ کی مثالیں

#### ریٹیل ایج ایجنٹ سسٹم

```python
# اسٹور میں گاہک کی مدد کے لیے ریٹیل کیوسک ایجنٹ
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### صحت کی مدد ایجنٹ

```python
# HIPAA کے مطابق مریض کی مددگار ایجنٹ
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # صحت کی دیکھ بھال کے لیے بہتر رازداری
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### مائیکروسافٹ ایجنٹ فریم ورک کے لیے بہترین طریقے

#### ترقیاتی رہنمائی

1. **سادہ شروع کریں**: پیچیدہ ملٹی ایجنٹ سسٹمز بنانے سے پہلے واحد ایجنٹ منظرناموں سے آغاز کریں
2. **ماڈل کی درست سائزنگ**: سب سے چھوٹا ماڈل منتخب کریں جو آپ کی درستگی کی ضروریات کو پورا کرے
3. **ٹول ڈیزائن**: پیچیدہ کثیر فعالیت کے بجائے مخصوص، یک مقصدی ٹول بنائیں
4. **خرابی کا ہینڈلنگ**: آف لائن منظرناموں اور ماڈل کی ناکامیوں کے لیے نرم کمی نافذ کریں
5. **جانچ**: آف لائن حالات اور وسائل کی محدودیت والے ماحول میں ایجنٹس کی مکمل جانچ کریں

#### تعیناتی کے بہترین طریقے

1. **آہستہ آغاز**: ابتدا میں چھوٹے صارف گروپس پر تعینات کریں، کارکردگی کے میٹرکس کی قریب سے نگرانی کریں
2. **وسائل کی نگرانی**: میموری، CPU، اور ردعمل کے وقت کی حد بندی کے لیے الرٹس قائم کریں
3. **متبادل حکمت عملیاں**: ماڈل کی ناکامیوں یا وسائل کی کمی کے لیے ہمیشہ بیک اپ منصوبے رکھیں
4. **سیکیورٹی سب سے پہلے**: ابتدا سے سیکیورٹی کنٹرول نافذ کریں، بعد میں نہیں
5. **دستاویزات**: ایجنٹ کی صلاحیتوں اور حدود کی واضح دستاویز رکھیں

### مستقبل کا روڈ میپ اور انضمام

مائیکروسافٹ ایجنٹ فریم ورک مسلسل ترقی کر رہا ہے، جس میں بہتر SLM موافقت، بہتر ایج تعیناتی کے اوزار، محدود ماحول کے لیے بہتر وسائل کا انتظام، اور عام انٹرپرائز مناظر کے لیے وسیع اوزار ایکوسسٹم شامل ہے۔

**آنے والی خصوصیات**:
- **ایجنٹ کی بہتری کے لیے AutoML**: خاص ایجنٹ کاموں کے لیے SLMs کا خودکار فائن ٹیوننگ
- **ایج میش نیٹ ورکنگ**: متعدد ایج ایجنٹ تعیناتیوں کے درمیان ہم آہنگی
- **جدید ٹیلیمیٹری**: ایجنٹ کی کارکردگی کے لیے بہتر نگرانی اور تجزیے
- **بصری ایجنٹ بلڈر**: کم کوڈ/کوڈ کے بغیر ایجنٹ ترقی کے اوزار

## SLM ایجنٹ نفاذ کے لیے بہترین طریقے

### ایجنٹس کے لیے SLM انتخاب کی رہنما اصول

ایجنٹ کی تعیناتی کے لیے SLMs منتخب کرتے وقت درج ذیل عوامل پر غور کریں:

**ماڈل سائز کے ملاحظات**: انتہائی موبائل ایجنٹس کے لیے الٹرا کمپریسڈ ماڈلز جیسے Q2_K، عام ایجنٹ حالات کے لیے متوازن ماڈلز جیسے Q4_K_M، اور معیار کی حساس ایجنٹ درخواستوں کے لیے زیادہ درست ماڈلز جیسے Q8_0 منتخب کریں۔

**ایجنٹ استعمال کے کیس کی مطابقت**: ایجنٹ کی ضروریات کے مطابق SLM صلاحیتوں کا میل کریں، جیسے کہ ایجنٹ کے فیصلوں میں درستگی کی حفاظت، حقیقی وقت کی ایجنٹ بات چیت کے لیے انفرنس کی رفتار، ایج ایجنٹ کی تعیناتی کے لیے میموری کی حدود، اور پرائیویسی مرکوز ایجنٹس کے لیے آف لائن آپریشن کی ضروریات۔

### SLM ایجنٹس کے لیے بہتری کی حکمت عملی کا انتخاب

**ایجنٹس کے لیے کوانٹائزیشن طریقہ کار**: ایجنٹ کے معیار کی ضروریات اور ہارڈویئر پابندیوں کی بنیاد پر مناسب کوانٹائزیشن سطحیں منتخب کریں۔ موبائل ایجنٹس میں زیادہ سے زیادہ کمپریشن کے لیے Q4_0، عام ایجنٹس میں معیار-کمپریشن کے توازن کے لیے Q5_1، اور حساس ایجنٹ درخواستوں میں قریب اصیل معیار کے لیے Q8_0 کا انتخاب کریں۔


**ایجنٹ کی تعیناتی کے لیے فریم ورک کا انتخاب**: ہدف ہارڈویئر اور ایجنٹ کی ضروریات کی بنیاد پر آپٹیمائزیشن فریم ورکس کا انتخاب کریں۔ CPU کے لیے بہتر کردہ ایجنٹ کی تعیناتی کے لیے Llama.cpp، ایپل سلیکن ایجنٹ ایپلی کیشنز کے لیے Apple MLX، اور کراس پلیٹ فارم ایجنٹ مطابقت کے لیے ONNX استعمال کریں۔

## عملی SLM ایجنٹ کنورژن اور استعمال کے کیسز

### حقیقی دنیا میں ایجنٹ تعیناتی کے منظرنامے

**موبائل ایجنٹ ایپلی کیشنز**: Q4_K فارمیٹس اسمارٹ فون ایجنٹس کی ایپلی کیشنز میں کم میموری استعمال کے ساتھ بہترین ہیں، جبکہ Q8_0 ٹیبلیٹ پر مبنی ایجنٹ نظاموں کے لیے متوازن کارکردگی فراہم کرتا ہے۔ Q5_K فارمیٹس موبائل پروڈکٹیویٹی ایجنٹس کے لیے اعلیٰ معیار پیش کرتے ہیں۔

**ڈیسک ٹاپ اور ایج ایجنٹ کمپیوٹنگ**: Q5_K ڈیسک ٹاپ ایجنٹ ایپلی کیشنز کے لیے بہترین کارکردگی دیتا ہے، Q8_0 ورک سٹیشن ایجنٹس کے ماحول میں اعلیٰ معیار کی انفرنس فراہم کرتا ہے، اور Q4_K ایج ایجنٹ ڈیوائسز پر مؤثر پروسسنگ کو ممکن بناتا ہے۔

**تحقیقی اور تجرباتی ایجنٹس**: ترقی یافتہ کوانٹائزیشن فارمیٹس انتہائی کم درستگی کے ایجنٹ انفرنس کو تحقیقی اور پروف آف کانسیپٹ ایپلی کیشنز کے لیے ممکن بناتے ہیں جنہیں سخت وسائل کی پابندیوں کی ضرورت ہوتی ہے۔

### SLM ایجنٹ کی کارکردگی کے معیار

**ایجنٹ انفرنس کی رفتار**: Q4_K موبائل CPUs پر سب سے تیز ایجنٹ ردعمل فراہم کرتا ہے، Q5_K عام ایجنٹ ایپلی کیشنز کے لیے رفتار اور معیار کے درمیان توازن دیتا ہے، Q8_0 پیچیدہ ایجنٹ کاموں کے لیے اعلیٰ معیار پیش کرتا ہے، اور تجرباتی فارمیٹس مخصوص ایجنٹ ہارڈویئر کے لیے زیادہ سے زیادہ تھروپٹ فراہم کرتے ہیں۔

**ایجنٹ کی میموری کی ضروریات**: ایجنٹس کے لیے کوانٹائزیشن کی سطح Q2_K (چھوٹے ایجنٹ ماڈلز کے لیے 500MB سے کم) سے لے کر Q8_0 (اصل سائز کا تقریباً 50%) تک ہوتی ہے، جبکہ تجرباتی کنفیگریشنز وسائل میں محدود ایجنٹ ماحول کے لیے زیادہ سے زیادہ کمپریشن حاصل کرتی ہیں۔

## SLM ایجنٹس کے لیے چیلنجز اور غور و خوض

### ایجنٹ نظاموں میں کارکردگی کے تبادلے

SLM ایجنٹ کی تعیناتی میں ماڈل کے سائز، ایجنٹ کے ردعمل کی رفتار، اور آؤٹ پٹ کے معیار کے درمیان احتیاط سے توازن کرنا ضروری ہے۔ جہاں Q4_K موبائل ایجنٹس کے لیے غیر معمولی رفتار اور مؤثریت فراہم کرتا ہے، وہیں Q8_0 پیچیدہ ایجنٹ کاموں کے لیے اعلیٰ معیار دیتا ہے۔ Q5_K زیادہ تر عام ایجنٹ ایپلی کیشنز کے لیے درمیانی راستہ اختیار کرتا ہے۔

### SLM ایجنٹس کے لیے ہارڈویئر مطابقت

مختلف ایج ڈیوائسز کی SLM ایجنٹ کی تعیناتی کی صلاحیت مختلف ہوتی ہے۔ Q4_K سادہ پروسیسرز پر آسان ایجنٹس کے لیے مؤثر ہے، Q5_K متوازن ایجنٹ کارکردگی کے لیے معتدل کمپیوٹیشنل وسائل کا تقاضا کرتا ہے، اور Q8_0 جدید ایجنٹ صلاحیتوں کے لیے اعلیٰ معیار کے ہارڈویئر سے فائدہ اٹھاتا ہے۔

### SLM ایجنٹ نظاموں میں سیکیورٹی اور پرائیویسی

اگرچہ SLM ایجنٹس بہتر پرائیویسی کے لیے لوکل پروسسنگ کو ممکن بناتے ہیں، لیکن ایج ماحول میں ایجنٹ ماڈلز اور ڈیٹا کی حفاظت کے لیے مناسب سیکیورٹی اقدامات ضروری ہیں۔ یہ خاص طور پر انٹرپرائز ماحول میں اعلیٰ درستگی والے ایجنٹ فارمیٹس یا حساس ڈیٹا کو ہینڈل کرنے والی کمپریسڈ ایجنٹ فارمیٹس کی تعیناتی کے دوران اہم ہے۔

## SLM ایجنٹ ترقی میں مستقبل کے رجحانات

SLM ایجنٹ کا منظرنامہ کمپریشن تکنیکوں، آپٹیمائزیشن طریقوں، اور ایج تعیناتی حکمت عملیوں میں پیش رفت کے ساتھ ترقی کر رہا ہے۔ مستقبل کی ترقیات میں ایجنٹ ماڈلز کے لیے زیادہ مؤثر کوانٹائزیشن الگورتھمز، ایجنٹ ورک فلو کے لیے بہتر کمپریشن طریقے، اور ایجنٹ پروسسنگ کے لیے ایج ہارڈویئر ایکسیلیریٹرز کے ساتھ بہتر انضمام شامل ہیں۔

**SLM ایجنٹس کے لیے مارکیٹ پیش گوئیاں**: حالیہ تحقیق کے مطابق، 2027 تک ایجنٹ سے چلنے والی خودکاریت انٹرپرائز ورک فلو میں 40–60٪ تک دوہرانے والے علمی کاموں کو ختم کر سکتی ہے، جہاں SLM اس تبدیلی کی قیادت کریں گے کیونکہ وہ لاگت مؤثر اور تعیناتی میں لچکدار ہیں۔

**SLM ایجنٹس میں تکنیکی رجحانات**:
- **خصوصی SLM ایجنٹس**: مخصوص ایجنٹ کاموں اور صنعتوں کے لیے تربیت یافتہ ڈومین مخصوص ماڈلز
- **ایج ایجنٹ کمپیوٹنگ**: بہتر پرائیویسی اور کم تاخیر کے ساتھ آن-ڈیوائس ایجنٹ صلاحیتیں
- **ایجنٹ کوآرڈینیشن**: متعدد SLM ایجنٹس کے درمیان بہتر ہم آہنگی، ڈائنامک روٹنگ اور لوڈ بیلنسنگ کے ساتھ
- **جمہوری لحاظ سے**: SLM لچک ایجنٹ ترقی میں تنظیموں کے درمیان وسیع تر شرکت کو ممکن بناتی ہے

## SLM ایجنٹس کے ساتھ شروع کریں

### مرحلہ 1: Microsoft Agent Framework ماحول قائم کریں

**انحصار انسٹال کریں**:
```bash
# مائیکروسافٹ ایجنٹ فریم ورک انسٹال کریں
pip install microsoft-agent-framework

# ایج ڈیپلائمنٹ کے لیے فاؤنڈری لوکل ایس ڈی کے انسٹال کریں
pip install foundry-local-sdk

# ایج ایجنٹس کے لیے اضافی انحصار انسٹال کریں
pip install openai asyncio
```

**Foundry Local کو شروع کریں**:
```bash
# فارنری لوکل سروس شروع کریں
foundry service start

# ایجنٹ کی ترقی کے لیے پہلے سے طے شدہ ماڈل لوڈ کریں
foundry model run phi-4-mini
```

### مرحلہ 2: اپنے ایجنٹ ایپلی کیشنز کے لیے SLM منتخب کریں
Microsoft Agent Framework کے لیے مشہور آپشنز:
- **Microsoft Phi-4 Mini (3.8B)**: عمومی ایجنٹ کاموں کے لیے متوازن کارکردگی کے ساتھ بہترین
- **Qwen2.5-0.5B (0.5B)**: سادہ روٹنگ اور درجہ بندی ایجنٹس کے لیے انتہائی مؤثر
- **Qwen2.5-Coder-0.5B (0.5B)**: کوڈ سے متعلق ایجنٹ کاموں کے لیے خاص
- **Phi-4 (7B)**: وسائل دستیاب ہوں تو پیچیدہ ایج سیناریوز کے لیے اعلیٰ درجے کا استدلال

### مرحلہ 3: Microsoft Agent Framework کے ساتھ اپنا پہلا ایجنٹ بنائیں

**بنیادی ایجنٹ سیٹ اپ**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# فاؤنڈری لوکل کنکشن کو شروع کریں
foundry = FoundryLocalManager("phi-4-mini")

# ایجنٹ کی ترتیب بنائیں
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# ایجنٹ بنائیں اور ترتیب دیں
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# ایک آسان ٹول کی تعریف کریں
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# ایجنٹ کا ٹیسٹ کریں
response = agent.chat("What time is it?")
print(response)
```

### مرحلہ 4: ایجنٹ کا دائرہ کار اور ضروریات طے کریں
Microsoft Agent Framework کے ساتھ مرکوز، واضح ایجنٹ ایپلی کیشنز سے شروع کریں:
- **واحد ڈومین ایجنٹس**: کسٹمر سروس OR شیڈولنگ OR تحقیق
- **واضح ایجنٹ مقاصد**: ایجنٹ کارکردگی کے لیے مخصوص، قابل پیمائش اہداف
- **محدود ٹول انضمام**: ابتدائی ایجنٹ تعیناتی کے لیے زیادہ سے زیادہ 3-5 ٹولز
- **متعین ایجنٹ حدود**: پیچیدہ حالات کے لیے واضح توسیعی راستے
- **ایج فرسٹ ڈیزائن**: آف لائن فعالیت اور لوکل پروسسنگ کو ترجیح دیں

### مرحلہ 5: Microsoft Agent Framework کے ساتھ ایج تعیناتی نافذ کریں

**وسائل کی ترتیب**:
```python
from microsoft_agent_framework import ResourceConfig

# کنارے کی تعیناتی کے لیے ترتیب دیں
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**ایج ایجنٹس کے لیے حفاظتی اقدامات نافذ کریں**:
- **لوکل ان پٹ کی تصدیق**: بادل کی غیر ضرورت کے بغیر درخواستوں کی جانچ کریں
- **آف لائن آؤٹ پٹ فلٹرنگ**: جوابوں کو معیار کے مطابق لوکل طور پر یقینی بنائیں
- **ایج سیکیورٹی کنٹرولز**: انٹرنیٹ کنیکٹیویٹی کی ضرورت کے بغیر سیکیورٹی نافذ کریں
- **لوکل مانیٹرنگ**: ایج ٹیلی میٹری سے کارکردگی کا پتہ لگائیں اور مسائل کی نشاندہی کریں

### مرحلہ 6: ایج ایجنٹ کی کارکردگی کی پیمائش اور بہتر بنائیں
- **ایجنٹ ٹاسک کی تکمیل کی شرحیں**: آف لائن حالات میں کامیابی کی شرحیں مانیٹر کریں
- **ایجنٹ ردعمل کے اوقات**: ایج تعیناتی کے لیے سیکنڈ کے اندر ردعمل کو یقینی بنائیں
- **وسائل کا استعمال**: ایج ڈیوائسز پر میموری، CPU، اور بیٹری کے استعمال کو ٹریک کریں
- **لاگت کی مؤثریت**: ایج تعیناتی کی لاگتوں کا کلاؤڈ متبادل کے ساتھ موازنہ کریں
- **آف لائن قابل اعتمادی**: نیٹ ورک کی بندش کے دوران ایجنٹ کی کارکردگی کی پیمائش کریں

## SLM ایجنٹ نفاذ کے لیے اہم نکات

1. **SLMs ایجنٹس کے لیے کافی ہیں**: زیادہ تر ایجنٹ کاموں کے لیے چھوٹے ماڈلز بڑے ماڈلز جتنے اچھے کام کرتے ہیں اور نمایاں فوائد پیش کرتے ہیں
2. **ایجنٹس میں لاگت کی مؤثریت**: SLM ایجنٹس چلانا 10-30 گنا سستا ہے، جس سے یہ وسیع تعیناتی کے لیے معاشی طور پر قابل عمل بن جاتے ہیں
3. **ایجنٹس کے لیے تخصص کارگر ہے**: باریک سے تربیت یافتہ SLM اکثر مخصوص ایجنٹ ایپلی کیشنز میں عام LLMs سے بہتر کارکردگی دکھاتے ہیں
4. **ہائبرڈ ایجنٹ فن تعمیر**: معمول کے ایجنٹ کاموں کے لیے SLMs، اور جب ضرورت ہو تو پیچیدہ استدلال کے لیے LLMs استعمال کریں
5. **Microsoft Agent Framework پیداواری تعیناتی کو ممکن بناتا ہے**: ایج ایجنٹس کی تعمیر، تعیناتی، اور انتظام کے لیے انٹرپرائز گریڈ ٹولز فراہم کرتا ہے
6. **ایج فرسٹ ڈیزائن اصول**: آف لائن قابل ایجنٹس جو پرائیویسی اور قابل اعتمادی کو لوکل پروسسنگ کے ساتھ یقینی بناتے ہیں
7. **Foundry Local انضمام**: Microsoft Agent Framework اور لوکل ماڈل انفرنس کے درمیان بے جوڑ کنیکٹیویٹی
8. **مستقبل SLM ایجنٹس کا ہے**: پروڈکشن فریم ورک کے ساتھ چھوٹے زبان کے ماڈل ایجنٹک AI کا مستقبل ہیں، جو جمہوری، مؤثر ایجنٹ تعیناتی کو ممکن بناتے ہیں

## حوالہ جات اور مزید مطالعہ

### بنیادی تحقیقی مقالے اور اشاعتیں

#### AI ایجنٹس اور ایجنٹک نظام
- **"Language Agents as Optimizable Graphs"** (2024) - ایجنٹ آرکیٹیکچر اور آپٹیمائزیشن پر بنیادی تحقیق
  - مصنفین: Wenyue Hua, Lishan Yang, وغیرہ
  - لنک: https://arxiv.org/abs/2402.16823
  - اہم بصیرت: گراف پر مبنی ایجنٹ ڈیزائن اور آپٹیمائزیشن کی حکمت عملیاں

- **"The Rise and Potential of Large Language Model Based Agents"** (2023)
  - مصنفین: Zhiheng Xi, Wenxiang Chen, وغیرہ
  - لنک: https://arxiv.org/abs/2309.07864
  - اہم بصیرت: LLM پر مبنی ایجنٹ صلاحیتوں اور ایپلی کیشنز کا جامع جائزہ

- **"Cognitive Architectures for Language Agents"** (2024)
  - مصنفین: Theodore Sumers, Shunyu Yao, وغیرہ
  - لنک: https://arxiv.org/abs/2309.02427
  - اہم بصیرت: ذہین ایجنٹس کے ڈیزائن کے لیے علمی فریم ورک

#### چھوٹے زبان کے ماڈلز اور آپٹیمائزیشن
- **"Phi-3 Technical Report: A Highly Capable Language Model Locally on Your Phone"** (2024)
  - مصنفین: Microsoft Research ٹیم
  - لنک: https://arxiv.org/abs/2404.14219
  - اہم بصیرت: SLM کے ڈیزائن اصول اور موبائل تعیناتی کی حکمت عملیاں

- **"Qwen2.5 Technical Report"** (2024)
  - مصنفین: Alibaba Cloud ٹیم
  - لنک: https://arxiv.org/abs/2407.10671
  - اہم بصیرت: ترقی یافتہ SLM تربیتی تکنیکیں اور کارکردگی کی بہتری

- **"TinyLlama: An Open-Source Small Language Model"** (2024)
  - مصنفین: Peiyuan Zhang, Guangtao Zeng, وغیرہ
  - لنک: https://arxiv.org/abs/2401.02385
  - اہم بصیرت: انتہائی کمپیکٹ ماڈل ڈیزائن اور تربیتی مؤثریت

### سرکاری دستاویزات اور فریم ورکس

#### Microsoft Agent Framework
- **سرکاری دستاویزات**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHub ریپوزٹری**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **پرائمری ریپوزٹری**: https://github.com/microsoft/foundry-local
- **دستاویزات**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **مین ریپوزٹری**: https://github.com/vllm-project/vllm
- **دستاویزات**: https://docs.vllm.ai/


#### Ollama
- **سرکاری ویب سائٹ**: https://ollama.ai/
- **GitHub ریپوزٹری**: https://github.com/ollama/ollama

### ماڈل آپٹیمائزیشن فریم ورکس

#### Llama.cpp
- **ریپوزٹری**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **دستاویزات**: https://microsoft.github.io/Olive/
- **GitHub ریپوزٹری**: https://github.com/microsoft/Olive

#### OpenVINO
- **سرکاری سائٹ**: https://docs.openvino.ai/

#### Apple MLX
- **ریپوزٹری**: https://github.com/ml-explore/mlx

### صنعت کی رپورٹس اور مارکیٹ تجزیہ

#### AI ایجنٹ مارکیٹ ریسرچ
- **"The State of AI Agents 2025"** - McKinsey Global Institute
  - لنک: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - اہم بصیرت: مارکیٹ کے رجحانات اور انٹرپرائز اپنانے کے نمونے

#### تکنیکی معیار

- **"Edge AI Inference Benchmarks"** - MLPerf
  - لنک: https://mlcommons.org/en/inference-edge/
  - اہم بصیرت: ایج تعیناتی کے معیاری کارکردگی میٹرکس

### معیارات اور تفصیلات

#### ماڈل فارمیٹس اور معیارات
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - کراس پلیٹ فارم ماڈل فارمیٹ برائے تعامل پذیری
- **GGUF وضاحت**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - CPU انفرنس کے لیے کوانٹائزڈ ماڈل فارمیٹ
- **OpenAI API وضاحت**: https://platform.openai.com/docs/api-reference
  - زبان ماڈل انضمام کے لیے معیاری API فارمیٹ

#### سیکیورٹی اور تعمیل
- **NIST AI رسک مینجمنٹ فریم ورک**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - AI سسٹمز**: AI سسٹمز اور حفاظتی فریم ورک
- **IEEE معیارات برائے AI**: https://standards.ieee.org/industry-connections/ai/

SLM سے چلنے والے ایجنٹس کی طرف منتقلی AI کی تعیناتی کے طریقہ کار میں ایک بنیادی تبدیلی کی نمائندگی کرتی ہے۔ Microsoft Agent Framework، مقامی پلیٹ فارمز اور مؤثر Small Language Models کے ساتھ، پیداواری طور پر تیار ایجنٹس کی تعمیر کے لیے ایک مکمل حل فراہم کرتا ہے جو ایج ماحول میں مؤثر طریقے سے کام کرتے ہیں۔ مؤثریت، تخصص، اور عملی افادیت پر توجہ دے کر، یہ ٹیکنالوجی اسٹیک AI ایجنٹس کو ہر صنعت اور ایج کمپیوٹنگ ماحول میں حقیقی دنیا کی ایپلی کیشنز کے لیے زیادہ قابل رسائی، قابل برداشت، اور مؤثر بناتی ہے۔

جیسے جیسے ہم 2025 کی طرف بڑھ رہے ہیں، بڑھتے ہوئے قابل چھوٹے ماڈلز، Microsoft Agent Framework جیسے پیچیدہ ایجنٹ فریم ورکس، اور مضبوط ایج تعیناتی پلیٹ فارمز کا مجموعہ خودمختار نظاموں کے لیے نئی ممکنات کھولے گا جو ایج ڈیوائسز پر مؤثر طریقے سے کام کر سکتے ہیں، پرائیویسی کو برقرار رکھتے ہوئے، لاگت کم کرتے ہوئے، اور شاندار صارف تجربات فراہم کرتے ہوئے۔

**نفاذ کے لیے اگلے اقدامات**:
1. **فنکشن کالنگ دریافت کریں**: سیکھیں کہ SLMs ٹول انضمام اور منظم آؤٹ پٹس کو کیسے سنبھالتے ہیں
2. **ماڈل کانٹیکسٹ پروٹوکول (MCP) ماسٹر کریں**: ایجنٹ کے جدید مواصلاتی طریقے سمجھیں
3. **پیداواری ایجنٹس بنائیں**: انٹرپرائز گریڈ تعیناتی کے لیے Microsoft Agent Framework استعمال کریں
4. **ایج کے لیے بہتر بنائیں**: وسائل میں محدود ماحول کے لیے جدید آپٹیمائزیشن تکنیکیں اپنائیں


## ➡️ آگے کیا

- [02: فنکشن کالنگ ان چھوٹے زبان کے ماڈلز (SLMs)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**ڈس کلیمر**:
یہ دستاویز AI ترجمہ سروس [Co-op Translator](https://github.com/Azure/co-op-translator) کے ذریعے ترجمہ کی گئی ہے۔ جبکہ ہم درستگی کے لیے کوشاں ہیں، براہ کرم اس بات سے آگاہ رہیں کہ خودکار ترجمے میں غلطیاں یا عدم درستیاں ہو سکتی ہیں۔ اصل دستاویز اپنے مادری زبان میں مستند ماخذ سمجھی جائے گی۔ حساس معلومات کے لیے پیشہ ور انسانی ترجمہ کی سفارش کی جاتی ہے۔ اس ترجمے کے استعمال سے پیدا ہونے والی کسی بھی غلط فہمی یا غلط تشریح کی ذمہ داری ہم قبول نہیں کرتے۔
<!-- CO-OP TRANSLATOR DISCLAIMER END -->