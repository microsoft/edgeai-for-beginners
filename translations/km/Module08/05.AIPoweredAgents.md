# Session 5: សាងសង់ភ្នាក់ងារដោយ AI ដោយរហ័ស ជាមួយ Foundry Local

Note: សមត្ថភាពភ្នាក់ងារ​នៅក្នុង Foundry Local កំពុងអភិវឌ្ឍ—សូមបញ្ជាក់ការគាំទ្រនៅក្នុងកំណត់ចេញថ្មីៗមុននឹងអនុវត្តន៍គំរូកម្រិតខ្ពស់។

## ទិដ្ឋភាពទូទៅ

ប្រើ Foundry Local ដើម្បីបង្កើតគំរូរហ័សសម្រាប់កម្មវិធីមានភ្នាក់ងារ: prompts ស៊ីស្តែម, grounding, និងគំរូ orchestration។ នៅពេលមានការគាំទ្រភ្នាក់ងារ អ្នកអាចស្តង់ដាដាក់លើការហៅប	function ដែលត្រូវជាមួយ OpenAI-compatible ឬប្រើ Azure AI Agents នៅផ្នែក cloud ក្នុងរចនាសម្ព័ន្ធ hybrid ។

> **🔄 Updated for Modern SDK**: Module នេះបានឲ្យស្របទៅនឹង patterns ចុងក្រោយនៃ repository Microsoft Foundry-Local និងផ្គូផ្គងនឹងការអនុវត្តលម្អិតនៅ `samples/05/`។ ឧទាហរណ៍ឥឡូវនេះប្រើ `foundry-local-sdk` ទំនើប និង client `OpenAI` ជំនួសការស្នើសុំដៃគូ។

**🏗️ Architecture Highlights:**
- **Specialist Agents**: ភ្នាក់ងារប្រភេទកម្រ (Retrieval), ការរំលឹកគំនិត (Reasoning), និងអនុវត្ត (Execution) ដែលមានសមត្ថភាពដាច់ផ្សេង
- **Coordinator Pattern**: អង្គជំនួយក្នុងការគ្រប់គ្រង workflow ពហុ-ភ្នាក់ងារជាមួយដំណើរការផ្ទាល់ត្រលប់យោល
- **Modern SDK Integration**: ប្រើ `FoundryLocalManager` និង client OpenAI
- **Production Ready**: រួមមានការដោះស្រាយកំហុស, ការត្រួតពិនិត្យសមត្ថភាព, និងការតេស្តសុខភាព
- **Comprehensive Examples**: សៀវភៅ Jupyter ដែលអន្តរកម្មមានលក្ខណៈខ្ពស់

**📁 Local Implementation:**
- `samples/05/multi_agent_orchestration.ipynb` - Interactive examples and benchmarks
- `samples/05/agents/specialists.py` - Agent implementations
- `samples/05/agents/coordinator.py` - Orchestration logic

References:
- Foundry Local docs: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- Azure AI Foundry Agents: https://learn.microsoft.com/en-us/azure/ai-services/agents/overview
- Function calling sample (Foundry Local samples): https://github.com/microsoft/Foundry-Local/tree/main/samples/python/functioncalling

## គោលបំណងការសិក្សា
- រៀបចំ prompts ស៊ីស្តែម និងយុទ្ធសាស្ត្រ grounding ដើម្បីឲ្យមានការប្រព្រឹត្តខុសតិច
- អនុវត្ត function calling (ការប្រើឧបករណ៍) ជាគំរូ
- គ្រប់គ្រង workflow ពហុ-ភ្នាក់ងារ (ក្នុងស្រុក និង hybrid)
- រៀបចំ observability និងសុវត្ថិភាព

## ផ្នែក 1: System Prompts និង Grounding

- កំណត់តួនាទី ធាតុសម្ងាត់ និង schemas លទ្ធផលយ៉ាងតឹងរឹង
- ដាក់កន្សោមចម្លើយជាមួយទិន្នន័យក្នុងស្រុកឬទិន្នន័យសហភាពសាជីវកម្ម
- បង្ខោចឲ្យលទ្ធផលនៅក្នុងទ្រង់ទ្រាយ JSON សម្រាប់អូតូម៉ាទ downstream

## ផ្នែក 2: Function Calling (Modern SDK Approach)

```python
# tools.py
import json
from typing import List, Dict, Any

def get_weather(city: str) -> str:
    return f"Weather in {city}: Sunny, 25C"

# ទ្រង់ទ្រាយឧបករណ៍ទំនើបសម្រាប់ API របស់ OpenAI
TOOLS = [
    {
        "type": "function",
        "function": {
            "name": "get_weather",
            "description": "Get current weather for a city",
            "parameters": {
                "type": "object",
                "properties": {
                    "city": {"type": "string", "description": "City name"}
                },
                "required": ["city"]
            }
        }
    }
]
```

```python
# agent.py
from foundry_local import FoundryLocalManager
from openai import OpenAI
import json
from tools import TOOLS, get_weather

# ចាប់ផ្តើមកម្មវិធីគ្រប់គ្រង Foundry Local
alias = "phi-4-mini"
manager = FoundryLocalManager(alias)

# បង្កើត client នៃ OpenAI ដោយប្រើ endpoint របស់ Foundry Local
client = OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key
)

SYSTEM_PROMPT = "You are a helpful assistant. Use tools when needed."

def process_function_call(messages: List[Dict], tools: List[Dict]) -> str:
    """Process function calling with modern OpenAI API."""
    try:
        response = client.chat.completions.create(
            model=manager.get_model_info(alias).id,
            messages=messages,
            tools=tools,
            tool_choice="auto"
        )
        
        message = response.choices[0].message
        
        if message.tool_calls:
            # គ្រប់គ្រងការហៅមុខងារ
            messages.append(message)
            
            for tool_call in message.tool_calls:
                if tool_call.function.name == "get_weather":
                    args = json.loads(tool_call.function.arguments)
                    result = get_weather(args["city"])
                    
                    # បន្ថែមលទ្ធផលនៃមុខងារទៅក្នុងសារ
                    messages.append({
                        "role": "tool",
                        "tool_call_id": tool_call.id,
                        "content": result
                    })
            
            # ទទួលចម្លើយចុងក្រោយ
            final_response = client.chat.completions.create(
                model=manager.get_model_info(alias).id,
                messages=messages
            )
            return final_response.choices[0].message.content
        else:
            return message.content
            
    except Exception as e:
        return f"Error: {str(e)}"

# ឧទាហរណ៍ការប្រើប្រាស់
messages = [
    {"role": "system", "content": SYSTEM_PROMPT},
    {"role": "user", "content": "What's the weather in Paris?"}
]

result = process_function_call(messages, TOOLS)
print(result)
```

រត់:
```powershell
# ​ធ្វើឱ្យប្រាកដថា Foundry Local កំពុងដំណើរការជាមួយម៉ូដែលមួយ
foundry model run phi-4-mini
python agent.py
```

## ផ្នែក 3: Multi-Agent Orchestration (Pattern)

រចនាកូអូឌីណាទ័រមួយដែលផ្តល់មុខងារបញ្ជូនភារកិច្ចទៅភ្នាក់ងារពិសេស (retrieval, reasoning, execution) ដោយប្រើចុងផ្លូវ OpenAI-compatible នៃ Foundry Local។

ជំហាន 1) កំណត់ភ្នាក់ងារពិសេសជាមួយ Modern SDK (មើល `samples/05/agents/specialists.py`)
```python
# agents/specialists.py
from foundry_local import FoundryLocalManager
from openai import OpenAI
from typing import List, Dict, Any

class FoundryClient:
    """Shared client for all specialist agents."""
    
    def __init__(self, model_alias: str = "phi-4-mini"):
        self.client = None
        self.model_name = None
        self.model_alias = model_alias
        self._initialize_client()
    
    def _initialize_client(self):
        """Initialize OpenAI client with Foundry Local."""
        try:
            manager = FoundryLocalManager(self.model_alias)
            model_info = manager.get_model_info(self.model_alias)
            
            self.client = OpenAI(
                base_url=manager.endpoint,
                api_key=manager.api_key
            )
            self.model_name = model_info.id
            print(f"✅ Foundry Local initialized with model: {self.model_name}")
        except Exception as e:
            print(f"❌ Error initializing Foundry Local: {e}")
            raise
    
    def chat(self, messages: List[Dict[str, str]], max_tokens: int = 300, temperature: float = 0.4) -> str:
        """Send chat completion request to the model."""
        try:
            response = self.client.chat.completions.create(
                model=self.model_name,
                messages=messages,
                max_tokens=max_tokens,
                temperature=temperature
            )
            return response.choices[0].message.content
        except Exception as e:
            return f"Error generating response: {str(e)}"

# អុីនស្តង់ client ជាសកល
_client = FoundryClient()

class RetrievalAgent:
    """Agent specialized in retrieving relevant information from knowledge sources."""
    
    SYSTEM = """You are a specialized retrieval agent. Your job is to extract and retrieve 
    the most relevant information from knowledge sources based on a given query. Focus on key facts, 
    data points, and contextual information that would be useful for decision-making."""
    
    def run(self, query: str) -> str:
        """Retrieve relevant information based on the query."""
        messages = [
            {"role": "system", "content": self.SYSTEM},
            {"role": "user", "content": f"Query: {query}\n\nRetrieve the most relevant key facts, data points, and contextual information that would help answer this query or support decision-making around it."}
        ]
        return _client.chat(messages)

class ReasoningAgent:
    """Agent specialized in step-by-step analysis and reasoning."""
    
    SYSTEM = """You are a specialized reasoning agent. Your job is to analyze inputs 
    step-by-step and produce structured, logical conclusions. Break down complex problems 
    into manageable parts and provide clear reasoning for your conclusions."""
    
    def run(self, context: str, question: str) -> str:
        """Analyze context and question to produce structured conclusions."""
        messages = [
            {"role": "system", "content": self.SYSTEM},
            {"role": "user", "content": f"Context:\n{context}\n\nQuestion: {question}\n\nAnalyze this step-by-step and provide a structured, logical conclusion with clear reasoning."}
        ]
        return _client.chat(messages, max_tokens=400)

class ExecutionAgent:
    """Agent specialized in creating actionable execution plans."""
    
    SYSTEM = """You are a specialized execution agent. Your job is to transform decisions 
    and conclusions into concrete, actionable steps. Always format your response as valid JSON 
    with an array of action items. Each action should be specific, measurable, and achievable."""
    
    def run(self, decision: str) -> str:
        """Transform decision into actionable steps in JSON format."""
        messages = [
            {"role": "system", "content": self.SYSTEM},
            {"role": "user", "content": f"Decision/Conclusion:\n{decision}\n\nCreate 3-5 specific, actionable steps to implement this decision. Format as JSON with this structure:\n{{\"actions\": [{{\"step\": 1, \"description\": \"...\", \"priority\": \"high/medium/low\", \"timeline\": \"...\"}}]}}"}
        ]
        return _client.chat(messages, max_tokens=400, temperature=0.3)
```

ជំហាន 2) សង់ coordinator ជាមួយលក្ខណៈខ្ពស់
```python
# agents/coordinator.py
from .specialists import RetrievalAgent, ReasoningAgent, ExecutionAgent
from typing import Dict, Any
import time
import json

class Coordinator:
    """Multi-agent coordinator that orchestrates specialist agents to handle complex tasks."""
    
    def __init__(self):
        """Initialize the coordinator with specialist agents."""
        self.retrieval = RetrievalAgent()
        self.reasoning = ReasoningAgent()
        self.execution = ExecutionAgent()
    
    def handle(self, user_goal: str) -> Dict[str, Any]:
        """
        Orchestrate multiple agents to handle a complex user goal.
        
        Args:
            user_goal: The user's high-level goal or request
            
        Returns:
            Dictionary containing the goal, context, decision, and actions
        """
        print(f"🎯 **Coordinator:** Processing goal: {user_goal}")
        print("=" * 60)
        
        start_time = time.time()
        
        # ជំហាន 1: ទាញយកបរិបទដែលពាក់ព័ន្ធ
        print("📚 **Step 1:** Retrieving context...")
        context = self.retrieval.run(user_goal)
        print(f"   ✅ Context retrieved ({len(context)} chars)")
        
        # ជំហាន 2: វិភាគ និងសន្មត់អំពីបរិបទ
        print("🧠 **Step 2:** Analyzing and reasoning...")
        decision = self.reasoning.run(context, user_goal)
        print(f"   ✅ Analysis completed ({len(decision)} chars)")
        
        # ជំហាន 3: បង្កើតផែនការអនុវត្តដែលអាចអនុវត្តបាន
        print("⚡ **Step 3:** Creating execution plan...")
        actions = self.execution.run(decision)
        print(f"   ✅ Execution plan created ({len(actions)} chars)")
        
        end_time = time.time()
        processing_time = end_time - start_time
        
        result = {
            "goal": user_goal,
            "context": context,
            "decision": decision,
            "actions": actions,
            "agent_flow": ["retrieval", "reasoning", "execution"],
            "processing_time": processing_time,
            "timestamp": time.strftime("%Y-%m-%d %H:%M:%S")
        }
        
        print(f"✅ **Coordination Complete** (⏱️ {processing_time:.2f}s)")
        return result
    
    def handle_with_feedback(self, user_goal: str, feedback_rounds: int = 1) -> Dict[str, Any]:
        """
        Handle a goal with multiple feedback rounds for refinement.
        
        Args:
            user_goal: The user's high-level goal or request
            feedback_rounds: Number of feedback rounds to perform
            
        Returns:
            Dictionary containing the refined result
        """
        result = self.handle(user_goal)
        
        for round_num in range(feedback_rounds):
            print(f"\n🔄 **Feedback Round {round_num + 1}:**")
            print("-" * 40)
            
            # ប្រើភ្នាក់ងារសន្មត់ដើម្បីកែលម្អផែនការអនុវត្ត
            refinement_prompt = f"""
            Original Goal: {user_goal}
            Current Decision: {result['decision']}
            Current Actions: {result['actions']}
            
            Review the above and suggest improvements or refinements to make the execution plan more effective.
            """
            
            refined_decision = self.reasoning.run(result['context'], refinement_prompt)
            refined_actions = self.execution.run(refined_decision)
            
            result['decision'] = refined_decision
            result['actions'] = refined_actions
            result['refinement_rounds'] = round_num + 1
            
            print(f"   ✅ Round {round_num + 1} refinement completed")
        
        return result

def main():
    """Main function demonstrating the multi-agent coordinator."""
    print("🤖 **Multi-Agent Coordinator Demo**")
    print("=" * 50)
    
    # បង្កើតអ្នកសម្របសម្រួល
    coord = Coordinator()
    
    # គោលដៅឧទាហរណ៍
    example_goals = [
        "Create a plan to onboard 5 new customers this month",
        "Develop a strategy to improve team productivity by 20%",
        "Design a customer feedback collection system"
    ]
    
    # ដំណើរការ​ឧទាហរណ៍​ជាមួយ​មតិយោបល់
    goal = example_goals[0]
    print(f"🎯 **Processing Goal:** {goal}")
    print("-" * 50)
    
    try:
        # ដំណើរការមូលដ្ឋាន
        result = coord.handle(goal)
        
        # ជាមួយការកែលម្អដោយមតិយោបល់
        refined_result = coord.handle_with_feedback(goal, feedback_rounds=1)
        
        print("\n📊 **Final Result:**")
        print("=" * 50)
        print(f"**Goal:** {refined_result['goal']}")
        print(f"**Processing Time:** {refined_result['processing_time']:.2f}s")
        
        # ព្យាយាមបកស្រាយសកម្មភាពជា JSON
        try:
            actions_json = json.loads(refined_result['actions'])
            print(f"\n**Formatted Actions:**")
            print(json.dumps(actions_json, indent=2))
        except (json.JSONDecodeError, TypeError):
            print(f"\n**Actions:** {refined_result['actions']}")
            
    except Exception as e:
        print(f"❌ **Error:** {e}")
        print("\nPlease ensure Foundry Local is running with a model loaded.")

if __name__ == "__main__":
    main()
```

ជំហាន 3) ត្រួតពិនិត្យចូល Foundry Local និងរត់ឧទាហរណ៍
```powershell
REM Confirm the local endpoint and model are available
foundry model list
foundry model run phi-4-mini
curl http://localhost:8000/v1/models

REM Run the coordinator from Module08 directory
cd Module08
python -m samples.05.agents.coordinator

REM Or explore the comprehensive Jupyter notebook
jupyter notebook samples/05/multi_agent_orchestration.ipynb
```

> **📚 ឧទាហរណ៍ក្នុងស្រុក:**
> - **ការអនុវត្តន៍សំខាន់**: `samples/05/agents/specialists.py` និង `samples/05/agents/coordinator.py`
> - **ឧទាហរណ៍ពេញលេញ**: `samples/05/multi_agent_orchestration.ipynb`
> - **សេចក្តីណែនាំការតំឡើង**: `samples/05/README.md`
> 
> **🔗 ឧទាហរណ៍ Foundry Local ដែលទាក់ទង:**
> - [Function Calling Sample](https://github.com/microsoft/Foundry-Local/tree/main/samples/python/functioncalling)
> - [Hello Foundry Local](https://github.com/microsoft/Foundry-Local/tree/main/samples/python/hello-foundry-local)

លក្ខណៈណែនាំ:
- អនុវត្ត retries និង timeouts ចន្លោះភ្នាក់ងារ
- បន្ថែមហ្ស្ដGORស្នូលនៅម៉ាស៊ីនមួយ (dict) សម្រាប់ស្ថានភាពសន្ទនា/ចងខ្សែ
- បញ្ចូលកំណត់អត្រា (rate-limiting) when chaining multiple calls

## ផ្នែក 4: Observability និង សុវត្ថិភាព

តាមដាន prompts, ចម្លើយ និងកំហុសនៅក្នុងស្រុក ខណៈពេលរក្សាសុភាពទិន្នន័យនៅក្នុងស្ទាក់ភ្នាក់ងារ។

ជំហាន 1) ការកត់ត្រាស្នើសុំជា light-weight (ជាជម្រើស)

Note: Helper ខាងក្រោមមិនបញ្ចូលដោយលំនាំដើមទេ។ បង្កើត `infra/obs.py` ប្រសិនបើអ្នកចង់ការ logging JSON នៅក្នុងស្រុកសម្រាប់ការពិសោធន៍។
```python
# ហេដ្ឋារចនាសម្ព័ន្ធ/obs.py
import time, json, os
from datetime import datetime

LOG_DIR = os.getenv("FOUNDRY_AGENT_LOG_DIR", "./agent_logs")
os.makedirs(LOG_DIR, exist_ok=True)

def log_event(kind: str, payload: dict):
    ts = datetime.utcnow().strftime("%Y%m%dT%H%M%SZ")
    path = os.path.join(LOG_DIR, f"{ts}_{kind}.json")
    with open(path, "w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)
```

បញ្ចូល logging ទៅក្នុងភ្នាក់ងារ (ជាជម្រើស):
```python
# នៅក្នុង agents/specialists.py បន្ទាប់ពីទទួលមាតិកា
from infra.obs import log_event
# ... នៅក្នុង chat(...)
resp = r.json()
log_event("chat_request", {"endpoint": f"{BASE_URL}/v1/chat/completions"})
log_event("chat_response", resp)
return resp["choices"][0]["message"]["content"]
```

ជំហាន 2) ត្រួតពិនិត្យភាពមានស្រាប់ និងសុខភាពមូលដ្ឋានតាម CLI
```powershell
REM Ensure Foundry Local is running a model
foundry model list
foundry model run phi-4-mini

REM Validate the OpenAI-compatible endpoint
curl http://localhost:8000/v1/models
```

ជំហាន 3) ការលុបចោលព័ត៌មានងងឹត និងសុភាព PII
- មុននឹងបញ្ជូនសារ​ទៅម៉ូដែល កាត់ចោល ឬ hash 필ឯកសារសំងាត់ (អ៊ីមែល លេខទូរស័ព្ទ អត្តសញ្ញាណ)
- រក្សាទិន្នន័យដើមនៅលើឧបករណ៍ ដើម្បីផ្ដល់តែខ្សែ context ដែលចាំបាច់តែប៉ុណ្ណោះ

ឧទាហរណ៍ helper សម្រាប់ redaction:
```python
# ហេដ្ឋារចនាសម្ព័ន្ធ/កែសម្រួល.py
import re
EMAIL_RE = re.compile(r"[\w\.-]+@[\w\.-]+")
PHONE_RE = re.compile(r"\+?\d[\d\s\-]{7,}\d")

def sanitize(text: str) -> str:
    text = EMAIL_RE.sub("[REDACTED_EMAIL]", text)
    text = PHONE_RE.sub("[REDACTED_PHONE]", text)
    return text
```

ប្រើក្នុងភ្នាក់ងារ:
```python
from infra.redact import sanitize
# user_goal = សម្អាត(user_goal)
# context = សម្អាត(context)
```

ជំហាន 4) Circuit breakers និងការដោះស្រាយកំហុស
- ចកក្រោមនៅលើការហៅភ្នាក់ងារ រាំងខ្ទប់ try/except និង exponential backoff
- ត្រួតពិនិត្យ pipeline នៅលើបរាជ័យស្ទើរតែជាបន្តបន្ទាប់

```python
import time

def with_retry(func, retries=3, base_delay=0.5):
    for i in range(retries):
        try:
            return func()
        except Exception as e:
            if i == retries - 1:
                raise
            time.sleep(base_delay * (2 ** i))
```

ជំហាន 5) ម៉ាស្សា audit ផ្ទាល់ស្រុក និងការបញ្ជូនចេញ
- រក្សាកំណត់ហេតុនៅក្នុង JSON ក្រោម `./agent_logs`
- ធ្វើការបង្មង់ និងបង្ហោះកំណត់ហេតុជាថ្ងៃចំណាត់
- នាំចេញសេចក្តីសង្ខេបសម្រាប់ការពិនិត្យ (ចំនួន, លំនាំម៉ោងមធ្យម, អត្រាកំហុស)

ជំហាន 6) ពិនិត្យមើលជាថ្មីជាមួយឯកសារ Microsoft Learn
- Foundry Local ផ្តល់ API ដែលស្របជាមួយ OpenAI (បានផ្ទៀងផ្ទាត់ជាមួយ `curl /v1/models`)
- ប្រើ `foundry model run <name>` ដើម្បីបញ្ជាក់ភាពមាននៃម៉ូដែល
- ធ្វើតាមដំណើរការផ្លូវការសម្រាប់ client integration និង sample apps (Open WebUI/how-tos)

References
- **Foundry Local Documentation**: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- **Azure AI Agents**: https://learn.microsoft.com/en-us/azure/ai-services/agents/overview
- **Local Samples**:
  - Multi-Agent Orchestration: `Module08/samples/05/multi_agent_orchestration.ipynb`
  - Agent Implementation: `Module08/samples/05/agents/`
  - Sample README: `Module08/samples/05/README.md`
- **Official Microsoft Samples**:
  - [Function Calling](https://github.com/microsoft/Foundry-Local/tree/main/samples/python/functioncalling)
  - [Hello Foundry Local](https://github.com/microsoft/Foundry-Local/tree/main/samples/python/hello-foundry-local)
  - [Foundry Local Python SDK](https://github.com/microsoft/Foundry-Local/tree/main/sdk/python)
- **Integration Examples**: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-chat-application-with-open-web-ui

## ជំហានបន្ទាប់
- ស្វែងយល់អំពី Azure AI Agents សម្រាប់ orchestration ដែលផ្ទុកនៅលើ cloud
- បន្ថែម connectors សហភាព (Microsoft Graph, Search, databases)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Disclaimer**:
ឯកសារនេះត្រូវបានបកប្រែដោយប្រើសេវាបកប្រែបញ្ញាសិប្បនិម្មិត [Co-op Translator](https://github.com/Azure/co-op-translator)។ ខណៈពេលដែលយើងខិតខំដើម្បីឲ្យបានត្រឹមត្រូវ សូមយល់ថាការបកប្រែដោយស្វ័យប្រវត្តិក៏អាចមានកំហុស ឬភាពមិនត្រឹមត្រូវបាន។ ឯកសារដើមក្នុងភាសាមូលដ្ឋានគួរត្រូវបានចាត់ទុកជាប្រភពដែលមានសុពលភាព។ សម្រាប់ព័ត៌មានសំខាន់ៗ សូមពិចារណាបកប្រែដោយអ្នកវិជ្ជាជីវៈមនុស្ស។ យើងមិនទទួលខុសត្រូវចំពោះការយល់ច្រឡំ ឬការបកស្រាយខុសណាមួយ ដែលកើតមានពីការប្រើប្រាស់ការបកប្រែនេះទេ។
<!-- CO-OP TRANSLATOR DISCLAIMER END -->