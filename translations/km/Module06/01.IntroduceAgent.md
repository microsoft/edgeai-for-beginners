# អ្នកភ្នាស់ AI និងម៉ូឌែលភាសាចម្រុះតូច៖ មគ្គុទេសក៍គ្រប់គ្រាន់

## ប្រឹក្សាបើក

នៅក្នុងមេរៀននេះ យើងនឹងសិក្សាពីអ្នកភ្នាស់ AI និងម៉ូឌែលភាសាចម្រុះតូច (SLMs) និងយុទ្ធសាស្ត្រអនុវត្តតწadvancedសម្រាប់បរិបទគណនា edge។ យើងនឹងគ្របដណ្តប់លើគំនិតមូលដ្ឋាននៃ AI agentic, បច្ចេកទេសធ្វើ​ឲ្យ SLMs អាចប្រើបានល្អបំផុត ក្របខណ្ឌអនុវត្តន៍សម្រាប់ឧបករណ៍ដែលដាក់ប្រាក់តិចនិង Microsoft Agent Framework សម្រាប់សាងសង់ប្រព័ន្ធអ្នកភ្នាស់ដែលអាចប្រើបានក្នុងផលិតកម្ម។

មហាសមុទ្រផ្ទៃ AI កំពុងមានការប្រែក្លាយជាស្ទីលដំណើរការថ្មីនៅឆ្នាំ ២០២៥។ ខណៈដែលឆ្នាំ ២០២៣ គឺជាឆ្នាំនៃ chatbots និងឆ្នាំ ២០២៤ គឺជាបរិបទនៃ copilots ឆ្នាំ ២០២៥ គឺជាឆ្នាំនៃអ្នកភ្នាស់ AI — ប្រព័ន្ធឆ្លាតវៃដែលគិត ធ្វើការវិភាគ ផែនការ ប្រើឧបករណ៍ និងបំពេញភារកិច្ចដោយមានការចូលរួមពីមនុស្សអប្បបរមា ដោយផ្អែកលើម៉ូឌែលភាសាចម្រុះតូចដែលមានប្រសិទ្ធភាពកើនឡើង។ Microsoft Agent Framework គឺជាកម្មវិធីដ៏សំខាន់សម្រាប់សាងសង់ប្រព័ន្ធឆ្លាតវៃទាំងនេះមានសមត្ថភាពប្រើបាននៅផ្នែក edge ហើយដំណើរការបើកបរ Offline។

## គោលបំណងសិក្សា

នៅចុងបញ្ចប់មេរៀននេះ អ្នកនឹងអាច៖

- 🤖 យល់ដឹងពីគំនិតមូលដ្ឋាននៃអ្នកភ្នាស់ AI និងប្រព័ន្ធ agentic
- 🔬 កំណត់គុណសម្បត្តិរបស់ម៉ូឌែលភាសាចម្រុះតូចទល់នឹងម៉ូឌែលភាសាធំក្នុងការអនុវត្ត agentic
- 🚀 រៀនយុទ្ធសាស្ត្រអនុវត្ត SLM ដែលស្រួលសម្រាប់បរិបទគណនា edge
- 📱 អនុវត្តអ្នកភ្នាស់ដែលដំណើរការដោយ SLM សម្រាប់ការអនុវត្តជាក់ស្តែង
- 🏗️ កសាងអ្នកភ្នាស់ដែលរួចរាល់សម្រាប់ផលិតកម្មដោយប្រើ Microsoft Agent Framework
- 🌐 បង្ហោះអ្នកភ្នាស់ដែលដំណើរការបើកបរ Offline នៅផ្នែក edge ជាមួយការរួមបញ្ចូល LLM និង SLM ក្នុងតំបន់មូលដ្ឋាន
- 🔧 រួមបញ្ចូល Microsoft Agent Framework ជាមួយ Foundry Local សម្រាប់បង្ហោះនៅផ្នែក edge

## យល់ដឹងពីអ្នកភ្នាស់ AI: គ្រឹះនិងចំណាត់ថ្នាក់

### ការបកស្រាយនិងគំនិតមូលដ្ឋាន

អ្នកភ្នាស់បញ្ញាសិប្បនិម្មិត (AI) អនុញ្ញាតឲ្យប្រព័ន្ធឬកម្មវិធីអាចអនុវត្តភារកិច្ចដោយឯងឯងសម្រាប់អ្នកប្រើ ឬប្រព័ន្ធផ្សេងទៀតដោយរៀបចំការប្រតិបត្ដិការដំណើរការនិងប្រើប្រាស់ឧបករណ៍ដែលមាន។ ខុសពី AI បុរាណដែលត្រឹមត្រូវតបសំណួររបស់អ្នក អ្នកភ្នាស់អាចបំពេញភារកិច្ចដោយឯងឯងដើម្បីសំរេចគោលដៅ។

### ប្រព័ន្ធចំណាត់ថ្នាក់អ្នកភ្នាស់

ការយល់ដឹងពីព្រំប្រទល់អ្នកភ្នាស់ជួយក្នុងការជ្រើសរើសប្រភេទអ្នកភ្នាស់សមរភាពសម្រាប់ស្ថានភាពគណនាតាមបរិបទផ្សេងៗ៖

- **🔬 អ្នកភ្នាស់ប្រតិកម្មអាចទ្រង់ទ្រាយ**៖ ប្រព័ន្ធសម្រាប់ចាប់តបភ្លាមៗទៅការយល់ដឹង (តេស្តសីតុណ្ហភាព, ឧបករណ៍ស្វ័យប្រវត្តិមូលដ្ឋាន)
- **📱 អ្នកភ្នាស់ម៉ូឌែលមូលដ្ឋាន**៖ ប្រព័ន្ធរក្សារស្ថានភាព និងអង្គចងចាំក្នុង (មួយចំនួនម៉ាស៊ីនបូមស្ពុត, ប្រព័ន្ធនាវេហ្គេស្យុង)
- **⚖️ អ្នកភ្នាស់មានគោលដៅ**៖ ប្រព័ន្ធបញ្ចុះបញ្ចូល និងអនុវត្តខ្សែបន្ទាត់ដើម្បីសម្រេចគោលដៅ (អ្នករៀបចំផ្លូវ, អ្នកកំណត់កាលវិភាគ)
- **🧠 អ្នកភ្នាស់រៀនបាន**៖ ប្រព័ន្ធដែលអាចបន្ថែមការសម្រួលកំណើនប្រសិទ្ធភាព (ប្រព័ន្ធផ្តល់អនុសាសន៍, ជំនួយផ្ទាល់ខ្លួន)

### គុណសម្បត្តិសំខាន់ៗរបស់អ្នកភ្នាស់ AI

អ្នកភ្នាស់ AI ផ្តល់ជូននូវគុណសម្បត្តិមូលដ្ឋានជាច្រើនដែលធ្វើអោយពួកគេដ៏សមរភាពសម្រាប់កម្មវិធីគណនាតាម edge៖

**អនុភាពគ្រប់គ្រងដោយខ្លួនឯង**៖ អ្នកភ្នាស់អាចអនុវត្តភារកិច្ចដោយឯករាជ្យដោយគ្មានការត្រួតពិនិត្យមនុស្សជាប្រចាំ ទៅនឹងកម្មវិធីពេលវេលាពិត។ ពួកគេត្រូវការត្រួតពិនិត្យច្រើនតិចបំផុតនៅពេលដែលរក្សាប្រសិទ្ធភាពនៃអាកប្បកិរិយា មានសមត្ថភាពដំឡើងលើឧបករណ៍ដែលមានធនធានកំណត់ជាមួយផាសុកភាព​នៅការប្រើប្រាស់។

**ភាពបត់បែនក្នុងការដំឡើង**៖ ប្រព័ន្ធទាំងនេះអាចធ្វើឱ្យមានសមត្ថភាព AI នៅលើឧបករណ៍ដោយមិនចាំបាច់តភ្ជាប់អ៊ីនធើណិត បង្កើនភាពឯកជន និងសុវត្ថិភាពតាមរយៈការដំណើរការផ្ទាល់ក្នុងតំបន់ និងអាចប្តូរជាប្រភេទសម្រាប់កម្មវិធីជាក់លាក់នៅក្នុងវិស័យផ្សេងៗ និងសម្រួលបានជាច្រើនបរិបទគណនា edge។

**សមត្ថភាពតម្លៃសមរម្យ**៖ ប្រព័ន្ធអ្នកភ្នាស់ផ្តល់ជូនការតម្លើងដែលមានតម្លៃសមរម្យជាងដំណោះស្រាយបណ្ដាញពពក ជាមួយការកាត់បន្ថយថ្លៃដើមបរិច្ចាគ និងតម្រូវការចរន្តទិន្នន័យតិចសម្រាប់កម្មវិធី edge។

## យុទ្ធសាស្រ្តម៉ូឌែលភាសាចម្រុះតូចកម្រិតខ្ពស់

### គ្រឹះម៉ូឌែលភាសាចម្រុះតូច (SLM)

ម៉ូឌែលភាសាចម្រុះតូច (SLM) គឺជា​ម៉ូឌែលភាសាដែលអាចប្រើបានលើឧបករណ៍អេឡិចត្រូនិកស៊ីវិលសាកល្បង និងអាចបំពេញការប៉ាន់ស្មានដោយមានពេលយូរពាក់កណ្តាលតូចគ្រប់គ្រាន់សម្រាប់ដំណើរការសំណើ agentic មួយអ្នកប្រើ។ ក្នុងការអនុវត្តជាក់ស្តែង SLM នៅជាទូទៅមានចំនួនពារ៉ាម៉ែត្រតិចជាង ១០ ពិបាក។

**បង្រ្កាបលក្ខណៈមុខងារ**៖ SLM ផ្តល់ការគាំទ្រលំដាប់ខ្ពស់សម្រាប់កម្រិត quantization ផ្សេងៗ ការសម្របសម្រួលចុះឆ្លងប្លាស្តហ្វមការងារ ការបង្កើនប្រសិទ្ធភាពពិតប្រាកដ និងសមត្ថភាពដំឡើង edge។ អ្នកប្រើអាចទទួលបានភាពឯកជនកាន់តែល្អតាមរយៈការប្រតិបត្ដិការផ្ទាល់ជាមួយការគាំទ្រពី WebGPU សម្រាប់ការបង្ហោះលើកម្មវិធីគេហទំព័រ។

**បណ្ណាល័យកម្រិត Quantization**៖ ទ្រង់ទ្រាយ SLM ដ៏គួរឱ្យកត់សម្គាល់រួមមាន Q4_K_M សម្រាប់ការបង្ហោះជាមួយការចុះបញ្ជាក់ទន់ សម្រាប់កម្មវិធីចល័ត Q5_K_S សម្រាប់ការតម្រឹមគុណភាពនៅភាគងារផ្ទាល់ Q8_0 សម្រាប់ភាពត្រឹមត្រូវនៅក្បែរដើមលើឧបករណ៍ edge មានអំណាច និងទ្រង់ទ្រាយប្រតិបត្តិការជាក់ស្តែងដូចជា Q2_K សម្រាប់ស្ថានភាពធនធានទាបជានិច្ច។

### GGUF (ទ្រង់ទ្រាយអេក្រង់អភិបាលតាមមូលដ្ឋាន GGML) សម្រាប់ការដំឡើង SLM

GGUF ជាទ្រង់ទ្រាយសំខាន់សម្រាប់ដំឡើង SLM ដែលត្រូវបាន quantize លើ CPU និងឧបករណ៍ edge ដែលបានបង្កើតរចនា​សម្រាប់កម្មវិធី agentic៖

**មុខងារតំរូវសម្រាប់អ្នកភ្នាស់**៖ ទ្រង់ទ្រាយនេះផ្តល់ធនធានទូលំទូលាយសម្រាប់ការបម្លែងនិងដំឡើង SLM ជាមួយការគាំទ្រការហៅឧបករណ៍ ការបង្កើតលទ្ធផលតាមរចនាសម្ព័ន្ធ និងការសន្ទនាច្រើនជំហាន។ ការគាំទ្រចូលគ្នាតាមប្លាស្តហ្វមធ្វើឲ្យអ្នកភ្នាស់មានអាកប្បកិរិយាក្នុងឧបករណ៍ edge ផ្សេងៗឲ្យបន្តទៀត។

**ការបង្កើនប្រសិទ្ធភាពបំពេញការងារ**៖ GGUF អាចប្រើប្រសិទ្ធភាពម៉េចអាមរីយ៍រស្មីសម្រាប់លំហូរឆ្នាំងអ្នកភ្នាស់ រួមទាំងគាំទ្រការផ្ទុកម៉ូឌែលឧទ្ទិសសម្រាប់ប្រព័ន្ធអ្នកភ្នាស់ច្រើន និងផ្តល់ការប៉ាន់ស្មានដែលមានប្រសិទ្ធភាពវិញសម្រាប់ការប្រាសាការផ្ទាល់ពេលវេលាពិត។

### ក្របខណ្ឌ SLM ឡើងវិញសម្រាប់ Edge

#### ការបង្កើនប្រសិទ្ធភាព Llama.cpp សម្រាប់អ្នកភ្នាស់

Llama.cpp ផ្តល់នូវបច្ចេកទេសចុះបញ្ជាក់ថ្មីដែលបានបង្កើតឡើងពិសេសសម្រាប់ការដំឡើង SLM ក្នុងប្រព័ន្ធអ្នកភ្នាស់៖

**ការចុះបញ្ជាក់សម្រាប់អ្នកភ្នាស់**៖ ក្របខណ្ឌគាំទ្រ Q4_0 (បំបែកទំហំចំនួន ៧៥% សម្រាប់ការដំឡើងនៅលើទូរស័ព្ទចល័ត) Q5_1 (តម្រឹមគុណភាព និងការចុះបញ្ជាក់ទន់សម្រាប់ agent inference នៅ edge) និង Q8_0 (គុណភាពក្បែរដើមសម្រាប់ប្រព័ន្ធផលិតកម្មអ្នកភ្នាស់)។ ទ្រង់ទ្រាយកម្រិតខ្ពស់ជួយបង្កើតអ្នកភ្នាស់ដែលបានចុះបញ្ជាក់យ៉ាងខ្លាំងសម្រាប់ស្ថានភាព edge ខ្ពស់។

**អត្ថប្រយោជន៍អនុវត្តន៍**៖ ការប៉ាន់ស្មានប្រសិទ្ធភាព CPU ជាមួយ SIMD ជួយអាចដំណើរការបានមេម៉ូរីមានសមត្ថភាពខ្លាំង។ ការគាំទ្រចូលគ្នានៅលើស្ថាពរអាគារដូចជា x86, ARM និង Apple Silicon អនុញ្ញាតឲ្យដំឡើងអ្នកភ្នាស់យ៉ាងទូលំទូលាយ។

#### Apple MLX Framework សម្រាប់អ្នកភ្នាស់ SLM

Apple MLX ផ្តល់នូវការបង្កើនប្រសិទ្ធភាពដើមដែលបានបង្កើតឡើងពិសេសសម្រាប់អ្នកភ្នាស់ដំណើរការដោយ SLM លើឧបករណ៍ Apple Silicon៖

**ការបង្កើនប្រសិទ្ធភាពApple Silicon SLM Agents**៖ ក្របខណ្ឌប្រើប្រាស់ស្ថាបត្យកម្ម memory រួមបញ្ចូលបានជាមួយ Metal Performance Shaders, ការប្រើប្រាស់ mixed precision ដើម្បីបង្កើនសមត្ថភាព agent និងការបង្កើត memory bandwidth សម្រាប់ប្រព័ន្ធ multi-agent។ អ្នកភ្នាស់ SLM បង្ហាញពីប្រសិទ្ធភាពល្អលើ chip M-series។

**មុខងារលើកកម្ពស់ការអភិវឌ្ឍ**៖ ការគាំទ្រ API Python និង Swift ជាមួយអនុវត្តការបញ្ចូល agent, ការប្រើប្រាស់ autodiff សម្រាប់រៀន agent និងការរួមបញ្ចូលដោយរលូនជាមួយឧបករណ៍អភិវឌ្ឍលើ Apple ផ្តល់សំណុំបរិយាកាសអភិវឌ្ឍ agent ដ៏ទូលំទូលាយ។

#### ONNX Runtime សម្រាប់អ្នកភ្នាស់ SLM លើប្លាស្តហ្វមឆ្លង

ONNX Runtime ផ្តល់វេនបោក universal សម្រាប់ធ្វើ inference ដែលអនុញ្ញាតឲ្យអ្នកភ្នាស់ SLM រត់បានយ៉ាងរលូនលើវាស់ហេដ្ឋារចនាសម្ព័ន្ធនិងប្រព័ន្ធប្រតិបត្តិការផ្សេងៗ៖

**ការបង្ហោះអនុគ្រោះ**៖ ONNX Runtime បញ្ជាក់ការប្រព្រឹត្តគ្នានៃអាកប្បកិរិយា SLM agents លើ Windows, Linux, macOS, iOS និងប្លាស្តហ្វម Android។ ការគាំទ្រចូលគ្នាឆ្លងប្លាស្តហ្វមនេះយកឲ្យអ្នកអភិវឌ្ឍរួមសរសេរមួយដងហើយដំឡើងឲ្យគ្រប់ទី ព្យាករណ៍និងថែទាំតិចសម្រាប់កម្មវិធីច្រើនប្លាស្តហ្វម។

**ជម្រើសការញឹតល្បឿនកម្មវិធីបញ្ចូលរឹង**៖ ក្របខណ្ឌនេះផ្តល់ជូនដោយអ្នកផ្គត់ផ្គង់ការប្រតិបត្ដិល្បឿនល្អសម្រាប់កំណត់រចនាសម្ព័ន្ធរឹងដូចជា CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm), និង accelerators ជាក់លាក់ (Intel VPU, Qualcomm NPU)។ អ្នកភ្នាស់ SLM អាចប្រើជាប្រព័ន្ធរឹងល្អបំផុតដោយឥតបំលែងកូដ។

**មុខងារសម្រាប់ផលិតកម្ម**៖ ONNX Runtime ផ្តល់ជូនមុខងារថ្នាក់សហគ្រាសដែលចាំបាច់សម្រាប់ការបង្ហោះក្នុងផលិតកម្មរួមមានការបង្កើតក្រាហ្វិកសម្រួលសម្រាប់ការតបអារម្មណ៍រហ័ស ការគ្រប់គ្រងម៉េម៉ូរីសម្រាប់បរិបទធនធានកំណត់ ហើយមានឧបករណ៍ទិន្នន័យសម្រាប់វិភាគការប្រតិបត្ដិការណ៍។ ក្របខណ្ឌគាំទ្រពី API Python និង C++ សម្រាប់ការរួមបញ្ចូលបត់បែន។

## ការប្រៀបធៀប SLM និង LLM ក្នុងប្រព័ន្ធ agentic: ការសន្ទនា​កម្រិតខ្ពស់

### គុណសម្បត្តិ SLM ក្នុងកម្មវិធី agent

**ប្រសិទ្ធភាពបំពេញការងារ**៖ SLM ផ្តល់នូវការកាត់បន្ថយថ្លៃ ១០-៣០ ដងបើប្រៀបធៀបនឹង LLM សម្រាប់ភារកិច្ចអ្នកភ្នាស់ ធ្វើឲ្យមានការឆ្លើយតប agentic ពេលវេលាពិតក្នុងកម្រិតធំ។ ពួកគេចាប់យកការប៉ាន់ស្មានបានលឿនដោយសារប្រភេទគណនា​តិចបន្ថែម ធ្វើឲ្យពួកគេសមរម្យសម្រាប់កម្មវិធី agent interactive។

**សមត្ថភាពដំឡើងនៅ Edge**៖ SLM អាចដំណើរការប្រព័ន្ធ agent លើឧបករណ៍ដោយមិនចាំបាច់អ៊ីនធើណិត ការការពារភាពឯកជនតាមការព្យួរកម្មវិធីភ្នាស់ក្នុងតំបន់ និងការតម្រឹមសម្រាប់កម្មវិធី agent នៅវិស័យជាក់លាក់នៅបរិបទគណនា edge ផ្សេងៗ។

**ការបង្កើនប្រសិទ្ធភាព agent-specific**៖ SLM ច្បាស់លាស់លើការហៅឧបករណ៍ ការបង្កើតលទ្ធផលចង់បាន និងលំហូរការទិញសំរាប់សេចក្តីសម្រេចចិត្តដែលធ្វើជាប្រហែល ៧០-៨០% នៃភារកិច្ចប្រចាំអ្នកភ្នាស់។

### ពេលណាចាំបាច់ប្រើ SLM ត្រូវធៀបដែល LLM នៅប្រព័ន្ធ agent

**សមស្របសម្រាប់ SLM**៖  
- **ភារកិច្ចលៃតម្រូវ**ៈ ការបញ្ចូលទិន្នន័យ, បំពេញសំណុំបែបបទ, ការហៅ API គន្លង  
- **ការរួមបញ្ចូលឧបករណ៍**៖ សំណួរទិន្នន័យ, ប្រតិបត្តិការឯកសារ, អន្តរកម្មប្រព័ន្ធ  
- **លំហូរការងារតាមរចនាសម្ព័ន្ធ**៖ ធ្វើតាមដំណើរការស្រៀមរួច  
- **អ្នកភ្នាស់តំបន់វិស័យជាក់លាក់**៖ សេវាអតិថិជន, រៀបចំកាលវិភាគ, វិភាគមូលដ្ឋាន  
- **ប្រតិបត្ដិការផ្ទាល់មូលដ្ឋាន**៖ អ្នកភ្នាស់សម្រាប់ការការពារភាពឯកជន

**ល្អប្រសើរជាងសម្រាប់ LLM**៖  
- **ការអភិវឌ្ឍន៍គំនិតចម្រុះ**៖ លំហាត់បញ្ហាថ្មី, ផែនការយុទ្ធសាស្ត្រ  
- **ការសន្ទនាធំទូលាយ**៖ ជជែកទូទៅ, ការពិភាក្សាសិល្បៈ  
- **ភារកិច្ចអំណាចចំណេះដឹងធំ**៖ ស្រាវជ្រាវដែលតម្រូវចំណេះទូទៅដ៏ទូលំទូលាយ  
- **ស្ថានភាពថ្មីទាំងស្រុង**៖ ដោះស្រាយស្ថានភាពអ្នកភ្នាស់ថ្មីៗ

### រចនាសម្ព័ន្ធអ្នកភ្នាស់ផ្សំ

វិធីសាស្ត្រល្អបំផុតគឺរួមបញ្ចូល SLM និង LLM ក្នុងប្រព័ន្ធ agentic មិនស្មើស្មើ៖

**ការ​គ្រប់គ្រងភ្នាស់ឆ្លាតវៃ**៖  
1. **SLM ជាធំ**៖ ដោះស្រាយ ៧០-៨០% ភារកិច្ចបំរើប្រចាំនៅក្នុងតំបន់  
2. **LLM ពេលចាំបាច់**៖ ផ្ញើសំណួរលំបាកទៅម៉ូឌែលធំផ្អែកលើពពក  
3. **SLM ជាពិសេស**៖ ម៉ូឌែលតូចផ្សេងៗសម្រាប់វិស័យអ្នកភ្នាស់ផ្សេងៗ  
4. **បង្កើនការចំណាយមានប្រសិទ្ធភាព**៖ កាត់បន្ថយការហៅ LLM ថ្លៃថ្នល់តាមរយៈបច្ចេកទេស routing ឆ្លាតវៃ

## យុទ្ធសាស្ត្រដំឡើងអ្នកភ្នាស់ SLM សម្រាប់ផលិតកម្ម

### Foundry Local: រត់ម៉ាស៊ីន AI Edge ថ្នាក់សហគ្រាស

Foundry Local (https://github.com/microsoft/foundry-local) គឺជាកម្មវិធីដឹកនាំរបស់ Microsoft សម្រាប់ដំឡើង Small Language Models នៅបរិបទ edge ក្នុងផលិតកម្ម។ វាចូលរួមបន្ទប់រត់ម៉ាស៊ីនពេញលេញដែលបានបង្កើតសម្រាប់អ្នកភ្នាស់ដំណើរការដោយ SLM ជាមួយមុខងារ​ថ្នាក់សហគ្រាសនិងសមត្ថភាពរួមបញ្ចូលល្អ។

**រចនាសម្ព័ន្ធមូលដ្ឋាននិងមុខងារ**:  
- **API តំណាងស្របនឹង OpenAI**៖ សមរម្យពេញលេញជាមួយ SDK និងក្របខណ្ឌអ្នកភ្នាស់  
- **ការបង្កើនប្រសិទ្ធភាពរឹងដោយស្វ័យប្រវត្តិ**៖ ជ្រើសរើសម៉ូឌែលដោយផ្អែកលើអេក្រង់រឹងមាន CUDA GPU, Qualcomm NPU, CPU  
- **ការគ្រប់គ្រងម៉ូឌែល**៖ ទាញយក បត់ចង និងគ្រប់គ្រងជីវិតម៉ូឌែល SLM ដោយស្វ័យប្រវត្តិ  
- **ការរកឃើញសេវា**៖ ការរកឃើញសេវាជាមួយកម្រិតសូន្យកំណត់សម្រាប់ក្របខណ្ឌភ្នាស់  
- **ការបង្កើនប្រសិទ្ធភាពធនធាន**៖ ការគ្រប់គ្រងម៉េចអាមរីយ៍និងប្រសិទ្ធភាពថាមពលសម្រាប់ការដំឡើង edge

#### ការដំឡើងនិងចាប់ផ្ដើម

**ការដំឡើងឆ្លងប្លាស្តហ្វម**:  
```bash
# វីនដូ (ផ្ដល់អនុសាសន៍)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# លីនុច (ដំឡើងដោយដៃ)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```
  
**ចាប់ផ្ដើមលឿនសម្រាប់អភិវឌ្ឍអ្នកភ្នាស់**:  
```bash
# ចាប់ផ្តើមសេវាកម្មជាមួយការផ្ទុកម៉ូដែលស្វ័យប្រវត្តិ
foundry model run phi-4-mini

# ផ្ទៀងផ្ទាត់ស្ថានភាពសេវាកម្ម និងចំណុចបញ្ចប់
foundry service status

# បញ្ជីម៉ូដែលដែលមាន
foundry model ls

# សាកល្បងចំណុចបញ្ចប់ API
curl http://localhost:<port>/v1/models
```
  
#### ការរួមបញ្ចូលក្របខណ្ឌអ្នកភ្នាស់

**រួមបញ្ចូល Foundry Local SDK**:  
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# បង្កើត Foundry Local ជាមួយការគ្រប់គ្រងសេវាកម្មស្វ័យប្រវត្តិ
manager = FoundryLocalManager("phi-4-mini")

# កំណត់រចនាសម្ព័ន្ធខល OpenAI សម្រាប់ការអនុវត្តនៅក្នុងតំបន់
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # បង្កើតដោយស្វ័យប្រវត្តិសម្រាប់ការប្រើប្រាស់ក្នុងតំបន់
)

# បង្កើតភ្នាក់ងារជាមួយ Foundry Local backend
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```
  
**ជ្រើសរើសម៉ូឌែលដោយស្វ័យប្រវត្តិនិងបង្កើនប្រសិទ្ធភាពរឹង**:  
```python
# Foundry Local ជ្រើសរើសម៉ូឌែលប្រភេទល្អបំផុតដោយស្វ័យប្រវត្តិ
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, លឿនពេក
    "general_conversation": "phi-4-mini",       # 2.4GB, សមតុល្យ
    "complex_reasoning": "phi-4",               # 7GB, សមត្ថភាពខ្ពស់
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, បង្កើតកូដប្រសើរ
}

# Foundry Local គ្រប់គ្រងការរកឃើញឧបករណ៍ និងការបែងចែកបរិមាណ
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # ឧទាហរណ៍លទ្ធផល៖
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (បណ្ដាល CPU ឱ្យប្រសើរ)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (បណ្ដាល GPU ដើម្បីបង្កើនល្បឿន)
```
  
#### គំរូដំឡើងផលិតកម្ម

**ការរៀបចំផលិតកម្មអ្នកភ្នាស់ម្នាក់**:  
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # បន្ថែមឧបករណ៍ផលិត
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# ការប្រើប្រាស់ក្នុងការផលិត
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```
  
**ការគ្រប់គ្រងផលិតកម្មអ្នកភ្នាស់ច្រើន**:  
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # អ្នកប្រតិបត្តិការបញ្ជូនទំនិញទំងន់ស្រាល
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # អ្នកជំនួយបណ្ដាញអតិថិជន
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # អ្នកជំនួយបច្ចេកទេស
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # បញ្ជូនសំណើទៅអ្នកប្រតិបត្តិការដែលសមស្រប
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # កំណត់អ្នកប្រតិបត្តិការគោលបំណងដោយផ្អែកលើការបញ្ជូន
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # ដំណើរការជាមួយអ្នកប្រតិបត្តិការដែលមានជំនាញពិសេស
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# ចេញផ្សាយផលិតកម្ម
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```
  
#### មុខងារថ្នាក់សហគ្រាសនិងការតាមដាន

**តាមដានសុខភាពនិងសមត្ថភាព**:  
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# ការតំឡើងការត្រួតពិនិត្យផលិតកម្ម
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# ការត្រួតពិនិត្យបន្ត
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # ប្រមូលចំនួនវាស់វែងរាល់ ៣០ វិនាទី
```
  
**ការគ្រប់គ្រងធនធាននិងការបណ្តុះបណ្តាលស្វ័យប្រវត្តិ**:  
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # បណ្តោយចុះ ប្រសិនបើការប្រើប្រាស់ឃ្លាំងមានកម្រិតខ្ពស់
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # បណ្តោយឡើង ប្រសិនបើតម្រូវការ​មានកម្រិតខ្ពស់ និងធនធានអនុញ្ញាត
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # ៥ នាទីមិនប្រើប្រាស់
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```
  
#### ការកំណត់រចនាសម្ព័ន្ធខ្ពស់និងបង្កើនប្រសិទ្ធភាព

**ការកំណត់ម៉ូឌែលផ្ទាល់ខ្លួន**:  
```python
# ការកំណត់ Foundry Local អភិវឌ្ឍន៍សម្រាប់ការផលិត
from foundry_local import FoundryLocalManager, ModelConfig

# ការកំណត់ផ្ទាល់ខ្លួនសម្រាប់ករណីប្រើប្រាស់ជាក់លាក់
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # កម្រិតបរិមាណវិជ្ជមានជាក់លាក់
    context_length=4096,    # បន្ថែមបរិបទសម្រាប់ភ្នាក់ងារស្មុគស្មាញ
    batch_size=1,          # បង្កើនប្រសិទ្ធភាពសម្រាប់ភ្នាក់ងារអ្នកប្រើប្រាស់តែម្នាក់
    threads=4,             # ការបង្កើនស្រទាប់ CPU
    gpu_layers=32,         # ស្រទាប់គំរូសំរាប់ការរហ័ស GPU
    memory_lock=True,      # ចាក់ម៉ូដែលនៅក្នុងសមាភិបាលចងចាំសម្រាប់មុខងារប្រសិទ្ធភាពថេរ
    numa=True              # ការបង្កើន NUMA សម្រាប់ប្រព័ន្ធមានច្រកច្រើន
)

manager = FoundryLocalManager(config=config)
```
  
**បញ្ជីពិនិត្យដំឡើងផលិតកម្ម**:

✅ **ការកំណត់សេវា**:  
- កំណត់ឈ្មោះម៉ូឌែលសមរម្យសម្រាប់ករណីប្រើប្រាស់  
- កំណត់ដែនកំណត់ធនធាន និងកម្រិតតាមដាន  
- បើកការត្រួតពិនិត្យសុខភាពនិងការប្រមូលមេត្រិក  
- កំណត់ការចាប់ផ្តើមឡើងវិញ និងការការពារបរាជ័យស្វ័យប្រវត្តិ  

✅ **ការរៀបចំសុវត្ថិភាព**:  
- បើកការចូលដំណើរការដោយ API ផ្ទាល់ក្នុងតំបន់ (គ្មានការបង្ហោះក្រៅ)  
- កំណត់គ្រប់គ្រងកន្លែងមុខសោ API  
- កំណត់ការចុះបញ្ជី audit សម្រាប់អន្តរាគមន៍អ្នកភ្នាស់  
- ប្រើការកំណត់កម្រិតអត្រាសម្រាប់ការប្រើប្រាស់ផលិតកម្ម  

✅ **បង្កើនប្រសិទ្ធភាព**:  
- សាកល្បងប្រសិទ្ធភាពម៉ូឌែលក្រោមផ្ទុកការងាររំពឹងទុក  
- កំណត់កម្រិត quantization សមរម្យ  
- កំណត់ពន្លឿនម៉ូឌែល និងយុទ្ធសាស្ត្រកំដៅ  
- តាមដានការប្រើប្រាស់ម៉េចអាមរីយ៍ និង CPU  

✅ **ការសាកល្បងរួមបញ្ចូល**:  
- សាកល្បងការរួមបញ្ចូលក្របខណ្ឌអ្នកភ្នាស់  
- បញ្ជាក់សមត្ថភាពដំណើរការមិនតម្រូវអ៊ីនធើណិត  
- សាកល្បងស្ថានភាពបរាជ័យនិងថយក្រោយ  
- ផ្ទៀងផ្ទាត់លំហូរការងារ agent ពេញលេញ  

### Ollama: ការដំឡើង SLM Agent ផ្ទាល់ខ្លួន

### Ollama: ការដំឡើង SLM Agent ដោយសហគមន៍ផ្ដោត

Ollama ផ្តល់វិធីសាស្រ្តបិទប្រព័ន្ធសហគមន៍សម្រាប់ដំឡើងអ្នកភ្នាស់ SLM ជាមួយការផ្ដោតលើភាពសាមញ្ញ សារពើភ័ណ្ឌម៉ូឌែលធំ និងលំហូរការងារសាមញ្ញសម្រាប់អ្នកអភិវឌ្ឍ។ ខណៈដែល Foundry Local ផ្ដោតលើមុខងារថ្នាក់សហគ្រាស Ollama មានភាពល្អឥតខ្ចោះសម្រាប់ល្បឿនលឿន ការចូលដំណើរការម៉ូឌែលសហគមន៍ និងស្ថានភាពដំឡើងយ៉ាងសាមញ្ញ។

**រចនាសម្ព័ន្ធមូលដ្ឋាននិងមុខងារ**:  
- **API តំណាងស្របនឹង OpenAI**៖ សមរម្យ REST API ពេញលេញសម្រាប់ការរួមបញ្ចូលក្របខណ្ឌអ្នកភ្នាស់  
- **បណ្ណាល័យម៉ូឌែលធំ**៖ ចូលដំណើរការហាប់រយម៉ូឌែលចូលរួមពីសហគមន៍និងផ្លូវការជាច្រើន  
- **ការគ្រប់គ្រងម៉ូឌែលសាមញ្ញ**៖ ការតំឡើងម៉ូឌែលនិងប្តូរម៉ូឌែលដោយបញ្ជារមួយ  
- **គាំទ្រឆ្លងប្លាស្តហ្វម**៖ គាំទ្រដើមមានលើ Windows, macOS និង Linux  
- **ការបង្កើនប្រសិទ្ធភាពធនធាន**៖ ការចុះបញ្ជាក់ quantization និងការរកឃើញរឹងដោយស្វ័យប្រវត្តិ

#### ការដំឡើងនិងចាប់ផ្ដើម

**ការដំឡើងឆ្លងប្លាស្តហ្វម**:  
```bash
# វីនដូว์
winget install Ollama.Ollama

# ម៉ាក់អូអេស
brew install ollama

# លីណុច
curl -fsSL https://ollama.com/install.sh | sh

# ការដាក់ប្រេី Docker
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```
  
**ចាប់ផ្ដើមលឿនសម្រាប់អភិវឌ្ឍអ្នកភ្នាស់**:  
```bash
# ចាប់ផ្ដើមសេវាកម្ម Ollama
ollama serve

# ទាញយក និងដំណើរការម៉ូដែលសម្រាប់ការអភិវឌ្ឍភ្នាក់ងារ
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# សាកល្បងភាព उपलब्धភាពម៉ូដែល
ollama list

# សាកល្បងចុងបញ្ចប់ API
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```
  
#### ការរួមបញ្ចូលក្របខណ្ឌអ្នកភ្នាស់

**Ollama ជាមួយ Microsoft Agent Framework**:  
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama មិនបានទាមទារការប្រាក់ API ពិតប្រាកដ
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# ចាប់ផ្តើម Ollama សម្រាប់ការអភិវឌ្ឍភ្នាក់ងារ
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# កំណត់ត្រាភ្នាក់ងារជាមួយផ្នែកខាងសម្រាប់ Ollama
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```
  
**ការរៀបចំអ្នកភ្នាស់ម៉ូឌែលច្រើនជាមួយ Ollama**:  
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # ៣៥០MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # ២.៣GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # ១.៩GB
            "coding": "codellama:7b-code-q4_K_M"                # ៤.១GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # ទាញម៉ូឌែល ប្រសិនបើមិនមាន
            self._pull_model(model_name)
            
            # បង្កើតអតិថិជន OpenAI សម្រាប់មូឌែលនីមួយ
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# ឧទាហរណ៍ការប្រើប្រាស់
manager = OllamaMultiModelManager()

# ទទួលបានភ្នាក់ងារដែលសមរម្យសម្រាប់ភារកិច្ចផ្សេងៗ
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```
  
#### គំរូដំឡើងផលិតកម្ម

**សេវាផលិតកម្មជាមួយ Ollama**:  
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # វាស់វែងថាម៉ូឌែលមានស្រាប់
                self._ensure_model_ready(model_name)
                
                # បង្កើតភ្នាក់ងារផលិត
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # បន្ថែមឧបករណ៍ផលិតជាមួយប្រភេទភ្នាក់ងារ
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # ពិនិត្យមើលថាម៉ូឌែលមានឬទេ
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # អង្គុយសម្លឹងមើលទិន្នន័យក្នុងមូលដ្ឋានទិន្នន័យ
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # បន្ទាន់សម័យវិមាត្រ
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # នៅក្នុងការផលិត នេះនឹងសរសេរទៅប្រព័ន្ធកត់ត្រាដែលសមរម្យ
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # ពិនិត្យសុខភាពសេវាកម្ម Ollama
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # ពិនិត្យភាពមានស្រាប់នៃម៉ូឌែល
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# ឧទាហរណ៍ نشر في الإنتاج
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# ដំណើរការសំណើ
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```
  
#### មុខងារថ្នាក់សហគ្រាសនិងការតាមដាន

**ការតាមដាននិងសមត្ថភាព Ollama**:  
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # ពិនិត្យសុខភាពសេវាកម្ម
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # ទទួលព័ត៌មានម៉ូដែល
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # សេចក្ដីសង្ខេបខ្លី
                    }
                
                # សំឡាញ់ប្រសិទ្ធភាពការព្យាករណ៍
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # រក្សាទុកតែចំនួនបញ្ចូលមេត្រិកចុងក្រោយ ១០០
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # ប៉ាន់ប្រមាណតុងខ្នាត (ការប៉ាន់ប្រមាណប្រហាក់ប្រហែល)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # ទទួលព័ត៌មានម៉ោងប្រសិនបើមាន
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # បំលែងនាណូវិនាទីទៅវិនាទី
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # ពិនិត្យពេលវេលาตอบតប
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # ពិនិត្យស្ថានភាពសេវាកម្ម
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # គណនាមធ្យមភាគ
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# ការតំឡើងមួយចំងាយត្រួតពិនិត្យផលិតកម្ម
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # កត់ត្រាសេចក្ដីសង្ខេបប្រសិទ្ធភាពរៀងរាល់ ១០ នាទី
            if int(time.time()) % 600 == 0:  # រៀងរាល់ ១០ នាទី
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # ពិនិត្យរៀងរាល់ ៣០ វិនាទី

# ចាប់ផ្ដើមត្រួតពិនិត្យ
# asyncio.create_task(monitoring_loop())
```
  
#### ការកំណត់រចនាសម្ព័ន្ធខ្ពស់និងបង្កើនប្រសិទ្ធភាព

**ការគ្រប់គ្រងម៉ូឌែលផ្ទាល់ខ្លួនជាមួយ Ollama**:  
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # ម៉ូដែលធំណែលសម្រាប់ចម្លើយឆាប់រហ័ស
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # ម៉ូដែលសមតុល្យសម្រាប់ការប្រើប្រាស់ទូទៅ
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # ម៉ូដែលឯកទេសសម្រាប់ភារកិច្ចជាក់លាក់
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # ម៉ូដែលសមត្ថភាពខ្ពស់
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # ម៉ូដែលទាញ
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # នេះជាទូទៅនឹងពិនិត្យបញ្ជាក់វត្ថុបច្ចេកទេសរបស់រឹងពិត
        # សម្រាប់គោលបំណងបង្ហាញ, យើងនឹងអំពើការស្ទង់រករៀបរាប់បច្ចេកទេសរឹង
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# ការតំឡើងម៉ូដែលផលិតកម្ម
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```
  
**បញ្ជីពិនិត្យដំឡើងផលិតកម្មសម្រាប់ Ollama**:

✅ **ការកំណត់សេវា**:  
- ដំឡើងសេវា Ollama ជាមួយការរួមបញ្ចូលប្រព័ន្ធត្រឹមត្រូវ  
- កំណត់ម៉ូឌែលសម្រាប់ករណីប្រើប្រាស់អ្នកភ្នាស់ជា ពិសេស  
- កំណត់ស្គ្រីបចាប់ផ្ដើម និងការគ្រប់គ្រងសេវា  
- សាកល្បងការផ្ទុកម៉ូឌែល និងការចូលដំណើរការ API  

✅ **ការគ្រប់គ្រងម៉ូឌែល**:  
- ទាញយកម៉ូឌែលចាំបាច់ និងផ្ទៀងផ្ទាត់ភាពសើច  
- កំណត់ដំណើរការបន្ទាន់ និងវិលជុំម៉ូឌែល  
- សម្រួលការចុះបញ្ជីម៉ូឌែល និងការបង្កើនសមត្ថភាពផ្ទុក  
- សាកល្បងប្រសិទ្ធភាពម៉ូឌែលក្រោមផ្ទុកការងាររំពឹងទុក  

✅ **ការរៀបចំសុវត្ថិភាព**:  
- កំណត់ចំណុច firewall សម្រាប់ការចូលក្នុងតំបន់តែប៉ុណ្ណោះ  
- កំណត់ការចូល API និងកម្រិតកំណត់អត្រា  
- ប្រើឧបករណ៍ audit សម្រាប់អន្តរាគមន៍ភ្នាស់  
- កំណត់ការផ្ទុកម៉ូឌែលសុវត្ថិភាព និងការចូល  

✅ **បង្កើនប្រសិទ្ធភាព**:  
- សាកល្បងគំរូសម្រាប់ករណីប្រើប្រាស់ចាំបាច់  
- ជ្រើសរើសជម្រើសកំណត់ល្បឿនរឹងសមរម្យ  
- កំណត់បច្ចេកទេសកំដៅ និង caches ម៉ូឌែល  
- តាមដានប្រើប្រាស់ធនធាន និងមេត្រិកសមត្ថភាព  

✅ **ការសាកល្បងរួមបញ្ចូល**:
- សាកល្បងការរួមបញ្ចូល Microsoft Agent Framework
- បញ្ជាក់សមត្ថភាពប្រតិបត្តិការផ្ទេរជាបន្ទាន់
- សាកល្បងសេណារីយោ failover និងការដោះស្រាយកំហុស
- បញ្ជាក់លំនាំប្រតិបត្តិការអ្នកតំណាងពីដើមដល់ចប់

**ប្រៀបធៀបជាមួយ Foundry Local**:

| លក្ខណៈ | Foundry Local | Ollama |
|---------|---------------|--------|
| **ការប្រើប្រាស់គោលដៅ** | ផលិតកម្មសហគ្រាស | ការអភិវឌ្ឍន៍ និងសហគមន៍ |
| **ប្រព័ន្ធម៉ូដែល** | ក្រោមការត្រួតពិនិត្យ Microsoft | សហគមន៍ធំទូលាយ |
| **ការប្រសើរឡើងរបស់បំណែករឹង** | ដោយស្វ័យប្រវត្តិ (CUDA/NPU/CPU) | ការកំណត់ដៃ |
| **លក្ខណៈសហគ្រាស** | ការត្រួតពិនិត្យ សុវត្ថិភាពជាបញ្ចូល | ឧបករណ៍សហគមន៍ |
| **ភាពស្មុគស្មាញក្នុងការដាក់ចេញ** | ងាយស្រួល (winget install) | ងាយស្រួល (curl install) |
| **ការគាំទ្រ API** | OpenAI + បន្ថែម | OpenAI ផ្តល់ជាធម្មតា |
| **ការគាំទ្រ** | ផ្លូវការរបស់ Microsoft | ដឹកនាំដោយសហគមន៍ |
| **ល្អបំផុតសម្រាប់** | អ្នកតំណាងក្នុងផលិតកម្ម | ការបង្កើតគំនិតដំបូង ការស្រាវជ្រាវ |

**ពេលដែលជ្រើស Ollama**:
- **ការអភិវឌ្ឍន៍ និងកំណត់គំរូ**: ការប្រមូលផ្តុំគំរូលឿនជាមួយម៉ូដែលផ្សេងៗ
- **ម៉ូដែលសហគមន៍**: ចូលដំណើរការម៉ូដែលទើបបញ្ចូលដោយសហគមន៍
- **ការប្រើប្រាស់ផ្នែកអប់រំ**: ការរៀន និងបង្រៀនអំពីការអភិវឌ្ឍភ្នាក់ងាររបស់ AI
- **គម្រោងស្រាវជ្រាវ**: ស្រាវជ្រាវវិទ្យាសាស្ត្រដែលតម្រូវឱ្យមានការចូលដំណើរការម៉ូដែលច្រើនប្រភេទ
- **ម៉ូដែលផ្ទាល់ខ្លួន**: ការបង្កើត និងសាកល្បងម៉ូដែលដែលបានត្រួតបន្ថែម

### VLLM: គំរូភ្នាក់ងារជំនាញខ្ពស់ SLM

VLLM (ការបកស្រាយម៉ូដែលភាសាធំមួយ) ផ្តល់ឧបករណ៍បកស្រាយដែលមានប្រសិទ្ធភាពខ្ពស់ ប្រើប្រាស់ប្រាក់កំចាយអង្គចងចាំបានមានប្រសិទ្ធភាព សម្រួលសម្រាប់ការចាក់បញ្ចូលតាមផលិតកម្ម SLM នៅវិមាត្រធំ។ ខណៈដែល Foundry Local ផ្តោតលើភាពងាយស្រួលក្នុងការប្រើប្រាស់ និង Ollama ផ្តល់សំខាន់លើម៉ូដែលសហគមន៍ VLLM មានល្បឿនខ្ពស់សាកសមនឹងសេណារីយោដែលតម្រូវឱ្យមានការផ្ទុកបន្ទុក និងរឹងមាំប្រើប្រាស់ធនធាន។

**រចនាសម្ព័ន្ធ និងលក្ខណៈសំខាន់ៗ**:
- **PagedAttention**: ការគ្រប់គ្រងអង្គចងចាំបែបបម្លែងសម្រាប់ការគណនាជ្រៅរហ័ស
- **Dynamic Batching**: ការចងញឹមសំណើយ៉ាងច្បាស់លាស់សម្រាប់ល្បឿនខ្ពស់
- **ការប្រសើរឡើង GPU**: គូបនិងការគាំទ្រពហុបែបគណនា CUDA ដំបូង
- **ការគាំទ្រ OpenAI**: ភ្ជាប់ API ទាំងមូលបានយ៉ាងរលូន
- **ការពន្លឿនសន្និដ្ឋាន**: បច្ចេកទេសបង្ការសម័យបកស្រាយកាន់តែរហ័ស
- **ការគាំទ្រការប្រែប្រួល**: INT4, INT8 និង FP16 សម្រាប់ប្រសិទ្ធភាពអង្គចងចាំ

#### ការដំឡើង និងដំណើរការ

**ជម្រើសដំឡើង**:
```bash
# ការដំឡើងស្តង់ដារ
pip install vllm

# ជាមួយនឹងការពឹងផ្អែកបន្ថែមសម្រាប់ស៊ុមភ្នាក់ងារ
pip install vllm[agent] openai

# ការបង្ហោះ Docker សម្រាប់ផលិតកម្ម
docker pull vllm/vllm-openai:latest

# ពីកូដប្រភពសម្រាប់មុខងារថ្មីៗ
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**ចាប់ផ្តើមឆាប់សម្រាប់ការអភិវឌ្ឍភ្នាក់ងារ**:
```bash
# ចាប់ផ្តើមម៉ាស៊ីនមេ VLLM ជាមួយម៉ូដែល SLM
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# ជំនួស: ចាប់ផ្តើមជាមួយ Qwen2.5 សម្រាប់ភ្នាក់ងារស្រាល
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# សាកល្បងច្រក API
curl http://localhost:8000/v1/models

# សាកល្បងការបញ្ចប់ការជជែក
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### រួមបញ្ចូល Agent Framework

**VLLM ជាមួយ Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # កាត់បន្ថយការចុះបញ្ជីសម្រាប់ភ្នាក់ងារ
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # រង់ចាំម៉ាស៊ីនបម្រើចាប់ផ្តើម
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# ចាប់ផ្តើម VLLM សម្រាប់ភ្នាក់ងារសមត្ថភាពខ្ពស់
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # កំណត់គេហទំព័រភ្នាក់ងារជាមួយសេវាកម្មដំណើរការផ្នែកក្រោយ VLLM
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM មិនត្រូវការពាក្យសម្ងាត់ API
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**ការតភ្ជាប់ភ្នាក់ងារច្រើនចំណុះខ្ពស់**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # ចាប់ផ្តើមម៉ាស៊ីនបម្រើក្នុងថ្រេដដើម្បីជៀសវាងការបិទខ្ទប់
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # បង្កើតភ្នាក់ងារ
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # បង្កើតទន្លេអតិថិជនសម្រាប់ការចេញភាគច្រើនខ្ពស់
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # ៥ អតិថិជនមួយម៉ូឌែលសម្រាប់ការចងក្រងរាង 병
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # ពិនិត្យមើលថាភ្នាក់ងារដែលចំណូលចិត្តមានស្រាប់ហើយមិនជាប់បន្ទុកពេក
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # កំណត់អតិបរមា១០រួមសកម្មភាពក្នុងមួយភ្នាក់ងារ
            return preferred_category
        
        # ត្រឡប់ទៅភ្នាក់ងារដែលមានបន្ទុកតិចបំផុតនៅស្រាប់
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # ការត្រឡប់ទៅលំនាំដើម
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # កំណត់ភ្នាក់ងារល្អបំផុត
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # បង្កើតភារកិច្ចដំណើរការ
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # ដំណើរការការស្នើសុំទាំងអស់ក្នុងរាង 병
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # ទ្រង់ទ្រាយលទ្ធផល
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# ឧទាហរណ៍ប្រើប្រាស់ខ្ពស់ភាពចេញ
throughput_manager = VLLMHighThroughputManager()

# ចាប់ផ្តើមម៉ូឌែលទាំងអស់
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# ដំណើរការការស្នើសុំជាកញ្ចប់
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### លំនាំដាក់ចេញក្នុងផលិតកម្ម

**សេវាកម្មផលិតកម្ម VLLM សហគ្រាស**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # ចាប់ផ្តើមដំណើរការជាង
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # ជាងអតិបរមា ៤ នាក់
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # បន្ថែមការបញ្ចាក់បរិមាណប្រសិនបើបានបញ្ជាក់
        if config.quantization:
            # នេះគឺនឹងត្រូវបន្ថែមទៅក្នុងពាក្យបញ្ជារចាប់ផ្តើមរបស់អ្នកគ្រប់គ្រង
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # បង្កើតភ្នាក់ងារផលិតកម្ម
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # បន្ថែមឧបករណ៍ផលិតកម្ម
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # ស្វែងរកមូលដ្ឋានទិន្នន័យផលិតកម្ម
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # បន្ថែមសំណើទៅក្នុងជួរតំណើរ
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # រងចាំដំណើរការ (ជាមួយពេលកំណត់)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # ទទួលសំណើពីជួរតំណើរ
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # ជ្រើសរើសភ្នាក់ងារឱ្យសមរម្យ
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # ដំណើរការសំណើ
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # បច្ចុប្បន្នភាពម៉េត្រីក
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # នេះគឺបានសំរួល - នៅក្នុងផលិតកម្មអ្នកនឹងតាមដានពេលអនាគតបានត្រឹមត្រូវ
        await asyncio.sleep(0.1)  # ទីតាំងបញ្ចូល
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # ជាងតែម្នាក់សម្រាប់ភាពសាមញ្ញ
        )
        server = uvicorn.Server(config)
        await server.serve()

# ឧទាហរណ៍ដាក់ចេញផលិតកម្ម
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# ប្រើផ្តើម និងចាប់ផ្តើមសេវាកម្មផលិតកម្ម
# រងចាំ production_service.initialize_production_environment()
# រងចាំ production_service.start_production_server()
```

#### លក្ខណៈនិងការត្រួតពិនិត្យសហគ្រាស

**ការត្រួតពិនិត្យសមត្ថភាព VLLM កម្រិតខ្ពស់**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # ចាប់ផ្តើម NVIDIA ML សម្រាប់ត្រួតពិនិត្យ GPU
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # បង្កើតវិមាត្រកំហុស
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # ទទួលបានវិមាត្រពិសេស VLLM តាមរយៈ API
        vllm_stats = await self._get_vllm_stats(manager)
        
        # ទទួលបានវិមាត្រប្រព័ន្ធ
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # ទទួលបានវិមាត្រក្នុង GPU ប្រសិនបើមាន
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # សន្មតថា GPU ទីមួយសម្រាប់ភាពសាមញ្ញ
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # សាកល្បងការអនុវត្តន៍ដើម្បីវាស់ពេលយឺត
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # ត្រូវការត្រូវបានបង្ហាញដោយ VLLM
                "active_requests": 1  # ការប៉ាន់ស្មាន
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # វិភាគពត៌មានរៀងរាល់ឱកាស
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # ការផ្ដល់អនុសាសន៍ពេលយឺតខ្ពស់
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # ការផ្ដល់អនុសាសន៍ការប្រើប្រាស់ GPU ខ្ពស់
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # ការផ្ដល់អនុសាសន៍ការប្រើប្រាស់ទាប
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# ការតំឡើងការត្រួតពិនិត្យជាន់ខ្ពស់
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # បន្ថែមអ្នកគ្រប់គ្រងផ្សេងទៀតតាមការចាំបាច់
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # រូមបញ្ចូលវិមាត្រ
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # រក្សាទុកតែចំនួន ១០០០ បញ្ចូលចុងក្រោយ
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # បង្កើតអនុសាសន៍រៀងរាល់ ៥ នាទី
            if len(monitoring.metrics_history) % 10 == 0:  # រៀងរាល់ការប្រមូលទី ១០ (5 នាទីបើប្រមូលរាល់ 30 វិនាទី)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # បង្កើតរបាយការណ៍ប្រសិទ្ធភាពរៀងរាល់ម៉ោង
            if len(monitoring.metrics_history) % 120 == 0:  # រៀងរាល់ការប្រមូលទី ១២០ (1 ម៉ោង)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # ប្រមូលវិមាត្ររៀងរាល់ ៣០ វិនាទី

# ចាប់ផ្តើមការត្រួតពិនិត្យជាន់ខ្ពស់
# asyncio.create_task(advanced_monitoring_loop())
```

#### កំណត់រចនា និងប្រសើរឡើង

**មាត្រដ្ឋានកំណត់រចនាសម្រាប់ផលិតកម្ម VLLM**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # បន្ថែមប៉ារ៉ាម៉ែត្រ​ជាជម្រើស
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# ឧទាហរណ៍ការប្រើប្រាស់
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**បញ្ជីពិនិត្យការដាក់ចេញសម្រាប់ VLLM**:

✅ **ការប្រសើរឡើងបំណែករឹង**:
- កំណត់ tensor parallelism សម្រាប់ការកំណត់ហ្ស៊ីប GPU ច្រើន
- បើកការប្រែប្រួល (AWQ/GPTQ) ដើម្បីបន្ថយការចំណាយអង្គចងចាំ
- កំណត់ការប្រើប្រាស់អង្គចងចាំ GPU ឱ្យល្អបំផុត (85-95%)
- កំណត់ទំហំចំណុះសម្រាប់ល្បឿនខ្ពស់

✅ **កំណត់ទិសប្រសិទ្ធភាព**:
- បើកការចងចាំ prefix សម្រាប់សំណូមពរច្រើនដង
- កំណត់ chunked prefill សម្រាប់លំដាប់វែង
- រៀបចំកំណត់ការបកស្រាយ speculative សម្រាប់ល្បឿនរហ័ស
- បញ្ជាក់ max_num_seqs តាមបំណែករឹង

✅ **លក្ខណៈផលិតកម្ម**:
- រៀបចំការត្រួតពិនិត្យសុខភាព និងការប្រមូលតុល្យភាព
- កំណត់ការចាប់ផ្តើមឡើងវិញស្វ័យ​ប្រវត្តិ និង failover
- អនុវត្តការចតត្រង់សំណើ និងការប្រមូលផ្ទុកបរំលែង
- រៀបចំប្រព័ន្ធកំណត់ហេតុនិងការជូនដំណឹងជ្រើសរើស

✅ **សុវត្ថិភាពនិងភាពជឿជាក់បាន**:
- កំណត់ច្បាប់ firewall និងការត្រួតពិនិត្យចូលដំណើរការ
- រៀបចំការរឹតត្បិត API rate និងការផ្ទៀងផ្ទាត់អត្តសញ្ញាណ
- អនុវត្តការបិទដំណើរការយ៉ាងរលូន និងការសម្អាត
- កំណត់ការបម្រុងទុក និងការជួសជុលករណីគ្រោះថ្នាក់

✅ **សាកល្បងការរួមបញ្ចូល**:
- សាកល្បងរួមបញ្ចូល Microsoft Agent Framework
- បញ្ជាក់សេណារីយោល្បឿនខ្ពស់
- សាកល្បង failover និងដំណើរការជួយសង្គ្រោះ
- វាស់វែងសមត្ថភាពក្រោមបន្ទុក

**ប្រៀបធៀបជាមួយដំណោះស្រាយផ្សេងទៀត**:

| លក្ខណៈ | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **ការប្រើប្រាស់គោលដៅ** | ការផលិតកម្មល្បឿនខ្ពស់ | ភាពងាយស្រួលសហគ្រាស | ការអភិវឌ្ឍន៍ និងសហគមន៍ |
| **សមត្ថភាព** | ល្បឿនអតិបរមា | តុល្យការ | ល្អ |
| **ប្រសិទ្ធភាពអង្គចងចាំ** | ការប្រសើរឡើង PagedAttention | ស្វ័យប្រវត្តិ | ផ្តល់ជាមូលដ្ឋាន |
| **ភាពស្មុគស្មាញនៅការតំឡើង** | ខ្ពស់ (ប៉ារម៉ែត្រ​ច្រើន) | ទាប (ស្វ័យប្រវត្តិ) | ទាប (ផែសូល) |
| **ការគ្រប់គ្រងនៅវិមាត្រ** | ល្អបំផុត (tensor/pipeline) | ល្អ | មានកំណត់ |
| **ការប្រែប្រួល** | ខ្ពស់ (AWQ, GPTQ, FP8) | ស្វ័យប្រវត្តិ | ផ្តល់ជាស្តង់ដារ GGUF |
| **លក្ខណៈសហគ្រាស** | តម្រូវការអនុវត្តផ្ទាល់ខ្លួន | រួមបញ្ចូល | ឧបករណ៍សហគមន៍ |
| **ល្អបំផុតសម្រាប់** | អ្នកតំណាងផលិតកម្មវិមាត្រធំ | ផលិតកម្មសហគ្រាស | ការអភិវឌ្ឍន៍ |

**ពេលដែលជ្រើស VLLM**:
- **តម្រូវការល្បឿនខ្ពស់**: ដំណើរការសំណើរ៥០០ក្នុងមួយវិនាទី
- **ការដាក់ចេញវិមាត្រទូលំទូលាយ**: ការចាក់បញ្ចូលពហុ GPU និងពហុ node
- **ការតម្រូវការសមត្ថភាពធ្ងន់ធ្ងរ**: ពេលសំណើត្រូវឆ្លើយតបក្រោមមួយវិនាទី
- **ការប្រសើរឡើងកម្រិតខ្ពស់**: តម្រូវការប្រែប្រួល និងការចងញឹមផ្ទាល់ខ្លួន
- **ប្រសិទ្ធភាពធនធាន**: ប្រើប្រាស់បំពង់ GPU អគ្គិសនីថ្លៃថោកបានអតិបរមា


## កម្មវិធីភ្នាក់ងារ SLM ជាការពិតនៅពិភពលោក

### ភ្នាក់ងារបម្រើអតិថិជន SLM
- **សមត្ថភាព SLM**: រំលឹកគណនី ការផ្លាស់ប្តូរពាក្យសម្ងាត់ ពិនិត្យស្ថានភាពការបញ្ជាទិញ
- **អត្ថប្រយោជន៍ថ្លៃចំណាយ**: ការកាត់បន្ថយថ្លៃបកស្រាយ១០ដងប្រៀបធៀបជាមួយភ្នាក់ងារ LLM
- **សមត្ថភាព**: ឆ្លើយតបលឿន និងគុណភាពថេរសម្រាប់សំណួរប្រចាំ

### ផ្នែកដំណើរការអាជីវកម្ម SLM
- **ភ្នាក់ងារចំណុះវិក័យបត្រ**: ដកទិន្នន័យ បញ្ជាក់ព័ត៌មាន បញ្ជូនសម្រាប់ការអនុម័ត
- **ភ្នាក់ងារគ្រប់គ្រងអ៊ីមែល**: ចាត់ថ្នាក់ អាទិភាព រចនាចម្លើយដោយស្វ័យប្រវត្តិ
- **ភ្នាក់ងារតារាងកំណត់ពេលវេលា**: សម្របសម្រួលកិច្ចប្រជុំ គ្រប់គ្រងប្រតិទិន ផ្ញើការជូនដំណឹង

### ជំនួយឌីជីថលផ្ទាល់ខ្លួន SLM
- **ភ្នាក់ងារគ្រប់គ្រងភារកិច្ច**: បង្កើត បន្ទាន់សម័យ រៀបចំបញ្ជីTo-Doយ៉ាងមានប្រសិទ្ធភាព
- **ភ្នាក់ងារប្រមូលព័ត៌មាន**: ស្រាវជ្រាវ បញ្ជាក់សង្ខេបនៅក្នុងតំបន់
- **ភ្នាក់ងារប្រាស្រ័យទាក់ទង**: រចនាអ៊ីមែល សារអូសស្រៀន និងប្រកាសតាមប្រព័ន្ធសង្គមឯកជន

### ភ្នាក់ងារជួញដូរ និងហិរញ្ញវត្ថុ SLM
- **ភ្នាក់ងារតាមដានទីផ្សារ**: តាមដានតម្លៃ ស្គាល់និន្នាការពេលវេលាពិត
- **ភ្នាក់ងារបង្កើតរបាយការណ៍**: បង្កើតសង្ខេបប្រចាំថ្ងៃ/អាទិត្យដោយស្វ័យប្រវត្តិ
- **ភ្នាក់ងារវាយតម្លៃហានិភ័យ**: វាយតម្លៃទីតាំងវិនិយោគដោយប្រើទិន្នន័យក្នុងតំបន់

### ជំនួយសុខាភិបាល SLM
- **ភ្នាក់ងារតារាងវេជ្ជបណ្ឌិត**: សម្របសម្រួលការណាត់ជួប ផ្ញើការជូនដំណឹងដោយស្វ័យប្រវត្តិ
- **ភ្នាក់ងារប្រឹក្សាឯកសារ**: បង្កើតសង្ខេបតំណារពេទ្យ និងរបាយការណ៍ក្នុងតំបន់
- **ភ្នាក់ងារគ្រប់គ្រងរបបគ្រឿងថ្នាំ**: តាមដានការបំពេញវិស្វកម្ម ពិនិត្យអន្តរប្រតិកម្មជា​ឯកជន

## Microsoft Agent Framework: ការអភិវឌ្ឍភ្នាក់ងារសម្រាប់ផលិតកម្ម

### ទិដ្ឋភាពទូទៅ និងរចនាសម្ព័ន្ធ

Microsoft Agent Framework ផ្តល់វេទិកាម៉ាស៊ីនកម្រិតសហគ្រាសសម្រាប់បង្កើត គ្រប់គ្រង និងដាក់ចេញភ្នាក់ងារ AI ដែលអាចដំណើរការបានទាំងលើពពកនិងនៅជំហានចុងក្រោយ (edge) ដោយរចនាឡើងឱ្យផ្គូរផ្គងល្អជាមួយម៉ូដែលភាសាខ្នាតតូច (SLM) និងទ្រឹស្តី edge computing ដែលសមស្របសម្រាប់ភាពឯកជន និងកំណត់ធនធាន។

**រចនាសម្ព័ន្ធសំខាន់របស់ Framework**:
- **Agent Runtime**: បរិយាកាសដំណើរការពន្លឺសម្រាប់ឧបករណ៍ edge
- **Tool Integration System**: រចនាសម្ព័ន្ធភ្លិចអ៊ិនបន្ថែមសម្រាប់ភ្ជាប់ប្រព័ន្ធផ្លាស់ប្តូរនានា និង API
- **State Management**: ចងចាំភ្នាក់ងារជាប្រចាំ និងការគ្រប់គ្រងcontext ក្នុងសម័យប្រតិបត្តិការ
- **Security Layer**: សុវត្ថិភាពជាបញ្ចូលសម្រាប់ការចាក់បញ្ចូលផលិតកម្មសហគ្រាស
- **Orchestration Engine**: ការសម្របសម្រួលភ្នាក់ងារច្រើន និងគ្រប់គ្រងលំនាំប្រតិបត្តការ

### លក្ខណៈសំខាន់សម្រាប់ការដាក់ចេញ Edge

**រចនាសម្ព័ន្ធ Offline-First**: Microsoft Agent Framework ရှိភាស៊ីបមុខងារមុន offline ដែលអនុញ្ញាតឲ្យភ្នាក់ងារដំណើរការដោយមានប្រសិទ្ធភាពដោយមិនចាំបាច់ភ្ជាប់អ៊ីនធឺរណែតជាបន្តបន្ទាប់។ រួមមានការបកស្រាយម៉ូដែលក្នុងតំបន់ ការចាំចំណេះដឹង ចំណុចប្រើឧបករណ៍ offline និងការធ្លាក់ចុះយ៉ាងស្ដើងនៅពេលសេវាកម្មពពកមិនអាចប្រើបាន។

**ការប្រសើរឡើងធនធាន**: Framework ផ្តល់ការគ្រប់គ្រងធនធានយ៉ាងប្រាជ្ញា ដោយមានសមត្ថភាពបង្រួមអង្គចងចាំស្វ័យប្រវត្តិសម្រាប់ SLMs, ការប្រហែលចំណុចប្រើ CPU/GPU សម្រាប់ឧបករណ៍ edge, ការជ្រើសម៉ូដែលផ្អែកលើធនធានដែលមាន, និងលំនាំបកស្រាយដែលប្រើថាមពលមានប្រសិទ្ធភាពសម្រាប់ការដាក់ចេញចល័ត។

**សុវត្ថិភាព និងភាពឯកជន**: លក្ខណៈសុវត្ថិភាពកម្រិតសហគ្រាស រួមមានការប្រព្រឹត្តផលិតទិន្នន័យនៅក្នុងតំបន់ដើម្បីរក្សាព័ត៌មានឯកជន, ប៉ារ៉ាម៉ែត្រទំនាក់ទំនងភ្នាក់ងារនិងការអនុញ្ញាតផ្តល់តុល្យភាពតាមតួ, និងកំណត់ហេតុ audit សម្រាប់ការអនុវត្តតាមបទប្បញ្ញតិ។

### ការរួមបញ្ចូលជាមួយ Foundry Local

Microsoft Agent Framework រួមបញ្ចូលបានយ៉ាងរលូនជាមួយ Foundry Local ដើម្បីផ្ដល់ដំណោះស្រាយ AI នៅជំហានចុងក្រោយពេញលេញ៖

**ការស្វែងរកម៉ូដែលស្វ័យប្រវត្តិ**: Framework ស្វែងរក និងភ្ជាប់ទៅកាន់ប្រព័ន្ធ Foundry Local ដោយស្វ័យប្រវត្តិ រកមើលម៉ូដែល SLM ដែលមាន និងជ្រើសម៉ូដែលល្អបំផុតផ្អែកលើទាមទារភ្នាក់ងារ និងសមត្ថភាពរឹង។

**ការផ្ទុកម៉ូដែលឌីណាមិក**: ភ្នាក់ងារអាចផ្ទុក SLM ផ្សេងៗសម្រាប់ការងារផ្សេងៗ ដើម្បីគ្រប់គ្រងប្រព័ន្ធភ្នាក់ងារច្រើនម៉ូដែលដែលរៀបចំបម្លែងប្រភេទសំណើ ខណៈបន្សំនឹងការបរំលែងស្វ័យប្រវត្តិពេលម៉ូដែលមិនមានឬត្រូវផ្តល់និរន្តរភាព។

**កំណត់ប្រសិទ្ធភាព**: ប្រព័ន្ធចងចាំបានបង្កើនល្បឿនដំណើរការ ម៉ាសុីនភ្ជាប់បានប្រសើរ API ធ្វើការចូលកាន់ Foundry Local, និងការត្រួតពិនិត្យសំណុំសំណើអោយមានភាពល្អឥតខ្ចោះសម្រាប់សំណើរ ភ្នាក់ងារច្រើន។

### ការបង្កើតភ្នាក់ងារជាមួយ Microsoft Agent Framework

#### ការបញ្ជាក់ និងកំណត់រចនា Agent

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# កំណត់រចនាសម្ព័ន្ធភ្នាក់ងារជាមួយការរួមបញ្ចូល Foundry Local
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# ចាប់ផ្តើមការតភ្ជាប់ Foundry Local
foundry = FoundryLocalManager("phi-4-mini")

# បង្កើតច្បាប់ភ្នាក់ងារ
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### ការរួមបញ្ចូលឧបករណ៍សម្រាប់សេណារីយ៍ Edge

```python
# កំណត់ឧបករណ៍សម្រាប់ប្រតិបត្តិការផ្ទាល់ខ្លួន
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # សំណួរទៅកាន់ទិន្នន័យមូលដ្ឋានក្នុងតំបន់ - ធ្វើសកម្មភាពក្រៅអ៊ីនធឺណិចបាន
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # បង្កើតសំបុត្រក្នុងតំបន់ជាមួយការសម្របសម្រួលពេលអនឡាញ
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # កំណត់កាលវិភាគក្នុងតំបន់ជាមួយការរួមបញ្ចូលប្រតិទិន្ធ
    return local_calendar.schedule(customer_id, preferred_time)
```

#### ការសម្របសម្រួលភ្នាក់ងារច្រើន

```python
from microsoft_agent_framework import AgentOrchestrator

# បង្កើតភ្នាក់ងារពិសេសសម្រាប់ដែនកំណត់ផ្សេងៗ
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # ស្រាលសម្រាប់ភារកិច្ចងាយស្រួល
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # មានសមត្ថភាពខ្ពស់សម្រាប់បញ្ហាស្មុគស្មាញ
        specialized_for="technical_support"
    )
)

# ចងក្រងភ្នាក់ងារច្រើន
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# បញ្ជូនសំណើផ្អែកលើចេតនា
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### លំនាំដាក់ចេញ Edge ថ្មីៗ

#### រចនាសម្ព័ន្ធភ្នាក់ងារចំណាត់ថ្នាក់

**ក្លឹបភ្នាក់ងារផ្ទាល់ក្នុងតំបន់**: ដាក់ចេញភ្នាក់ងារ SLM ជាច្រើនដែលមានការបញ្ជាក់រចនាសម្ព័ន្ធតាមភារកិច្ចនៅឧបករណ៍ edge។ ប្រើម៉ូដែលមានទំងន់ស្រាលដូចជា Qwen2.5-0.5B សម្រាប់ការដឹកជញ្ជូន និងកំណត់ត្រា ដូច​ម៉ូដែលមធ្យមដូចជា Phi-4-Mini សម្រាប់បម្រើអតិថិជន និងឯកសារ និងម៉ូដែលធំសម្រាប់ការវិភាគស្មុគស្មាញពេលធនធានគ្រប់គ្រាន់។

**ការសម្របសម្រួល Edge ទៅ Cloud**: អនុវត្តលំនាំការលើកកម្ពស់ដោយភ្នាក់ងារផ្ទាល់រួមបញ្ចូលភ្នាក់ងារពពកពេលមានការតភ្ជាប់ និងបម្លែងរលូនចន្លោះ edge និង cloud ដើម្បីរក្សាការបន្ត។

#### ការកំណត់រចនាសម្ព័ន្ធដាក់ចេញ

**ដាក់ចេញលើឧបករណ៍តែមួយ**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**ដាក់ចេញ Edge ចែកចាយ**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### ការប្រសើរឡើងសមត្ថភាពសម្រាប់ភ្នាក់ងារលើ Edge

#### វិធីសាស្រ្តជ្រើសម៉ូដែល

**ការបែងចែកម៉ូដែលតាមភារកិច្ច**: Microsoft Agent Framework អនុញ្ញាតឲ្យជ្រើសម៉ូដែលយ៉ាងប្រាជ្ញាតាមកម្រិតស្មុគស្មាញនិងតម្រូវការ៖

- **ភារកិច្ចសាមញ្ញ** (Q&A, routing): Qwen2.5-0.5B (500MB, ចម្លើយ <100ms)
- **ភារកិច្ចមធ្យម** (បម្រើអតិថិជន, កំណត់ពេលវេលា): Phi-4-Mini (2.4GB, 200-500ms)
- **ភារកិច្ចស្មុគស្មាញ** (វិភាគបច្ចេកទេស, ផែនការ): Phi-4 (7GB, 1-3វិនាទីពេលធនធានគ្រប់គ្រាន់)

**ការផ្លាស់ប្តូរម៉ូដែលឌីណាមិក**: ភ្នាក់ងារអាចផ្លាស់ប្តូរម៉ូដែលតាមបន្ទុកប្រព័ន្ធ បរិមាណភារកិច្ច កម្រិតអាទិភាពអ្នកប្រើនិងធនធានរឹងមាន។

#### ការគ្រប់គ្រងអង្គចងចាំ និងធនធាន

```python
# បំពេញការកំណត់កំណត់ធនធានសម្រាប់ការចាត់ចែងគែមៀប។
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### លំនាំរួមបញ្ចូលសហគ្រាស

#### សុវត្ថិភាពនិងការអនុវត្តតាមបទប្បញ្ញតិ

**ការប្រមូលផលិតទិន្នន័យក្នុងតំបន់**: ការប្រមូលផ្តុំការពារទិន្នន័យគ្រប់លំហូរនៅក្នុងតំបន់ ដើម្បីធានាបានថាឯកសារសំខាន់មិនចេញពីឧបករណ៍ edge។ រួមមានការការពារព័ត៌មានអតិថិជន ការអនុវត្ត HIPAA សម្រាប់អ្នកជំនួយសុខភាព ការគ្រប់គ្រងទិន្នន័យហិរញ្ញវត្ថុសម្រាប់ធនាគារ និងការអនុវត្ត GDPR សម្រាប់ដែនក្រុងអឺរ៉ុប។

**ការត្រួតពិនិត្យចូលដំណើរការ**: ការបែងចែកសិទ្ធិផ្អែកតាមតួនាទី ការផ្ទៀងផ្ទាត់អ្នកប្រើ និងកំណត់ហេតុ audit សម្រាប់សកម្មភាព និងសេចក្តីសម្រេចទាំងអស់របស់ភ្នាក់ងារ។

#### ការត្រួតពិនិត្យ និងសង្កេតមើល

```python
from microsoft_agent_framework import AgentMonitor

# កំណត់ការតាមដានសម្រាប់ភ្នាក់ងារដ្ឋាន
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # រក្សាទុកមាត្រដ្ឋាននៅក្នុងតំបន់សម្រាប់ប្រតិបត្ដិការផ្អាកអនឡាញ
)

agent.add_monitor(monitor)
```

### ឧទាហរណ៍អនុវត្តជាក់ស្តែង

#### ប្រព័ន្ធភ្នាក់ងារលក់រាយ Edge

```python
# អ្នកតំណាងកីឡូស្ល្យោមលក់រាយសម្រាប់ជំនួយអតិថិជននៅក្នុងហាង
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### ភ្នាក់ងារជំនួយសុខាភិបាល

```python
# អ្នកគាំទ្រឈ្មោះអ្នកជំងឺដែលអនុវត្តដូច HIPAA
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # ភាពឯកជនដែលបានបង្កើនសម្រាប់សុខាភិបាល
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### អនុវត្តន៍ល្អបំផុតសម្រាប់ Microsoft Agent Framework

#### គោលការណ៍អភិវឌ្ឍ

1. **ចាប់ផ្តើមអោយសាមញ្ញ**: ចាប់ផ្តើមពីសេណារីយោភ្នាក់ងារតែមួយមុនពេលបង្កើតប្រព័ន្ធភ្នាក់ងារច្រើនស្មុគស្មាញ
2. **ជ្រើសម៉ូដែលត្រឹមត្រូវ**: ជ្រើសម៉ូដែលតូចបំផុតដែលបំពេញតម្រូវការត្រឹមត្រូវរបស់អ្នក
3. **រចនាឧបករណ៍**: បង្កើតឧបករណ៍ផ្តោតគោលដៅ មិនមែនឧបករណ៍មានមុខងារច្រើនចម្រុះ
4. **ដោះស្រាយកំហុស**: អនុវត្តការធ្លាក់ចុះយ៉ាងរលូននៅសេណារីយោ offline និងម៉ូដែលខូច
5. **សាកល្បង**: សាកល្បងភ្នាក់ងារយ៉ាងទូលំទូលាយនៅលក្ខខណ្ឌ offline និងដែនដីកំណត់ធនធាន

#### លំនាំអនុវត្តដាក់ចេញល្អបំផុត

1. **ដាក់ចេញជាកំហំហសម្រុក**: ប្រើអ្នកប្រើតូចជាដំបូង ហើយតាមដានសមត្ថភាពយ៉ាងម៉ត់ចត់
2. **ត្រួតពិនិត្យធនធាន**: រៀបចំការជូនដំណឹងថ្នាក់កម្រិតអង្គចងចាំ CPU និងពេលឆ្លើយតប
3. **យុទ្ធសាស្រ្ដថយក្រោយ**: មានផែនការបម្រុងសម្រាប់ករណីម៉ូដែលខូច ឬធនធានមិនគ្រប់
4. **សុវត្ថិភាពជាលំដាប់ដើម**: ដាក់អនុវត្តតាមការគ្រប់គ្រងសុវត្ថិភាពចាប់ពីដំបូង មិនមែនបន្ទប់ក្រោយ
5. **ឯកសារយោង**: រក្សាទុកឯកសារត្រឹមត្រូវអំពីសមត្ថភាព និងកំណត់កំហុសរបស់ភ្នាក់ងារ

### ផែនទីអនាគតនិងការរួមបញ្ចូល

Microsoft Agent Framework កំពុងអភិវឌ្ឍបន្ដនៅជាមួយការប្រសើរឡើងរបស់ SLM, ឧបករណ៍ដាក់ចេញ edge, ការគ្រប់គ្រងធនធានដើម្បីបង្រួមតម្រូវការ និងការពង្រីកប្រព័ន្ធឧបករណ៍សម្រាប់សេណារីយ៍សហគ្រាសទូទៅ។

**លក្ខណៈពិសេសកំពុងមកដល់**:
- **AutoML សម្រាប់ការប្រសើរឡើងភ្នាក់ងារ**: ការត្រួតបន្ថែមស្វ័យប្រវត្តិសម្រាប់ SLM ក្នុងភារកិច្ចកំណត់ត្រា
- **Edge Mesh Networking**: ការសម្របសម្រួលរវាងការដាក់ចេញភ្នាក់ងារចុងក្រោយច្រើន
- **ទ្រឹស្តីត្រួតពិនិត្យខ្ពស់**: ការត្រួតពិនិត្យនិងវិភាគលើសមត្ថភាពភ្នាក់ងារលម្អិត
- **កម្មសិទ្ធិភ្នាក់ងារមើលឃើញ**: ឧបករណ៍បង្កើតភ្នាក់ងារលាងកូដ/គ្មានកូដ

## អនុវត្តល្អបំផុតសម្រាប់ការអនុវត្តភ្នាក់ងារ SLM

### គោលការណ៍ជ្រើស SLM សម្រាប់ភ្នាក់ងារ

ពេលជ្រើស SLM សម្រាប់ការដាក់ចេញភ្នាក់ងារ សូមពិនិត្យបញ្ហាដូចខាងក្រោម៖

**ពិនិត្យទំហំម៉ូដែល**: ជ្រើសម៉ូដែលចងខ្សែខ្ពស់ដូចជា Q2_K សម្រាប់ការប្រើប្រាស់ភ្នាក់ងារចល័តធ្ងន់ តុល្យថាមពលម៉ូដែលដូចជា Q4_K_M សម្រាប់សេណារីយ៍ភ្នាក់ងារទូទៅ និងម៉ូដែលមានភាពប្រសើរឡើង Q8_0 សម្រាប់ភ្នាក់ងារដែលតម្រូវការគុណភាពខ្ពស់។

**ការទាក់ទងនឹងករណីប្រើប្រាស់ភ្នាក់ងារ**: ទាក់ទងសមត្ថភាព SLM ទៅតាមតម្រូវការរបស់ភ្នាក់ងារដូចជាការរក្សាកំណត់ត្រឹមត្រូវសម្រាប់សេចក្តីសម្រេចជំហាន ឡើងល្បឿនបកស្រាយសម្រាប់ការប្រាស្រ័យទាក់ទងពេលវេលាពិត ការកំណត់ទំហំអង្គចងចាំសម្រាប់ការដាក់ចេញនៅ edge និងការប្រើ offline សម្រាប់ការរក្សាភាពឯកជន។

### របៀបសម្រាប់សម្រួលការបំលែងសម្រាប់ភ្នាក់ងារ SLM

**វិធីសាស្រ្តបំលែងសម្រាប់ភ្នាក់ងារ**: ជ្រើសកម្រិតបំលែងសមរម្យផ្អែកលើតម្រូវការគុណភាព និងកំណត់ធនធានរឹង។ ពិនិត្យ Q4_0 សម្រាប់ការចងខ្សែខ្ពស់ សម្រាប់ភ្នាក់ងារចល័ត Q5_1 សម្រាប់តុល្យភាពគុណភាព និងការចងខ្សែសម្រាប់ភ្នាក់ងារទូទៅ និង Q8_0 សម្រាប់គុណភាពជិតដើមក្នុងសេណារីយ៍ភ្នាក់ងារសំខាន់។
**ការជ្រើសរើសស៊ុមស្កេតសម្រាប់ការបញ្ចូលភ្នាក់ងារ**៖ ជ្រើសរើសស៊ុមស្កេតបង្ហូរល្អបម្រើតាមឧបករណ៍បម្រើ និងតម្រូវការរបស់ភ្នាក់ងារ។ ប្រើ Llama.cpp សម្រាប់ការបញ្ចូលភ្នាក់ងារចំពោះ CPU ជាស្តង់ដារ, Apple MLX សម្រាប់កម្មវិធីភ្នាក់ងារនៅលើ Apple Silicon, និង ONNX សម្រាប់ភាពអាចប្រើបានលើវេទិកាច្រើនសម្រាប់ភ្នាក់ងារ។

## ករណីការបម្លែង និងការប្រើប្រាស់ស្នូលភ្នាក់ងារ SLM ផ្លាស់ប្តូរប្រកបដោយការអនុវត្ត

### ស្ថានការណ៍ការបញ្ចូលភ្នាក់ងារពិតប្រាកដ

**កម្មវិធីភ្នាក់ងារក្មេងហ្វូន**៖ ទ្រង់ទ្រាយ Q4_K ល្អឥតខ្ចោះសម្រាប់កម្មវិធីភ្នាក់ងារលើទូរស័ព្ទមានដៃដែលមានចំណុចរំលងអង្គចងចាំតិចបំផុត ខណៈពេលដែល Q8_0 ផ្តល់សមត្ថភាពត្រឹមត្រូវសម្រាប់ប្រព័ន្ធភ្នាក់ងារលើថេប្លេត។ ទ្រង់ទ្រាយ Q5_K ផ្តល់គុណភាពល្អសម្រាប់ភ្នាក់ងារផលិតភាពចល័ត។

**កំព្យូទ័រតុ និងការគណនារបស់ភ្នាក់ងារសម្ព័ន្ធ**៖ Q5_K ផ្តល់សមត្ថភាពល្អបំផុតសម្រាប់កម្មវិធីភ្នាក់ងារតុ, Q8_0 ផ្តល់បទវិភាគមានគុណភាពខ្ពស់សម្រាប់បរិយាកាសភ្នាក់ងារការិយាល័យ, ហើយ Q4_K អនុញ្ញាតឲ្យដំណើរការជា​ប្រសិទ្ធភាពលើឧបករណ៍ភ្នាក់ងាររង។

**ការស្រាវជ្រាវ និងភ្នាក់ងារបច្ចេកទេស**៖ ទ្រង់ទ្រាយបរិមាណកម្រិតខ្ពស់ធ្វើអោយអាចស្រាវជ្រាវការវិភាគបែបតិចតួចនៃភ្នាក់ងារសម្រាប់ការស្រាវជ្រាវវិទ្យាសាស្ត្រនិងឧទាហរណ៍សាកល្បងដែលតម្រូវឲ្យមានការកំណត់ធនធានយ៉ាងខ្លាំង។

### តារាងវាយតម្លៃសមត្ថភាព SLM ភ្នាក់ងារ

**ល្បឿនវិភាគភ្នាក់ងារ**៖ Q4_K បង្កើតពេលវេលាចម្លើយភ្នាក់ងារលឿនបំផុតលើ CPU ចល័ត, Q5_K ផ្តល់អត្រាប្រៀបធៀបល្បឿន-គុណភាពសម្រាប់កម្មវិធីសរុប, Q8_0 ផ្តល់គុណភាពល្អបំផុតសម្រាប់ភារកិច្ចភ្នាក់ងារលំបាក, ហើយទ្រង់ទ្រង់បច្ចេកទេសផ្តល់ចំណូល throughput ខ្លាំងបំផុតសម្រាប់ឧបករណ៍ភ្នាក់ងារពិសេស។

**តម្រូវការអង្គចងចាំនៃភ្នាក់ងារ**៖ កម្រិតបរិមាណសម្រាប់ភ្នាក់ងារចាប់ពី Q2_K (ក្រោម ៥០០MB សម្រាប់គំរូភ្នាក់ងារតូច) ដល់ Q8_0 (ប្រហែល ៥០% នៃទំហំដើម), ជាមួយការកំណត់បច្ចេកទេសដែលបង្កើតការបង្ហាប់ខ្ពស់បំផុតសម្រាប់បរិយាកាសភ្នាក់ងារដែលមានកំណត់ធនធាន។

## បញ្ហា និងការពិចារណាសម្រាប់ភ្នាក់ងារ SLM

### ផ្លាស់ប្តូរសមត្ថភាពក្នុងប្រព័ន្ធភ្នាក់ងារ

ការបញ្ចូលភ្នាក់ងារ SLM តម្រូវឲ្យពិចារណយ៉ាងម៉ត់ចត់អំពីការផ្លាស់ប្តូរពីទំហំគំរូ, ល្បឿនចម្លើយភ្នាក់ងារ និងគុណភាពលទ្ធផល។ ខណៈ Q4_K ផ្តល់ល្បឿននិងប្រសិទ្ធភាពលើសសម្រាប់ភ្នាក់ងារចល័ត, Q8_0 ផ្តល់គុណភាពល្អបំផុតសម្រាប់ភារកិច្ចលំបាក។ Q5_K ជាជម្រើសមធ្យមសមស្របសម្រាប់កម្មវិធីភ្នាក់ងារទូទៅភាគច្រើន។

### ភាពសមរក្សដោយឧបករណ៍សម្រាប់ភ្នាក់ងារ SLM

ឧបករណ៍ភ្នាក់ងាររងមានសមត្ថភាពខុសៗគ្នាសម្រាប់ការបញ្ចូលសមាសភ្នាក់ងារ SLM។ Q4_K ប្រតិបត្តិបានទាន់សម័យលើក្រុមប្រើប្រាស់ត្រឹមត្រូវចំពោះភ្នាក់ងារងាយៗ, Q5_K តម្រូវធនធានគណនា​មធ្យមសម្រាប់សមត្ថភាពភ្នាក់ងារ​សមស្រប, ហើយ Q8_0 ទទួលបានអត្ថប្រយោជន៍ពីឧបករណ៍កំពូលសម្រាប់សមត្ថភាពភ្នាក់ងារវ្រឹកវរ។

### សុវត្ថិភាព និងភាពឯកជនក្នុងប្រព័ន្ធភ្នាក់ងារ SLM

ខណៈភ្នាក់ងារ SLM អនុញ្ញាតឲ្យដំណើរការផ្ទាល់ក្នុងតំបន់ដើម្បីបង្កើនភាពឯកជន ប៉ុន្តែតម្រូវឲ្យមានវិធានការសុវត្ថិភាពសម្រាប់ការការពារគំរូភ្នាក់ងារ និងទិន្នន័យក្នុងបរិយាកាសរង។ នេះមានសារៈសំខាន់ពិសេសនៅពេលបញ្ចូលទ្រង់ទ្រាយភ្នាក់ងារមានភាពត្រឹមត្រូវខ្ពស់ក្នុងបរិយាកាសសហគ្រាស ឬទ្រង់ទ្រាយភ្នាក់ងារបង្ហាប់ក្នុងកម្មវិធីដែលចំណាយទិន្នន័យដែលមានសាលសំខាន់។

## ឆន្ទៈអនាគតក្នុងការអភិវឌ្ឍភ្នាក់ងារ SLM

ទេសភាពភ្នាក់ងារ SLM បន្តចំរើនជាមួយនឹងចំណុចចង្អុលបង្ហាញថ្មីៗទាក់ទងនឹងបច្ចេកទេសបង្ហាប់, វិធីសាស្ត្របញ្ចូលល្អប្រសើរ និងយុទ្ធសាស្ត្របញ្ចូលលើឧបករណ៍រង។ ការអភិវឌ្ឍន៍នាពេលអនាគតរួមបញ្ចូលវិធីសាស្ត្របរិមាណឡើងវិញមានប្រសិទ្ធភាព សម្រាប់គំរូភ្នាក់ងារ, វិធីសាស្ត្របង្ហាប់ល្អប្រសើរសម្រាប់លំហូរចំណុចភ្នាក់ងារ និងការរួមបញ្ចូលល្អជាមួយអេលចរណ៍របស់ឧបករណ៍រងសម្រាប់ដំណើរការភ្នាក់ងារ។

**ការព្យាករណ៍ទីផ្សារសម្រាប់ភ្នាក់ងារ SLM**៖ យោងតាមការស្រាវជ្រាវថ្មីៗ ការស្វ័យប្រវត្តិដោយភ្នាក់ងារអាចបំផ្លាញ ៤០–៦០% នៃភារកិច្ចផ្ទាល់ដដែលនៅក្នុងលំហូរការងារ​សហគ្រាសឆាប់ៗពាក់កណ្តាលឆ្នាំ ២០២៧ ដោយសារតែ SLM គឺជាអ្នកដឹកនាំក្នុងការផ្លាស់ប្តូរនេះដោយសារតម្លៃថោក និងភាពបត់បែនក្នុងការបញ្ចូល។

**ឧទាហរណ៍បច្ចេកវិទ្យានៅក្នុងភ្នាក់ងារ SLM**៖
- **ភ្នាក់ងារ SLM ផ្ដាច់មុខ**៖ គំរូវិស្ស័យជាក់លាក់បណ្តុះបណ្តាលសម្រាប់ភារកិច្ចនិងឧស្សាហកម្មជាក់លាក់
- **ការគណនា Edge Agent**៖ សមត្ថភាពភ្នាក់ងារលើឧបករណ៍ជាមួយភាពឯកជនកាន់តែប្រសើរ និងការផ្លាស់ប្តូរបន្ថយ
- **ការត្រួតពិនិត្យភ្នាក់ងារ**៖ ការសម្រុះសម្រួលល្អរវាងភ្នាក់ងារ SLM ជាច្រើនជាមួយចរន្តសំរោងរត់ និងកំណត់ទម្ងន់ធារាសាស្ត្រ
- **ការបង្វិលធម្មតា**៖ ភាពបត់បែន SLM ផ្ដល់ឱកាសចូលរួមទូលំទូលាយក្នុងការអភិវឌ្ឍភ្នាក់ងារ នៅក្នុងអង្គការផ្សេងៗ

## ចាប់ផ្តើមជាមួយភ្នាក់ងារ SLM

### ជំហានទី ១៖ រៀបចំបរិយាកាស Microsoft Agent Framework

**ដំឡើងការពឹងផ្អែក**៖
```bash
# តំឡើង Microsoft Agent Framework
pip install microsoft-agent-framework

# តំឡើង Foundry Local SDK សម្រាប់ប្រើប្រាស់នៅមួកស្រោច
pip install foundry-local-sdk

# តំឡើងការគាំទ្របន្ថែមសម្រាប់ភ្នាក់ងារមួកស្រោច
pip install openai asyncio
```

**ចាប់ផ្តើម Foundry Local**៖
```bash
# ចាប់ផ្ដើមសេវាកម្ម Foundry Local
foundry service start

# ផ្ទុកម៉ូឌែលលំនាំដើមសម្រាប់ការអភិវឌ្ឍក្រុមហ៊ុនភ្នាក់ងារ
foundry model run phi-4-mini
```

### ជំហានទី ២៖ ជ្រើសរើស SLM របស់អ្នកសម្រាប់កម្មវិធីភ្នាក់ងារ
ជម្រើសល្បីសម្រាប់ Microsoft Agent Framework៖
- **Microsoft Phi-4 Mini (3.8B)**៖ ល្អសម្រាប់ភារកិច្ចភ្នាក់ងារទូទៅជាមួយសមត្ថភាពត្រឹមត្រូវ
- **Qwen2.5-0.5B (0.5B)**៖ លឿនខ្ពស់សម្រាប់ភ្នាក់ងារបង្វិលរ៉ូតទិងចាត់ថ្នាក់ធ្វើ
- **Qwen2.5-Coder-0.5B (0.5B)**៖ ផ្ដាច់មុខចំពោះភាយភាគិលដែលទាក់ទងនឹងកូដ
- **Phi-4 (7B)**៖ ការវិភាគកម្រិតខ្ពស់សម្រាប់ស្ថានភាព edge ភ្នំពេញពិបាកពេលមានធនធានគ្រប់គ្រាន់

### ជំហានទី ៣៖ បង្កើតភ្នាក់ងារដំបូងរបស់អ្នកជាមួយ Microsoft Agent Framework

**ការរៀបចំភ្នាក់ងារមូលដ្ឋាន**៖
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# កំណត់ការតភ្ជាប់ Foundry ទីតាំង​លោក
foundry = FoundryLocalManager("phi-4-mini")

# បង្កើតកំណត់រចនាសម្ព័ន្ធភ្នាក់ងារ
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# បង្កើត និងកំណត់រចនាសម្ព័ន្ធភ្នាក់ងារ
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# កំណត់ឧបករណ៍សាមញ្ញ
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# សាកល្បងភ្នាក់ងារ
response = agent.chat("What time is it?")
print(response)
```

### ជំហានទី ៤៖ កំណត់វិសាលភាព និងតម្រូវការភ្នាក់ងារ
ចាប់ផ្តើមជាមួយកម្មវិធីភ្នាក់ងារដែលមានការប្តុះប្តូរ និងកំណត់លម្អិតប្រើប្រាស់ Microsoft Agent Framework៖
- **ភ្នាក់ងារផ្នែកមួយ**៖ សេវាកម្មអតិថិជន ឬ ការ​រៀបចំ​កាលវិធី ឬ ស្រាវជ្រាវ
- **គោលបំណងភ្នាក់ងារប្រកាច់ច្បាស់**៖ គោលដៅដ៏ច្បាស់លាស់ និងអាចវាស់ទុនបានសម្រាប់ការសម្តែង
- **បាំងការរួមបញ្ចូលឧបករណ៍កំណត់ដែន**៖ ឧបករណ៍៣-៥អតិបរមាសម្រាប់ការបញ្ចូលភ្នាក់ងារដំបូង
- **ការកំណត់ដែនភ្នាក់ងារប្រកាច់ច្បាស់**៖ ផ្លូវបង្កើនការត្រួតពិនិត្យបញ្ហាករណីពិបាក
- **រចនាបថ Edge-First**៖ ផ្តល់អាទិភាពដំណើរការផ្ទាល់ និងមុខងារ Offline

### ជំហានទី ៥៖ អនុវត្តការបញ្ចូលបរិយាកាស Edge ជាមួយ Microsoft Agent Framework

**ការកំណត់ធនធាន**៖
```python
from microsoft_agent_framework import ResourceConfig

# កំណត់កម្រាប់ការចែកចាយទីតាំងមួយ
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**ដំណើរការ Measures សុវត្ថិភាពសម្រាប់ភ្នាក់ងារ Edge**៖
- **ការត្រួតពិនិត្យបញ្ចូលក្នុងតំបន់**៖ ពិនិត្យសំណើដោយគ្មានការពឹងផ្អែកលើ cloud
- **ការចម្រាញ់លទ្ធផល Offline**៖ បញ្ជាក់ចម្លើយឲ្យគុណភាពត្រូវបានគ្រប់គ្រងក្នុងតំបន់
- **ការគ្រប់គ្រងសុវត្ថិភាព Edge**៖ អនុវត្តសុវត្ថិភាពដោយមិនចាំបាច់មានការភ្ជាប់អ៊ីនធឺណិត
- **ការត្រួតពិនិត្យក្នុងតំបន់**៖ តាមដានសមត្ថភាព និងបញ្ជាក់បញ្ហាក្នុងការប្រើប្រាស់ telemetry របស់ edge

### ជំហានទី ៦៖ វាស់វែង និងបញ្ចេញភាពប្រសើរនៃសមត្ថភាពភ្នាក់ងារ Edge
- **អត្រាបញ្ចប់ភារកិច្ចភ្នាក់ងារ**៖ តាមដានអត្រាជោគជ័យក្នុងលក្ខខណ្ឌអ៊ិលុយ
- **ពេលវេលាចម្លើយភ្នាក់ងារ**៖ ធានាពេលវេលាចម្លើយក្រោមមួយវិនាទីសម្រាប់ការបញ្ចូល Edge
- **ការប្រើប្រាស់ធនធាន**៖ តាមដានអង្គចងចាំ, CPU និងថ្មឧបករណ៍ Edge
- **ប្រសិទ្ធិភាពថ្លៃថោក**៖ ប្រៀបធៀបថ្លៃការបញ្ចូល Edge នឹងជម្រើសCloud
- **ភាពជឿជាក់ឥតភ្ជាប់បណ្ដាញ**៖ វាស់វែងសមត្ថភាពភ្នាក់ងារនៅពេលបាត់បណ្ដាញ

## ចំណុចសំខាន់សម្រាប់ការអនុវត្តភ្នាក់ងារ SLM

1. **SLMs គ្រប់គ្រងបាននាពេលភ្នាក់ងារ**៖ សម្រាប់ភារកិច្ចភ្នាក់ងារភាគច្រើន គំរូតូចមុខតំណែងដូចគ្នានឹងគំរូធំទាំងអស់ក្នុងការផ្តល់អត្ថប្រយោជន៍សំខាន់
2. **ប្រសិទ្ធិភាពថ្លៃថោកក្នុងភ្នាក់ងារ**៖ អ្នកដំណើរការ SLM ភ្នាក់ងារថ្លៃថោក ១០-៣០ដង គ្រប់គ្រាន់សម្រាប់ការបញ្ចូលជាយូរអង្វែង
3. **ការបង្រួមបង្គុំនៅក្នុងភ្នាក់ងារ**៖ SLM ដែលបានបង្រួមតែដូចជាLMM ទូទៅល្អប្រសើរជាងគំរូទូទៅនៅក្នុងកម្មវិធីជាក់លាក់
4. **ស្ថាបត្យកម្មភ្នាក់ងារជម្រុះ**៖ ប្រើប្រាស់ SLM សម្រាប់ភារកិច្ចធម្មតា LLM សម្រាប់ការវិភាគភ្នាក់ងារលំបាកពេលចាំបាច់
5. **Microsoft Agent Framework ផ្គត់ផ្គង់ការបញ្ចូលផលិតកម្ម**៖ ផ្តល់ឧបករណ៍ថ្នាក់សហគ្រាសសម្រាប់បង្កើត បញ្ចូល និងគ្រប់គ្រងភ្នាក់ងារខាងលើ
6. **គោលការណ៍រចនាបថ Edge-First**៖ ភ្នាក់ងារអាចដំណើរការនៅក្រៅបណ្តាញ ជាមួយដំណើរការប្រក់ផ្អែកក្នុងតំបន់ បញ្ជាក់ភាពឯកជន និងភាពជឿជាក់
7. **ការរួមបញ្ចូលFoundry Local**៖ ភ្ជាប់បានល្អរវាង Microsoft Agent Framework និងការវិភាគលទ្ធផលក្នុងតំបន់
8. **អនាគតគឺជា ភ្នាក់ងារ SLM**៖ គំរូភាសាតូចជាមួយស៊ុមស្កេតផលិតកម្ម គឺអនាគតនៃ AI ប្រតិបត្តិការភ្នាក់ងារ បង្កើតឱកាសចូលរួមទូលំទូលាយ និងមានប្រសិទ្ធភាព

## ឯកសារយោង និងអានបន្ថែម

### ឯកសារស្រាវជ្រាវមូលដ្ឋាន និងផ្សាយផ្សាយ

#### ភ្នាក់ងារ AI និងប្រព័ន្ធភ្នាក់ងារទំនើប
- **"ភ្នាក់ងារភាសាផ្ទាល់ការអាចបង្កើតនៅជាជាប្លង់ចំណាំ"** (2024) - ការស្រាវជ្រាវមូលដ្ឋានលើស្ថាបត្យកម្មភ្នាក់ងារ និងវិធានចំណុចល្អបន្ត
  - អ្នកនិពន្ធ៖ Wenyue Hua, Lishan Yang, ជាដំណាក
  - តំណភ្ជាប់៖ https://arxiv.org/abs/2402.16823
  - ទេសនា៖ ការរចនាភ្នាក់ងារពីផ្លង់ផ្សេងៗ និងយុទ្ធសាស្ត្រកែលម្អ

- **"ការកើនឡើង និងសក្តានុពលនៃភ្នាក់ងារជាគំរូភាសាចំណុះធំ"** (2023)
  - អ្នកនិពន្ធ៖ Zhiheng Xi, Wenxiang Chen, ជាដំណាក
  - តំណភ្ជាប់៖ https://arxiv.org/abs/2309.07864
  - ទេសនា៖ សេចក្តីស្រាវជ្រាវស្រួលៗលើសមត្ថភាព និងការប្រើប្រាស់ភ្នាក់ងារតាមគំរូ LLM

- **"ស្ថាបត្យកម្មផ្លូវចិត្តសម្រាប់ភ្នាក់ងារភាសា"** (2024)
  - អ្នកនិពន្ធ៖ Theodore Sumers, Shunyu Yao, ជាដំណាក
  - តំណភ្ជាប់៖ https://arxiv.org/abs/2309.02427
  - ទេសនា៖ ស្ថាបត្យកម្មផ្លូវចិត្តសម្រាប់ការរចនាភ្នាក់ងារពូកែល្អ

#### គំរូភាសាតូច និងការអប្បបរមាបន្ថយ
- **"របាយការណ៍បច្ចេកទេស Phi-3៖ គំរូភាសាមានសមត្ថភាពខ្ពស់នៅតាមទូរស័ព្ទរបស់អ្នក"** (2024)
  - ក្រុមអ្នកស្រាវជ្រាវ Microsoft
  - តំណភ្ជាប់៖ https://arxiv.org/abs/2404.14219
  - ទេសនា៖ គោលការណ៍រចនាសម្ព័ន្ធ SLM និងយុទ្ធសាស្ត្របញ្ចូលចល័ត

- **"របាយការណ៍បច្ចេកទេស Qwen2.5"** (2024)
  - ក្រុម Alibaba Cloud
  - តំណភ្ជាប់៖ https://arxiv.org/abs/2407.10671
  - ទេសនា៖ វិធីបង្រៀន SLM ជាប់ខ្ពស់ និងកែលម្អល្បឿន

- **"TinyLlama៖ គំរូភាសាតូចបើកចំហ"** (2024)
  - អ្នកនិពន្ធ៖ Peiyuan Zhang, Guangtao Zeng, ជាដំណាក
  - តំណភ្ជាប់៖ https://arxiv.org/abs/2401.02385
  - ទេសនា៖ ការរចនាគំរូតូចក្នុងទំហំ និងភាពប្រសើរនៃការបង្រៀន

### ឯកសារផ្លូវការនិងស៊ុមស្កេត

#### Microsoft Agent Framework
- **ឯកសារផ្លូវការ**៖ https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **Repository GitHub**៖ https://github.com/microsoft/agent-framework

#### Foundry Local
- **Repository លេខមួយ**៖ https://github.com/microsoft/foundry-local
- **ឯកសារ**៖ https://github.com/microsoft/foundry-local/blob/main/docs/README.md

#### VLLM
- **Repository លេខមួយ**៖ https://github.com/vllm-project/vllm
- **ឯកសារ**៖ https://docs.vllm.ai/

#### Ollama
- **គេហទំព័រផ្លូវការ**៖ https://ollama.ai/
- **Repository GitHub**៖ https://github.com/ollama/ollama

### ស៊ុមស្កេតបង្ហើបគំរូ

#### Llama.cpp
- **Repository**៖ https://github.com/ggml-org/llama.cpp

#### Microsoft Olive
- **ឯកសារ**៖ https://microsoft.github.io/Olive/
- **Repository GitHub**៖ https://github.com/microsoft/Olive

#### OpenVINO
- **គេហទំព័រផ្លូវការ**៖ https://docs.openvino.ai/

#### Apple MLX
- **Repository**៖ https://github.com/ml-explore/mlx

### របាយការណ៍ឧស្សាហកម្ម និងវិភាគទីផ្សារ

#### ស្រាវជ្រាវទីផ្សារភ្នាក់ងារ AI
- **"ស្ថានភាពភ្នាក់ងារ AI ឆ្នាំ ២០២៥"** - McKinsey Global Institute
  - តំណភ្ជាប់៖ https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - ទេសនា៖ ទំនោរទីផ្សារ និងការទទួលយកសហគ្រាស

#### ការវាស់តម្លៃបច្ចេកទេស

- **"កម្រិតវាយតម្លៃ AI ឆមាស Edge"** - MLPerf
  - តំណភ្ជាប់៖ https://mlcommons.org/en/inference-edge/
  - ទេសនា៖ មាត្រដ្ឋានថ្មីសំរាប់វាយតម្លៃសមត្ថភាពបញ្ចូលលើឧបករណ៍ Edge

### ស្តង់ដារ និងលក្ខណៈបច្ចេកទេស

#### ទ្រង់ទ្រាយគំរូ និងស្តង់ដារ
- **ONNX (Open Neural Network Exchange)**៖ https://onnx.ai/
  - ទ្រង់ទ្រាយគំរូពហុវេទិកាសម្រាប់ប្រើប្រាស់រួម
- **រចនាសម្ព័ន្ធ GGUF**៖ https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - ទ្រង់ទ្រាយគំរូបរិមាណសម្រាប់វិភាគ CPU
- **OpenAI API Specification**៖ https://platform.openai.com/docs/api-reference
  - ទ្រង់ទ្រាយAPI ស្តង់ដាសម្រាប់រួមបញ្ចូលគំរូភាសា

#### សុវត្តិភាព និងការអនុវត្ដគោលការណ៍
- **NIST AI Risk Management Framework**៖ https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - ប្រព័ន្ធ AI**៖ ស៊ុមស្កេតនៃប្រព័ន្ធ AI និងសុវត្ថិភាព
- **ស្តង់ដារ IEEE សម្រាប់ AI**៖ https://standards.ieee.org/industry-connections/ai/

ការផ្លាស់ប្តូរទៅកាន់ភ្នាក់ងារ SLM ជារូបមន្តមួយសំខាន់ក្នុងរបៀបដែលយើងដំណើរការ AI។ Microsoft Agent Framework រួមជាមួយវេទិកាតំបន់ក្នុងតំបន់ និងគំរូភាសាតូចមានប្រសិទ្ធភាពផ្តល់ដំណោះស្រាយពេញលេញសម្រាប់បង្កើតភ្នាក់ងារផលិតកម្មដែលដំណើរការបានល្អក្នុងបរិយាកាស Edge។ ដោយផ្តោតលើប្រសិទ្ធភាព ការផ្តាច់មុខ និងប្រយោជន៍ជាក់ស្តែង បច្ចេកវិទ្យានេះធ្វើឲ្យភ្នាក់ងារ AI ទៅកាន់មានភាពងាយស្រួល ក្រៅពីថ្លៃថោក និងមានប្រសិទ្ធភាពសម្រាប់កម្មវិធីពិតប្រាកដនៅគ្រប់ឧស្សាហកម្ម និងបរិយាកាស Edge។

នៅពេលដែលយើងបន្តទៅឆ្នាំ ២០២៥ ការរួមបញ្ចូលគំរូតូចមានសមត្ថភាព ការស៊ុមស្កេតភ្នាក់ងារ Microsoft Agent Framework និងវេទិកាបញ្ចូល Edge រឹងមាំ នឹងបង្ហាញអោយឃើញឱកាសថ្មីសម្រាប់ប្រព័ន្ធស្វ័យប្រវត្តិដែលអាចដំណើរការងាយស្រួលលើឧបករណ៍ Edge ខណៈបញ្ជាក់ភាពឯកជន កាត់បន្ថយថ្លៃថោក និងផ្ដល់បទពិសោធន៍ប្រើប្រាស់ល្អឥតឈប់។

**ជំហានបន្ទាប់សម្រាប់ការអនុវត្ត**៖
1. **ស្រាវជ្រាវការហៅមុខងារ**៖ រៀនពីរបៀប SLM គ្រប់គ្រងរួមបញ្ចូលឧបករណ៍ និងលទ្ធផលបានរៀបចំ
2. **ជំនាញ Model Context Protocol (MCP)**៖ យល់ដឹងពីគំរូទំនាក់ទំនងភ្នាក់ងារកម្រិតខ្ពស់
3. **សាងសង់ភ្នាក់ងារផលិតកម្ម**៖ ប្រើ Microsoft Agent Framework សម្រាប់ការបញ្ចូលថ្នាក់សហគ្រាស
4. **បង្កើតប្រសិទ្ធភាពសម្រាប់ Edge**៖ អនុវត្តវិធីសាស្ត្រកែលម្អកម្រិតខ្ពស់សម្រាប់បរិយាកាសមានកំណត់ធនធាន

## ➡️ តើអ្វីទៅជាដំណាក់កាលបន្ទាប់

- [02: ការហៅមុខងារ​នៅក្នុងគំរូភាសាតូច (SLMs)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**ការបញ្ជាក់**៖  
ឯកសារនេះត្រូវបានបំលែងជាភាសាខ្មែរ ដោយប្រើសេវាកម្មបកប្រែ AI [Co-op Translator](https://github.com/Azure/co-op-translator)។ ទោះបីយើងខំប្រឹងប្រែងសម្រាប់ភាពត្រឹមត្រូវក៍ដោយសារតែការបកប្រែដោយស្វ័យប្រវត្តិអាចមានកំហុស ឬភាពមិនត្រឹមត្រូវ។ ឯកសារដើមនៅក្នុងភាសាទីមូលដ្ឋាន គួរត្រូវបានគិតថាជាប្រភពដែលមានសម្បទាន។ សម្រាប់ព័ត៌មានសំខាន់ៗ ការបកប្រែដោយមនុស្សជំនាញគឺគួរត្រូវបានណែនាំ។ យើងមិនទទួលខុសត្រូវចំពោះការយល់ច្រឡំ ឬការបកប្រែអកុយសម្រាប់ការប្រើប្រាស់បកប្រែនេះឡើយ។
<!-- CO-OP TRANSLATOR DISCLAIMER END -->