# កិច្ចសម័យទី ៥៖ កសាងភ្នាក់ងារបំពាក់បច្ចេកវិទ្យា AI ដោយរហ័សជាមួយ Foundry Local

## សេចក្តីសង្ខេប

រចនានិងលៃតម្រូវភ្នាក់ងារអ្នកសម្តែងច្រើនតួនាទី ដោយប្រើប្រាស់រត់ផ្លូវ Foundry Local ដែលមានការពន្យារពេលទាប និងការការពារបញ្ហាភាពឯកជន។ អ្នកនឹងកំណត់តួនាទីភ្នាក់ងារ នីតិវិធីអង្គចងចាំ គំរូការហៅឧបករណ៍ និងក្រាហ្វិកអនុវត្ត។ កិច្ចសម័យនេះណែនាំគំរូ scaffolding ដែលអ្នកអាចពង្រីកដោយ Chainlit ឬ LangGraph។ គំរូគម្រោងចាប់ផ្តើមពង្រីកសំណុំរចនាសម្ព័ន្ធភ្នាក់ងារបច្ចុប្បន្ន ដើម្បីបន្ថែមការរក្សាទុកអង្គចងចាំ និងចំណុចវាយតម្លៃ។

## គោលបំណងសិក្សា

- **កំណត់តួនាទី**៖ ការជាអ្នកបញ្ជា និងដែនកំណត់សមត្ថភាពប្រព័ន្ធ
- **អនុវត្តអង្គចងចាំ**៖ អង្គចងចាំរយៈពេលខ្លី (ការសន្ទនា), រយៈពេលវែង (vector / ឯកសារ), សៀវភៅរាយកម្រិតបណ្តោះអាសន្ន
- **គ្រោងប្លង់ការងារ**៖ ជំហានភ្នាក់ងារចំណាប់បន្ទាត់ ប្រកួតប្រជែង និងថ្នាក់ទំនាក់ទំនង
- **បញ្ចូលឧបករណ៍**៖ គំរូពង្រីកហៅមុខងារប្រសើរដោយទំងន់ស្រាល
- **វាយតម្លៃ**៖ តាមដានមូលដ្ឋាន និងវាយតម្លៃលទ្ធផលដោយវិធានការ

## លក្ខខណ្ឌមុនពេលចូលរួម

- កិច្ចសម័យទី ១–៤ បានបញ្ចប់
- Python ជាមួយ `foundry-local-sdk`, `openai` និងជម្រើស `chainlit`
- ម៉ូឌែលមូលដ្ឋានដំណើរការជាផ្លូវការបណ្តុំ (យ៉ាងហោចណាស់ជា `phi-4-mini`)

### ខ្លឹមសារបរិស្ថានលើគុណភាពពហុវេទិកា

Windows:
```powershell
py -m venv .venv
 .\.venv\Scripts\Activate.ps1
pip install --upgrade pip
pip install foundry-local-sdk openai
```

macOS:
```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
pip install foundry-local-sdk openai
```

បើដំណើរការភ្នាក់ងារពី macOS ធ្វើប្រតិបត្តិការជាមួយសេវាកម្មម៉ាស៊ីនផ្ទាល់ពីចម្ងាយលើ Windows:
```bash
export FOUNDRY_LOCAL_ENDPOINT=http://<windows-host>:5273/v1
```

## សម្រង់ការបង្ហាញ (៣០ នាទី)

### ១. កំណត់តួនាទីភ្នាក់ងារ និងអង្គចងចាំ (៧ នាទី)

បង្កើត `samples/05-agents/agents_core.py`៖

```python
#!/usr/bin/env python3
"""Minimal multi-agent scaffolding using Foundry Local (OpenAI-compatible)."""
from openai import OpenAI
from dataclasses import dataclass, field
from typing import List, Dict, Any, Callable
import time, json

client = OpenAI(base_url="http://localhost:5273/v1", api_key="not-needed")

@dataclass
class AgentMessage:
    role: str
    content: str
    meta: Dict[str, Any] = field(default_factory=dict)

@dataclass
class Agent:
    name: str
    system_prompt: str
    tools: Dict[str, Callable] = field(default_factory=dict)
    memory: List[AgentMessage] = field(default_factory=list)

    def remember(self, role: str, content: str, **meta):
        self.memory.append(AgentMessage(role=role, content=content, meta=meta))

    def context(self, window:int=8):
        recent = self.memory[-window:]
        msgs = [ {"role": "system", "content": self.system_prompt}]
        msgs += [ {"role": m.role, "content": m.content} for m in recent ]
        return msgs

    def act(self, user_input: str, model: str = "phi-4-mini", temperature: float=0.4):
        self.remember("user", user_input)
        resp = client.chat.completions.create(
            model=model,
            messages=self.context() + [{"role": "user", "content": user_input}],
            temperature=temperature,
            max_tokens=400
        )
        answer = resp.choices[0].message.content
        self.remember("assistant", answer, model=model)
        return answer

researcher = Agent(
    name="Researcher",
    system_prompt="You gather factual, structured insights concisely."
)
writer = Agent(
    name="Writer",
    system_prompt="You rewrite content for clarity and engagement while preserving facts."
)

def demo():
    q = "Explain why edge inference matters for privacy and latency."
    r = researcher.act(q)
    print("Researcher ->", r[:200], "...\n")
    w = writer.act(f"Rewrite more user-friendly: {r}")
    print("Writer ->", w[:200], "...")

if __name__ == "__main__":
    demo()
```

### ២. គំរូ scaffolding CLI (៣ នាទី)

```powershell
python samples/05-agents/agents_core.py
```

### ៣. បន្ថែមការហៅឧបករណ៍ (៧ នាទី)

ពង្រីកជាមួយ `samples/05-agents/tools.py`៖

```python
from datetime import datetime
import math, json

def tool_time(_:str)->str:
    return f"Current UTC time: {datetime.utcnow().isoformat()}"

def tool_estimate_tokens(text:str)->str:
    approx = len(text.split()) * 1.35
    return f"Estimated tokens ~ {int(approx)}"

TOOLS = {
    "get_time": tool_time,
    "estimate_tokens": tool_estimate_tokens
}
```

កែប្រែ `agents_core.py` ដើម្បីអនុញ្ញាតឲ្យមាន구syntax សាមញ្ញសម្រាប់ឧបករណ៍៖ អ្នកប្រើសរសេរ `#tool:get_time` ហើយភ្នាក់ងារបន្ថែមលទ្ធផលឧបករណ៍ចូលក្នុងបរិបទមុនសរសេរ។

### ៤. លំហូរការ​អនុវត្ត​ធ្វើការតាមគ្រប់គ្រង (៦ នាទី)

បង្កើត `samples/05-agents/orchestrator.py`៖

```python
from agents_core import researcher, writer, Agent
from tools import TOOLS
from openai import OpenAI

client = OpenAI(base_url="http://localhost:5273/v1", api_key="not-needed")

def inject_tools(agent: Agent, user_input: str) -> str:
    if user_input.startswith('#tool:'):
        name = user_input.split(':',1)[1].strip()
        if name in TOOLS:
            out = TOOLS[name](../../../Workshop/"")
            agent.remember("tool", out, tool=name)
            return f"Tool[{name}] => {out}"
    return None

def pipeline(question: str):
    tool_note = inject_tools(researcher, '#tool:get_time')
    r = researcher.act(question)
    w = writer.act(f"Improve readability:\n{r}\nAdd a friendly summary line.")
    return {"raw": r, "refined": w, "tool": tool_note}

if __name__ == '__main__':
    result = pipeline("List three concrete benefits of local SLM inference for regulated industries.")
    for k,v in result.items():
        print(f"== {k.upper()} ==\n{v}\n")
```

### ៥. គំរូគម្រោងចាប់ផ្តើម៖ ពង្រីក `05-agent-architecture` (៧ នាទី)

បន្ថែម៖
1. ស្រទាប់អង្គចងចាំថែរក្សាជាប់មាប់ (ឧទាហរណ៍ JSON lines សម្រាប់ការសន្ទនា)
2. វិធានការវាយតម្លៃសាមញ្ញ៖ ត្រឹមត្រូវភាព / ភាពច្បាស់លាស់ / ចលាចលស្ទីល
3. ជម្រើសផ្ទាំងមុខ Chainlit (រុសពីរតាបៈ សន្ទនា និងតាមដាន)
4. ជម្រើសម៉ាស៊ីនរដ្ឋ LangGraph ស្ទីល (បើបន្ថែមអាសយដ្ឋាន) សម្រាប់សេចក្តីសម្រេចចិត្តពហុសាខា

## បញ្ជីត្រួតពិនិត្យបញ្ជាក់

```powershell
foundry model run phi-4-mini
python samples/05-agents/orchestrator.py
```

រំពឹងថាលទ្ធផលផ្តល់តាមស្តង់ដារបង្ហាញកំណត់សម្គាល់ការវាយបញ្ចូលឧបករណ៍។

## មើលទិដ្ឋភាពយុទ្ធសាស្រ្តអង្គចងចាំ

| ស្រទាប់ | បំណង | ឧទាហរណ៍ |
|-------|---------|---------|
| រយៈពេលខ្លី | តំណក់តំណនាដំណើរការ | សារ N ចុងក្រោយ |
| កំណត់ហេតុជាស៊េសชั่น | ការចងការសម័យ | JSON គ្រប់សម័យ |
| ឧត្តមសម្ព័ន្ធ | ការពិនិត្យពីរយៈពេលវែង | រាយបញ្ជីសង្ខេបជា vector |
| សៀវភៅរាយកម្រិត | ជំហានចំឡើយ | ខ្សែគំនិតនៅក្នុង (ឯកជន) |

## ចំណុចវាយតម្លៃ (គំនិតទូទៅ)

```python
evaluation = {
  "factuality": None,  # ម៉ានុយអាល់ ឬ តាមចរិតវិជ្ជា
  "clarity": None,
  "style": None,
  "latency_sec": generation_time,
  "model": model_used
}
```

## ការដោះស្រាយបញ្ហា

| បញ្ហា | មូលហេតុ | ដំណោះស្រាយ |
|-------|------|------------|
| ឆ្លើយតបស្ទ 반복 | បង្អួចបរិបទធំឬតិចពេក | កំណត់បរិមាណរយៈអង្គចងចាំ |
| មិនហៅឧបករណ៍ | សyntaxខុស | ប្រើ f​ormat `#tool:tool_name` |
| ការត្រួតត្រាអនុវត្តយឺត | ម៉ូឌែលត្រជាក់ច្រើន | ប្រតិបត្តិការប្រូមpt ចាប់ផ្តើមកំដៅ |

## ឯកសារយោង

- Foundry Local SDK៖ https://github.com/microsoft/Foundry-Local/tree/main/sdk/python
- LangGraph (គំនិតជម្រើស)៖ https://github.com/langchain-ai/langgraph
- Chainlit៖ https://docs.chainlit.io

---

**រយៈពេលសម័យ**៖ ៣០ នាទី  
**កម្រិតភាពស្មុគស្មាញ**៖ ជំនាញខ្ពស់

## ដំណើរការឧទាហរណ៍ និងការតភ្ជាប់វគ្គបណ្តុះបណ្តាល

| ស្គ្រីបវគ្គបណ្តុះបណ្តាល | សេណារីយ៉ោ | គោលបំណង | ឧទាហរណ៍ការបញ្ចូល |
|-----------------|----------|-----------|----------------|
| `samples/session05/agents_orchestrator.py` / `notebooks/session05_agents_orchestrator.ipynb` | ប៊ុតស្រាវជ្រាវចំណេះដឹង បង្កើតសេចក្តីសង្ខេបសម្រួលសម្រាប់អ្នកប្រតិបត្តិការ | ផ្លូវចរន្តភ្នាក់ងារចំនួនពីរ (ស្រាវជ្រាវ → កែសម្រួល) ជាមួយម៉ូឌែលផ្ទាច្រើនជាជម្រើស | ពន្យល់មូលហេតុដែលការសន្និដ្ឋានចំណុចគែមមានសារៈសំខាន់សម្រាប់ការទទួលអនុគ្រោះ។ |
| (ពង្រីក) គំនិត `tools.py` | បន្ថែមឧបករណ៍វាយតម្លៃពេលវេលា និងจำนวนទឹកតOKEN | បង្ហាញគំរូហៅឧបករណ៍ទំងន់ស្រាល | #tool:get_time |

### ពិពណ៌នាសេណារីយ៉ោ  
ក្រុមឯកសារអនុវត្តន៍តម្រូវការបង្ហាញសេចក្តីសង្ខេបក្នុងរយៈពេលខ្លីពីចំណេះដឹងក្នុងស្រុកដោយមិនបញ្ចូនខ្លឹមសារទៅបម្រើការបណ្តាញ។ ភ្នាក់ងារស្រាវជ្រាវប្រមូលកំណត់ហេតុភាពត្រឹមត្រូវខ្លីៗ; ភ្នាក់ងារកែសម្រួលបញ្ចូលស្ទីលសម្រាប់អ្នកប្រតិបត្តិការ។ អ្នកអាចផ្ដល់ឈ្មោះម៉ូឌែលផ្សេងៗដើម្បីសម្រួលការពន្យារពេល (ម៉ូឌែលរហ័ស SLM) និងកែសម្រួលស្ទីល (ម៉ូឌែលធំទាំងនេះពេលចាំបាច់)។

### បរិស្ថានម៉ូឌែលច្រើនឧទាហរណ៍  
```powershell
cd Workshop/samples
set AGENT_MODEL_PRIMARY=phi-4-mini
set AGENT_MODEL_EDITOR=gpt-oss-20b
python -m session05.agents_orchestrator
```
  
### រចនាសម្ព័ន្ធការតាមដាន (ជាជម្រើស)  
```json
{
    "step": 1,
    "agent": "Researcher",
    "latency_ms": 412.3,
    "tokens_in": 22,
    "tokens_out": 168,
    "model": "phi-4-mini"
}
```
  
រក្សាទុកគ្រប់ជំហានទៅក្នុងឯកសារ JSONL សម្រាប់វាយតម្លៃក្រោយ។

### ការកែលម្អជាជម្រើស

| ប្រធានបទ | ការកែលម្អ | អត្ថប្រយោជន៍ | គំនិតអនុវត្ត |
|-------|------------|---------|-----------------------|
| តួនាទីម៉ូឌែលច្រើន | ម៉ូឌែលផ្សេងៗសម្រាប់ភ្នាក់ងារ (`AGENT_MODEL_PRIMARY`, `AGENT_MODEL_EDITOR`) | ជំនាញពិសេស និងល្បឿន | ជ្រើសរើសអាស័យដ្ឋាន alias លើបរិស្ថាន ហៅ `chat_once` ជាមួយ alias តាមតួនាទី |
| តាមដានមានរចនាសម្ព័ន្ធ | JSON ប្រតិបត្តិការគ្រប់កម្មវិធី(act(tool, input, latency, tokens)) | សំរាប់វាយតម្លៃនិងរកកំហុស | បន្ថែម dict ទៅបញ្ជី; សរសេរ `.jsonl` នៅចុង |
| រក្សាទុកអង្គចងចាំ | អាចផ្ទុកឡើងឡើងវិញបាន ក្នុងការសន្ទនា | ភាពបន្តរបស់សម័យ | ដំឡើង `Agent.memory` ទៅ `sessions/<ts>.json` |
| កំណត់បញ្ជីឧបករណ៍ | រកឧបករណ៍ដោយដៃយ៉ាងមានឥទ្ធិពល | មានភាពបន្ថែមសមត្ថភាព | រក្សា `TOOLS` dict និងពិចារណាឈ្មោះ/ពណ៌នា |
| ព្យាយាមឡើងវិញ និងបន្ថយភាពយឺត | លំហូរចំរូងធន់ត្រូវ | បន្ថយករណីខ្វះចិត្តកណ្តាល | ឈរនៅជុំវិញ `act` ជាមួយ try/except + backoff ឧបសគ្គ |
| វាយតម្លៃដោយ rubrics | បញ្ចូលស្លាកគុណភាពស្វ័យប្រវត្តិ | តាមដានការកែលម្អ | ប្រើម៉ូឌែលបញ្ចូលជាថ្មី "វាយតម្លៃភាពច្បាស់ ១-៥" |
| អង្គចងចាំ vector | ការស្តារមាតិកា | បរិបទវែងមានផ្ទុក | បញ្ចូលសង្ខេបនិងយកនាំចូល top-k ក្នុងសារ system |
| ផ្ទេរឆ្លើយតបបន្ទាន់ | ឆ្លើយតបឆាប់រហ័ស | បង្កើនប្រសិទ្ធភាពអ្នកប្រើ | ប្រើបច្ចេកវិទ្យាបញ្ចាក់បន្ទាន់រូបមន្តលើកដំបូង |
| តេស្តកំណត់ច្បាស់ | គ្រប់គ្រងកំណត់ផ្ទាល់ | ស្ថិរភាព CI | ប្រើ `temperature=0` ជាមួយប្រើ prompt ដាក់ពូកែ |
| ដំណាក់កាលពហុសាខា | ការស្វែងរកលឿន | ល្បឿនចរាចរណ៍ | ប្រើ `concurrent.futures` សម្រាប់ជំហានភ្នាក់ងារឯករាជ្យ |

#### ឧទាហរណ៍កំណត់តាមដាន

```python
trace.append({
    "agent": agent.name,
    "input": prompt,
    "output_tokens": getattr(usage,'completion_tokens',None),
    "latency_ms": round((end-start)*1000,2),
    "tools_used": list(tool_calls)
})
```
  
#### សំណើរសំរាប់វាយតម្លៃសាមញ្ញ

```python
score_prompt = f"Rate clarity (1-5) ONLY as a number for this answer:\n{answer}"
rating, _ = chat_once(PRIMARY_ALIAS, messages=[{"role":"user","content":score_prompt}], max_tokens=4, temperature=0)
```
  
រក្សាទុកគូជា (`answer`, `rating`) ដើម្បីបង្កើតក្រាហ្វគុណភាពប្រវត្តិសាស្ត្រ។

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**ការបដិសេធ**៖  
ឯកសារនេះត្រូវបានបកប្រែដោយប្រើសេវាកម្មបកប្រែ AI [Co-op Translator](https://github.com/Azure/co-op-translator)។ ខណៈពេលយើងខំប្រឹងព្យាយាមឱ្យមានភាពត្រឹមត្រូវ សូមយល់ដឹងថាការបកប្រែដោយស្វ័យប្រវត្តិអាចមានកំហុសឬការបញ្ចូលមិនត្រឹមត្រូវ។ ឯកសារដើមផ្អែកលើភាសាមូលដ្ឋានគួរត្រូវបានយកជាឯកសារដើមដែលមានសិទ្ធិគ្រប់គ្រង។ សម្រាប់ព័ត៌មានសំខាន់ៗ ការបកប្រែដោយមនុស្សដែលមានជំនាញគឺត្រូវបានណែនាំ។ យើងមិនទទួលខុសត្រូវចំពោះការយល់ច្រឡំ ឬការបកស្រាយខុសខាតណាមួយដែលកើតចេញពីការប្រើប្រាស់ការបកប្រែនេះទេ។
<!-- CO-OP TRANSLATOR DISCLAIMER END -->