# Yapay Zeka Ajanları ve Küçük Dil Modelleri: Kapsamlı Bir Rehber

## Giriş

Bu öğreticide, Yapay Zeka Ajanları ve Küçük Dil Modelleri (KDM'ler) ile bunların edge bilişim ortamları için gelişmiş uygulama stratejilerini inceleyeceğiz. Ajanik yapay zekanın temel kavramlarını, KDM optimizasyon tekniklerini, kaynak kısıtlı cihazlar için pratik dağıtım stratejilerini ve üretime hazır ajan sistemleri oluşturmak için Microsoft Agent Framework'ü ele alacağız.

Yapay zeka alanı, 2025 yılında paradigmatik bir değişim yaşıyor. 2023 chatbotların yılıyken, 2024 copilots patlaması gördü, 2025 ise yapay zeka ajanlarına ait — düşünme, muhakeme etme, planlama yapma, araç kullanma ve insan müdahalesi olmadan görevleri yürütme yeteneğine sahip, giderek daha verimli Küçük Dil Modelleri tarafından desteklenen zeki sistemler. Microsoft Agent Framework, çevrimdışı edge tabanlı yeteneklerle bu zeki sistemleri oluşturmak için önde gelen çözümlerden biri olarak ortaya çıkıyor.

## Öğrenme Hedefleri

Bu öğretici sonunda, şunları yapabileceksiniz:

- 🤖 Yapay Zeka ajanları ve ajanik sistemlerin temel kavramlarını anlamak
- 🔬 Küçük Dil Modellerinin ajanik uygulamalarda Büyük Dil Modellerine kıyasla avantajlarını belirlemek
- 🚀 Edge bilişim ortamları için gelişmiş KDM dağıtım stratejilerini öğrenmek
- 📱 Gerçek dünya uygulamaları için KDM destekli pratik ajanlar uygulamak
- 🏗️ Microsoft Agent Framework kullanarak üretime hazır ajanlar oluşturmak
- 🌐 Yerel LLM ve KDM entegrasyonu ile çevrimdışı edge tabanlı ajanlar dağıtmak
- 🔧 Microsoft Agent Framework’ü Foundry Local ile edge dağıtımı için entegre etmek

## Yapay Zeka Ajanlarını Anlamak: Temeller ve Sınıflandırmalar

### Tanım ve Temel Kavramlar

Yapay zeka (YZ) ajanı, bir kullanıcı veya başka bir sistem adına, iş akışını tasarlayarak ve mevcut araçları kullanarak otonom şekilde görevler yapabilen sistem veya programdır. Sorularınıza sadece yanıt veren geleneksel yapay zekadan farklı olarak, bir ajan amaçlara ulaşmak için bağımsızca hareket edebilir.

### Ajan Sınıflandırma Çerçevesi

Ajan sınırlarını anlamak, farklı bilişim senaryoları için uygun ajan türlerinin seçilmesine yardımcı olur:

- **🔬 Basit Refleks Ajanlar**: Anında algılara tepki veren kural tabanlı sistemler (termostat, temel otomasyon)
- **📱 Model Tabanlı Ajanlar**: İç durum ve belleği koruyan sistemler (robot süpürgeler, navigasyon sistemleri)
- **⚖️ Amaç Tabanlı Ajanlar**: Hedeflere ulaşmak için plan yapan ve yürütme yapan sistemler (rota planlayıcıları, görev zamanlayıcıları)
- **🧠 Öğrenen Ajanlar**: Zamanla performansını geliştiren uyarlanabilir sistemler (tavsiye sistemleri, kişisel asistanlar)

### Yapay Zeka Ajanlarının Temel Avantajları

Yapay zeka ajanları edge bilişim uygulamaları için ideal olan birçok temel avantaj sunar:

**Operasyonel Otonomi**: Ajanlar, sürekli insan gözetimi olmadan bağımsız görev yürütme sağlar, bu da onları gerçek zamanlı uygulamalar için ideal kılar. Adaptif davranışlarını sürdürürken minimum denetim gerektirirler ve kaynak sınırlı cihazlarda daha düşük operasyonel yükle dağıtılabilirler.

**Dağıtım Esnekliği**: Bu sistemler internet bağlantısı gerektirmeyen cihaz içi yapay zeka yetenekleri sağlar, yerel işlemle gizlilik ve güvenliği artırır, alan özgü uygulamalar için özelleştirilebilir ve çeşitli edge bilişim ortamlarına uygundur.

**Maliyet Etkinliği**: Ajan sistemleri, bulut tabanlı çözümlere kıyasla maliyet açısından etkili dağıtımlar sunar, operasyonel maliyetleri azaltır ve edge uygulamalar için daha az bant genişliği gerektirir.

## Gelişmiş Küçük Dil Modeli Stratejileri

### KDM (Küçük Dil Modeli) Temelleri

Küçük Dil Modeli (KDM), yaygın tüketici elektronik cihazlara sığabilen ve bir kullanıcının ajanik taleplerini karşılayacak kadar pratik gecikmeyle çıkarım yapabilen bir dil modelidir. Pratikte, KDM'ler tipik olarak 10 milyar parametreden az modele sahiptir.

**Format Keşif Özellikleri**: KDM'ler, çeşitli kantizasyon seviyeleri, çoklu platform uyumluluğu, gerçek zamanlı performans optimizasyonu ve edge dağıtım yetenekleri için gelişmiş destek sunar. Kullanıcılar yerel işlem yoluyla artırılmış gizliliğe ve tarayıcı tabanlı dağıtım için WebGPU desteğine erişebilir.

**Kantizasyon Seviyesi Koleksiyonları**: Popüler KDM formatları arasında mobil uygulamalar için dengeli sıkıştırma sağlayan Q4_K_M, kalite odaklı edge dağıtımı için Q5_K_S serisi, güçlü edge cihazlarda neredeyse orijinal hassasiyet sunan Q8_0 ve ultra-düşük kaynak senaryoları için deneysel Q2_K formatları bulunmaktadır.

### KDM Dağıtımı İçin GGUF (Genel GGML Evrensel Format)

GGUF, kantize KDM’lerin CPU ve edge cihazlarda dağıtımı için öncelikli format olup, özellikle ajanik uygulamalar için optimize edilmiştir:

**Ajan Optimizasyon Özellikleri**: Format, araç çağrısı, yapılandırılmış çıktı üretimi ve çoklu tur sohbetler için geliştirilmiş destekle KDM dönüştürme ve dağıtımı için kapsamlı kaynaklar sağlar. Çoklu platform uyumluluğu farklı edge cihazlarda tutarlı ajan davranışı garanti eder.

**Performans Optimizasyonu**: GGUF, ajan iş akışları için verimli bellek kullanımı sağlar, çoklu ajan sistemleri için dinamik model yüklemeyi destekler ve gerçek zamanlı ajan etkileşimleri için optimize edilmiş çıkarım sunar.

### Edge Optimizasyonlu KDM Çerçeveleri

#### Ajanlar İçin Llama.cpp Optimizasyonu

Llama.cpp, ajanik KDM dağıtımı için özel olarak optimize edilmiş son teknoloji kantizasyon teknikleri sunar:

**Ajanlara Özgü Kantizasyon**: Çerçeve, mobil ajan dağıtımı için %75 boyut azalması sağlayan Q4_0, edge çıkarım ajanları için dengeli kalite-sıkıştırma sunan Q5_1 ve üretim ajan sistemleri için neredeyse orijinal kalite sunan Q8_0 formatlarını destekler. Gelişmiş formatlar ekstrem edge senaryolar için ultra-sıkıştırılmış ajanlar oluşturur.

**Uygulama Avantajları**: SIMD hızlandırmalı CPU optimize çıkarım, bellek verimli ajan yürütmesi sağlar. x86, ARM ve Apple Silicon mimarileri arasında çapraz platform uyumluluğu evrensel ajan dağıtım yetenekleri sunar.

#### Apple MLX Çerçevesi KDM Ajanları İçin

Apple MLX, Apple Silicon cihazlarda KDM destekli ajanlar için özel olarak tasarlanmış yerel optimizasyon sağlar:

**Apple Silicon Ajan Optimizasyonu**: Çerçeve, Metal Performance Shaders entegrasyonlu birleşik bellek mimarisi, ajan çıkarımı için otomatik karışık hassasiyet ve çoklu ajan sistemleri için optimize edilmiş bellek bant genişliği kullanır. KDM ajanları M-serisi çiplerde üstün performans gösterir.

**Geliştirme Özellikleri**: Python ve Swift API desteği, ajanlara özgü optimizasyonlar, ajan öğrenimi için otomatik türev alma ve Apple geliştirme araçlarıyla sorunsuz entegrasyon kapsamlı bir ajan geliştirme ortamı sağlar.

#### ONNX Runtime: Çoklu Platform KDM Ajanları İçin

ONNX Runtime, KDM ajanlarının çeşitli donanım platformlarında ve işletim sistemlerinde tutarlı şekilde çalışmasını sağlayan evrensel bir çıkarım motorudur:

**Evrensel Dağıtım**: ONNX Runtime, Windows, Linux, macOS, iOS ve Android platformlarında tutarlı KDM ajan davranışı sağlar. Bu çapraz platform uyumluluğu, geliştiricilerin bir kez yazıp her yere dağıtmasına olanak tanır ve çoklu platform uygulamaları için geliştirme ve bakım yükünü önemli ölçüde azaltır.

**Donanım Hızlandırma Seçenekleri**: Çerçeve, farklı donanım konfigürasyonları için optimize edilmiş yürütme sağlayıcıları sunar; CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm) ve özel hızlandırıcılar (Intel VPU, Qualcomm NPU). KDM ajanları kod değişikliği olmadan en iyi mevcut donanımı otomatik olarak kullanabilir.

**Üretime Hazır Özellikler**: ONNX Runtime, hızlı çıkarım için grafik optimizasyonu, kaynak kısıtlı ortamlar için bellek yönetimi ve performans analizleri için kapsamlı profil araçları gibi üretim ajan dağıtımı için gerekli kurumsal özellikler sunar. Çerçeve, esnek entegrasyon için Python ve C++ API desteği sağlar.


## Ajanik Sistemlerde KDM ve LDM Karşılaştırması: Gelişmiş İnceleme

### Ajan Uygulamalarında KDM Avantajları

**Operasyonel Verimlilik**: KDM’ler, ajan görevleri için LDM’lere kıyasla 10-30× maliyet indirimi sağlar, böylece ölçekli gerçek zamanlı ajanik yanıtlar sunar. Azalmış hesaplama karmaşıklığı nedeniyle daha hızlı çıkarım süreleri sunar; etkileşimli ajan uygulamaları için idealdir.

**Edge Dağıtım Yetkinlikleri**: KDM’ler internet bağımlılığı olmadan cihazda ajan yürütme imkanı tanır, yerel ajan işleme ile artırılmış gizlilik sağlar ve çeşitli edge bilişim ortamlarına uygun alan özgü ajan uygulamaları için özelleştirilebilir.

**Ajanlara Özgü Optimizasyon**: KDM’ler, araç çağrısı, yapılandırılmış çıktı oluşturma ve tipik ajan görevlerinin %70-80’ini oluşturan rutin karar alma iş akışlarında üstünlük sağlar.

### Ajan Sistemlerinde KDM ve LDM Ne Zaman Kullanılır?

**KDM’ler İçin Mükemmel Durumlar**:
- **Tekrarlayan ajan görevleri**: Veri girişi, form doldurma, rutin API çağrıları
- **Araç entegrasyonu**: Veri tabanı sorguları, dosya işlemleri, sistem etkileşimleri
- **Yapılandırılmış iş akışları**: Önceden tanımlanmış ajan süreçlerini izleme
- **Alan özgü ajanlar**: Müşteri hizmetleri, planlama, temel analiz
- **Yerel işlem**: Gizlilik hassasiyetine sahip ajan operasyonları

**LDM’ler İçin Daha İyi Durumlar**:
- **Karmaşık muhakeme**: Yeni sorun çözümü, stratejik planlama
- **Açık uçlu sohbetler**: Genel sohbet, yaratıcı tartışmalar
- **Geniş bilgi gerektiren görevler**: Kapsamlı genel bilgi gerektiren araştırmalar
- **Yeni durumlar**: Tamamen yeni ajan senaryolarının yönetimi

### Hibrit Ajan Mimarisi

Optimal yaklaşım, heterojen ajanik sistemlerde KDM ve LDM’lerin kombinasyonunu içerir:

**Akıllı Ajan Orkestrasyonu**:
1. **Birincil KDM**: Temel ajan görevlerinin %70-80’ini yerel olarak yönetmek
2. **Gerektiğinde LDM**: Karmaşık sorguları bulut tabanlı daha büyük modellere yönlendirmek
3. **Uzmanlaşmış KDM’ler**: Farklı ajan alanları için farklı küçük modeller
4. **Maliyet optimizasyonu**: Akıllı yönlendirme ile pahalı LDM çağrılarını en aza indirmek

## Üretim KDM Ajan Dağıtım Stratejileri

### Foundry Local: Kurumsal Düzey Edge YZ Çalışma Zamanı

Foundry Local (https://github.com/microsoft/foundry-local), Microsoft’un üretim edge ortamlarında Küçük Dil Modellerini dağıtmak için amiral gemisi çözümüdür. Kurumsal düzey özelliklere ve sorunsuz entegrasyon yeteneklerine sahip, KDM destekli ajanlar için özel tasarlanmış tam bir çalışma zamanı ortamı sunar.

**Temel Mimari ve Özellikler**:
- **OpenAI Uyumluluğu**: OpenAI SDK ve Agent Framework entegrasyonları ile tam uyumluluk
- **Otomatik Donanım Optimizasyonu**: Mevcut donanım (CUDA GPU, Qualcomm NPU, CPU) baz alınarak model varyantlarının akıllı seçimi
- **Model Yönetimi**: KDM modellerinin otomatik indirilmesi, önbellekleme ve yaşam döngüsü yönetimi
- **Servis Keşfi**: Ajan çerçeveleri için sıfır konfigürasyon servis tespiti
- **Kaynak Optimizasyonu**: Edge dağıtımı için akıllı bellek yönetimi ve güç verimliliği

#### Kurulum ve Ayar

**Çapraz Platform Kurulumu**:
```bash
# Windows (önerilen)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# Linux (manuel kurulum)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**Ajan Geliştirme İçin Hızlı Başlangıç**:
```bash
# Otomatik model yüklemeyle servisi başlat
foundry model run phi-4-mini

# Servis durumu ve uç noktasını doğrula
foundry service status

# Mevcut modelleri listele
foundry model ls

# API uç noktasını test et
curl http://localhost:<port>/v1/models
```

#### Ajan Çerçevesi Entegrasyonu

**Foundry Local SDK Entegrasyonu**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Otomatik servis yönetimi ile Foundry Local'i başlat
manager = FoundryLocalManager("phi-4-mini")

# Yerel çıkarım için OpenAI istemcisini yapılandır
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # Yerel kullanım için otomatik oluşturuldu
)

# Foundry Local arka ucu ile ajan oluştur
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**Otomatik Model Seçimi ve Donanım Optimizasyonu**:
```python
# Foundry Local otomatik olarak en uygun model varyantını seçer
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, ultra hızlı
    "general_conversation": "phi-4-mini",       # 2.4GB, dengeli
    "complex_reasoning": "phi-4",               # 7GB, yüksek kapasiteli
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, kod optimize edilmiş
}

# Foundry Local donanım tespiti ve kuantizasyonu yönetir
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # Çıktı örnekleri:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (CPU optimize edilmiş)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (GPU hızlandırılmış)
```

#### Üretim Dağıtım Modelleri

**Tek Ajanlı Üretim Kurulumu**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # Üretim araçları ekle
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# Üretim kullanımı
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**Çoklu Ajanlı Üretim Orkestrasyonu**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # Hafif yönlendirme ajanı
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # Müşteri hizmetleri ajanı
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # Teknik destek ajanı
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # İsteği uygun ajana yönlendir
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # Yönlendirmeye göre hedef ajani belirle
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # Uzman ajani ile işle
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# Üretim dağıtımı
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### Kurumsal Özellikler ve İzleme

**Sağlık İzleme ve Görünürlük**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# Üretim izleme kurulumu
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# Sürekli izleme
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # Metrikleri her 30 saniyede bir topla
```

**Kaynak Yönetimi ve Otomatik Ölçeklendirme**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # Bellek kullanımı yüksekse ölçek küçült
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # Talep yüksek ve kaynaklar izin veriyorsa ölçek büyüt
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 dakika boşta
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### Gelişmiş Yapılandırma ve Optimizasyon

**Özel Model Yapılandırması**:
```python
# Üretim için Gelişmiş Foundry Yerel yapılandırması
from foundry_local import FoundryLocalManager, ModelConfig

# Belirli kullanım durumları için özel yapılandırma
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # Belirli kuantalizasyon seviyesi
    context_length=4096,    # Karmaşık ajanlar için genişletilmiş bağlam
    batch_size=1,          # Tek kullanıcı ajanları için optimize edilmiş
    threads=4,             # CPU iş parçacığı optimizasyonu
    gpu_layers=32,         # GPU hızlandırma katmanları
    memory_lock=True,      # Tutarlı performans için modeli bellekte kilitle
    numa=True              # Çok soketli sistemler için NUMA optimizasyonu
)

manager = FoundryLocalManager(config=config)
```

**Üretim Dağıtım Kontrol Listesi**:

✅ **Servis Yapılandırması**:
- Kullanım durumları için uygun model takma adlarını yapılandırın
- Kaynak limitleri ve izleme eşiklerini ayarlayın
- Sağlık kontrollerini ve metrik toplama özelliklerini etkinleştirin
- Otomatik yeniden başlatma ve hata durumunda geçiş yapılandırmaları

✅ **Güvenlik Ayarları**:
- Yalnızca yerel API erişimi etkinleştirin (dışa açık olmasın)
- Uygun API anahtarı yönetimini yapılandırın
- Ajan etkileşimleri için denetim kayıtlarını kurun
- Üretim kullanımı için hız limitleme uygulayın

✅ **Performans Optimizasyonu**:
- Beklenen yük altında model performansını test edin
- Uygun kantizasyon seviyelerini yapılandırın
- Model önbellekleme ve ısınma stratejilerini ayarlayın
- Bellek ve CPU kullanım örüntülerini izleyin

✅ **Entegrasyon Testleri**:
- Ajan çerçevesi entegrasyonunu test edin
- Çevrimdışı çalışma yeteneklerini doğrulayın
- Hata toleransı ve kurtarma senaryolarını test edin
- Uçtan uca ajan iş akışlarını doğrulayın

### Ollama: Basitleştirilmiş KDM Ajan Dağıtımı

### Ollama: Topluluk Odaklı KDM Ajan Dağıtımı

Ollama, basitlik, geniş model ekosistemi ve geliştirici dostu iş akışlarına vurgu yaparak KDM ajan dağıtımı için topluluk destekli bir yaklaşım sunar. Foundry Local kurumsal özelliklere odaklanırken, Ollama hızlı prototipleme, topluluk modeli erişimi ve basitleştirilmiş dağıtım senaryolarında üstünlük sağlar.

**Temel Mimari ve Özellikler**:
- **OpenAI Uyumluluğu**: Sorunsuz ajan çerçevesi entegrasyonu için tam REST API uyumluluğu
- **Geniş Model Kütüphanesi**: Yüzlerce topluluk katkılı ve resmi modele erişim
- **Basit Model Yönetimi**: Tek komutla model kurulumu ve değiştirme
- **Çapraz Platform Desteği**: Windows, macOS ve Linux üzerinde yerel destek
- **Kaynak Optimizasyonu**: Otomatik kantizasyon ve donanım algılama

#### Kurulum ve Ayar

**Çapraz Platform Kurulumu**:
```bash
# Windows
winget install Ollama.Ollama

# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Docker dağıtımı
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**Ajan Geliştirme İçin Hızlı Başlangıç**:
```bash
# Ollama hizmetini başlat
ollama serve

# Ajan geliştirme için modelleri çek ve çalıştır
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# Model kullanılabilirliğini test et
ollama list

# API uç noktasını test et
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### Ajan Çerçevesi Entegrasyonu

**Microsoft Agent Framework ile Ollama**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama gerçek API anahtarı gerektirmez
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# Ajan geliştirme için Ollama'yı başlat
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# Ajanı Ollama arka ucu ile yapılandır
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Ollama ile Çoklu Model Ajan Kurulumu**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # Kullanılabilir değilse çekme modeli
            self._pull_model(model_name)
            
            # Her model için OpenAI istemcisi oluştur
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# Kullanım örneği
manager = OllamaMultiModelManager()

# Farklı görevler için uygun ajanları al
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### Üretim Dağıtım Modelleri

**Ollama ile Üretim Servisi**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # Modelin kullanılabilir olduğundan emin olun
                self._ensure_model_ready(model_name)
                
                # Üretim ajanı oluştur
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # Ajan türüne göre üretim araçları ekle
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # Modelin var olup olmadığını kontrol et
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # Veritabanı sorgulamasını simüle et
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # Metrikleri güncelle
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # Üretimde, bu uygun bir kayıt sistemine yazacaktır
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # Ollama servis sağlığını kontrol et
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # Model kullanılabilirliğini kontrol et
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# Üretim dağıtım örneği
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# İstekleri işle
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### Kurumsal Özellikler ve İzleme

**Ollama İzleme ve Görünürlük**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # Hizmet sağlığını kontrol et
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # Model bilgilerini al
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # Kısa özet
                    }
                
                # Çıkarım performansını test et
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # Sadece son 100 metrik girişini tut
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # Tokenleri tahmin et (kabaca yaklaşık)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # Mevcutsa zamanlama bilgilerini al
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # Nanosaneyi saniyeye çevir
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # Yanıt süresini kontrol et
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # Hizmet durumunu kontrol et
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # Ortalamaları hesapla
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# Üretim izleme kurulumu
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # Performans özetini her 10 dakikada bir kaydet
            if int(time.time()) % 600 == 0:  # Her 10 dakikada bir
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # Her 30 saniyede bir kontrol et

# İzlemeye başla
# asyncio.create_task(monitoring_loop())
```

#### Gelişmiş Yapılandırma ve Optimizasyon

**Ollama ile Özel Model Yönetimi**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # Hızlı yanıtlar için hafif modeller
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # Genel kullanım için dengeli modeller
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # Belirli görevler için özelleşmiş modeller
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # Yüksek yetenekli modeller
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # Çekme modeli
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # Bu genellikle gerçek donanım özelliklerini kontrol eder
        # Demo amaçlı, donanım tespiti simüle edilecektir
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# Üretim modeli kurulumu
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Ollama için Üretim Dağıtım Kontrol Listesi**:

✅ **Servis Yapılandırması**:
- Ollama servisini doğru sistem entegrasyonu ile kurun
- Spesifik ajan kullanım durumları için modelleri yapılandırın
- Doğru başlangıç betikleri ve servis yönetimini ayarlayın
- Model yükleme ve API kullanılabilirliğini test edin

✅ **Model Yönetimi**:
- Gerekli modelleri çekin ve bütünlüğünü doğrulayın
- Model güncelleme ve rotasyon prosedürlerini kurun
- Model önbellekleme ve depolama optimizasyonu yapılandırın
- Beklenen yük altında model performansını test edin

✅ **Güvenlik Ayarları**:
- Yalnızca yerel erişim için güvenlik duvarı kuralları yapılandırın
- API erişim kontrolleri ve hız limitleme ayarlarını yapın
- Ajan etkileşimleri için denetim kayıtlarını uygulayın
- Güvenli model depolama ve erişim yapılandırması oluşturun

✅ **Performans Optimizasyonu**:
- Beklenen kullanım durumları için modelleri kıyaslayın
- Uygun donanım hızlandırmayı yapılandırın
- Model ısınma ve önbellekleme stratejileri uygulayın
- Kaynak kullanımı ve performans metriklerini izleyin

✅ **Entegrasyon Testleri**:
- Microsoft Agent Framework entegrasyonunu test edin
- Çevrimdışı çalışma yeteneklerini doğrulayın

- Test failover senaryoları ve hata yönetimi
- Uçtan uca ajan iş akışlarını doğrula

**Foundry Local ile Karşılaştırma**:

| Özellik | Foundry Local | Ollama |
|---------|---------------|--------|
| **Hedef Kullanım Durumu** | Kurumsal üretim | Geliştirme ve topluluk |
| **Model Ekosistemi** | Microsoft tarafından yönetilen | Geniş topluluk |
| **Donanım Optimizasyonu** | Otomatik (CUDA/NPU/CPU) | Manuel yapılandırma |
| **Kurumsal Özellikler** | Yerleşik izleme, güvenlik | Topluluk araçları |
| **Dağıtım Karmaşıklığı** | Basit (winget yükleme) | Basit (curl yükleme) |
| **API Uyumluluğu** | OpenAI + uzantılar | OpenAI standart |
| **Destek** | Microsoft resmi | Topluluk tarafından yönetilen |
| **En İyi Kullanım** | Üretim ajanları | Prototip oluşturma, araştırma |

**Ollama'yı Ne Zaman Seçmeli**:
- **Geliştirme ve Prototipleme**: Farklı modellerle hızlı deneyler
- **Topluluk Modelleri**: En yeni topluluk katkılı modellere erişim
- **Eğitim Amaçlı Kullanım**: Yapay zeka ajan geliştirmeyi öğrenme ve öğretme
- **Araştırma Projeleri**: Çeşitli modellere ihtiyaç duyan akademik araştırmalar
- **Özel Modeller**: Özel ince ayarlı modelleri oluşturma ve test etme

### VLLM: Yüksek Performanslı SLM Ajan Çıkarımı

VLLM (Çok Büyük Dil Modeli çıkarımı), özellikle üretim ölçekli SLM dağıtımları için optimize edilmiş, yüksek verimli ve bellek açısından etkin çıkarım motoru sağlar. Foundry Local kullanım kolaylığına odaklanırken ve Ollama topluluk modellerini vurgularken, VLLM maksimum verim ve kaynak kullanımı gerektiren yüksek performans senaryolarında üstünlük sağlar.

**Temel Mimari ve Özellikler**:
- **PagedAttention**: Verimli dikkat hesaplaması için devrim niteliğinde bellek yönetimi
- **Dinamik Toplama**: Optimum verim için akıllı istek toplama
- **GPU Optimizasyonu**: Gelişmiş CUDA çekirdekleri ve tensör paralelliği desteği
- **OpenAI Uyumluluğu**: Kesintisiz entegrasyon için tam API uyumluluğu
- **Spekülatif Kod Çözümleme**: Gelişmiş çıkarım hızlandırma teknikleri
- **Kuantizasyon Desteği**: Bellek verimliliği için INT4, INT8 ve FP16 kuantizasyonu

#### Kurulum ve Ayarlar

**Kurulum Seçenekleri**:
```bash
# Standart kurulum
pip install vllm

# Ajan çerçeveleri için ek bağımlılıklarla
pip install vllm[agent] openai

# Üretim için Docker dağıtımı
docker pull vllm/vllm-openai:latest

# En son özellikler için kaynaktan
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**Ajan Geliştirme için Hızlı Başlangıç**:
```bash
# SLM modeli ile VLLM sunucusunu başlat
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# Alternatif: Hafif ajanlar için Qwen2.5 ile başlat
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# API uç noktasını test et
curl http://localhost:8000/v1/models

# Sohbet tamamlamasını test et
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### Ajan Çatısı Entegrasyonu

**Microsoft Ajan Çatısı ile VLLM**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # Ajanlar için kayıt tutmayı azalt
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # Sunucunun başlamasını bekle
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# Yüksek performanslı ajanlar için VLLM'i başlat
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # Ajanı VLLM arka ucuyla yapılandır
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM API anahtarı gerektirmez
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**Yüksek Verimli Çoklu Ajan Kurulumu**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # Engelleme olmaması için sunucuyu iş parçacığında başlat
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # Ajan oluştur
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # Yüksek verimlilik için istemci havuzu oluştur
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # Paralellik için model başına 5 istemci
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # Tercih edilen ajanın kullanılabilir ve aşırı yüklü olmadığını kontrol et
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # Ajan başına en fazla 10 eşzamanlı işlem
            return preferred_category
        
        # En az yüklü uygun ajana geçiş yap
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # Varsayılan geri dönüş
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # Optimal ajanı belirle
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # İşleme görevi oluştur
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # Tüm istekleri paralel işleme al
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Sonuçları formatla
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# Yüksek verimlilik kullanım örneği
throughput_manager = VLLMHighThroughputManager()

# Tüm modelleri başlat
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# Toplu istekleri işle
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### Üretim Dağıtım Kalıpları

**Kurumsal VLLM Üretim Hizmeti**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # İşçi işlemlerini başlat
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # Maksimum 4 işçi
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # Belirtilmişse kuantizasyon ekle
        if config.quantization:
            # Bu yönetici başlangıç komutuna eklenecektir
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # Üretim ajanı oluştur
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # Üretim araçlarını ekle
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # Üretim veri tabanı sorgulaması
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # İsteği kuyruğa ekle
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # İşleme için bekle (zaman aşımı ile)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # Kuyruktan isteği al
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # Uygun ajanı seç
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # İsteği işle
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # Metrikleri güncelle
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # Bu basitleştirilmiştir - üretimde futures doğru şekilde takip edilmelidir
        await asyncio.sleep(0.1)  # Yer tutucu
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # Basitlik için tek işçi
        )
        server = uvicorn.Server(config)
        await server.serve()

# Üretim dağıtım örneği
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# Üretim servisini başlat ve initialize et
# await production_service.initialize_production_environment()
# await production_service.start_production_server()
```

#### Kurumsal Özellikler ve İzleme

**Gelişmiş VLLM Performans İzleme**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # GPU izleme için NVIDIA ML'yi başlat
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # Hata metrikleri oluştur
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # API aracılığıyla VLLM'e özgü metrikleri al
        vllm_stats = await self._get_vllm_stats(manager)
        
        # Sistem metriklerini al
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # Mevcutsa GPU metriklerini al
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # Basitlik için ilk GPU'yu varsay
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # Gecikmeyi ölçmek için test çıkarımı yap
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # VLLM tarafından sunulması gerekir
                "active_requests": 1  # Yaklaşık değer
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # Her örneği analiz et
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # Yüksek gecikme önerisi
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # Yüksek GPU kullanımı önerisi
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # Düşük kullanım önerisi
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# Gelişmiş izleme kurulumu
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # Gerekirse diğer yöneticileri ekle
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # Metrikleri topla
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # Sadece son 1000 girişi tut
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # Her 5 dakikada öneriler oluştur
            if len(monitoring.metrics_history) % 10 == 0:  # Her 10. toplama (30 saniyede bir toplanıyorsa 5 dakika)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # Her saat performans raporu oluştur
            if len(monitoring.metrics_history) % 120 == 0:  # Her 120. toplama (1 saat)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # Metrikleri her 30 saniyede topla

# Gelişmiş izlemeyi başlat
# asyncio.create_task(advanced_monitoring_loop())
```

#### Gelişmiş Yapılandırma ve Optimizasyon

**Üretim VLLM Konfigürasyon Şablonları**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # İsteğe bağlı parametreler ekleyin
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# Kullanım örnekleri
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**VLLM için Üretim Dağıtım Kontrol Listesi**:

✅ **Donanım Optimizasyonu**:
- Çoklu GPU kurulumları için tensör paralelliğini yapılandır
- Bellek verimliliği için kuantizasyonu (AWQ/GPTQ) etkinleştir
- Optimal GPU bellek kullanımını ayarla (yüzde 85-95)
- Verim için uygun toplu işlem boyutlarını ayarla

✅ **Performans Ayarları**:
- Tekrarlanan sorgular için ön ek önbelleklemesini etkinleştir
- Uzun diziler için parça ön doldurmayı yapılandır
- Daha hızlı çıkarım için spekülatif kod çözümlemeyi ayarla
- Donanıma göre max_num_seqs değerini optimize et

✅ **Üretim Özellikleri**:
- Sağlık izleme ve metrik toplama ayarla
- Otomatik yeniden başlatma ve failover yapılandır
- İstek sıralama ve yük dengeleme uygula
- Kapsamlı günlükleme ve uyarı sistemi kur

✅ **Güvenlik ve Güvenilirlik**:
- Güvenlik duvarı kuralları ve erişim kontrollerini yapılandır
- API hız sınırlandırma ve kimlik doğrulamayı kur
- Sorunsuz kapatma ve temizlemeyi uygula
- Yedekleme ve felaket kurtarmayı yapılandır

✅ **Entegrasyon Testleri**:
- Microsoft Ajan Çatısı entegrasyonunu test et
- Yüksek verim senaryolarını doğrula
- Failover ve kurtarma prosedürlerini test et
- Yük altındaki performansı ölç

**Diğer Çözümlerle Karşılaştırma**:

| Özellik | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **Hedef Kullanım Durumu** | Yüksek verimli üretim | Kurumsal kullanım kolaylığı | Geliştirme ve topluluk |
| **Performans** | Maksimum verim | Dengeli | İyi |
| **Bellek Verimliliği** | PagedAttention optimizasyonu | Otomatik optimizasyon | Standart |
| **Kurulum Karmaşıklığı** | Yüksek (çok parametreli) | Düşük (otomatik) | Düşük (basit) |
| **Ölçeklenebilirlik** | Mükemmel (tensör/işlem hattı paralelliği) | İyi | Sınırlı |
| **Kuantizasyon** | Gelişmiş (AWQ, GPTQ, FP8) | Otomatik | Standart GGUF |
| **Kurumsal Özellikler** | Özel uygulama gerekli | Yerleşik | Topluluk araçları |
| **En İyi Kullanım** | Yüksek ölçekli üretim ajanları | Kurumsal üretim | Geliştirme |

**VLLM'yi Ne Zaman Seçmeli**:
- **Yüksek Verim Gereksinimleri**: Saniyede yüzlerce isteği işlemek
- **Büyük Ölçekli Dağıtımlar**: Çoklu GPU, çoklu düğüm dağıtımları
- **Performans Kritik**: Ölçekli olarak saniyenin altında yanıt süreleri
- **Gelişmiş Optimizasyon**: Özel kuantizasyon ve toplama ihtiyacı
- **Kaynak Verimliliği**: Pahalı GPU donanımının maksimum kullanımı


## Gerçek Dünya SLM Ajan Uygulamaları

### Müşteri Hizmetleri SLM Ajanları
- **SLM yetenekleri**: Hesap sorgulamaları, şifre sıfırlama, sipariş durumu kontrolü
- **Maliyet avantajları**: LLM ajanlara kıyasla çıkarım maliyetlerinde 10 kat azalma
- **Performans**: Rutin sorgular için tutarlı kaliteyle daha hızlı yanıt süreleri

### İş Süreci SLM Ajanları
- **Fatura işleme ajanları**: Veri çıkarma, bilgi doğrulama, onay için yönlendirme
- **E-posta yönetimi ajanları**: Kategorize etme, önceliklendirme, otomatik yanıt taslağı
- **Planlama ajanları**: Toplantıları koordine etme, takvim yönetimi, hatırlatıcı gönderme

### Kişisel SLM Dijital Asistanlar
- **Görev yönetimi ajanları**: Yapılacaklar listelerini oluşturma, güncelleme ve düzenleme
- **Bilgi toplama ajanları**: Konuları araştırma, yerelde özetleme
- **İletişim ajanları**: E-posta, mesaj, sosyal medya paylaşımlarını özel olarak taslak oluşturma

### Ticaret ve Finans SLM Ajanları
- **Piyasa izleme ajanları**: Fiyatları takip etme, gerçek zamanlı eğilim belirleme
- **Rapor oluşturma ajanları**: Günlük/haftalık özetleri otomatik oluşturma
- **Risk değerlendirme ajanları**: Yerel verilerle portföy pozisyonlarını değerlendirme

### Sağlık Destek SLM Ajanları
- **Hasta planlama ajanları**: Randevuları koordine etme, otomatik hatırlatıcılar gönderme
- **Dokümantasyon ajanları**: Medikal özetler, raporlar yerel oluşturma
- **Reçete yönetimi ajanları**: Yenilemeleri takip etme, etkileşimleri gizli kontrol etme

## Microsoft Ajan Çatısı: Üretime Hazır Ajan Geliştirme

### Genel Bakış ve Mimari

Microsoft Ajan Çatısı, bulut ve çevrimdışı uç ortamlarda çalışabilen yapay zeka ajanları geliştirmek, dağıtmak ve yönetmek için kapsamlı, kurumsal düzeyde bir platform sağlar. Çatı, özellikle Küçük Dil Modelleri ve uç bilişim senaryolarıyla kesintisiz çalışacak şekilde tasarlanmış olup, gizlilik duyarlı ve kaynak kısıtlı dağıtımlar için idealdir.

**Temel Çatı Bileşenleri**:
- **Ajan Runtime**: Uç cihazlar için optimize edilmiş hafif çalışma ortamı
- **Araç Entegrasyon Sistemi**: Harici servisler ve API'ler için genişletilebilir eklenti mimarisi
- **Durum Yönetimi**: Oturumlar arası kalıcı ajan bellek ve bağlam yönetimi
- **Güvenlik Katmanı**: Kurumsal dağıtım için yerleşik güvenlik kontrolleri
- **Orkestrasyon Motoru**: Çoklu ajan koordinasyonu ve iş akışı yönetimi

### Uç Dağıtımı için Temel Özellikler

**Çevrimdışı-Öncelikli Mimari**: Microsoft Ajan Çatısı çevrimdışı-öncelikli prensiplerle tasarlanmış, ajanların sürekli internet bağlantısı olmadan etkili çalışmasını sağlar. Bu, yerel model çıkarımı, önbelleğe alınmış bilgi tabanları, çevrimdışı araç çalıştırma ve bulut servisleri kullanılamadığında kademeli düşüşü içerir.

**Kaynak Optimizasyonu**: Çatı, SLM'ler için otomatik bellek optimizasyonu, uç cihazlar için CPU/GPU yük dengeleme, mevcut kaynaklara göre uyarlanabilir model seçimi ve mobil dağıtım için güç tasarruflu çıkarım desenleri gibi akıllı kaynak yönetimi sağlar.

**Güvenlik ve Gizlilik**: Kurumsal düzeyde güvenlik özellikleri arasında gizliliği korumak için yerel veri işleme, şifrelenmiş ajan iletişim kanalları, ajan yetenekleri için rol tabanlı erişim kontrolleri ve uyumluluk için denetim kayıtları bulunur.

### Foundry Local ile Entegrasyon

Microsoft Ajan Çatısı, tam bir uç yapay zeka çözümü için Foundry Local ile sorunsuz entegre olur:

**Otomatik Model Keşfi**: Çatı, Foundry Local örneklerini otomatik olarak tespit eder ve bağlanır, mevcut SLM modellerini keşfeder ve ajan gereksinimleri ile donanım yeteneklerine göre optimal modelleri seçer.

**Dinamik Model Yükleme**: Ajanlar, belirli görevler için farklı SLM'leri dinamik olarak yükleyebilir, böylece farklı modellerin farklı istem türlerini yönettiği çok modelli ajan sistemleri ve modeller arasında kullanılabilirlik ve performansa dayalı otomatik geçiş mümkündür.

**Performans Optimizasyonu**: Entegre önbellekleme mekanizmaları model yükleme sürelerini azaltır, bağlantı havuzu Foundry Local için API çağrılarını optimize eder ve akıllı toplama birden çok ajan isteği için verimi artırır.

### Microsoft Ajan Çatısı ile Ajan Oluşturma

#### Ajan Tanımı ve Yapılandırması

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# Ajanı Foundry Local entegrasyonu ile yapılandır
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# Foundry Local bağlantısını başlat
foundry = FoundryLocalManager("phi-4-mini")

# Ajan örneği oluştur
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### Uç Senaryolar için Araç Entegrasyonu

```python
# Çevrimdışı çalışma için araçlar tanımlayın
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # Yerel veritabanı sorgusu - çevrimdışı çalışır
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # Çevrimiçi olunca senkronize edilen yerel bilet oluşturma
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # Takvim entegrasyonlu yerel planlama
    return local_calendar.schedule(customer_id, preferred_time)
```

#### Çoklu Ajan Orkestrasyonu

```python
from microsoft_agent_framework import AgentOrchestrator

# Farklı alanlar için özel ajanlar oluşturun
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # Basit görevler için hafif
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # Karmaşık sorunlar için daha yetenekli
        specialized_for="technical_support"
    )
)

# Birden fazla ajanı düzenleyin
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# İstekleri niyete göre yönlendirin
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### İleri Düzey Uç Dağıtım Kalıpları

#### Hiyerarşik Ajan Mimarisi

**Yerel Ajan Kümeleri**: Uç cihazlarda her biri belirli görevlere optimize edilmiş birden fazla özel SLM ajanı dağıtın. Basit yönlendirme ve planlama için Qwen2.5-0.5B gibi hafif modeller, müşteri hizmetleri ve dokümantasyon için Phi-4-Mini gibi orta modeller ve kaynaklar izin verdiğinde karmaşık akıl yürütme için daha büyük modeller kullanın.

**Uçtan Buluta Koordinasyon**: Yerel ajanların rutin görevleri yönettiği, bağlantı olduğunda bulut ajanlarının karmaşık akıl yürütme sağladığı ve uç ile bulut işlemleri arasında kesintisiz devrin sağlandığı akıllı yükseltme kalıpları uygulayın.

#### Dağıtım Yapılandırmaları

**Tek Cihaz Dağıtımı**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**Dağıtık Uç Dağıtımı**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### Uç Ajanlar için Performans Optimizasyonu

#### Model Seçim Stratejileri

**Görev Bazlı Model Ataması**: Microsoft Ajan Çatısı, görev karmaşıklığı ve gereksinimlerine göre akıllı model seçimi sağlar:

- **Basit Görevler** (Soru-Cevap, yönlendirme): Qwen2.5-0.5B (500MB, <100ms yanıt)
- **Orta Düzey Görevler** (müşteri hizmetleri, planlama): Phi-4-Mini (2.4GB, 200-500ms yanıt)
- **Karmaşık Görevler** (teknik analiz, planlama): Phi-4 (7GB, kaynaklara göre 1-3s yanıt)

**Dinamik Model Değiştirme**: Ajanlar mevcut sistem yükü, görev karmaşıklığı değerlendirmesi, kullanıcı öncelik seviyeleri ve mevcut donanım kaynaklarına göre modeller arasında geçiş yapabilir.

#### Bellek ve Kaynak Yönetimi

```python
# Kenar dağıtımı için kaynak kısıtlamalarını yapılandırın
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### Kurumsal Entegrasyon Kalıpları

#### Güvenlik ve Uyumluluk

**Yerel Veri İşleme**: Tüm ajan işlemleri yerelde gerçekleşir, hassas veriler asla uç cihazı terk etmez. Bu, müşteri bilgisi koruması, sağlık ajanları için HIPAA uyumluluğu, bankacılık ajanları için finansal veri güvenliği ve Avrupa dağıtımları için GDPR uyumluluğunu kapsar.

**Erişim Kontrolü**: Rol tabanlı izinler hangi araçlara ajanların erişebileceğini kontrol eder, ajan etkileşimleri için kullanıcı kimlik doğrulaması ve tüm ajan eylem ve kararlarının denetim kayıtları sağlanır.

#### İzleme ve Görünürlük

```python
from microsoft_agent_framework import AgentMonitor

# Kenar ajanlar için izleme kurun
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # Çevrimdışı çalışma için ölçümleri yerel olarak depolayın
)

agent.add_monitor(monitor)
```

### Gerçek Dünya Uygulama Örnekleri

#### Perakende Uç Ajan Sistemi

```python
# Mağaza içi müşteri yardımı için perakende kiosk görevlisi
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### Sağlık Destek Ajanı

```python
# HIPAA uyumlu hasta destek temsilcisi
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # Sağlık hizmetleri için artırılmış gizlilik
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### Microsoft Ajan Çatısı için En İyi Uygulamalar

#### Geliştirme Rehberleri

1. **Basitleştirerek Başla**: Karmaşık çoklu ajan sistemleri oluşturmadan önce tek ajan senaryolarıyla başla
2. **Model Boyutunda Doğru Seçim**: Doğruluk gereksinimlerinizi karşılayan en küçük modeli seç
3. **Araç Tasarımı**: Karmaşık çok fonksiyonlu araçlar yerine odaklı, tek amaçlı araçlar oluştur
4. **Hata Yönetimi**: Çevrimdışı senaryolar ve model hataları için kademeli düşüş uygula
5. **Test Etme**: Ajanları çevrimdışı koşullarda ve kaynak kısıtlı ortamlarda kapsamlı test et

#### Dağıtımın En İyi Uygulamaları

1. **Kademeli Yayınlama**: Öncelikle küçük kullanıcı gruplarına dağıt, performans metriklerini yakından izle
2. **Kaynak İzleme**: Bellek, CPU ve yanıt süresi eşiklerinde uyarılar kur
3. **Yedekleme Stratejileri**: Model hataları veya kaynak tükenmesinde daima yedek planların olsun
4. **Öncelikli Güvenlik**: Güvenlik kontrollerini baştan uygula, sonradan düşünme
5. **Dokümantasyon**: Ajan yetenekleri ve sınırlamalarının açık dokümantasyonunu tut

### Gelecek Yol Haritası ve Entegrasyon

Microsoft Ajan Çatısı, gelişmiş SLM optimizasyonu, iyileştirilmiş uç dağıtım araçları, kısıtlı ortamlar için daha iyi kaynak yönetimi ve yaygın kurumsal senaryolar için genişletilmiş araç ekosistemi ile evrimleşmeye devam ediyor.

**Yaklaşan Özellikler**:
- **Ajan Optimizasyonu için AutoML**: Belirli ajan görevleri için SLM'lerin otomatik ince ayarı
- **Uç Mesh Ağ Kurulumu**: Birden fazla uç ajan dağıtımı arasında koordinasyon
- **Gelişmiş Telemetri**: Ajan performansı için geliştirilmiş izleme ve analiz
- **Görsel Ajan Oluşturucu**: Az kod/no-code ajan geliştirme araçları

## SLM Ajan Uygulaması için En İyi Uygulamalar

### Ajanlar için SLM Seçim Kılavuzu

Ajan dağıtımı için SLM seçerken şu faktörleri göz önünde bulundurun:

**Model Boyutu Dikkatleri**: Aşırı mobil ajan uygulamaları için Q2_K gibi ultra sıkıştırılmış modeller, genel ajan senaryoları için dengeli modeller (Q4_K_M) ve kalite kritik ajan uygulamaları için daha yüksek hassasiyetli modeller (Q8_0) seçin.

**Ajan Kullanım Durumu Uyumu**: Ajan kararları için doğruluk korunumu, gerçek zamanlı etkileşimler için çıkarım hızı, uç ajan dağıtımı için bellek kısıtlamaları ve gizlilik odaklı ajanlar için çevrimdışı çalışma gereksinimlerini dikkate alarak SLM yeteneklerini eşleştirin.

### SLM Ajanları için Optimizasyon Stratejisi Seçimi

**Ajanlar için Kuantizasyon Yaklaşımı**: Ajan kalitesi gereksinimleri ve donanım kısıtlamalarına göre uygun kuantizasyon seviyelerini seçin. Mobil ajanlarda maksimum sıkıştırma için Q4_0, genel ajanlarda dengeleme için Q5_1 ve kritik ajan uygulamalarında orijinal kaliteye yakınlık için Q8_0 göz önüne alın.


**Ajan Dağıtımı için Çerçeve Seçimi**: Hedef donanım ve ajan gereksinimlerine göre optimizasyon çerçeveleri seçin. CPU için optimize edilmiş ajan dağıtımı için Llama.cpp, Apple Silicon ajan uygulamaları için Apple MLX ve çapraz platform ajan uyumluluğu için ONNX kullanın.

## Pratik SLM Ajan Dönüşümü ve Kullanım Durumları

### Gerçek Dünya Ajan Dağıtım Senaryoları

**Mobil Ajan Uygulamaları**: Q4_K formatları, akıllı telefon ajan uygulamalarında minimal bellek kullanımıyla üstün performans gösterirken, Q8_0 tablet tabanlı ajan sistemleri için dengeli performans sağlar. Q5_K formatları mobil üretkenlik ajanları için üstün kalite sunar.

**Masaüstü ve Uç Ajan Hesaplama**: Q5_K masaüstü ajan uygulamaları için optimal performans sağlar, Q8_0 iş istasyonu ajan ortamlarında yüksek kaliteli çıkarım sunar ve Q4_K uç ajan cihazlarında verimli işlemeye olanak tanır.

**Araştırma ve Deneysel Ajanlar**: Gelişmiş kuantizasyon formatları, aşırı kaynak kısıtlamaları gerektiren akademik araştırmalar ve kavram kanıtı ajan uygulamaları için ultra düşük hassasiyetli ajan çıkarımı keşfetmeye olanak tanır.

### SLM Ajan Performans Kıyaslamaları

**Ajan Çıkarım Hızı**: Q4_K mobil CPU’larda en hızlı ajan tepki sürelerini sağlar; Q5_K genel ajan uygulamaları için dengeli hız-kalite oranı sunar; Q8_0 karmaşık ajan görevlerinde üstün kalite sağlar; deneysel formatlar ise özel ajan donanımları için maksimum verim sunar.

**Ajan Bellek Gereksinimleri**: Ajanlar için kuantizasyon seviyeleri, küçük ajan modelleri için 500MB altında olan Q2_K’den, orijinal boyutun yaklaşık %50’si kadar olan Q8_0’a kadar değişir; deneysel yapılandırmalar kaynak kısıtlı ajan ortamlarında maksimum sıkıştırma sağlar.

## SLM Ajanları için Zorluklar ve Dikkat Edilmesi Gerekenler

### Ajan Sistemlerinde Performans Takasları

SLM ajan dağıtımı, model boyutu, ajan tepki hızı ve çıktı kalitesi arasındaki takasların dikkatle değerlendirilmesini gerektirir. Q4_K mobil ajanlar için olağanüstü hız ve verimlilik sunarken, Q8_0 karmaşık ajan görevlerinde üstün kalite sağlar. Q5_K ise çoğu genel ajan uygulaması için uygun orta bir yol sunar.

### SLM Ajanlar için Donanım Uyumluluğu

Farklı uç cihazlar, SLM ajan dağıtımı için değişen yeteneklere sahiptir. Q4_K basit ajanlar için temel işlemcilerde verimli çalışırken, Q5_K dengeli performans için orta düzey hesaplama kaynakları gerektirir ve Q8_0 gelişmiş ajan yetenekleri için üst düzey donanımdan faydalanır.

### SLM Ajan Sistemlerinde Güvenlik ve Gizlilik

SLM ajanlar yerel işlemeyi mümkün kılarak gizliliği artırsa da, ajan modelleri ve verileri uç ortamlarında korumak için uygun güvenlik önlemleri alınmalıdır. Bu, özellikle kurumsal ortamda yüksek hassasiyetli ajan formatlarının veya hassas verilerle çalışan sıkıştırılmış ajan formatlarının dağıtımında önemlidir.

## SLM Ajan Geliştirmede Gelecek Trendler

SLM ajan alanı, sıkıştırma teknikleri, optimizasyon yöntemleri ve uç dağıtım stratejilerindeki ilerlemelerle gelişmeye devam ediyor. Gelecekte, ajan modelleri için daha verimli kuantizasyon algoritmaları, ajan iş akışları için geliştirilmiş sıkıştırma yöntemleri ve ajan işlemeyi hızlandıran uç donanım hızlandırıcıları ile daha iyi entegrasyon yer alacak.

**SLM Ajanlar için Pazar Tahminleri**: Son araştırmalara göre, ajan destekli otomasyon 2027’ye kadar kurumsal iş akışlarında tekrarlayan bilişsel görevlerin %40–60’ını ortadan kaldırabilir; SLM'ler, maliyet etkinliği ve dağıtım esnekliği nedeniyle bu dönüşümün öncüsü olacak.

**SLM Ajanlarda Teknoloji Trendleri**:
- **Uzmanlaşmış SLM Ajanlar**: Belirli ajan görevleri ve sektörler için eğitilmiş alan spesifik modeller
- **Uç Ajan Hesaplama**: Geliştirilmiş gizlilik ve azalmış gecikme ile cihaz üzerinde iyileştirilmiş ajan yetenekleri
- **Ajan Orkestrasyonu**: Dinamik yönlendirme ve yük dengeleme ile birden çok SLM ajan arasında daha iyi koordinasyon
- **Demokratikleşme**: SLM esnekliği, organizasyonlar arasında ajan geliştirme katılımını genişletir

## SLM Ajanlarla Başlarken

### Adım 1: Microsoft Agent Framework Ortamını Kurun

**Bağımlılıkları Kurun**:
```bash
# Microsoft Agent Çerçevesini Yükleyin
pip install microsoft-agent-framework

# Edge dağıtımı için Foundry Yerel SDK'yı Yükleyin
pip install foundry-local-sdk

# Edge ajanları için ek bağımlılıkları yükleyin
pip install openai asyncio
```

**Foundry Local'ı Başlatın**:
```bash
# Foundry Yerel hizmeti başlat
foundry service start

# Ajan geliştirme için varsayılan modeli yükle
foundry model run phi-4-mini
```

### Adım 2: Ajan Uygulamaları İçin SLM'nizi Seçin
Microsoft Agent Framework için popüler seçenekler:
- **Microsoft Phi-4 Mini (3.8B)**: Dengeli performansla genel ajan görevleri için mükemmel
- **Qwen2.5-0.5B (0.5B)**: Basit yönlendirme ve sınıflandırma ajanları için ultra verimli
- **Qwen2.5-Coder-0.5B (0.5B)**: Kodla ilgili ajan görevleri için uzmanlaşmış
- **Phi-4 (7B)**: Kaynaklar izin verdiğinde karmaşık uç senaryolar için gelişmiş akıl yürütme

### Adım 3: Microsoft Agent Framework ile İlk Ajanınızı Oluşturun

**Temel Ajan Kurulumu**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# Foundry Yerel bağlantısını başlat
foundry = FoundryLocalManager("phi-4-mini")

# Ajan yapılandırması oluştur
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# Ajan oluştur ve yapılandır
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# Basit bir araç tanımla
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# Ajanı test et
response = agent.chat("What time is it?")
print(response)
```

### Adım 4: Ajan Kapsamı ve Gereksinimlerini Belirleyin
Microsoft Agent Framework ile odaklanmış, iyi tanımlanmış ajan uygulamalarıyla başlayın:
- **Tek alan ajanları**: Müşteri hizmetleri YA DA programlama YA DA araştırma
- **Net ajan hedefleri**: Ajan performansı için spesifik, ölçülebilir amaçlar
- **Sınırlı araç entegrasyonu**: İlk ajan dağıtımı için maksimum 3-5 araç
- **Tanımlı ajan sınırları**: Karmaşık senaryolar için net yükseltme yolları
- **Uç-öncelikli tasarım**: Çevrimdışı işlevsellik ve yerel işlemeye öncelik verin

### Adım 5: Microsoft Agent Framework ile Uç Dağıtımı Uygulayın

**Kaynak Yapılandırması**:
```python
from microsoft_agent_framework import ResourceConfig

# Uç dağıtımı için yapılandırma
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**Uç Ajanlar için Güvenlik Önlemleri Dağıtımı**:
- **Yerel giriş doğrulama**: Bulut bağımlılığı olmadan istekleri denetleyin
- **Çevrimdışı çıktı filtreleme**: Yanıtların kalite standartlarına yerelde uyduğundan emin olun
- **Uç güvenlik kontrolleri**: İnternet bağlantısı gerektirmeyen güvenlik uygulayın
- **Yerel izleme**: Uç telemetri kullanarak performansı takip edin ve sorunları işaretleyin

### Adım 6: Uç Ajan Performansını Ölçün ve Optimize Edin
- **Ajan görev tamamlama oranları**: Çevrimdışı senaryolarda başarı oranlarını izleyin
- **Ajan tepki süreleri**: Uç dağıtım için saniyenin altında tepki süreleri sağlayın
- **Kaynak kullanımı**: Uç cihazlarda bellek, CPU ve pil kullanımını takip edin
- **Maliyet etkinliği**: Uç dağıtım maliyetlerini bulut tabanlı alternatiflerle karşılaştırın
- **Çevrimdışı güvenilirlik**: Ağ kesintileri sırasında ajan performansını ölçün

## SLM Ajan Uygulaması için Temel Noktalar

1. **SLM'ler ajanlar için yeterlidir**: Çoğu ajan görevi için küçük modeller, büyük modeller kadar iyi performans gösterirken önemli avantajlar sunar
2. **Ajanlarda maliyet etkinliği**: SLM ajanlarını çalıştırmak 10-30 kat daha ucuzdur, yaygın dağıtımları ekonomik olarak mümkün kılar
3. **Uzmanlaşma ajanlar için işe yarar**: İnce ayarlanmış SLM'ler, belirli ajan uygulamalarında genel amaçlı LLM'lerden daha iyi performans sergiler
4. **Hibrit ajan mimarisi**: Rutin ajan görevleri için SLM'ler, karmaşık akıl yürütme için gerektiğinde LLM'ler kullanın
5. **Microsoft Agent Framework üretim dağıtımını sağlar**: Uç ajanlar oluşturmak, dağıtmak ve yönetmek için kurumsal sınıf araçlar sağlar
6. **Uç-öncelikli tasarım ilkeleri**: Yerel işlemeye sahip çevrimdışı ajanlar gizlilik ve güvenilirlik sağlar
7. **Foundry Local entegrasyonu**: Microsoft Agent Framework ile yerel model çıkarımı arasında kusursuz bağlantı
8. **Gelecek SLM ajanlarda**: Üretim çerçeveleri ile küçük dil modelleri, ajan AI’nın demokratikleşmiş ve verimli dağıtımını mümkün kılar

## Kaynaklar ve İleri Okuma

### Temel Araştırma Makaleleri ve Yayınlar

#### AI Ajanları ve Ajan Sistemleri
- **"Dil Ajanları Optimizasyon Yapılabilir Grafikler Olarak"** (2024) - Ajan mimarisi ve optimizasyonu üzerine temel araştırma
  - Yazarlar: Wenyue Hua, Lishan Yang, ve diğerleri
  - Link: https://arxiv.org/abs/2402.16823
  - Ana İçgörüler: Grafik tabanlı ajan tasarımı ve optimizasyon stratejileri

- **"Büyük Dil Modeli Tabanlı Ajanların Yükselişi ve Potansiyeli"** (2023)
  - Yazarlar: Zhiheng Xi, Wenxiang Chen, ve diğerleri
  - Link: https://arxiv.org/abs/2309.07864
  - Ana İçgörüler: LLM tabanlı ajan yetenekleri ve uygulamalarının kapsamlı anketi

- **"Dil Ajanları için Bilişsel Mimariler"** (2024)
  - Yazarlar: Theodore Sumers, Shunyu Yao, ve diğerleri
  - Link: https://arxiv.org/abs/2309.02427
  - Ana İçgörüler: Zeki ajanlar tasarlamak için bilişsel çerçeveler

#### Küçük Dil Modelleri ve Optimizasyon
- **"Phi-3 Teknik Raporu: Telefonunuzda Yüksek Kapasiteli Bir Dil Modeli"** (2024)
  - Yazarlar: Microsoft Araştırma Ekibi
  - Link: https://arxiv.org/abs/2404.14219
  - Ana İçgörüler: SLM tasarım prensipleri ve mobil dağıtım stratejileri

- **"Qwen2.5 Teknik Raporu"** (2024)
  - Yazarlar: Alibaba Bulut Ekibi
  - Link: https://arxiv.org/abs/2407.10671
  - Ana İçgörüler: Gelişmiş SLM eğitim teknikleri ve performans optimizasyonu

- **"TinyLlama: Açık Kaynak Küçük Dil Modeli"** (2024)
  - Yazarlar: Peiyuan Zhang, Guangtao Zeng, ve diğerleri
  - Link: https://arxiv.org/abs/2401.02385
  - Ana İçgörüler: Ultra kompakt model tasarımı ve eğitim verimliliği

### Resmi Dokümantasyon ve Çerçeveler

#### Microsoft Agent Framework
- **Resmi Dokümantasyon**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHub Deposu**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **Ana Depo**: https://github.com/microsoft/foundry-local
- **Dokümantasyon**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **Ana Depo**: https://github.com/vllm-project/vllm
- **Dokümantasyon**: https://docs.vllm.ai/


#### Ollama
- **Resmi Web Sitesi**: https://ollama.ai/
- **GitHub Deposu**: https://github.com/ollama/ollama

### Model Optimizasyon Çerçeveleri

#### Llama.cpp
- **Depo**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **Dokümantasyon**: https://microsoft.github.io/Olive/
- **GitHub Deposu**: https://github.com/microsoft/Olive

#### OpenVINO
- **Resmi Site**: https://docs.openvino.ai/

#### Apple MLX
- **Depo**: https://github.com/ml-explore/mlx

### Sektör Raporları ve Pazar Analizi

#### AI Ajan Pazar Araştırması
- **"2025 AI Ajanlarının Durumu"** - McKinsey Global Institute
  - Link: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - Ana İçgörüler: Pazar trendleri ve kurumsal benimseme kalıpları

#### Teknik Kıyaslamalar

- **"Uç AI Çıkarım Kıyaslamaları"** - MLPerf
  - Link: https://mlcommons.org/en/inference-edge/
  - Ana İçgörüler: Uç dağıtımı için standart performans metrikleri

### Standartlar ve Spesifikasyonlar

#### Model Formatları ve Standartları
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - Platformlar arası model formatı için birlikte çalışabilirlik
- **GGUF Spesifikasyonu**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - CPU çıkarımı için kuantize edilmiş model formatı
- **OpenAI API Spesifikasyonu**: https://platform.openai.com/docs/api-reference
  - Dil modeli entegrasyonu için standart API formatı

#### Güvenlik ve Uyumluluk
- **NIST AI Risk Yönetim Çerçevesi**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - AI Sistemleri**: AI sistemleri ve güvenlik için çerçeve
- **IEEE AI Standartları**: https://standards.ieee.org/industry-connections/ai/

SLM destekli ajanlara geçiş, AI dağıtımına yaklaşımımızda temel bir değişikliği temsil eder. Microsoft Agent Framework, yerel platformlar ve verimli Küçük Dil Modelleri ile birleşerek, uç ortamlarında etkili çalışan üretime hazır ajanlar oluşturmak için eksiksiz bir çözüm sunar. Verimlilik, uzmanlaşma ve pratik faydaya odaklanarak, bu teknoloji yığını AI ajanlarını her endüstri ve uç bilişim ortamında daha erişilebilir, uygun maliyetli ve etkili hale getirir.

2025 yılına ilerlerken, giderek daha yetenekli küçük modeller, Microsoft Agent Framework gibi gelişmiş ajan çerçeveleri ve sağlam uç dağıtım platformları birleşerek, gizliliği koruyan, maliyetleri azaltan ve olağanüstü kullanıcı deneyimleri sunan uç cihazlarda verimli çalışan otonom sistemler için yeni olanaklar açacaktır.

**Uygulama için Sonraki Adımlar**:
1. **Fonksiyon Çağrısını Keşfedin**: SLM'lerin araç entegrasyonu ve yapılandırılmış çıktıları nasıl ele aldığını öğrenin
2. **Model Bağlam Protokolü (MCP) Ustası Olun**: Gelişmiş ajan iletişim kalıplarını anlayın
3. **Üretim Ajanları Oluşturun**: Kurumsal sınıf dağıtımlar için Microsoft Agent Framework'ü kullanın
4. **Uç için Optimize Edin**: Kaynak kısıtlı ortamlar için gelişmiş optimizasyon tekniklerini uygulayın


## ➡️ Sırada Ne Var

- [02: Küçük Dil Modellerinde (SLM) Fonksiyon Çağrısı](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Feragatname**:
Bu belge, AI çeviri hizmeti [Co-op Translator](https://github.com/Azure/co-op-translator) kullanılarak çevrilmiştir. Doğruluk için çaba sarf etsek de, otomatik çevirilerin hata veya yanlışlık içerebileceğini lütfen unutmayınız. Orijinal belge, kendi dilinde yetkili kaynak olarak kabul edilmelidir. Kritik bilgiler için profesyonel insan çevirisi önerilir. Bu çevirinin kullanımı sonucu ortaya çıkabilecek yanlış anlamalardan veya yanlış yorumlamalardan sorumlu değiliz.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->