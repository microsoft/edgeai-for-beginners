# AI Agenti i Mali Jezični Modeli: Sveobuhvatan Vodič

## Uvod

U ovom tutorijalu istražit ćemo AI agente i Male Jezične Modelle (SLM) te njihove napredne strategije implementacije u okruženjima edge računarstva. Pokrit ćemo osnovne pojmove agentnog AI-a, tehnike optimizacije SLM-a, praktične strategije implementacije za uređaje s ograničenim resursima i Microsoft Agent Framework za izgradnju sustava agenata spremnih za proizvodnju.

Pejzaž umjetne inteligencije doživljava paradigmsku promjenu 2025. godine. Dok je 2023. bila godina chatbotova, a 2024. godina copilotova, 2025. pripada AI agentima — inteligentnim sustavima koji razmišljaju, zaključuju, planiraju, koriste alate i izvršavaju zadatke uz minimalni ljudski doprinos, sve snažnije potpomognuti učinkovitim Malim Jezičnim Modelima. Microsoft Agent Framework izdvaja se kao vodeće rješenje za izgradnju ovih inteligentnih sustava s mogućnostima rada offline na edge uređajima.

## Ciljevi Učenja

Do kraja ovog tutorijala moći ćete:

- 🤖 Razumjeti osnovne pojmove AI agenata i agentnih sustava
- 🔬 Prepoznati prednosti Malih Jezičnih Modela u odnosu na Velike Jezične Modelle u agentnim aplikacijama
- 🚀 Naučiti napredne strategije implementacije SLM-a za edge računarstvo
- 📱 Primijeniti praktične agente pokretane SLM-om za stvarne primjene
- 🏗️ Izgraditi agente spremne za proizvodnju korištenjem Microsoft Agent Frameworka
- 🌐 Implementirati offline edge agente s lokalnom integracijom LLM-a i SLM-a
- 🔧 Integrirati Microsoft Agent Framework s Foundry Local za edge implementaciju

## Razumijevanje AI Agenata: Osnove i Klasifikacije

### Definicija i Temeljni Pojmovi

AI agent odnosi se na sustav ili program sposoban autonomno obavljati zadatke u ime korisnika ili drugog sustava dizajniranjem vlastitog tijeka rada i korištenjem dostupnih alata. Za razliku od tradicionalnog AI-a koji samo odgovara na vaša pitanja, agent može samostalno djelovati kako bi postigao ciljeve.

### Okvir Klasifikacije Agenta

Razumijevanje granica agenata pomaže u odabiru odgovarajućih tipova agenata za različite računalne scenarije:

- **🔬 Jednostavni Refleksni Agenti**: Sustavi temeljeni na pravilima koji reagiraju na neposredne percepcije (termostati, osnovna automatizacija)
- **📱 Agenti Temeljeni na Modelu**: Sustavi koji održavaju unutarnje stanje i memoriju (robotski usisavači, navigacijski sustavi)
- **⚖️ Agenti Temeljeni na Ciljevima**: Sustavi koji planiraju i izvršavaju sekvence radi postizanja ciljeva (planeri ruta, raspoređivači zadataka)
- **🧠 Učeći Agenti**: Adaptivni sustavi koji poboljšavaju izvedbu tijekom vremena (sustavi preporuka, personalizirani asistenti)

### Ključne Prednosti AI Agenata

AI agenti nude nekoliko temeljnih prednosti koje ih čine idealnima za primjene u edge računarstvu:

**Operativna Autonomija**: Agenti omogućuju neovisno izvršavanje zadataka bez stalnog nadzora čovjeka, čineći ih idealnima za aplikacije u stvarnom vremenu. Potrebna im je minimalna supervizija uz održavanje prilagodljivog ponašanja, omogućujući implementaciju na uređajima s ograničenim resursima uz smanjeni operativni teret.

**Fleksibilnost Implementacije**: Ovi sustavi omogućuju AI mogućnosti na uređaju bez potrebe za internetskom vezom, poboljšavaju privatnost i sigurnost lokalnom obradom, mogu se prilagoditi za domenske aplikacije te su pogodni za različita edge računalna okruženja.

**Isplativost**: Sustavi agenata omogućuju isplativu implementaciju u usporedbi s rješenjima na oblaku, s manjim operativnim troškovima i nižim zahtjevima za propusnost za edge aplikacije.

## Napredne Strategije za Male Jezične Modelle

### Osnove SLM-a (Malih Jezičnih Modela)

Mali Jezični Model (SLM) je jezični model koji može stati na uobičajeni potrošački elektronički uređaj i obavljati inferenciju s dovoljnom niskom latencijom da bude praktičan za ispunjavanje agentnih zahtjeva jednog korisnika. U praktičnim terminima, SLM-ovi su obično modeli s manje od 10 milijardi parametara.

**Značajke Otkrivanja Formata**: SLM-ovi nude naprednu podršku za različite razine kvantizacije, kompatibilnost među platformama, optimizaciju izvedbe u stvarnom vremenu i mogućnosti implementacije na rubnim uređajima. Korisnici imaju pristup poboljšanoj privatnosti zahvaljujući lokalnoj obradi i podršci za WebGPU za implementaciju u pregledniku.

**Sklopovi Razina Kvantizacije**: Popularni SLM formati uključuju Q4_K_M za uravnoteženu kompresiju u mobilnim aplikacijama, seriju Q5_K_S za vrhunsku kvalitetu u edge implementacijama, Q8_0 za gotovo izvornu preciznost na snažnim edge uređajima te eksperimentalne formate poput Q2_K za scenarije s ultra niskim resursima.

### GGUF (General GGML Universal Format) za Implementaciju SLM-a

GGUF služi kao primarni format za implementaciju kvantiziranih SLM-ova na CPU i edge uređajima, posebno optimiziran za agentne aplikacije:

**Značajke Optimizirane za Agente**: Format pruža sveobuhvatne resurse za konverziju i implementaciju SLM-a s proširenom podrškom za pozivanje alata, strukturirano generiranje izlaza i višekratne konverzacije. Kompatibilnost među platformama osigurava dosljedno ponašanje agenata na različitim edge uređajima.

**Optimizacija Izvedbe**: GGUF omogućuje učinkovito korištenje memorije za agentne tijekove rada, podržava dinamičko učitavanje modela za višestruke agente i pruža optimiziranu inferenciju za interakcije agenata u stvarnom vremenu.

### Edge-Optimizirani Okviri za SLM

#### Llama.cpp Optimizacija za Agente

Llama.cpp pruža napredne tehnike kvantizacije posebno optimizirane za agentnu implementaciju SLM-a:

**Kvantiacija Specifična za Agente**: Okvir podržava Q4_0 (optimalno za mobilnu implementaciju agenata s 75% smanjenjem veličine), Q5_1 (uravnotežena kvaliteta-kompresija za edge inferencijske agente) i Q8_0 (gotovo izvrsna kvaliteta za proizvodne agentne sustave). Napredni formati omogućuju ultra-komprimirane agente za ekstremne edge scenarije.

**Prednosti Implementacije**: Inferencija optimizirana za CPU s SIMD ubrzanjem pruža memorijski učinkovito izvršavanje agenata. Kompatibilnost među platformama na x86, ARM i Apple Silicon arhitekturama omogućuje univerzalne mogućnosti implementacije agenata.

#### Apple MLX Okvir za SLM Agente

Apple MLX nudi nativnu optimizaciju posebno dizajniranu za agente pokretane SLM-om na Apple Silicon uređajima:

**Optimizacija za Apple Silicon Agente**: Okvir koristi jedinstvenu memorijsku arhitekturu s integracijom Metal Performance Shadera, automatsku mješovitu preciznost za agentnu inferenciju i optimiziranu propusnost memorije za višestruke agente. SLM agenti pokazuju iznimne performanse na M-seriji čipova.

**Razvojne Značajke**: Podrška za Python i Swift API sa agentnim optimizacijama, automatska diferencijacija za učenje agenata i besprijekorna integracija s Apple razvojnim alatima pružaju sveobuhvatno razvojno okruženje za agente.

#### ONNX Runtime za Cross-Platform SLM Agente

ONNX Runtime pruža univerzalni inferencijski motor koji omogućuje SLM agentima dosljedan rad preko raznih hardverskih platformi i operativnih sustava:

**Univerzalna Implementacija**: ONNX Runtime osigurava dosljedno ponašanje SLM agenata na Windows, Linux, macOS, iOS i Android platformama. Ova kompatibilnost među platformama omogućava developerima da napišu jednom i implementiraju svugdje, značajno smanjujući troškove razvoja i održavanja za višestruke platforme.

**Opcije Hardverskog Ubrzanja**: Okvir pruža optimizirane izvršne pružatelje za razne hardverske konfiguracije uključujući CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm) i specijalizirane akceleratore (Intel VPU, Qualcomm NPU). SLM agenti mogu automatski koristiti najbolji dostupan hardver bez promjena u kodu.

**Značajke Spremne za Proizvodnju**: ONNX Runtime nudi enterprise razinu značajki bitnih za produkcijsku implementaciju agenata uključujući optimizaciju grafa za bržu inferenciju, upravljanje memorijom za okruženja s ograničenim resursima i sveobuhvatne alate za profiliranje performansi. Okvir podržava Python i C++ API-je za fleksibilnu integraciju.


## SLM naspram LLM u Agentnim Sustavima: Napredna Usporedba

### Prednosti SLM-a u Agentnim Primjenama

**Operativna Učinkovitost**: SLM-ovi pružaju 10-30× smanjenje troškova u usporedbi s LLM-ovima za zadatke agenata, omogućujući agentne odgovore u stvarnom vremenu na velikoj skali. Nude brža vremena inferencije zbog smanjene računarske složenosti, čineći ih idealnima za interaktivne agentne aplikacije.

**Mogućnosti Implementacije na Rubu**: SLM-ovi omogućuju izvođenje agenata na uređaju bez ovisnosti o internetu, poboljšanu privatnost lokalnom obradom i prilagodbu za specifične domene agenata prikladne za različita edge računalna okruženja.

**Optimizacija Specifična za Agente**: SLM-ovi su izvrsni za pozivanje alata, strukturirano generiranje izlaza i rutinske tijekove donošenja odluka koji čine 70-80% tipičnih zadataka agenata.

### Kada koristiti SLM naspram LLM u Agentnim Sustavima

**Savršeno za SLM-ove**:
- **Ponavljajući agentni zadaci**: Unos podataka, popunjavanje obrazaca, rutinski API pozivi
- **Integracija alata**: Upiti u bazu podataka, upravljanje datotekama, sistemske interakcije
- **Strukturirani tijekovi rada**: Slijeđenje unaprijed definiranih agentnih procesa
- **Domenski specifični agenti**: Korisnička služba, raspoređivanje, osnovna analiza
- **Lokalna obrada**: Operacije agenata osjetljive na privatnost

**Bolje za LLM-ove**:
- **Složeno zaključivanje**: Rješavanje novih problema, strateško planiranje
- **Otvorene konverzacije**: Opći chat, kreativne rasprave
- **Široki zadaci znanja**: Istraživanja koja zahtijevaju opsežno opće znanje
- **Nove situacije**: Rukovanje potpuno novim agentnim scenarijima

### Hibridna Agentna Arhitektura

Optimalan pristup kombinira SLM-ove i LLM-ove u heterogenim agentnim sustavima:

**Pametna Orkestracija Agenata**:
1. **SLM kao primarni**: Rukuje 70-80% rutinskih agentnih zadataka lokalno
2. **LLM po potrebi**: Usmjerava složene upite na veće modele bazirane u oblaku
3. **Specijalizirani SLM-ovi**: Različiti mali modeli za različite domene agenata
4. **Optimizacija troškova**: Minimizira skupe pozive LLM-ovima inteligentnim usmjeravanjem

## Strategije Proizvodne Implementacije SLM Agenata

### Foundry Local: Enterprise-razina Edge AI Runtime

Foundry Local (https://github.com/microsoft/foundry-local) služi kao glavna Microsoftova platforma za implementaciju Malih Jezičnih Modela u proizvodnim edge okruženjima. Pruža kompletno runtime okruženje posebno dizajnirano za agentne sustave pokretane SLM-om, s enterprise razinom značajki i besprijekornom integracijom.

**Temeljna Arhitektura i Značajke**:
- **API Kompatibilan s OpenAI**: Potpuna kompatibilnost s OpenAI SDK i Agent Framework integracijama
- **Automatska Optimizacija Hardvera**: Inteligentan odabir varijanti modela na temelju dostupnog hardvera (CUDA GPU, Qualcomm NPU, CPU)
- **Upravljanje Modelima**: Automatsko preuzimanje, keširanje i upravljanje životnim ciklusom SLM modela
- **Otkriće Usluga**: Detekcija usluga bez konfiguracije za frameworks agenata
- **Optimizacija Resursa**: Inteligentno upravljanje memorijom i energetska učinkovitost za edge implementacije

#### Instalacija i Postavljanje

**Instalacija na Više Platformi**:
```bash
# Windows (preporučeno)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# Linux (ručna instalacija)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**Brzi Početak za Razvoj Agenata**:
```bash
# Pokreni uslugu s automatskim učitavanjem modela
foundry model run phi-4-mini

# Provjeri status usluge i krajnju točku
foundry service status

# Popis dostupnih modela
foundry model ls

# Testiraj API krajnju točku
curl http://localhost:<port>/v1/models
```

#### Integracija Agent Frameworka

**Integracija Foundry Local SDK-a**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Inicijaliziraj Foundry Local s automatskim upravljanjem servisima
manager = FoundryLocalManager("phi-4-mini")

# Konfiguriraj OpenAI klijent za lokalnu inferenciju
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # Automatski generirano za lokalnu upotrebu
)

# Kreiraj agenta s Foundry Local backendom
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**Automatski Odabir Modela i Optimizacija Hardvera**:
```python
# Foundry Local automatski odabire optimalnu varijantu modela
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, ultra brzo
    "general_conversation": "phi-4-mini",       # 2.4GB, uravnoteženo
    "complex_reasoning": "phi-4",               # 7GB, visoke sposobnosti
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, optimizirano za kod
}

# Foundry Local upravlja detekcijom hardvera i kvantizacijom
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # Primjeri izlaza:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (optimizirano za CPU)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (ubrzano na GPU)
```

#### Obrasci za Proizvodnu Implementaciju

**Proizvodna Postavka za Jednog Agenta**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # Dodajte proizvodne alate
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# Upotreba u proizvodnji
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**Orkestracija Više Agenata za Proizvodnju**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # Lagani agent za usmjeravanje
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # Agent korisničke službe
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # Agent tehničke podrške
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # Usmjeri zahtjev odgovarajućem agentu
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # Odredi ciljanog agenta na temelju usmjeravanja
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # Obradi sa specijaliziranim agentom
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# Produkcijsko postavljanje
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### Enterprise Značajke i Nadzor

**Nadzor Zdravlja i Promatranje**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# Postavljanje praćenja proizvodnje
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# Kontinuirano praćenje
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # Prikupljanje metrike svake 30 sekundi
```

**Upravljanje Resursima i Automatsko Skaliranje**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # Smanjite ako je upotreba memorije visoka
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # Povećajte ako je potražnja velika i resursi dopuštaju
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 minuta neaktivnosti
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### Napredno Konfiguriranje i Optimizacija

**Prilagođena Konfiguracija Modela**:
```python
# Napredna lokalna konfiguracija Foundry za proizvodnju
from foundry_local import FoundryLocalManager, ModelConfig

# Prilagođena konfiguracija za specifične slučajeve korištenja
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # Specifična razina kvantizacije
    context_length=4096,    # Prošireni kontekst za složene agente
    batch_size=1,          # Optimizirano za agente s jednim korisnikom
    threads=4,             # Optimizacija CPU niti
    gpu_layers=32,         # Slojevi GPU ubrzanja
    memory_lock=True,      # Zaključaj model u memoriji za dosljedne performanse
    numa=True              # NUMA optimizacija za višesoketne sustave
)

manager = FoundryLocalManager(config=config)
```

**Kontrolna Lista za Proizvodnu Implementaciju**:

✅ **Konfiguracija Usluge**:
- Konfigurirajte odgovarajuće nadimke modela za slučajeve uporabe
- Postavite ograničenja resursa i pragove nadzora
- Omogućite zdravstvene provjere i prikupljanje metrika
- Konfigurirajte automatsko ponovno pokretanje i failover mehanizme

✅ **Sigurnosna Postavka**:
- Omogućite pristup API-ju samo lokalno (bez vanjskog izlaganja)
- Konfigurirajte upravljanje API ključevima
- Postavite zapisnik revizije interakcija agenata
- Implementirajte ograničenje brzine za produkciju

✅ **Optimizacija Performansi**:
- Testirajte izvedbu modela pod očekivanim opterećenjem
- Konfigurirajte odgovarajuće razine kvantizacije
- Postavite keširanje modela i strategije zagrijavanja
- Pratite obrasce korištenja memorije i CPU-a

✅ **Testiranje Integracije**:
- Testirajte integraciju agent frameworka
- Potvrdite mogućnosti rada offline
- Testirajte scenarije prekida i oporavka
- Validirajte kraj do kraja tijekove rada agenata

### Ollama: Pojednostavljena Implementacija SLM Agenata

### Ollama: Zajednici Prilagođena Implementacija SLM Agenata

Ollama nudi pristup usmjeren na zajednicu za implementaciju SLM agenata s naglaskom na jednostavnost, opsežan ekosustav modela i razvojno prijazne tijekove rada. Dok se Foundry Local fokusira na enterprise značajke, Ollama se ističe u brzom prototipiranju, pristupu zajedničkim modelima i pojednostavljenim scenarijima implementacije.

**Temeljna Arhitektura i Značajke**:
- **API Kompatibilan s OpenAI**: Potpuna REST API kompatibilnost za besprijekornu integraciju agent frameworka
- **Opsežna Biblioteka Modela**: Pristup stotinama modela iz zajednice i službenih izvora
- **Jednostavno Upravljanje Modelima**: Instalacija i mijenjanje modela s jednom naredbom
- **Podrška za Više Platformi**: Izvorna podrška za Windows, macOS i Linux
- **Optimizacija Resursa**: Automatska kvantizacija i detekcija hardvera

#### Instalacija i Postavljanje

**Instalacija na Više Platformi**:
```bash
# Windows
winget install Ollama.Ollama

# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Docker implementacija
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**Brzi Početak za Razvoj Agenata**:
```bash
# Pokreni Ollama uslugu
ollama serve

# Povuci i pokreni modele za razvoj agenata
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# Testiraj dostupnost modela
ollama list

# Testiraj API krajnju točku
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### Integracija Agent Frameworka

**Ollama s Microsoft Agent Framework-om**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama ne zahtijeva stvarni API ključ
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# Inicijalizirajte Ollama za razvoj agenta
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# Konfigurirajte agenta s Ollama pozadinom
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Višemodelska Postavka Agenta s Ollama**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # Povuci model ako nije dostupan
            self._pull_model(model_name)
            
            # Kreiraj OpenAI klijenta za svaki model
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# Primjer upotrebe
manager = OllamaMultiModelManager()

# Dobij odgovarajuće agente za različite zadatke
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### Obrasci za Proizvodnu Implementaciju

**Produkcijska Usluga s Ollama**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # Osigurajte dostupnost modela
                self._ensure_model_ready(model_name)
                
                # Kreirajte proizvodnog agenta
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # Dodajte proizvodne alate temeljem tipa agenta
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # Provjerite postoji li model
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # Simulirajte pregled baze podataka
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # Ažurirajte metrike
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # U produkciji bi ovo zapisivalo u pravi sustav za evidentiranje
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # Provjerite zdravlje Ollama usluge
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # Provjerite dostupnost modela
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# Primjer produkcijskog postavljanja
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# Obradite zahtjeve
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### Enterprise Značajke i Nadzor

**Nadzor i Promatranje u Ollama-i**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # Provjeri zdravlje usluge
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # Dohvati informacije o modelu
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # Kratki sažetak
                    }
                
                # Testiraj performanse zaključivanja
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # Zadrži samo posljednjih 100 zapisa metrike
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # Procijeni tokene (gruba procjena)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # Dohvati informacije o vremenu ako su dostupne
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # Pretvori nanosekunde u sekunde
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # Provjeri vrijeme odgovora
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # Provjeri status usluge
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # Izračunaj prosjeke
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# Postavljanje nadzora u produkciji
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # Zabilježi sažetak performansi svakih 10 minuta
            if int(time.time()) % 600 == 0:  # Svakih 10 minuta
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # Provjeri svakih 30 sekundi

# Pokreni nadzor
# asyncio.create_task(monitoring_loop())
```

#### Napredno Upravljanje i Optimizacija

**Prilagođeno Upravljanje Modelima s Ollama-om**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # Laki modeli za brze odgovore
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # Uravnoteženi modeli za opću upotrebu
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # Specijalizirani modeli za određene zadatke
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # Modeli visoke sposobnosti
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # Pull model
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # Ovo bi obično provjeravalo stvarne specifikacije hardvera
        # Za potrebe demonstracije simulirat ćemo detekciju hardvera
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# Postavljanje produkcijskog modela
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Kontrolna Lista za Produkcijsku Implementaciju za Ollama**:

✅ **Konfiguracija Usluge**:
- Instalirajte Ollama uslugu s proper sistemskom integracijom
- Konfigurirajte modele za specifične agente slučajeve upotrebe
- Postavite odgovarajuće skripte za pokretanje i upravljanje uslugama
- Testirajte učitavanje modela i dostupnost API-ja

✅ **Upravljanje Modelima**:
- Preuzmite potrebne modele i provjerite njihovu integritet
- Postavite procedure za ažuriranje i rotaciju modela
- Konfigurirajte keširanje modela i optimizaciju pohrane
- Testirajte performanse modela pod očekivanim opterećenjem

✅ **Sigurnosna Postavka**:
- Konfigurirajte firewall pravila za lokalni pristup
- Postavite kontrole pristupa API-ju i ograničenja brzine
- Implementirajte zapisnik revizije interakcija agenata
- Konfigurirajte sigurnu pohranu modela i pristup

✅ **Optimizacija Performansi**:
- Benchmark modeli za očekivane slučajeve upotrebe
- Konfigurirajte odgovarajuće hardversko ubrzanje
- Postavite strategije zagrijavanja i keširanja modela
- Pratite korištenje resursa i metrike performansi

✅ **Testiranje Integracije**:
- Testirajte integraciju Microsoft Agent Framework-a
- Provjerite mogućnosti rada offline

- Testirajte scenarije prestanka rada i obradu pogrešaka
- Potvrdite krajnje tijekove rada agenata

**Usporedba s Foundry Local**:

| Značajka | Foundry Local | Ollama |
|---------|---------------|--------|
| **Ciljani slučaj uporabe** | Poduzeće u proizvodnji | Razvoj i zajednica |
| **Ekosustav modela** | Microsoftov odabir | Opsežna zajednica |
| **Optimizacija hardvera** | Automatska (CUDA/NPU/CPU) | Ručna konfiguracija |
| **Značajke za poduzeća** | Ugrađeni nadzor, sigurnost | Alati zajednice |
| **Složenost implementacije** | Jednostavno (winget install) | Jednostavno (curl install) |
| **API kompatibilnost** | OpenAI + proširenja | OpenAI standard |
| **Podrška** | Microsoft službena | Vođena zajednicom |
| **Najbolje za** | Produktne agente | Prototipiziranje, istraživanje |

**Kada odabrati Ollama**:
- **Razvoj i prototipiziranje**: Brza eksperimentiranja s različitim modelima
- **Modeli zajednice**: Pristup najnovijim modelima koje doprinosi zajednica
- **Obrazovna uporaba**: Učenje i podučavanje razvoja AI agenata
- **Istraživački projekti**: Akademska istraživanja koja zahtijevaju raznolik pristup modelima
- **Prilagođeni modeli**: Izrada i testiranje prilagođenih modela fino podešenih

### VLLM: Visokoizvedbeni SLM agent za izvođenje upita

VLLM (Vrlo velik model jezika za izvođenje upita) pruža visokoprotočni, memorijski učinkovit motor za izvođenje upita posebno optimiziran za proizvodne SLM implementacije u velikom opsegu. Dok se Foundry Local fokusira na jednostavnost korištenja, a Ollama naglašava modele zajednice, VLLM izvrsno funkcionira u visokoučinkovitim scenarijima koji zahtijevaju maksimalni protok i učinkovito korištenje resursa.

**Temeljna arhitektura i značajke**:
- **PagedAttention**: Revolucionarno upravljanje memorijom za učinkovito računanje pažnje
- **Dinamično grupiranje**: Inteligentno grupiranje zahtjeva za optimalni protok
- **Optimizacija za GPU**: Napredni CUDA kernele i podrška za paralelizam tenzora
- **OpenAI kompatibilnost**: Potpuna API kompatibilnost za besprijekornu integraciju
- **Spekulativno dekodiranje**: Napredne tehnike ubrzanja izvođenja upita
- **Podrška za kvantizaciju**: INT4, INT8 i FP16 kvantizacija za memorijsku učinkovitost

#### Instalacija i postavljanje

**Opcije instalacije**:
```bash
# Standardna instalacija
pip install vllm

# S dodatnim ovisnostima za agent okvire
pip install vllm[agent] openai

# Docker implementacija za produkciju
docker pull vllm/vllm-openai:latest

# Iz izvornog koda za najnovije značajke
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**Brzi početak za razvoj agenata**:
```bash
# Pokreni VLLM poslužitelj s SLM modelom
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# Alternativa: Pokreni s Qwen2.5 za lagane agente
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# Testiraj API kraj točku
curl http://localhost:8000/v1/models

# Testiraj dovršetak chata
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### Integracija u okvir agenata

**VLLM s Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # Smanjite zapisivanje za agente
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # Pričekajte da se poslužitelj pokrene
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# Inicijalizirajte VLLM za agente visoke performanse
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # Konfigurirajte agenta s VLLM pozadinom
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM ne zahtijeva API ključ
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**Visokoprotočna multi-agent konfiguracija**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # Pokreni server u dretvi kako bi se izbjeglo blokiranje
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # Kreiraj agenta
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # Kreiraj bazen klijenata za visoku propusnost
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # 5 klijenata po modelu za paralelizam
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # Provjeri je li preferirani agent dostupan i nije preopterećen
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # Maksimalno 10 istovremenih po agentu
            return preferred_category
        
        # Povratak na najmanje opterećenog dostupnog agenta
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # Zadani povratak
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # Odredi optimalnog agenta
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # Kreiraj zadatak obrade
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # Obradi sve zahtjeve paralelno
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Formatiraj rezultate
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# Primjer korištenja za visoku propusnost
throughput_manager = VLLMHighThroughputManager()

# Inicijaliziraj sve modele
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# Obradi seriju zahtjeva
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### Obrasci za proizvodnu implementaciju

**Enterprise VLLM proizvodna usluga**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # Pokreni obradu radnika
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # Maksimalno 4 radnika
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # Dodaj kvantizaciju ako je navedena
        if config.quantization:
            # Ovo bi se dodalo u naredbu za pokretanje upravitelja
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # Kreiraj proizvodnog agenta
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # Dodaj proizvodne alate
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # Pretraživanje proizvodne baze podataka
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # Dodaj zahtjev u red čekanja
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # Čekaj obradu (s vremenskim ograničenjem)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # Dohvati zahtjev iz reda čekanja
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # Izaberi odgovarajućeg agenta
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # Obradi zahtjev
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # Ažuriraj metrike
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # Ovo je pojednostavljeno - u proizvodnji biste ispravno pratili futures
        await asyncio.sleep(0.1)  # Rezervirano mjesto
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # Jedan radnik radi jednostavnosti
        )
        server = uvicorn.Server(config)
        await server.serve()

# Primjer proizvodnog postavljanja
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# Inicijaliziraj i pokreni proizvodnu uslugu
# čekaj production_service.initialize_production_environment()
# čekaj production_service.start_production_server()
```

#### Značajke za poduzeća i nadzor

**Napredni nadzor performansi VLLM**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # Inicijaliziraj NVIDIA ML za nadzor GPU-a
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # Kreiraj metrike pogrešaka
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # Dohvati metrike specifične za VLLM putem API-ja
        vllm_stats = await self._get_vllm_stats(manager)
        
        # Dohvati sistemske metrike
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # Dohvati metrike GPU-a ako su dostupne
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # Pretpostavlja prvi GPU radi jednostavnosti
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # Testiraj inferenciju za mjerenje latencije
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # Trebalo bi biti izloženo od strane VLLM-a
                "active_requests": 1  # Aproksimacija
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # Analiziraj svaki primjerak
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # Preporuka za visoku latenciju
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # Preporuka za visoku iskorištenost GPU-a
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # Preporuka za nisku iskorištenost
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# Napredno postavljanje nadzora
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # Dodaj ostale upravitelje po potrebi
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # Prikupljaj metrike
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # Zadrži samo posljednjih 1000 zapisa
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # Generiraj preporuke svakih 5 minuta
            if len(monitoring.metrics_history) % 10 == 0:  # Svaka 10. kolekcija (5 minuta ako se prikuplja svakih 30s)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # Generiraj izvještaj o performansama svaki sat
            if len(monitoring.metrics_history) % 120 == 0:  # Svaka 120. kolekcija (1 sat)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # Prikupljaj metrike svakih 30 sekundi

# Pokreni napredni nadzor
# asyncio.create_task(advanced_monitoring_loop())
```

#### Napredna konfiguracija i optimizacija

**Predlošci za konfiguraciju proizvodnje VLLM**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # Dodajte opcionalne parametre
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# Primjeri upotrebe
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**Kontrolni popis za proizvodnu implementaciju VLLM**:

✅ **Optimizacija hardvera**:
- Konfigurirajte paralelizam tenzora za multi-GPU postavke
- Omogućite kvantizaciju (AWQ/GPTQ) za memorijsku učinkovitost
- Postavite optimalnu iskorištenost GPU memorije (85-95%)
- Konfigurirajte odgovarajuće veličine skupova za protok

✅ **Podešavanje performansi**:
- Omogućite keširanje prefiksa za ponovljene upite
- Konfigurirajte podijeljenu predmemoriju za duge nizove
- Postavite spekulativno dekodiranje za brže izvođenje upita
- Optimizirajte max_num_seqs prema hardveru

✅ **Proizvodne značajke**:
- Postavite nadzor zdravlja i prikupljanje metrike
- Konfigurirajte automatski restart i prestanak rada
- Implementirajte stavljanje zahtjeva u red i balansiranje opterećenja
- Postavite sveobuhvatno zapisivanje i upozorenja

✅ **Sigurnost i pouzdanost**:
- Konfigurirajte pravila vatrozida i kontrole pristupa
- Postavite ograničenja brzine API-ja i autentifikaciju
- Implementirajte uredno gašenje i čišćenje
- Konfigurirajte izradu sigurnosnih kopija i oporavak od katastrofe

✅ **Testiranje integracije**:
- Testirajte integraciju Microsoft Agent Frameworka
- Potvrdite scenarije visokog protoka
- Testirajte postupke prestanka rada i oporavka
- Izvršite usporedbu performansi pod opterećenjem

**Usporedba s drugim rješenjima**:

| Značajka | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **Ciljani slučaj uporabe** | Visokoprotočna proizvodnja | Jednostavnost za poduzeća | Razvoj i zajednica |
| **Performanse** | Maksimalni protok | Uravnoteženo | Dobro |
| **Učinkovitost memorije** | Optimizacija PagedAttention | Automatska optimizacija | Standardno |
| **Složenost postavljanja** | Visoka (mnogo parametara) | Niska (automatska) | Niska (jednostavno) |
| **Skalabilnost** | Izvrsna (paralelizam tenzora/potoka) | Dobra | Ograničena |
| **Kvantizacija** | Napredno (AWQ, GPTQ, FP8) | Automatska | Standardni GGUF |
| **Značajke za poduzeća** | Potrebna prilagođena implementacija | Ugrađene | Alati zajednice |
| **Najbolje za** | Proizvodne agente velikog opsega | Proizvodnju poduzeća | Razvoj |

**Kada odabrati VLLM**:
- **Zahtjevi za visok protok**: Obrada stotina zahtjeva u sekundi
- **Implementacije velikog opsega**: Multi-GPU, višeklemenetske implementacije
- **Kritične performanse**: Vremena odziva ispod sekunde u velikom opsegu
- **Napredna optimizacija**: Potreba za prilagođenom kvantizacijom i grupiranjem
- **Učinkovitost korištenja resursa**: Maksimalno korištenje skupog GPU hardvera


## Praktične primjene SLM agenata

### SLM agenti za korisničku službu
- **Mogućnosti SLM-a**: Pretrage računa, resetiranje lozinki, provjere statusa narudžbe
- **Uštede troškova**: 10x smanjenje troškova izvođenja u odnosu na LLM agente
- **Performanse**: Brže vrijeme odgovora s dosljednom kvalitetom za rutinske upite

### SLM agenti za poslovne procese
- **Agenti za obradu faktura**: Izvlačenje podataka, provjera informacije, usmjeravanje na odobrenje
- **Agenti za upravljanje e-poštom**: Kategorizacija, prioritet, automatsko sastavljanje odgovora
- **Agenti za zakazivanje**: Koordinacija sastanaka, upravljanje kalendarima, slanje podsjetnika

### Osobni digitalni asistenti SLM
- **Agenti za upravljanje zadacima**: Efikasno kreiranje, ažuriranje, organizacija popisa zadataka
- **Agenti za prikupljanje informacija**: Istraživanje tema, sažimanje nalaza lokalno
- **Agenti za komunikaciju**: Privatno sastavljanje e-mailova, poruka, objava na društvenim mrežama

### SLM agenti za trgovanje i financije
- **Agenti za praćenje tržišta**: Praćenje cijena, identificiranje trendova u stvarnom vremenu
- **Agenti za izvještavanje**: Automatsko kreiranje dnevnih/tjednih sažetaka
- **Agenti za procjenu rizika**: Procjena portfeljskih pozicija korištenjem lokalnih podataka

### SLM agenti za podršku u zdravstvu
- **Agenti za zakazivanje pacijenata**: Koordinacija termina, slanje automatskih podsjetnika
- **Agenti za dokumentaciju**: Generiranje medicinskih sažetaka i izvještaja lokalno
- **Agenti za upravljanje receptima**: Praćenje ponovnih isporuka, privatna provjera interakcija

## Microsoft Agent Framework: Razvoj agenata spremnih za proizvodnju

### Pregled i arhitektura

Microsoft Agent Framework pruža cjelovitu, razinu poduzeća, platformu za izgradnju, implementaciju i upravljanje AI agentima koji mogu raditi i u oblaku i u offline edge okruženjima. Okvir je posebno dizajniran za besprijekoran rad sa Small Language Models i edge računalnim scenarijima, čineći ga idealnim za primjene osjetljive na privatnost i s ograničenim resursima.

**Temeljne komponente okvira**:
- **Agent Runtime**: Lagan izvršni okoliš optimiziran za edge uređaje
- **Sustav integracije alata**: Proširiva arhitektura dodataka za povezivanje s vanjskim uslugama i API-jima
- **Upravljanje stanjima**: Trajna memorija agenata i rukovanje kontekstom kroz sesije
- **Sigurnosni sloj**: Ugrađene sigurnosne kontrole za implementaciju u poduzeću
- **Orkestracijski motor**: Koordinacija više agenata i upravljanje tijekovima rada

### Ključne značajke za edge implementaciju

**Offline-pristup**: Microsoft Agent Framework je dizajniran s principima offline-pristupa, omogućujući agentima učinkovito funkcioniranje bez stalne internetske veze. To uključuje lokalno izvođenje modela, keširane baze znanja, offline izvršavanje alata i elegantno smanjenje funkcionalnosti kada usluge u oblaku nisu dostupne.

**Optimizacija resursa**: Okvir pruža inteligentno upravljanje resursima s automatskom optimizacijom memorije za SLM, balansiranjem opterećenja CPU/GPU za edge uređaje, adaptivnim odabirom modela na temelju dostupnih resursa i energetski učinkovitim obrascima izvođenja za mobilne implementacije.

**Sigurnost i privatnost**: Sigurnosne značajke razine poduzeća uključuju lokalnu obradu podataka radi očuvanja privatnosti, šifrirane kanale komunikacije agenata, kontrole pristupa temeljene na ulogama za mogućnosti agenata te zapisivanje audita za potrebe usklađenosti.

### Integracija s Foundry Local

Microsoft Agent Framework besprijekorno se integrira s Foundry Local za pružanje kompletne edge AI solucije:

**Automatsko otkrivanje modela**: Okvir automatski detektira i povezuje se na Foundry Local instance, otkriva dostupne SLM modele te odabire optimalne modele na temelju zahtjeva agenata i mogućnosti hardvera.

**Dinamično učitavanje modela**: Agenti mogu dinamički učitavati različite SLM-ove za specifične zadatke, omogućujući multi-modelne agentne sustave gdje se različiti modeli koriste za različite vrste zahtjeva, te automatsko prebacivanje između modela na temelju dostupnosti i performansi.

**Optimizacija performansi**: Integrirani mehanizmi keširanja smanjuju vrijeme učitavanja modela, pooling veza optimizira API pozive prema Foundry Local, a inteligentno grupiranje poboljšava protok za višestruke zahtjeve agenata.

### Izrada agenata s Microsoft Agent Framework

#### Definicija i konfiguracija agenata

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# Konfigurirajte agenta s Foundry Local integracijom
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# Inicijalizirajte Foundry Local vezu
foundry = FoundryLocalManager("phi-4-mini")

# Kreirajte instancu agenta
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### Integracija alata za edge scenarije

```python
# Definirajte alate za rad bez mreže
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # Upit lokalne baze podataka - radi bez mreže
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # Lokalno kreiranje tiketa s sinkronizacijom kada ste online
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # Lokalno zakazivanje s integracijom kalendara
    return local_calendar.schedule(customer_id, preferred_time)
```

#### Orkestracija više agenata

```python
from microsoft_agent_framework import AgentOrchestrator

# Kreirajte specijalizirane agente za različita područja
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # Lagani za jednostavne zadatke
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # Sposobniji za složene probleme
        specialized_for="technical_support"
    )
)

# Orkestrirajte više agenata
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# Usmjeravajte zahtjeve na temelju namjere
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### Napredni obrasci za edge implementaciju

#### Hijerarhijska arhitektura agenata

**Lokalni skupovi agenata**: Implementirajte više specijaliziranih SLM agenata na edge uređajima, svaki optimiziran za specifične zadatke. Koristite lagane modele poput Qwen2.5-0.5B za jednostavno usmjeravanje i zakazivanje, srednje modele poput Phi-4-Mini za korisničku službu i dokumentaciju te veće modele za složene rezoniranja kada resursi dopuštaju.

**Koordinacija edge-oblak**: Implementirajte inteligentne obrasce eskalacije gdje lokalni agenti obavljaju rutinske zadatke, oblak agenti pružaju složena rezoniranja kada je veza dostupna, a besprijekorno prebacivanje između edge i oblačnog procesiranja održava kontinuitet.

#### Konfiguracije implementacije

**Implementacija na jednom uređaju**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**Distribuirana edge implementacija**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### Optimizacija performansi za edge agente

#### Strategije odabira modela

**Dodjela modela prema zadatku**: Microsoft Agent Framework omogućuje inteligentan odabir modela temeljen na složenosti i zahtjevima zadatka:

- **Jednostavni zadaci** (pitanja i odgovori, usmjeravanje): Qwen2.5-0.5B (500MB, <100ms odgovor)
- **Umjereni zadaci** (korisnička služba, zakazivanje): Phi-4-Mini (2.4GB, 200-500ms odgovor)
- **Složeni zadaci** (tehnička analiza, planiranje): Phi-4 (7GB, 1-3s odgovor kad resursi dozvoljavaju)

**Dinamična promjena modela**: Agenti mogu mijenjati modele na temelju trenutnog opterećenja sustava, procjene složenosti zadataka, prioriteta korisnika i dostupnosti hardverskih resursa.

#### Upravljanje memorijom i resursima

```python
# Konfigurirajte ograničenja resursa za implementaciju na rubu mreže
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### Obrasci integracije u poduzećima

#### Sigurnost i usklađenost

**Lokalna obrada podataka**: Cjelokupna obrada agenata odvija se lokalno, osiguravajući da osjetljivi podaci nikad ne napuste edge uređaj. To uključuje zaštitu podataka o klijentima, usklađenost s HIPAA za zdravstvene agente, sigurnost financijskih podataka za bankarske agente i usklađenost s GDPR-om za europske implementacije.

**Kontrola pristupa**: Ovlaštenja temeljena na ulogama kontroliraju pristup alatima agenata, autentifikaciju korisnika za interakcije s agentima i evidenciju svih radnji i odluka agenata.

#### Nadzor i preglednost

```python
from microsoft_agent_framework import AgentMonitor

# Postavite nadzor za edge agente
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # Pohranite metrike lokalno za rad u offline načinu rada
)

agent.add_monitor(monitor)
```

### Primjeri primjene u stvarnom svijetu

#### Sustav edge agenata za maloprodaju

```python
# Agent kioska za maloprodaju za pomoć kupcima u trgovini
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### Agent za zdravstvenu podršku

```python
# Agent za podršku pacijentima u skladu s HIPAA-om
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # Povećana privatnost za zdravstvenu skrb
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### Najbolje prakse za Microsoft Agent Framework

#### Smjernice za razvoj

1. **Počnite jednostavno**: Prvo izgrađujte scenarije s jednim agentom prije kompleksnih multi-agentnih sustava
2. **Pravilan odabir veličine modela**: Izaberite najmanji model koji ispunjava vaše zahtjeve točnosti
3. **Dizajn alata**: Izradite usredotočene alate za jednu svrhu, a ne složene višefunkcionalne alate
4. **Rukovanje pogreškama**: Implementirajte elegantno smanjenje funkcionalnosti za offline scenarije i neuspjehe modela
5. **Testiranje**: Detaljno testirajte agente u offline uvjetima i okruženjima s ograničenim resursima

#### Najbolje prakse za implementaciju

1. **Postupno puštanje**: Prvo implementirajte za male grupe korisnika i pažljivo pratite metrike performansi
2. **Praćenje resursa**: Postavite upozorenja za pragove memorije, CPU-a i vremena odziva
3. **Strategije povratka**: Uvijek imajte rezervne planove za neuspjehe modela ili iscrpljivanje resursa
4. **Prvo sigurnost**: Implementirajte sigurnosne kontrole od samog početka, a ne naknadno
5. **Dokumentacija**: Održavajte jasnu dokumentaciju o mogućnostima i ograničenjima agenata

### Budući plan i integracija

Microsoft Agent Framework se nastavlja razvijati s boljom optimizacijom SLM-a, poboljšanim alatima za edge implementacije, boljim upravljanjem resursima u ograničenim okruženjima i proširenim ekosustavom alata za uobičajene scenarije u poduzećima.

**Nadolazeće značajke**:
- **AutoML za optimizaciju agenata**: Automatsko fino podešavanje SLM-a za specifične zadatke agenata
- **Edge Mesh umrežavanje**: Koordinacija između višestrukih edge agent implementacija
- **Napredna telemetrija**: Poboljšani nadzor i analitika performansi agenata
- **Vizualni graditelj agenata**: Alati za razvoj agenata s malo ili bez koda

## Najbolje prakse za implementaciju SLM agenata

### Smjernice za odabir SLM-a za agente

Prilikom odabira SLM-ova za implementaciju agenata razmotrite sljedeće čimbenike:

**Razmatranja veličine modela**: Izaberite ultra-komprimirane modele poput Q2_K za ekstremne mobilne agente, uravnotežene modele poput Q4_K_M za opće agenske scenarije i modele više preciznosti poput Q8_0 za aplikacije agenata gdje je kvaliteta kritična.

**Usuglašenost slučaja uporabe s agentom**: Uskladite SLM mogućnosti sa specifičnim zahtjevima agenta, uzimajući u obzir očuvanje točnosti za odluke agenata, brzinu izvođenja za interakcije u stvarnom vremenu, ograničenja memorije za edge implementacije i zahtjeve za offline rad radi privatnosti.

### Strategija optimizacije za SLM agente

**Pristup kvantizaciji za agente**: Odaberite odgovarajuću razinu kvantizacije prema zahtjevima kvalitete agenta i hardverskim ograničenjima. Razmotrite Q4_0 za maksimalnu kompresiju u mobilnim agentima, Q5_1 za uravnoteženu kvalitetu i kompresiju u općim agentima te Q8_0 za gotovo izvornu kvalitetu u kritičnim agentnim aplikacijama.


**Odabir okvira za implementaciju agenata**: Odaberite okvire za optimizaciju na temelju ciljane hardverske opreme i zahtjeva agenata. Koristite Llama.cpp za optimiziranu implementaciju agenata na CPU-u, Apple MLX za aplikacije agenata na Apple Silicon uređajima, te ONNX za međuprostornu kompatibilnost agenata.

## Praktična konverzija SLM agenata i primjeri korištenja

### Scenariji implementacije agenata u stvarnom svijetu

**Mobilne aplikacije agenata**: Formati Q4_K izvrsni su za aplikacije agenata na pametnim telefonima s minimalnim zahtjevom za memorijom, dok Q8_0 pruža uravnotežene performanse za sustave agenata na tabletima. Formati Q5_K nude vrhunsku kvalitetu za mobilne agente za produktivnost.

**Radne stanice i edge računalstvo agenata**: Q5_K pruža optimalne performanse za aplikacije agenata na desktop računalima, Q8_0 osigurava visokokvalitetnu inferenciju u radnim okruženjima agenata, dok Q4_K omogućuje učinkovitu obradu na edge uređajima za agente.

**Istraživački i eksperimentalni agenti**: Napredni formati kvantizacije omogućuju istraživanje inferencije agenata s ultra niskom preciznošću za akademska istraživanja i proof-of-concept aplikacije agenata koje zahtijevaju ekstremna ograničenja resursa.

### Benchmark rezultati performansi SLM agenata

**Brzina inferencije agenata**: Q4_K postiže najbrže vrijeme odziva agenata na mobilnim CPU-ima, Q5_K pruža uravnotežen omjer brzine i kvalitete za opće agent aplikacije, Q8_0 nudi superiornu kvalitetu za složene zadatke agenata, dok eksperimentalni formati postižu maksimalnu propusnost za specijalizirani hardver agenata.

**Zahtjevi za memoriju agenata**: Razina kvantizacije za agente varira od Q2_K (ispod 500MB za male modele agenata) do Q8_0 (približno 50% originalne veličine), dok eksperimenti postižu maksimalnu kompresiju za agente s ograničenim resursima.

## Izazovi i razmatranja za SLM agente

### Kompromisi u performansama u agent sustavima

Implementacija SLM agenata zahtijeva pažljivo razmatranje kompromisa između veličine modela, brzine odziva agenata i kvalitete izlaza. Dok Q4_K nudi izvanrednu brzinu i učinkovitost za mobilne agente, Q8_0 pruža superiornu kvalitetu za složene zadatke agenata. Q5_K zauzima sredinu prikladnu za većinu općih agent aplikacija.

### Hardverska kompatibilnost za SLM agente

Različiti edge uređaji imaju varijabilne kapacitete za implementaciju SLM agenata. Q4_K učinkovit je na osnovnim procesorima za jednostavne agente, Q5_K zahtijeva umjerene računalne resurse za uravnotežene performanse agenata, a Q8_0 koristi vrhunsku opremu za napredne mogućnosti agenata.

### Sigurnost i privatnost u SLM agent sustavima

Iako SLM agenti omogućuju lokalnu obradu radi veće privatnosti, potrebno je implementirati odgovarajuće sigurnosne mjere za zaštitu modela agenata i podataka u edge okruženjima. To je posebno važno pri implementaciji formata agenata visoke preciznosti u poslovnim okruženjima ili kompresiranih formata u aplikacijama koje obrađuju osjetljive podatke.

## Budući trendovi u razvoju SLM agenata

Landskap SLM agenata nastavlja se razvijati uz napredak u tehnikama kompresije, metodama optimizacije i strategijama implementacije na edge uređajima. Budući razvoj uključuje učinkovitije algoritme kvantizacije modela agenata, poboljšane metode kompresije u tijeku rada agenata te bolju integraciju s hardverskim akceleratorima za obradu agenata na edge-u.

**Tržišne prognoze za SLM agente**: Prema najnovijim istraživanjima, automatizacija potpomognuta agentima mogla bi eliminirati 40–60% repetitivnih kognitivnih zadataka u poslovnim procesima do 2027. godine, s SLM-ovima kao vodećim pokretačima ove transformacije zbog njihove troškovne učinkovitosti i fleksibilnosti implementacije.

**Tehnološki trendovi u SLM agentima**:
- **Specijalizirani SLM agenti**: Modelski pristupi specifični za domenu, obučeni za određenje zadatke i industrije agenata
- **Edge računalstvo agenata**: Poboljšane mogućnosti agenata na uređajima s većom privatnošću i smanjenom latencijom
- **Orkestracija agenata**: Bolja koordinacija između više SLM agenata s dinamičkim usmjeravanjem i balansiranjem opterećenja
- **Demokratizacija**: Fleksibilnost SLM-ova omogućava šire sudjelovanje u razvoju agenata kroz organizacije

## Početak rada sa SLM agentima

### Korak 1: Postavite Microsoft Agent Framework okruženje

**Instalirajte ovisnosti**:
```bash
# Instalirajte Microsoft Agent Framework
pip install microsoft-agent-framework

# Instalirajte Foundry Local SDK za edge implementaciju
pip install foundry-local-sdk

# Instalirajte dodatne ovisnosti za edge agente
pip install openai asyncio
```

**Inicijalizirajte Foundry Local**:
```bash
# Pokreni Foundry Local uslugu
foundry service start

# Učitaj zadani model za razvoj agenta
foundry model run phi-4-mini
```

### Korak 2: Odaberite svoj SLM za aplikacije agenata
Popularne opcije za Microsoft Agent Framework:
- **Microsoft Phi-4 Mini (3.8B)**: Izvrsno za opće zadatke agenata s uravnoteženim performansama
- **Qwen2.5-0.5B (0.5B)**: Ultra učinkovito za jednostavne agente usmjeravanja i klasifikacije
- **Qwen2.5-Coder-0.5B (0.5B)**: Specijalizirano za zadatke agenata vezane uz kodiranje
- **Phi-4 (7B)**: Napredno rezoniranje za složene edge scenarije kada resursi dopuštaju

### Korak 3: Kreirajte svog prvog agenta s Microsoft Agent Framework

**Osnovna postavka agenta**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# Inicijaliziraj Foundry Local vezu
foundry = FoundryLocalManager("phi-4-mini")

# Kreiraj konfiguraciju agenta
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# Kreiraj i konfiguriraj agenta
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# Definiraj jednostavan alat
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# Testiraj agenta
response = agent.chat("What time is it?")
print(response)
```

### Korak 4: Definirajte opseg i zahtjeve agenta
Započnite s fokusiranim, dobro definiranim aplikacijama agenata koristeći Microsoft Agent Framework:
- **Agenti jedne domene**: Korisnička služba ILI zakazivanje ILI istraživanje
- **Jasni ciljevi agenata**: Specifični, mjerljivi ciljevi za performanse agenata
- **Ograničena integracija alata**: Maksimalno 3-5 alata za početnu implementaciju agenata
- **Definirane granice agenata**: Jasni putevi eskalacije za složene scenarije
- **Dizajn s prednošću edge-a**: Prioritizirajte offline funkcionalnost i lokalnu obradu

### Korak 5: Implementirajte implementaciju na edge uređajima s Microsoft Agent Framework

**Konfiguracija resursa**:
```python
from microsoft_agent_framework import ResourceConfig

# Konfigurirajte za ugradnju na rubu mreže
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**Implementirajte sigurnosne mjere za edge agente**:
- **Lokalna validacija unosa**: Provjeravajte zahtjeve bez ovisnosti o oblaku
- **Filtriranje izlaza offline**: Osigurava da odgovori zadovoljavaju standarde kvalitete lokalno
- **Sigurnosne kontrole na edge-u**: Implementirajte sigurnost bez potrebe za internet vezom
- **Lokalno praćenje**: Pratite performanse i detektirajte probleme pomoću edge telemetrije

### Korak 6: Mjerite i optimizirajte performanse edge agenata
- **Stope dovršetka zadataka agenata**: Pratite stope uspješnosti u offline scenarijima
- **Vrijeme odziva agenata**: Osigurajte vrijeme odziva ispod sekunde za edge implementaciju
- **Iskorištenost resursa**: Pratite upotrebu memorije, CPU-a i baterije na edge uređajima
- **Troškovna učinkovitost**: Usporedite troškove edge implementacije s alternativama u oblaku
- **Pouzdanost offline**: Mjerite performanse agenata tijekom prekida mreže

## Ključni zaključci za implementaciju SLM agenata

1. **SLM-ovi su dovoljni za agente**: Za većinu agenata mali modeli imaju istu učinkovitost kao i veliki, uz značajne prednosti
2. **Troškovna učinkovitost u agentima**: 10–30 puta jeftinija implementacija SLM agenata, čineći ih ekonomski održivima za široku primjenu
3. **Specijalizacija djeluje za agente**: Fino podešeni SLM-ovi često nadmašuju opće LLM-ove u specifičnim agent aplikacijama
4. **Hibridna arhitektura agenata**: Koristite SLM-ove za rutinske zadatke agenata, LLM-ove za složeno rezoniranje kada je potrebno
5. **Microsoft Agent Framework omogućava produkcijsku implementaciju**: Pruža alate razine poduzeća za izgradnju, implementaciju i upravljanje edge agentima
6. **Dizajn s prednošću edge-a**: Offline sposobni agenti s lokalnom obradom osiguravaju privatnost i pouzdanost
7. **Integracija Foundry Local**: Besprijekorna veza između Microsoft Agent Frameworka i lokalne inferencije modela
8. **Budućnost su SLM agenti**: Mali jezični modeli s produkcijskim okvirima predstavljaju budućnost agentske umjetne inteligencije, omogućujući demokratičnu i učinkovitu implementaciju agenata

## Reference i dodatna literatura

### Glavni istraživački radovi i publikacije

#### AI agenti i agentski sustavi
- **"Language Agents as Optimizable Graphs"** (2024) - Temeljna istraživanja o arhitekturi agenata i optimizaciji
  - Autori: Wenyue Hua, Lishan Yang, i dr.
  - Link: https://arxiv.org/abs/2402.16823
  - Ključni uvidi: Dizajn agenata baziran na grafovima i strategije optimizacije

- **"The Rise and Potential of Large Language Model Based Agents"** (2023)
  - Autori: Zhiheng Xi, Wenxiang Chen, i dr.
  - Link: https://arxiv.org/abs/2309.07864
  - Ključni uvidi: Sveobuhvatni pregled mogućnosti i primjena agenata temeljenih na LLM-ovima

- **"Cognitive Architectures for Language Agents"** (2024)
  - Autori: Theodore Sumers, Shunyu Yao, i dr.
  - Link: https://arxiv.org/abs/2309.02427
  - Ključni uvidi: Kognitivni okviri za dizajn inteligentnih agenata

#### Mali jezični modeli i optimizacija
- **"Phi-3 Technical Report: A Highly Capable Language Model Locally on Your Phone"** (2024)
  - Autori: Microsoft Research Tim
  - Link: https://arxiv.org/abs/2404.14219
  - Ključni uvidi: Principi dizajna SLM i strategije mobilne implementacije

- **"Qwen2.5 Technical Report"** (2024)
  - Autori: Alibaba Cloud Tim
  - Link: https://arxiv.org/abs/2407.10671
  - Ključni uvidi: Napredne tehnike treniranja SLM-a i optimizacija performansi

- **"TinyLlama: An Open-Source Small Language Model"** (2024)
  - Autori: Peiyuan Zhang, Guangtao Zeng, i dr.
  - Link: https://arxiv.org/abs/2401.02385
  - Ključni uvidi: Ultra kompaktan dizajn modela i učinkovitost treniranja

### Službena dokumentacija i okviri

#### Microsoft Agent Framework
- **Službena dokumentacija**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHub repozitorij**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **Glavni repozitorij**: https://github.com/microsoft/foundry-local
- **Dokumentacija**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **Glavni repozitorij**: https://github.com/vllm-project/vllm
- **Dokumentacija**: https://docs.vllm.ai/


#### Ollama
- **Službena stranica**: https://ollama.ai/
- **GitHub repozitorij**: https://github.com/ollama/ollama

### Okviri za optimizaciju modela

#### Llama.cpp
- **Repozitorij**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **Dokumentacija**: https://microsoft.github.io/Olive/
- **GitHub repozitorij**: https://github.com/microsoft/Olive

#### OpenVINO
- **Službena stranica**: https://docs.openvino.ai/

#### Apple MLX
- **Repozitorij**: https://github.com/ml-explore/mlx

### Izvještaji iz industrije i analize tržišta

#### Istraživanje tržišta AI agenata
- **"The State of AI Agents 2025"** - McKinsey Global Institute
  - Link: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - Ključni uvidi: Tržišni trendovi i obrasci usvajanja u poduzećima

#### Tehnički benchmark testovi

- **"Edge AI Inference Benchmarks"** - MLPerf
  - Link: https://mlcommons.org/en/inference-edge/
  - Ključni uvidi: Standardizirani metrike performansi za edge implementacije

### Standardi i specifikacije

#### Formati i standardi modela
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - Čvorni format modela za interoperabilnost
- **GGUF specifikacija**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - Kvantizirani modelni format za inferenciju na CPU-u
- **OpenAI API specifikacija**: https://platform.openai.com/docs/api-reference
  - Standardizirani API format za integraciju jezičnih modela

#### Sigurnost i usklađenost
- **NIST AI Risk Management Framework**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - AI sustavi**: Okvir za AI sustave i sigurnost
- **IEEE standardi za AI**: https://standards.ieee.org/industry-connections/ai/

Prijelaz prema agentima pokretanim SLM modelima predstavlja temeljnu promjenu u pristupu implementaciji AI-a. Microsoft Agent Framework, u kombinaciji s lokalnim platformama i učinkovitim malim jezičnim modelima, pruža kompletno rješenje za izgradnju produkcijski spremnih agenata koji djeluju učinkovito u edge okruženjima. Fokusirajući se na učinkovitost, specijalizaciju i praktičnu primjenu, ovaj tehnološki skup čini AI agente pristupačnijima, povoljnijima i učinkovitijima za stvarne primjene u svim industrijama i edge računalnim okruženjima.

Kako ulazimo u 2025. godinu, kombinacija sve sposobnijih malih modela, sofisticiranih agentskih okvira poput Microsoft Agent Frameworka i robusnih platformi za edge implementaciju otvara nove mogućnosti za autonomne sustave koji mogu učinkovito raditi na edge uređajima održavajući privatnost, smanjujući troškove i pružajući izvrsno korisničko iskustvo.

**Sljedeći koraci za implementaciju**:
1. **Istražite pozivanje funkcija**: Naučite kako SLM modeli upravljaju integracijom alata i strukturiranim izlazima
2. **Ovladavanje protokolom konteksta modela (MCP)**: Razumijevanje naprednih obrazaca komunikacije agenata
3. **Izgradite produkcijske agente**: Koristite Microsoft Agent Framework za implementacije razine poduzeća
4. **Optimizirajte za edge**: Primijenite napredne tehnike optimizacije za okruženja s ograničenim resursima


## ➡️ Što slijedi

- [02: Pozivanje funkcija u malim jezičnim modelima (SLM)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Napomena**:
Ovaj dokument je preveden korištenjem AI prevoditeljskog servisa [Co-op Translator](https://github.com/Azure/co-op-translator). Iako težimo točnosti, imajte na umu da automatski prijevodi mogu sadržavati greške ili netočnosti. Izvorni dokument na izvornom jeziku treba smatrati autoritativnim izvorom. Za važne informacije preporuča se profesionalni ljudski prijevod. Nismo odgovorni za bilo kakva nesporazumevanja ili pogrešne interpretacije koje proizlaze iz korištenja ovog prijevoda.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->