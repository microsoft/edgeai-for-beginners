# DI Agentai ir Maži Kalbos Modeliai: Išsamus Vadovas

## Įvadas

Šiame vadove apžvelgsime DI Agentus ir Mažus Kalbos Modelius (MKM) bei jų pažangias įgyvendinimo strategijas krašto kompiuterijos aplinkose. Aptarsime agentinio DI pagrindines sąvokas, MKM optimizavimo metodikas, praktines diegimo strategijas resursais apribotuose įrenginiuose ir Microsoft agentų sistemų kūrimo sistemą – Microsoft Agent Framework.

Dirbtinio intelekto sritis patiria paradigminį poslinkį 2025 metais. Jei 2023-ieji buvo pokalbių robotų metai, o 2024-ieji – pagalbininkų proveržio metas, 2025-ieji priklauso DI agentams – intelektualioms sistemoms, kurios mąsto, sprendžia, planuoja, naudoja įrankius ir atlieka užduotis beveik be žmogaus įsikišimo, vis labiau naudodamos efektyvius Mažus Kalbos Modelius. Microsoft Agent Framework iškyla kaip pirmaujanti sprendimų sistema tokioms intelektualioms sistemoms, turinčioms neprisijungus veikiančias krašto galimybes.

## Mokymosi Tikslai

Šio vadovo pabaigoje sugebėsite:

- 🤖 Suprasti pagrindines DI agentų ir agentinių sistemų sąvokas
- 🔬 Nustatyti Mažų Kalbos Modelių pranašumus lyginant su Dideliais Kalbos Modeliais agentinėse taikomoji programose
- 🚀 Išmokti pažangias MKM diegimo strategijas krašto kompiuterijos aplinkose
- 📱 Įgyvendinti praktiškus MKM pagrindu veikiančius agentus realioms programoms
- 🏗️ Kurti gamybinius agentus naudojant Microsoft Agent Framework
- 🌐 Diegti neprisijungus veikiančius krašte agentus su vietine LLM ir MKM integracija
- 🔧 Integruoti Microsoft Agent Framework su Foundry Local krašto diegimui

## DI Agentų Supratimas: Pagrindai ir Klasifikacijos

### Apibrėžimas ir Pagrindinės Sąvokos

Dirbtinio intelekto (DI) agentas – tai sistema arba programa, galinti savarankiškai atlikti užduotis vartotojo ar kitos sistemos naudai, kurdama savo veiklos eigą ir naudodamasi prieinamais įrankiais. Skirtingai nuo tradicinio DI, kuris tik atsako į jūsų klausimus, agentas gali veikti savarankiškai siekdamas tikslų.

### Agentų Klasifikacijos Sistema

Suvokti agentų ribas padeda tinkamai pasirinkti agentų tipus skirtingoms kompiuterijos situacijoms:

- **🔬 Paprasti refleksiniai agentai**: Taisyčių pagrindu veikiantys, reaguojantys į tiesioginius suvokimus (termostatai, bazinė automatika)
- **📱 Modeliu pagrįsti agentai**: Sistemos, palaikančios vidinę būseną ir atmintį (robotų dulkių siurbliai, navigacijos sistemos)
- **⚖️ Tikslų pagrindu veikiantys agentai**: Sistemos, planuojančios ir vykdančios sekas, siekdamos tikslų (maršrutų planuotojai, užduočių tvarkyklės)
- **🧠 Mokymosi agentai**: Adaptuojančios sistemos, gerinančios našumą laikui bėgant (rekomendacijų sistemos, personalizuoti asistentai)

### Pagrindiniai DI Agentų Privalumai

DI agentai suteikia keletą esminių privalumų, dėl kurių jie puikiai tinka krašto kompiuterijos programoms:

**Veiklos savarankiškumas**: Agentai vykdo užduotis nepriklausomai, be nuolatinės žmogaus priežiūros, kas daro juos idealiais realaus laiko programoms. Jiems reikalinga minimali priežiūra, išlaikant adaptacinį elgesį, leidžiantį diegti juos resursais apribotuose įrenginiuose su mažesne operacine našta.

**Diegimo lankstumas**: Šios sistemos suteikia galimybę naudoti DI tiesiog įrenginyje be interneto reikalingumo, gerina privatumą ir saugumą dėl vietinio apdorojimo, gali būti pritaikytos konkrečioms sritims ir tinka įvairioms krašto kompiuterijos aplinkoms.

**Kainos efektyvumas**: Agentų sistemos yra ekonomiškai naudingos diegti palyginti su debesų sprendimais, sumažinant operacines išlaidas ir mažinant pralaidumo reikalavimus krašto programoms.

## Pažangios Mažų Kalbos Modelių Strategijos

### MKM (Mažų Kalbos Modelių) Pagrindai

Mažas Kalbos Modelis (MKM) yra kalbos modelis, tinkantis įprastame vartotojo elektroniniame įrenginyje ir galintis atlikti išvedimą su pakankamai mažu delsos laiku, kad būtų praktiškas aptarnaujant vieno vartotojo agentines užklausas. Praktikoje MKM paprastai turi mažiau nei 10 milijardų parametrų.

**Formatų paieškos galimybės**: MKM palaiko pažangų skirtingų kvantizacijos lygių palaikymą, kryžminę platformų suderinamumą, realaus laiko našumo optimizavimą ir krašto diegimo galimybes. Vartotojai gali pasiekti pagerintą privatumo lygį per vietinį apdorojimą ir WebGPU palaikymą naršyklės pagrindu diegimui.

**Kvantizacijos lygių kolekcijos**: Populiarūs MKM formatai apima Q4_K_M – subalansuotai suspaudimui mobilioms programoms, Q5_K_S seriją – kokybei orientuotam krašto diegimui, Q8_0 – beveik originaliai tikslumui galinguose krašto įrenginiuose, ir eksperimentinius formatus kaip Q2_K skirtus itin mažo resurso situacijoms.

### GGUF (Bendras GGML Universalus Formatavimas) MKM Diegimui

GGUF tarnauja kaip pagrindinis formatas kvantizuotiems MKM diegimui CPU ir krašto įrenginiuose, specialiai optimizuotas agentinėms programoms:

**Agentams optimizuotos galimybės**: Formatas teikia išsamias priemones MKM konvertavimui ir diegimui su pagerintu įrankių kvietimų palaikymu, struktūruoto rezultato generavimu ir daugiatunkiamu pokalbių palaikymu. Kryžminė platformų suderinamumas užtikrina nuoseklų agentų elgesį skirtinguose krašto įrenginiuose.

**Našumo optimizavimas**: GGUF leidžia efektyviai naudoti atmintį agentų darbo eigai, palaiko dinaminį modelių pakrovimą daugagentinėms sistemoms ir teikia optimizuotą išvedimą realaus laiko agentų sąveikoms.

### Krašte optimizuoti MKM karkasai

#### Llama.cpp optimizavimas agentams

Llama.cpp siūlo pažangias kvantizacijos technologijas, specialiai optimizuotas agentiniam MKM diegimui:

**Agentams specifinė kvantizacija**: Rėmelis palaiko Q4_0 (optimalų mobiliam agentų diegimui su 75 % dydžio sumažinimu), Q5_1 (subalansuota kokybės ir suspaudimo parama krašto išvedimo agentams), ir Q8_0 (beveik originalios kokybės gamybos agentų sistemoms). Pažangūs formatai leidžia itin suspaustus agentus ekstremaliosioms krašto sąlygoms.

**Įgyvendinimo privalumai**: CPU optimizuotas išvedimas su SIMD pagreitinimu suteikia atminties efektyvumą agentų vykdymui. Kryžminė suderinamumas tarp x86, ARM ir Apple Silicon architektūrų užtikrina universalias agentų diegimo galimybes.

#### Apple MLX karkasas MKM agentams

Apple MLX suteikia gimtos optimizacijos sprendimus SLM agentams Apple Silicon įrenginiuose:

**Apple Silicon optimizacija agentams**: Karkasas naudoja vieningą atminties architektūrą su Metal Performance Shaders integracija, automatinį mišraus tikslumo agentų išvedimą ir optimizuotą atminties pralaidumą daugagentinėms sistemoms. MKM agentai demonstruoja puikų našumą M serijos mikroschemose.

**Vystymo galimybės**: Python ir Swift API palaikymas su agentams specifinėmis optimizacijomis, automatinė diferenciacija agentų mokymuisi, integracija su Apple vystymo įrankiais suteikia pilnas agentų kūrimo aplinkas.

#### ONNX Runtime kryžminiams MKM agentams

ONNX Runtime suteikia universalią išvedimo variklį, leidžiantį MKM agentams veikti nuosekliai įvairiose aparatūros platformose ir operacinėse sistemose:

**Universali diegimo galimybė**: ONNX Runtime užtikrina nuoseklų MKM agentų elgesį Windows, Linux, macOS, iOS ir Android platformose. Toks kryžminis suderinamumas leidžia kūrėjams rašyti kodą vieną kartą ir diegti bet kur, reikšmingai sumažinant kūrimo ir priežiūros naštą daugplatformėms programoms.

**Aparatūros pagreitinimo galimybės**: Karkasas teikia optimizuotus vykdymo tiekėjus įvairioms aparatūros konfigūracijoms, įskaitant CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm) ir specializuotus pagreitintuvus (Intel VPU, Qualcomm NPU). MKM agentai automatiškai panaudoja geriausią turimą aparatūrą be kodo pakeitimų.

**Gamybos lygio galimybės**: ONNX Runtime siūlo verslo klasės funkcijas, būtinas gamybiniam agentų diegimui, įskaitant grafų optimizavimą greitesniam išvedimui, atminties valdymą resursų apribotoms aplinkoms ir pilnus profilaktikos įrankius našumo analizei. Karkasas palaiko Python ir C++ API lankstiam integravimui.


## MKM prieš DKM agentinėse sistemose: Pažangi Palyginimas

### MKM pranašumai agentinėse programose

**Veiklos efektyvumas**: MKM sumažina agentinių užduočių kaštus 10-30 kartų, palyginti su DKM, leidžiant realaus laiko agentų atsakymus mastu. Jie pasižymi greitesniu išvedimo laiku dėl mažesnės skaičiavimo sudėtingumo, todėl puikiai tinka interaktyvioms agentų programoms.

**Krašto diegimo galimybės**: MKM leidžia įrenginyje vykdyti agentų užduotis be priklausomybės nuo interneto, gerina privatumą per vietinį agentų apdorojimą ir leidžia pritaikyti domain-specific agentų programas įvairioms krašto kompiuterijos aplinkoms.

**Agentams specifinė optimizacija**: MKM puikiai atlieka įrankių kvietimus, struktūruoto rezultato generavimą ir įprastas sprendimų priėmimo darbo eigos, sudarančias 70-80 % tipinių agentų užduočių.

### Kada naudoti MKM ar DKM agentinėse sistemose

**Tinka MKM**:
- **Pasikartojančios agentų užduotys**: Duomenų įvedimas, formų pildymas, rutininių API kvietimai
- **Įrankių integracija**: Duomenų bazės užklausos, failų operacijos, sistemos sąveikos
- **Struktūruotos darbo eigos**: Laikymasis iš anksto nustatytų agentinių procesų
- **Srities specifiniai agentai**: Klientų aptarnavimas, planavimas, bazinė analizė
- **Vietinis apdorojimas**: Privatumą reikalaujančios agentų operacijos

**Geriau tinka DKM**:
- **Sudėtingas mąstymas**: Naujos problemos sprendimas, strateginis planavimas
- **Atviri pokalbiai**: Bendras pokalbis, kūrybinės diskusijos
- **Platus žinių spektras**: Tyrimai, reikalaujantys plačių bendrųjų žinių
- **Naujos situacijos**: Visiškai naujų agentų scenarijų valdymas

### Hibridinė agentų architektūra

Optimalus požiūris sujungia MKM ir DKM heterogeninėse agentinėse sistemose:

**Išmanus agentų orkestravimas**:
1. **MKM kaip pagrindinis**: Vietoje tvarkykite 70-80 % rutininių agentų užduočių
2. **DKM prireikus**: Kompleksines užklausas nukreipkite į debesyje veikiančius didesnius modelius
3. **Specializuoti MKM**: Skirtingi maži modeliai skirtingiems agentų sritims
4. **Kainos optimizavimas**: Sumažinkite brangius DKM kvietimus per išmanų maršrutizavimą

## Gamybiniai MKM Agentų Diegimo Metodai

### Foundry Local: Įmonių klasės krašto DI vykdymo aplinka

Foundry Local (https://github.com/microsoft/foundry-local) yra Microsoft pagrindinis sprendimas Mažų Kalbos Modelių gamybiniam diegimui krašto aplinkose. Jis suteikia pilną vykdymo aplinką specialiai MKM pagrindu veikiančioms agentų sistemoms su įmonių klasės funkcijomis ir sklandžia integracija.

**Pagrindinė architektūra ir funkcijos**:
- **OpenAI suderinamas API**: Pilnas suderinamumas su OpenAI SDK ir Agent Framework integracijomis
- **Automatinė aparatūros optimizacija**: Išmanus modelių variantų parinkimas pagal turimą aparatūrą (CUDA GPU, Qualcomm NPU, CPU)
- **Modelių valdymas**: Automatizuotas MKM modelių atsisiuntimas, kešavimas ir gyvenimo ciklo valdymas
- **Paslaugų atradimas**: Nulinės konfigūracijos paslaugų aptikimas agentų sistemoms
- **Išteklių optimizavimas**: Išmanus atminties valdymas ir energijos efektyvumas krašto diegimui

#### Diegimas ir Konfigūravimas

**Kryžminė platforma diegimas**:
```bash
# Windows (rekomenduojama)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# Linux (rankinė įdiegimas)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**Greitas pradžiamokslis agentų kūrimui**:
```bash
# Paleisti paslaugą su automatiniu modelio įkėlimu
foundry model run phi-4-mini

# Patikrinti paslaugos būseną ir pabaigos tašką
foundry service status

# Išvardinti prieinamus modelius
foundry model ls

# Patikrinti API pabaigos tašką
curl http://localhost:<port>/v1/models
```

#### Agentų sistemos integracija

**Foundry Local SDK integracija**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Inicializuokite Foundry Local su automatiniu paslaugų valdymu
manager = FoundryLocalManager("phi-4-mini")

# Konfigūruokite OpenAI klientą vietiniam spėjimui
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # Automatiškai sugeneruota vietiniam naudojimui
)

# Sukurkite agentą su Foundry Local backend'u
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**Automatinis modelių pasirinkimas ir aparatūros optimizacija**:
```python
# Foundry Local automatiškai pasirenka optimalią modelio versiją
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, labai greitas
    "general_conversation": "phi-4-mini",       # 2.4GB, subalansuotas
    "complex_reasoning": "phi-4",               # 7GB, didelės galios
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, optizuotas kodui
}

# Foundry Local valdo aparatūros aptikimą ir kvantavimą
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # Išvesties pavyzdžiai:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (CPU optimizuotas)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (GPU pagreitintas)
```

#### Gamybinio diegimo modeliai

**Vieno agento gamybinė instaliacija**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # Pridėti gamybos įrankius
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# Gamybinis naudojimas
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**Daugiagentinis gamybinis orkestravimas**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # Lengvas maršrutizavimo agentas
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # Klientų aptarnavimo agentas
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # Techninės pagalbos agentas
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # Nukreipti užklausą tinkamam agentui
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # Nustatyti tikslinį agentą pagal maršrutizavimą
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # Apdoroti su specializuotu agentu
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# Gamybinis diegimas
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### Įmonių klasės funkcijos ir stebėsena

**Sveikatos stebėsena ir observability**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# Gamybos stebėjimo nustatymas
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# Nuolatinis stebėjimas
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # Rinkti metrikas kas 30 sekundžių
```

**Išteklių valdymas ir automatinis skalavimas**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # Sumažink mastelį, jei atminties naudojimas yra didelis
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # Padidink mastelį, jei paklausa yra didelė ir resursai leidžia
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 minutės neveiklumo
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### Pažangus konfigūravimas ir optimizavimas

**Individuali modelių konfigūracija**:
```python
# Pažangi Foundry vietinė konfigūracija gamybai
from foundry_local import FoundryLocalManager, ModelConfig

# Individuali konfigūracija specifiniams naudojimo atvejams
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # Konkretus kiekinimo lygis
    context_length=4096,    # Išplėstinis kontekstas sudėtingiems agentams
    batch_size=1,          # Optimizuota vieno vartotojo agentams
    threads=4,             # CPU gijų optimizavimas
    gpu_layers=32,         # GPU pagreitinimo sluoksniai
    memory_lock=True,      # Užrakinti modelį atmintyje dėl nuoseklaus našumo
    numa=True              # NUMA optimizavimas daugiasluoksnėms sistemoms
)

manager = FoundryLocalManager(config=config)
```

**Gamybinio diegimo kontrolinis sąrašas**:

✅ **Paslaugų konfigūracija**:
- Nustatykite tinkamus modelių slapyvardžius naudojimo atvejams
- Nustatykite išteklių ribas ir stebėsenos slenksčius
- Įjunkite sveikatos patikrinimus ir metrikų rinkimą
- Nustatykite automatinį perkrovimą ir gedimų valdymą

✅ **Saugumo nustatymai**:
- Įjunkite API prieigą tik vietoje (be išorinės prieigos)
- Nustatykite tinkamą API raktų valdymą
- Įdiekite audito žurnalus agentų sąveikoms
- Įgyvendinkite ribojimą gamybos naudojimui

✅ **Našumo optimizavimas**:
- Išbandykite modelių našumą numatomu apkrovimu
- Nustatykite tinkamus kvantizacijos lygius
- Įdiekite modelių kešavimo ir įšilimo strategijas
- Stebėkite atminties ir CPU naudojimo modelius

✅ **Integracijos testavimas**:
- Išbandykite agentų sistemos integraciją
- Patikrinkite neprisijungus veikimo galimybes
- Išbandykite gedimų valdymo ir atkūrimo scenarijus
- Patvirtinkite pilną agentų darbo eigą

### Ollama: Supaprastintas MKM Agentų Diegimas

### Ollama: Bendruomenės orientuotas MKM agentų diegimas

Ollama siūlo bendruomenės valdomą požiūrį į MKM agentų diegimą, pabrėžiant paprastumą, plačią modelių ekosistemą ir kūrėjams patogias darbo eigas. Nors Foundry Local orientuojasi į įmonių klasės funkcijas, Ollama išsiskiria greitu prototipų kūrimu, bendruomenės modelių prieiga ir supaprastintais diegimo scenarijais.

**Pagrindinė architektūra ir funkcijos**:
- **OpenAI suderinamas API**: Pilnas REST API suderinamumas sklandžiai agentų sistemos integracijai
- **Plati modelių biblioteka**: Prieiga prie šimtų bendruomenės prisidėjusių ir oficialių modelių
- **Paprastas modelių valdymas**: Modelių įdiegimas ir keitimas vienu komandos paleidimu
- **Kryžminė platformų parama**: Gimtoji parama Windows, macOS ir Linux sistemose
- **Išteklių optimizavimas**: Automatinė kvantizacija ir aparatūros aptikimas

#### Diegimas ir Konfigūravimas

**Kryžminė platforma diegimas**:
```bash
# Windows
winget install Ollama.Ollama

# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Docker diegimas
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**Greitas pradžiamokslis agentų kūrimui**:
```bash
# Paleisti Ollama paslaugą
ollama serve

# Atsisiųsti ir paleisti modelius agentų kūrimui
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# Patikrinti modelio prieinamumą
ollama list

# Patikrinti API galinį tašką
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### Agentų sistemos integracija

**Ollama su Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama nereikalauja tikro API rakto
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# Inicializuokite Ollama agento kūrimui
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# Konfigūruokite agentą su Ollama pagrindu
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Daugiamodelių agentų nustatymas su Ollama**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # Traukimo modelis, jei nepasiekiamas
            self._pull_model(model_name)
            
            # Sukurkite OpenAI klientą kiekvienam modeliui
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# Naudojimo pavyzdys
manager = OllamaMultiModelManager()

# Gaukite tinkamus agentus skirtingoms užduotims atlikti
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### Gamybinio diegimo modeliai

**Gamybinė paslauga su Ollama**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # Užtikrinkite, kad modelis būtų prieinamas
                self._ensure_model_ready(model_name)
                
                # Sukurkite gamybos agentą
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # Pridėkite gamybos įrankius pagal agente tipus
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # Patikrinkite, ar modelis egzistuoja
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # Simuliuokite duomenų bazės paiešką
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # Atnaujinkite metrikas
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # Gamyboje tai rašytų į tinkamą registracijos sistemą
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # Patikrinkite Ollama paslaugos sveikatą
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # Patikrinkite modelio prieinamumą
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# Gamybinio diegimo pavyzdys
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# Apdorokite užklausas
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### Įmonių klasės funkcijos ir stebėsena

**Ollama stebėsena ir observability**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # Patikrinti paslaugos būklę
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # Gauti modelio informaciją
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # Trumpas santrauka
                    }
                
                # Išbandyti spėjimo našumą
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # Laikyti tik paskutinius 100 metrikos įrašų
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # Įvertinti žetonų skaičių (apytikslis įvertinimas)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # Gauti laikui skirtą informaciją, jei yra
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # Paversti nanoseconds į sekundes
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # Patikrinti atsako laiką
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # Patikrinti paslaugos būseną
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # Apskaičiuoti vidurkius
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# Gamybinio stebėjimo nustatymas
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # Rašyti našumo santrauką kas 10 minučių
            if int(time.time()) % 600 == 0:  # Kas 10 minučių
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # Patikrinti kas 30 sekundžių

# Pradėti stebėjimą
# asyncio.create_task(monitoring_loop())
```

#### Pažangus konfigūravimas ir optimizavimas

**Individualus modelių valdymas su Ollama**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # Lengvi modeliai greitiems atsakymams
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # Subalansuoti modeliai bendram naudojimui
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # Specializuoti modeliai konkrečioms užduotims
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # Aukštos galios modeliai
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # Traukimo modelis
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # Tai paprastai tikrina faktines aparatinės įrangos specifikacijas
        # Demonstraciniais tikslais simuliuosime aparatinės įrangos aptikimą
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# Produkcijos modelio nustatymas
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Gamybinio diegimo kontrolinis sąrašas Ollamai**:

✅ **Paslaugų konfigūracija**:
- Įdiekite Ollama paslaugą su tinkama sistemos integracija
- Konfigūruokite modelius specifiniams agentų naudojimo atvejams
- Nustatykite tinkamus paleidimo scenarijus ir paslaugų valdymą
- Išbandykite modelių pakrovimą ir API prieinamumą

✅ **Modelių valdymas**:
- Atsisiųskite reikiamus modelius ir patikrinkite jų vientisumą
- Nustatykite modelių atnaujinimo ir rotacijos procedūras
- Konfigūruokite modelių kešavimą ir saugojimo optimizavimą
- Išbandykite modelių našumą numatomu apkrovimu

✅ **Saugumo nustatymai**:
- Suplanuokite užkardos taisykles vietinei prieigai
- Nustatykite API prieigos kontrolę ir ribojimą
- Įdiekite audito žurnalus agentų sąveikoms
- Konfigūruokite saugią modelių saugyklą ir prieigą

✅ **Našumo optimizavimas**:
- Atlikite modelių našumo testavimus pagal numatytus naudojimo atvejus
- Nustatykite tinkamą aparatūros pagreitinimą
- Nustatykite modelių įšilimo ir kešavimo strategijas
- Stebėkite išteklių naudojimą ir našumo metrikas

✅ **Integracijos testavimas**:
- Išbandykite Microsoft Agent Framework integraciją
- Patikrinkite neprisijungus veikimo galimybes

- Testuoti avarines situacijas ir klaidų valdymą
- Patikrinti agentų end-to-end darbo eigas

**Palyginimas su Foundry Local**:

| Savybė | Foundry Local | Ollama |
|---------|---------------|--------|
| **Tikslinė paskirtis** | Įmonių gamyba | Kūrimas ir bendruomenė |
| **Modelių ekosistema** | Microsoft kuruojama | Plati bendruomenės |
| **Aparatūros optimizavimas** | Automatinis (CUDA/NPU/CPU) | Rankinis nustatymas |
| **Įmonių funkcijos** | Integruotas stebėjimas, saugumas | Bendruomenės įrankiai |
| **Diegimo sudėtingumas** | Paprasta (winget install) | Paprasta (curl install) |
| **API suderinamumas** | OpenAI + plėtiniai | OpenAI standartas |
| **Palaikymas** | Microsoft oficialus | Bendruomenės valdomas |
| **Geriau tinka** | Gamybos agentai | Prototipavimui, tyrimams |

**Kada rinktis Ollama**:
- **Kūrimas ir prototipavimas**: Greiti eksperimentai su skirtingais modeliais
- **Bendruomenės modeliai**: Pritaikymas prie naujausių bendruomenės modelių
- **Švietimui**: Mokymasis ir mokymas apie AI agentų kūrimą
- **Tyrimų projektams**: Akademiniai tyrimai su įvairiais modeliais
- **Individualūs modeliai**: Kurti ir testuoti pritaikytus priderintus modelius

### VLLM: Aukštos našumo SLM agentų inferencija

VLLM (Very Large Language Model inferencija) suteikia didelio pralaidumo, atmintį taupantį inferencijos variklį, specialiai optimizuotą gamybos SLM diegimams mastu. Nors Foundry Local orientuojasi į paprastumą, o Ollama akcentuoja bendruomenės modelius, VLLM pasižymi aukšto našumo scenarijuose, reikalaujančiuose maksimalaus pralaidumo ir efektyvaus išteklių naudojimo.

**Pagrindinė architektūra ir funkcijos**:
- **PagedAttention**: Revoliucinė atminties valdymo sistema efektyviam dėmesio skaičiavimui
- **Dinaminis grupezimas**: Išmanus užklausų grupavimas optimaliam pralaidumui
- **GPU optimizavimas**: Pažangūs CUDA branduoliai ir tensorių lygiagrečios apdorojimo palaikymas
- **OpenAI suderinamumas**: Pilnas API suderinamumas sklandžiam integravimui
- **Spekuliatyvus dekodavimas**: Pažangios inferencijos pagreitinimo technikos
- **Kvantizacijos palaikymas**: INT4, INT8 ir FP16 kvantizacijos atminties efektyvumui

#### Diegimas ir nustatymas

**Diegimo galimybės**:
```bash
# Standartinė instaliacija
pip install vllm

# Su papildomomis priklausomybėmis agentų sistemoms
pip install vllm[agent] openai

# Docker diegimas produkcijai
docker pull vllm/vllm-openai:latest

# Iš šaltinio kodų naujausioms funkcijoms
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**Greitas pradėjimas agentų kūrimui**:
```bash
# Paleiskite VLLM serverį su SLM modeliu
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# Alternatyva: Paleiskite su Qwen2.5 lengvoms agentūroms
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# Išbandykite API pabaigos tašką
curl http://localhost:8000/v1/models

# Išbandykite pokalbio užbaigimą
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### Agentų platformos integracija

**VLLM su Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # Sumažinti žurnalų įrašymą agentams
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # Palaukti serverio paleidimo
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# Inicializuoti VLLM aukštos raiškos agentams
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # Sukonfigūruoti agentą su VLLM pagrindu
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM nereikalauja API rakto
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**Aukšto pralaidumo kelių agentų nustatymas**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # Paleisti serverį gijoje, kad išvengtumėte užblokavimo
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # Sukurti agentą
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # Sukurti klientų baseiną dideliam pralaidumui
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # 5 klientai vienam modeliui dėl lygiagrečumo
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # Patikrinti, ar pageidaujamas agentas yra prieinamas ir nėra perkrautas
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # Maksimaliai 10 vienu metu vienam agentui
            return preferred_category
        
        # Pereiti prie mažiausiai apkrauto prieinamo agento
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # Numatytoji atsarginė parinktis
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # Nustatyti optimalų agentą
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # Sukurti apdorojimo užduotį
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # Apdoroti visas užklausas lygiagrečiai
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Suformatuoti rezultatus
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# Aukšto pralaidumo naudojimo pavyzdys
throughput_manager = VLLMHighThroughputManager()

# Inicijuoti visus modelius
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# Apdoroti užklausų paketą
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### Gamybos diegimo modeliai

**Įmonių VLLM gamybos paslauga**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # Pradėti darbininkų apdorojimą
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # Maksimaliai 4 darbininkai
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # Pridėti kiekybinimą, jei nurodyta
        if config.quantization:
            # Tai būtų pridėta prie valdytojo pradžios komandos
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # Sukurti gamybos agentą
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # Pridėti gamybos įrankius
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # Gamybos duomenų bazės paieška
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # Pridėti užklausą į eilę
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # Laukti apdorojimo (su laiko limitu)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # Gauti užklausą iš eilės
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # Pasirinkti tinkamą agentą
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # Apdoroti užklausą
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # Atnaujinti metrikas
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # Tai supaprastinta - gamyboje reikėtų tinkamai sekti ateities įvykius
        await asyncio.sleep(0.1)  # Vietos rezervas
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # Vienas darbininkas dėl paprastumo
        )
        server = uvicorn.Server(config)
        await server.serve()

# Gamybos diegimo pavyzdys
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# Inicializuoti ir paleisti gamybos paslaugą
# await production_service.initialize_production_environment()
# await production_service.start_production_server()
```

#### Įmonių funkcijos ir stebėjimas

**Išplėstinis VLLM našumo stebėjimas**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # Inicializuoti NVIDIA ML GPU stebėjimui
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # Sukurti klaidų metrikas
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # Gauti VLLM specifines metrikas per API
        vllm_stats = await self._get_vllm_stats(manager)
        
        # Gauti sistemos metrikas
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # Gauti GPU metrikas, jei yra
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # Supaprastinimui laukiama pirmo GPU
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # Testuoti spėjimą, kad būtų galima pamatuoti delsą
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # Turėtų būti atskleista VLLM
                "active_requests": 1  # Apytikslė vertė
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # Analizuoti kiekvieną instanciją
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # Aukšto delsos rekomendacija
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # Aukšto GPU naudojimo rekomendacija
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # Žemo naudojimo rekomendacija
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# Išplėstinės stebėsenos nustatymas
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # Pridėti kitus valdytojus pagal poreikį
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # Surinkti metrikas
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # Laikyti tik paskutinius 1000 įrašų
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # Generuoti rekomendacijas kas 5 minutes
            if len(monitoring.metrics_history) % 10 == 0:  # Kiekvienas 10-as surinkimas (5 minutės, jei surinkimas kas 30s)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # Generuoti našumo ataskaitą kas valandą
            if len(monitoring.metrics_history) % 120 == 0:  # Kiekvienas 120-as surinkimas (1 valanda)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # Surinkti metrikas kas 30 sekundžių

# Pradėti išplėstinę stebėseną
# asyncio.create_task(advanced_monitoring_loop())
```

#### Išplėstinis konfigūravimas ir optimizavimas

**Gamybos VLLM konfigūracijos šablonai**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # Pridėti pasirenkamus parametrus
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# Naudojimo pavyzdžiai
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**VLLM gamybos diegimo kontrolinis sąrašas**:

✅ **Aparatūros optimizavimas**:
- Konfigūruoti tensorių lygiagrečiuosius nustatymus kelių GPU sistemoms
- Įgalinti kvantizaciją (AWQ/GPTQ) atminties taupymui
- Nustatyti optimalų GPU atminties naudojimą (85-95%)
- Konfigūruoti tinkamus partijų dydžius pralaidumui

✅ **Našumo derinimas**:
- Įgalinti prefikso talpyklą kartotiniams užklausoms
- Konfigūruoti suskaidytą išankstinį užpildymą ilgoms sekvencijoms
- Nustatyti spekuliatyvų dekodavimą greitesnei inferencijai
- Optimizuoti max_num_seqs pagal aparatūrą

✅ **Gamybos funkcijos**:
- Nustatyti sveikatingumo stebėjimą ir metrikų surinkimą
- Konfigūruoti automatinį perkrovimą ir avarinį perėjimą
- Įgyvendinti užklausų eilinimą ir apkrovos balansavimą
- Nustatyti išsamų registravimą ir perspėjimus

✅ **Saugumas ir patikimumas**:
- Konfigūruoti ugniasienės taisykles ir prieigos valdymą
- Nustatyti API kvotų ribojimą ir autentifikaciją
- Įgyvendinti tvarkingą uždarymą ir valymą
- Konfigūruoti atsargines kopijas ir avarinį atkūrimą

✅ **Integracinių testų atlikimas**:
- Išbandyti Microsoft Agent Framework integraciją
- Patikrinti aukšto pralaidumo scenarijus
- Testuoti avarinių situacijų atsigavimą
- Atlikti našumo matavimus apkrovos metu

**Palyginimas su kitais sprendimais**:

| Savybė | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **Tikslinė paskirtis** | Didelio pralaidumo gamyba | Įmonių paprastumas | Kūrimas ir bendruomenė |
| **Našumas** | Maksimalus pralaidumas | Subalansuotas | Geras |
| **Atminties efektyvumas** | PagedAttention optimizavimas | Automatinis optimizavimas | Standartinis |
| **Nustatymo sudėtingumas** | Sudėtingas (daug parametrų) | Žemas (automatinis) | Žemas (paprastas) |
| **Mastelio keitimas** | Puikus (tensorius ir mazgų lygiagrečiai) | Geras | Ribotas |
| **Kvantizacija** | Pažangi (AWQ, GPTQ, FP8) | Automatinė | Standartinė GGUF |
| **Įmonių funkcijos** | Reikia individualaus sprendimo | Integruotos | Bendruomenės įrankiai |
| **Geriau tinka** | Didelio masto gamybos agentams | Įmonių gamybai | Kūrimui |

**Kada rinktis VLLM**:
- **Didelio pralaidumo poreikiai**: Šimtai užklausų per sekundę apdorojimas
- **Didelio masto diegimai**: Multi-GPU, kelių mazgų diegimai
- **Kritinis našumas**: Reakcijos laikai mažiau nei sekundė mastu
- **Pažangi optimizacija**: Poreikis individualiai kvantizacijai ir grupavimui
- **Išteklių efektyvumas**: Maksimalus brangios GPU aparatinės įrangos panaudojimas


## Tikro pasaulio SLM agentų panaudojimai

### Klientų aptarnavimo SLM agentai
- **SLM galimybės**: Sąskaitų paieškos, slaptažodžių atstatymai, užsakymų statuso tikrinimas
- **Kainų pranašumai**: 10 kartų sumažėjusios inferencijos sąnaudos, palyginti su LLM agentais
- **Našumas**: Greitesni atsakymų laikai ir pastovi kokybė įprastoms užklausoms

### Verslo procesų SLM agentai
- **Sąskaitų apdorojimo agentai**: Duomenų išgavimas, informacijos patikrinimas, patvirtinimų persiuntimas
- **El. pašto valdymo agentai**: Kategorizavimas, prioritetų nustatymas, atsakymų juodraščių kūrimas automatiškai
- **Tvarkaraščių agentai**: Susitikimų koordinavimas, kalendorių valdymas, priminimų siuntimas

### Asmeniniai skaitmeniniai SLM asistenta
- **Užduočių valdymo agentai**: Kurti, atnaujinti, organizuoti užduočių sąrašus efektyviai
- **Informacijos rinkimo agentai**: Tirti temas, apibendrinti rezultatus vietoje
- **Komunikacijos agentai**: Kurti el. laiškus, žinutes, socialinės medijos įrašus privačiai

### Prekybos ir finansų SLM agentai
- **Rinkos stebėjimo agentai**: Realiojo laiko kainų stebėjimas, tendencijų atpažinimas
- **Ataskaitų sudarymo agentai**: Automatiniai dienos/savaičių santraukų kūrimas
- **Rizikos vertinimo agentai**: Portfelio pozicijų įvertinimas naudojant vietinius duomenis

### Sveikatos priežiūros palaikymo SLM agentai
- **Pacientų tvarkaraščių agentai**: Tvarkyti susitikimus, siųsti automatinius priminimus
- **Dokumentacijos agentai**: Kurti medicinines santraukas, ataskaitas vietoje
- **Receptų valdymo agentai**: Stebėti pakartotinius užsakymus, tikrinti sąveikas privačiai

## Microsoft Agent Framework: Gamybai parengtas agentų kūrimas

### Apžvalga ir architektūra

Microsoft Agent Framework suteikia visapusišką, įmonių lygio platformą AI agentų kūrimui, diegimui ir valdymui, galinčią dirbti tiek debesies, tiek atjungtuose krašto aplinkose. Ši sistema yra skirta sklandžiai veikti su Small Language Models ir krašto kompiuterijos scenarijais, todėl puikiai tinka privatumui jautriems ir ribotų išteklių diegimams.

**Pagrindinės sistemos sudedamosios dalys**:
- **Agentų vykdymo aplinka**: Lengvas vykdymo variklis optimizuotas krašto įrenginiams
- **Įrankių integracijos sistema**: Išplečiama plėtinių architektūra ryšiui su išorinėmis paslaugomis ir API
- **Būsenos valdymas**: Nuolatinė agentų atmintis ir konteksto valdymas tarp sesijų
- **Saugumo sluoksnis**: Integruoti saugumo valdikliai įmonių diegimams
- **Orkestravimo variklis**: Daugiagentinis koordinavimas ir darbo eigų valdymas

### Pagrindinės funkcijos krašto diegimams

**Offline-First architektūra**: Microsoft Agent Framework sukurtas su offline-priėjimu pirmiausia, leidžiančiu agentams efektyviai veikti be nuolatinio interneto ryšio. Tai apima vietinę modelio inferenciją, talpyklas, offline įrankių vykdymą ir sklandų veikimo degradavimą, kai debesies paslaugos neprieinamos.

**Išteklių optimizavimas**: Sistema siūlo išmanų išteklių valdymą su automatinė atminties optimizacija SLM modeliams, CPU/GPU apkrovos balansavimu krašto įrenginiams, adaptatyviu modelių pasirinkimu pagal turimus resursus ir energiją taupančiais inferencijos modeliais mobiliajai diegimui.

**Saugumas ir privatumas**: Įmonių lygio saugumo funkcijos apima vietinį duomenų apdorojimą privatumo išlaikymui, užšifruotus agentų komunikacijos kanalus, vaidmenimis pagrįstą prieigos kontrolę agentų galimybėms ir audito registravimą atitikties reikalavimams.

### Integracija su Foundry Local

Microsoft Agent Framework sklandžiai integruojasi su Foundry Local, kad suteiktų pilną krašto AI sprendimą:

**Automatinis modelių aptikimas**: Sistema automatiškai aptinka ir jungiasi prie Foundry Local instancijų, nustato prieinamus SLM modelius ir pasirenka optimaliausius pagal agentų poreikius bei aparatūros galimybes.

**Dinaminis modelių užkrovimas**: Agentai gali dinamiškai krauti skirtingus SLM specifinėms užduotims, leidžiant daugmodelių agentų sistemas, kur skirtingi modeliai apdoroja skirtingų tipų užklausas, ir automatinis keitimas tarp modelių pagal prieinamumą bei našumą.

**Našumo optimizavimas**: Integruotos talpyklos mechanizmai mažina modelių užkrovimo laiką, jungties telkinys optimizuoja API skambučius į Foundry Local, o išmanus grupavimas gerina pralaidumą kelioms užklausoms.

### Agentų kūrimas su Microsoft Agent Framework

#### Agentų apibrėžimas ir konfigūracija

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# Sukonfigūruokite agentą su Foundry Local integracija
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# Inicializuokite Foundry Local ryšį
foundry = FoundryLocalManager("phi-4-mini")

# Sukurkite agente instanciją
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### Įrankių integracija krašto scenarijams

```python
# Apibrėžkite įrankius offline režimui
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # Vietinis duomenų bazės užklausimas - veikia offline režimu
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # Vietinis bilieto sukūrimas su sinchronizavimu prisijungus prie interneto
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # Vietinis tvarkaraščio sudarymas su kalendoriaus integracija
    return local_calendar.schedule(customer_id, preferred_time)
```

#### Daugiagentinis orkestravimas

```python
from microsoft_agent_framework import AgentOrchestrator

# Kurkite specializuotus agentus skirtingoms sritims
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # Lengvas paprastoms užduotims
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # Galingesnis sudėtingoms problemoms
        specialized_for="technical_support"
    )
)

# Koordinuokite kelis agentus
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# Maršrutizuokite užklausas pagal ketinimą
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### Išplėstiniai krašto diegimo modeliai

#### Hierarchinė agentų architektūra

**Vietinės agentų grupės**: Diegti kelis specializuotus SLM agentus krašto įrenginiuose, kiekvieną optimizuotą specifinėms užduotims. Naudoti lengvus modelius, tokius kaip Qwen2.5-0.5B, paprastam maršrutavimui ir tvarkaraščių sudarymui, vidutinius modelius, pavyzdžiui, Phi-4-Mini klientų aptarnavimui ir dokumentacijai, ir didesnius modelius sudėtingam mąstymui, kai leidžia ištekliai.

**Kraštas-debesys koordinavimas**: Įgyvendinti išmanius eskalavimo modelius, kai vietiniai agentai tvarko įprastas užduotis, debesų agentai vykdo sudėtingą mąstymą esant ryšiui, ir sklandus perėjimas tarp krašto ir debesies apdorojimo užtikrina tęstinumą.

#### Diegimo konfigūracijos

**Vieno įrenginio diegimas**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**Pasiskirstęs krašto diegimas**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### Našumo optimizavimas krašto agentams

#### Modelių pasirinkimo strategijos

**Užduočių pagrindu modelių priskyrimas**: Microsoft Agent Framework leidžia išmanų modelių pasirinkimą pagal užduoties sudėtingumą ir reikalavimus:

- **Paprasčiausios užduotys** (Klausimų ir atsakymų, maršrutavimas): Qwen2.5-0.5B (500MB, <100ms atsakymas)
- **Vidutinės užduotys** (klientų aptarnavimas, tvarkaraščiai): Phi-4-Mini (2.4GB, 200-500ms atsakymas)
- **Sudėtingos užduotys** (techninė analizė, planavimas): Phi-4 (7GB, 1-3s atsakymas, kai leidžia ištekliai)

**Dinaminis modelių keitimas**: Agentai gali perjunginėti modelius pagal dabartinį sistemos apkrovimą, užduoties sudėtingumo vertinimą, vartotojo prioriteto lygius ir turimus aparatūros resursus.

#### Atminties ir išteklių valdymas

```python
# Sukonfigūruoti išteklių apribojimus įrenginio diegimui
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### Įmonių integracijos modeliai

#### Saugumas ir atitiktis

**Vietinis duomenų apdorojimas**: Visa agentų veikla vykdoma vietoje, užtikrinant, kad jautri informacija niekada neišeina iš krašto įrenginio. Tai apima klientų duomenų apsaugą, HIPAA atitiktį sveikatos priežiūros agentams, finansinių duomenų saugumą bankų agentams bei GDPR atitiktį Europos diegimams.

**Prieigos kontrolė**: Vaidmenimis pagrįstos teisės valdo agentų prieigą prie įrankių, vartotojų autentifikacija agentų sąveikoms ir audito žurnalų fiksavimas visoms agentų veikloms bei sprendimams.

#### Stebėsena ir matomumas

```python
from microsoft_agent_framework import AgentMonitor

# Nustatyti stebėjimą krašto agentams
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # Vietoje saugoti metrikas neprisijungus veikimui
)

agent.add_monitor(monitor)
```

### Tikro pasaulio įgyvendinimo pavyzdžiai

#### Mažmeninės prekybos krašto agentų sistema

```python
# Mažmeninės prekybos kiosko agentas klientų pagalbai parduotuvėje
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### Sveikatos priežiūros palaikymo agentas

```python
# Pacientų aptarnavimo agentas, atitinkantis HIPAA reikalavimus
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # Patobulintas privatumas sveikatos priežiūroje
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### Geriausios praktikos Microsoft Agent Framework

#### Kūrimo gairės

1. **Pradėkite paprastai**: Pradėkite nuo vieno agento scenarijų prieš kuriant sudėtingus daugiagentinius sprendimus
2. **Tinkamas modelio dydis**: Pasirinkite mažiausią modelį, atitinkantį jūsų tikslumo reikalavimus
3. **Įrankių dizainas**: Kurkite orientuotus, vieno tikslo įrankius, o ne sudėtingus daugiafunkcinius
4. **Klaidų valdymas**: Įgyvendinkite sklandų veikimo praradimą offline situacijose ir modelių gedimų atvejais
5. **Testavimas**: Išsamiai testuokite agentus offline sąlygose ir ribotų išteklių aplinkose

#### Diegimo gerosios praktikos

1. **Palaipsnis diegimas**: Iš pradžių diegti mažoms vartotojų grupėms, atidžiai stebėti našumo rodiklius
2. **Išteklių stebėsena**: Nustatyti įspėjimus dėl atminties, CPU ir atsakymo laiko ribų
3. **Rezervinės strategijos**: Visada turėti atsargines parinktis modelių gedimams ar išteklių trūkumui
4. **Saugumas pirmoje vietoje**: Įgyvendinti saugumo kontrolę nuo pat pradžių, o ne vėliau
5. **Dokumentacija**: Išlaikyti aiškią agentų galimybių ir apribojimų dokumentaciją

### Ateities planai ir integracija

Microsoft Agent Framework toliau tobulėja su patobulinta SLM optimizacija, geresniais krašto diegimo įrankiais, efektyvesniu išteklių valdymu ribotose aplinkose ir išplėstu įrankių ekosistemų palaikymu dažnuose įmonių scenarijuose.

**Artimiausios funkcijos**:
- **AutoML agentų optimizavimui**: Automatinis SLM priderinimas specifinėms agentų užduotims
- **Krašto tinklo „mesh“**: Koordinavimas tarp kelių krašto agentų diegimų
- **Išplėstinė telemetrija**: Patobulintas agentų našumo stebėjimas ir analizė
- **Vizualus agentų kūrėjas**: Mažo kodo / be kodo agentų kūrimo įrankiai

## Geriausios praktikos SLM agentų įgyvendinimui

### SLM pasirinkimo gairės agentams

Renkantis SLM agentų diegimui, atkreipkite dėmesį į šiuos veiksnius:

**Modelių dydžio svarstymai**: Pasirinkite itin suspaustus modelius, tokius kaip Q2_K, ekstriminiams mobiliųjų agentų panaudojimams, subalansuotus modelius, tokius kaip Q4_K_M, bendriems agentų scenarijams, ir aukštesnės raiškos modelius, pavyzdžiui, Q8_0, kokybės kritikams agentams.

**Agentų paskirties suderinamumas**: Suderinkite SLM galimybes su konkrečiais agentų poreikiais, atsižvelgdami į sprendimų tikslumą, inferencijos greitį realiu laiku, atminties apribojimus krašto agentų diegimui ir offline veikimo reikalavimus privatumui.

### Optimizavimo strategijos pasirinkimas SLM agentams

**Kvantizacijos metodas agentams**: Pasirinkite tinkamus kvantizacijos lygius pagal agentų kokybės reikalavimus ir aparatūros apribojimus. Apsvarstykite Q4_0 maksimaliam suspaudimui mobiliuose agentuose, Q5_1 subalansuotam kokybės ir suspaudimo santykiui bendruose agentuose ir Q8_0 beveik originaliai kokybei kritinėse agentų programose.


**Agentų diegimo pagrindų pasirinkimas**: Pasirinkite optimizavimo pagrindus pagal tikslinę aparatūrą ir agentų reikalavimus. Naudokite Llama.cpp CPU optimizuotam agentų diegimui, Apple MLX Apple Silicon agentų programėlėms ir ONNX tarpplatforminiam agentų suderinamumui.

## Praktiniai SLM agentų konvertavimo ir naudojimo atvejai

### Realūs agentų diegimo scenarijai

**Mobiliosios agentų programėlės**: Q4_K formatai puikiai tinka išmaniųjų telefonų agentų programėlėms su minimaliu atminties naudojimu, tuo tarpu Q8_0 suteikia subalansuotą našumą planšetinių agentų sistemoms. Q5_K formatai užtikrina aukštesnę kokybę mobiliesiems produktyvumo agentams.

**Staliniai ir krašto agentų kompiuteriai**: Q5_K užtikrina optimalų našumą stalinėms agentų programėlėms, Q8_0 teikia aukštos kokybės išvadas darbo stoties agentų aplinkoje, o Q4_K leidžia efektyviai apdoroti krašto agentų įrenginiuose.

**Tyrimų ir eksperimentiniai agentai**: Pažangūs kvantizacijos formatai leidžia tirti itin žemos tikslumo agentų išvadas akademiniams tyrimams ir konceptų įrodymo agentų programėlėms su itin ribotais ištekliais.

### SLM agentų našumo etalonai

**Agentų išvadų greitis**: Q4_K pasiekia greičiausią agentų atsakymo laiką mobiliuosiuose CPU, Q5_K suteikia subalansuotą greičio ir kokybės santykį bendrosioms agentų programėlėms, Q8_0 užtikrina aukštesnę kokybę sudėtingiems agentų užduotims, o eksperimentiniai formatai suteikia maksimalų pralaidumą specializuotai agentų aparatūrai.

**Agentų atminties reikalavimai**: Agentų kvantizacijos lygiai svyruoja nuo Q2_K (mažiems agentų modeliams – mažiau nei 500MB) iki Q8_0 (apytiksliai 50% pradinio dydžio), o eksperimentinės konfigūracijos pasiekia maksimalų suspaudimą ribotų išteklių agentų aplinkose.

## Iššūkiai ir svarstymai SLM agentams

### Našumo kompromisai agentų sistemose

SLM agentų diegimas reikalauja kruopštaus kompromisų tarp modelio dydžio, agentų atsakymo greičio ir išeigos kokybės įvertinimo. Nors Q4_K suteikia išskirtinį greitį ir efektyvumą mobiliesiems agentams, Q8_0 siūlo aukštesnę kokybę sudėtingoms agentų užduotims. Q5_K užima vidurinę poziciją, tinkamą daugumai bendrųjų agentų programėlių.

### Aparatūros suderinamumas SLM agentams

Skirtingi krašto įrenginiai turi įvairias galimybes SLM agentų diegimui. Q4_K veikia efektyviai ant paprastų procesorių paprastiems agentams, Q5_K reikalauja vidutinių skaičiavimo išteklių subalansuotam agentų našumui, o Q8_0 naudoja aukštesnės klasės aparatūrą pažangioms agentų funkcijoms.

### Saugumas ir privatumas SLM agentų sistemose

Nors SLM agentai leidžia vietinį apdorojimą dėl pagerinto privatumo, būtina tinkamai įgyvendinti saugumo priemones modeliams ir duomenims apsaugoti krašto aplinkoje. Ypač svarbu diegiant aukštos tikslumo agentų formatus įmonių aplinkose arba suspaustus agentų formatus programėlėse, kurios tvarko jautrius duomenis.

## Ateities tendencijos SLM agentų vystymui

SLM agentų kraštovaizdis nuolat tobulėja dėl suspaudimo metodų, optimizavimo būdų ir krašto diegimo strategijų pažangos. Būsimi pokyčiai apims efektyvesnius kvantizacijos algoritmus agentų modeliams, pažangesnius suspaudimo metodus agentų veikloms ir geresnę integraciją su krašto aparatūros spartintuvais agentų apdorojimui.

**Rinkos prognozės SLM agentams**: Remiantis naujausiais tyrimais, agentų varoma automatizacija iki 2027 m. gali pašalinti 40–60% pasikartojančių kognityvinių užduočių įmonių darbo srautuose, o SLM šioje transformacijoje dominuos dėl jų kaštų efektyvumo ir diegimo lankstumo.

**Technologijų tendencijos SLM agentams**:
- **Specializuoti SLM agentai**: Ispecifikuoti modeliai, apmokyti konkrečioms agentų užduotims ir pramonės šakoms
- **Krašto agentų kompiuterija**: Patobulintos įrenginio agentų galimybės su geresniu privatumu ir mažesniu delsos laiku
- **Agentų orkestracija**: Geresnis koordinavimas tarp daugelio SLM agentų su dinamine maršrutizacija ir apkrovos balansavimu
- **Demokratizacija**: SLM lankstumas leidžia plačiau dalyvauti agentų kūrime organizacijose

## Pradžia su SLM agentais

### 1 žingsnis: Įdiekite Microsoft Agent Framework aplinką

**Įdiekite priklausomybes**:
```bash
# Įdiegti Microsoft Agent Framework
pip install microsoft-agent-framework

# Įdiegti Foundry Local SDK tinklo krašto dislokavimui
pip install foundry-local-sdk

# Įdiegti papildomas priklausomybes krašto agentams
pip install openai asyncio
```

**Inicijuokite Foundry Local**:
```bash
# Paleisti Foundry vietinę paslaugą
foundry service start

# Įkelti numatytąjį agento kūrimo modelį
foundry model run phi-4-mini
```

### 2 žingsnis: Pasirinkite savo SLM agentų programėlėms
Populiarūs pasirinkimai Microsoft Agent Framework:
- **Microsoft Phi-4 Mini (3.8B)**: Puikus bendrosioms agentų užduotims su subalansuotu našumu
- **Qwen2.5-0.5B (0.5B)**: Itin efektyvus paprastiems maršrutizavimo ir klasifikacijos agentams
- **Qwen2.5-Coder-0.5B (0.5B)**: Specializuotas kodavimo užduotims agentams
- **Phi-4 (7B)**: Pažangus samprotavimas sudėtingiems krašto scenarijams, kai leidžiami ištekliai

### 3 žingsnis: Sukurkite pirmąjį agentą su Microsoft Agent Framework

**Pagrindinis agento nustatymas**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# Inicializuoti Foundry vietinį ryšį
foundry = FoundryLocalManager("phi-4-mini")

# Sukurti agento konfigūraciją
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# Sukurti ir sukonfigūruoti agentą
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# Apibrėžti paprastą įrankį
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# Išbandyti agentą
response = agent.chat("What time is it?")
print(response)
```

### 4 žingsnis: Apibrėžkite agento apimtį ir reikalavimus
Pradėkite nuo sutelktų, aiškiai apibrėžtų agentų programėlių, naudojant Microsoft Agent Framework:
- **Vienos srities agentai**: Klientų aptarnavimas ARBA tvarkaraščio sudarymas ARBA tyrimai
- **Aiškūs agento tikslai**: Specifiški, išmatuojami agento veiklos tikslai
- **Ribotas įrankių integravimas**: pradiniam diegimui – 3-5 įrankiai maksimaliai
- **Apibrėžtos agento ribos**: Aiškios eskalavimo gairės sudėtingiems scenarijams
- **Prioritetas kraštui**: Prioritizuokite neprisijungimo funkcionalumą ir vietinį apdorojimą

### 5 žingsnis: Įgyvendinkite krašto diegimą su Microsoft Agent Framework

**Išteklių konfigūravimas**:
```python
from microsoft_agent_framework import ResourceConfig

# Suconfigūruoti krašto diegimui
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**Diegite saugumo priemones krašto agentams**:
- **Vietinė įvesties tikrinimas**: Patikrinkite užklausas be debesijos priklausomybių
- **Neprijungtas išvesties filtravimas**: Užtikrinkite, kad atsakymai vietoje atitiktų kokybės standartus
- **Krašto saugumo kontrolės**: Įgyvendinkite saugumą be interneto ryšio poreikio
- **Vietinė stebėsena**: Stebėkite našumą ir ženklinkite problemas naudojant krašto telemetriją

### 6 žingsnis: Išmatuokite ir optimizuokite krašto agentų našumą
- **Agentų užduočių užbaigimo lygiai**: Stebėkite sėkmės rodiklius neprisijungimo scenarijuose
- **Agentų atsakymo laikai**: Užtikrinkite subsekundinius atsakymų laikus krašto diegime
- **Išteklų naudojimas**: Sekite atminties, CPU ir baterijos naudojimą krašto įrenginiuose
- **Kaštų efektyvumas**: Palyginkite krašto diegimo kaštus su debesijos alternatyvomis
- **Neprisijungimo patikimumas**: Išmatuokite agento veikimą tinklo nutrūkimų metu

## Pagrindinės SLM agentų įgyvendinimo išvados

1. **SLM pakanka agentams**: Daugumai agentų užduočių mažas modelis veikia taip pat gerai kaip didelis, suteikdamas reikšmingų pranašumų
2. **Kaštų efektyvumas agentams**: SLM agentų paleidimas yra 10-30 kartų pigesnis, todėl jie ekonomiškai tinkami plačiam diegimui
3. **Specializacija veikia agentams**: Gerai pritaikyti SLM dažnai lenkia bendro pobūdžio LLM specifinėse agentų programėlėse
4. **Hibridinė agentų architektūra**: Naudokite SLM rutininėms agentų užduotims, LLM sudėtingam samprotavimui prireikus
5. **Microsoft Agent Framework leidžia gamybinį diegimą**: Teikia įmonių lygio įrankius agentų kūrimui, diegimui ir valdymui krašte
6. **Krašto pirmumo dizaino principai**: Agentai, galintys veikti neprisijungę su vietiniu apdorojimu, garantuoja privatumą ir patikimumą
7. **Foundry Local integracija**: Sklandus ryšys tarp Microsoft Agent Framework ir vietinio modelio inferencijos
8. **Ateitis yra SLM agentai**: Maži kalbos modeliai su gamybos pagrindais yra agentinės AI ateitis, leidžianti demokratizuotą ir efektyvų agentų diegimą

## Nuorodos ir papildoma literatūra

### Pagrindiniai tyrimų darbai ir publikacijos

#### AI agentai ir agentinės sistemos
- **"Language Agents as Optimizable Graphs"** (2024) - Pagrindiniai tyrimai apie agentų architektūrą ir optimizavimą
  - Autoriai: Wenyue Hua, Lishan Yang ir kt.
  - Nuoroda: https://arxiv.org/abs/2402.16823
  - Pagrindinės įžvalgos: Agentų projektavimas ir optimizavimo strategijos pagrįstos grafais

- **"The Rise and Potential of Large Language Model Based Agents"** (2023)
  - Autoriai: Zhiheng Xi, Wenxiang Chen ir kt.
  - Nuoroda: https://arxiv.org/abs/2309.07864
  - Pagrindinės įžvalgos: Išsamus LLM pagrįstų agentų galimybių ir pritaikymo apžvalga

- **"Cognitive Architectures for Language Agents"** (2024)
  - Autoriai: Theodore Sumers, Shunyu Yao ir kt.
  - Nuoroda: https://arxiv.org/abs/2309.02427
  - Pagrindinės įžvalgos: Kognityviniai pagrindai intelektualių agentų projektavimui

#### Maži kalbos modeliai ir optimizavimas
- **"Phi-3 Technical Report: A Highly Capable Language Model Locally on Your Phone"** (2024)
  - Autoriai: Microsoft Research komanda
  - Nuoroda: https://arxiv.org/abs/2404.14219
  - Pagrindinės įžvalgos: SLM dizaino principai ir mobiliojo diegimo strategijos

- **"Qwen2.5 Technical Report"** (2024)
  - Autoriai: Alibaba Cloud komanda
  - Nuoroda: https://arxiv.org/abs/2407.10671
  - Pagrindinės įžvalgos: Pažangios SLM mokymo technikos ir našumo optimizavimas

- **"TinyLlama: An Open-Source Small Language Model"** (2024)
  - Autoriai: Peiyuan Zhang, Guangtao Zeng ir kt.
  - Nuoroda: https://arxiv.org/abs/2401.02385
  - Pagrindinės įžvalgos: Labai kompaktiško modelio dizainas ir mokymo efektyvumas

### Oficiali dokumentacija ir pagrindai

#### Microsoft Agent Framework
- **Oficiali dokumentacija**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHub saugykla**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **Pagrindinė saugykla**: https://github.com/microsoft/foundry-local
- **Dokumentacija**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **Pagrindinė saugykla**: https://github.com/vllm-project/vllm
- **Dokumentacija**: https://docs.vllm.ai/


#### Ollama
- **Oficiali svetainė**: https://ollama.ai/
- **GitHub saugykla**: https://github.com/ollama/ollama

### Modelių optimizavimo pagrindai

#### Llama.cpp
- **Saugykla**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **Dokumentacija**: https://microsoft.github.io/Olive/
- **GitHub saugykla**: https://github.com/microsoft/Olive

#### OpenVINO
- **Oficiali svetainė**: https://docs.openvino.ai/

#### Apple MLX
- **Saugykla**: https://github.com/ml-explore/mlx

### Pramonės ataskaitos ir rinkos analizė

#### AI agentų rinkos tyrimai
- **"The State of AI Agents 2025"** – McKinsey Global Institute
  - Nuoroda: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - Pagrindinės įžvalgos: Rinkos tendencijos ir įmonių įsisavinimo modeliai

#### Techniniai etalonai

- **"Edge AI Inference Benchmarks"** - MLPerf
  - Nuoroda: https://mlcommons.org/en/inference-edge/
  - Pagrindinės įžvalgos: Standartizuoti našumo rodikliai krašto diegimui

### Standartai ir specifikacijos

#### Modelių formatai ir standartai
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - Tarpplatforminis modelio formatas suderinamumui
- **GGUF specifikacija**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - Kvantizuotas modelio formatas CPU išvadoms
- **OpenAI API specifikacija**: https://platform.openai.com/docs/api-reference
  - Standartinis API formatas kalbos modelių integracijai

#### Saugumas ir atitiktis
- **NIST AI rizikos valdymo pagrindas**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - AI sistemos**: AI sistemų ir saugumo pagrindas
- **IEEE standartai AI**: https://standards.ieee.org/industry-connections/ai/

Perėjimas prie SLM varomų agentų reiškia pagrindinį pokytį, kaip mes žiūrime į AI diegimą. Microsoft Agent Framework, kartu su vietinėmis platformomis ir efektyviais mažais kalbos modeliais, suteikia pilną sprendimą gamybos lygio agentų kūrimui, kurie efektyviai veikia krašto aplinkose. Susitelkus į efektyvumą, specializaciją ir praktinį naudingumą, ši technologijų sistema daro AI agentus prieinamesnius, pigesnius ir veiksmingesnius realių pritaikymų visose pramonės šakose ir krašto kompiuterijos aplinkose.

Eikvojant 2025 metus, vis labiau pajėgių mažų modelių, pažangių agentų pagrindų kaip Microsoft Agent Framework ir patikimų krašto diegimo platformų derinys atvers naujas galimybes autonominėms sistemoms, kurios gali efektyviai veikti krašto įrenginiuose tuo pačiu užtikrindamos privatumą, mažindamos kaštus ir teikdamos išskirtinę naudotojo patirtį.

**Kiti diegimo žingsniai**:
1. **Išnagrinėkite funkcijų iškvietimą**: Sužinokite, kaip SLM tvarko įrankių integraciją ir struktūruotas išvestis
2. **Išmokite modelio konteksto protokolą (MCP)**: Supraskite pažangius agentų komunikacijos modelius
3. **Kurkite gamybinius agentus**: Naudokite Microsoft Agent Framework įmonių lygio diegimui
4. **Optimizuokite kraštui**: Taikykite pažangias optimizavimo technikas ribotų išteklių aplinkoms


## ➡️ Kas toliau

- [02: Funkcijų iškvietimas mažose kalbos modeliuose (SLMs)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Atsakomybės apribojimas**:
Šis dokumentas buvo išverstas naudojant dirbtinio intelekto vertimo paslaugą [Co-op Translator](https://github.com/Azure/co-op-translator). Nors siekiame tikslumo, prašome atkreipti dėmesį, kad automatiniai vertimai gali turėti klaidų ar netikslumų. Originalus dokumentas jo gimtąja kalba laikomas autoritetingu šaltiniu. Svarbiai informacijai rekomenduojama naudoti profesionalų žmogiškąjį vertimą. Mes neatsakome už jokius nesusipratimus ar neteisingą interpretaciją, kilusią naudojantis šiuo vertimu.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->