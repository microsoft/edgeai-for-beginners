# Сессия 1: Начало работы с Foundry Local

## Аннотация

Научитесь устанавливать, настраивать и запускать свои первые модели ИИ с помощью Microsoft Foundry Local. Этот практический семинар предоставляет пошаговое введение в локальную обработку, начиная с установки и заканчивая созданием вашего первого чат-приложения с использованием моделей, таких как Phi-4, Qwen и DeepSeek.

## Цели обучения

К концу этой сессии вы сможете:

- **Установить и настроить**: Настроить Foundry Local с проверкой установки
- **Освоить операции CLI**: Использовать Foundry Local CLI для управления и развертывания моделей
- **Запустить свою первую модель**: Успешно развернуть и взаимодействовать с локальной моделью ИИ
- **Создать чат-приложение**: Разработать базовое чат-приложение с использованием Python SDK Foundry Local
- **Понять локальный ИИ**: Освоить основы локальной обработки и управления моделями

## Предварительные требования

### Системные требования

- **Windows**: Windows 11 (22H2 или новее) ИЛИ **macOS**: macOS 11+ (ограниченная поддержка)
- **ОЗУ**: минимум 8 ГБ, рекомендуется 16 ГБ+
- **Хранилище**: минимум 10 ГБ свободного места для моделей
- **Python**: установлен Python версии 3.10 или новее
- **Администраторский доступ**: права администратора для установки

### Среда разработки

- Visual Studio Code с расширением Python (рекомендуется)
- Доступ к командной строке (PowerShell на Windows, Terminal на macOS)
- Git для клонирования репозиториев (опционально)

## План семинара (30 минут)

### Шаг 1: Установка Foundry Local (5 минут)

#### Установка на Windows

Установите Foundry Local с помощью пакетного менеджера Windows:

```powershell
# Install via winget (recommended)
winget install Microsoft.FoundryLocal
```

Альтернатива: Загрузите напрямую с [Microsoft Learn](https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/install)

#### Установка на macOS (ограниченная поддержка)

> [!NOTE] 
> Поддержка macOS в настоящее время находится в стадии предварительного просмотра. Ознакомьтесь с официальной документацией для получения актуальной информации.

Если доступно, установите через Homebrew:

```bash
# If Homebrew formula is available
brew update
brew install foundry-local

# Or manual download (check official docs for latest)
curl -L -o foundry-local.tar.gz "https://download.microsoft.com/foundry-local/latest/macos/foundry-local.tar.gz"
tar -xzf foundry-local.tar.gz
sudo ./install.sh
```

**Альтернатива для пользователей macOS:**
- Используйте виртуальную машину Windows 11 (Parallels/UTM) и следуйте шагам для Windows
- Запустите через контейнер, если доступно, и настройте `FOUNDRY_LOCAL_ENDPOINT`

### Шаг 2: Проверка установки (3 минуты)

После установки перезапустите терминал и убедитесь, что Foundry Local работает:

```powershell
# Check if Foundry Local is installed correctly
foundry --version

# View available commands
foundry --help
```

Ожидаемый вывод должен содержать информацию о версии и доступных командах.

### Шаг 3: Настройка Python-окружения (5 минут)

Создайте выделенное Python-окружение для этого семинара:

**Windows:**
```powershell
# Create virtual environment
py -m venv .venv

# Activate environment
.\.venv\Scripts\Activate.ps1

# Upgrade pip and install dependencies
python -m pip install --upgrade pip
pip install foundry-local-sdk openai
```

**macOS/Linux:**
```bash
# Create virtual environment
python3 -m venv .venv

# Activate environment
source .venv/bin/activate

# Upgrade pip and install dependencies
python -m pip install --upgrade pip
pip install foundry-local-sdk openai
```

### Шаг 4: Запуск вашей первой модели (7 минут)

Теперь давайте запустим нашу первую модель ИИ локально!

#### Начнем с Phi-4 Mini (рекомендуемая первая модель)

```powershell
# Download and start phi-4-mini (lightweight, fast)
foundry model run phi-4-mini

# Test the model with a simple prompt
foundry model run phi-4-mini --prompt "Hello, introduce yourself in one sentence"
```

> [!TIP]
> Эта команда загружает модель (в первый раз) и автоматически запускает службу Foundry Local.

#### Проверка запущенных процессов

```powershell
# List available models (shows downloaded models)
foundry model list

# Check service status
foundry service status

# See what models are cached locally
foundry cache list
```

#### Попробуйте другие модели

После успешного запуска phi-4-mini попробуйте другие модели:

```powershell
# Larger model with better capabilities
foundry model run gpt-oss-20b --prompt "Explain edge AI in simple terms"

# Fast, efficient model
foundry model run qwen2.5-0.5b --prompt "What are the benefits of local AI inference?"
```

### Шаг 5: Создание вашего первого чат-приложения (10 минут)

Теперь давайте создадим Python-приложение, которое использует запущенные модели.

#### Создание скрипта чата

Создайте новый файл с именем `my_first_chat.py` (или используйте предоставленный пример):

```python
#!/usr/bin/env python3
"""
My First Foundry Local Chat Application
Using FoundryLocalManager for automatic service management
"""

import os
from foundry_local import FoundryLocalManager
from openai import OpenAI

def main():
    # Get model alias from environment or use default
    alias = os.getenv("FOUNDRY_LOCAL_ALIAS", "phi-4-mini")
    
    try:
        # Initialize Foundry Local Manager (auto-starts service, downloads model)
        manager = FoundryLocalManager(alias)
        
        # Create OpenAI client pointing to local endpoint
        client = OpenAI(
            base_url=manager.endpoint,
            api_key=manager.api_key or "not-needed"
        )
        
        # Get the actual model ID for this alias
        model_id = manager.get_model_info(alias).id
        
        print("🤖 Welcome to your first local AI chat!")
        print(f"� Using model: {alias} -> {model_id}")
        print(f"🌐 Endpoint: {manager.endpoint}")
        print("�💡 Type 'quit' to exit\n")
        
    except Exception as e:
        print(f"❌ Failed to initialize Foundry Local: {e}")
        print("💡 Make sure Foundry Local is installed: foundry --version")
        return
    
    while True:
        # Get user input
        user_message = input("You: ").strip()
        
        if user_message.lower() in ['quit', 'exit', 'bye']:
            print("👋 Goodbye!")
            break
            
        if not user_message:
            continue
            
        try:
            # Send message to local AI model
            response = client.chat.completions.create(
                model=model_id,
                messages=[
                    {"role": "system", "content": "You are a helpful AI assistant running locally."},
                    {"role": "user", "content": user_message}
                ],
                max_tokens=200,
                temperature=0.7
            )
            
            # Display the response
            ai_response = response.choices[0].message.content
            print(f"🤖 AI: {ai_response}\n")
            
        except Exception as e:
            print(f"❌ Error: {e}")
            print("💡 Check service status: foundry service status\n")

if __name__ == "__main__":
    main()
```

> [!TIP]
> **Связанные примеры**: Для более сложного использования смотрите:
>
> - **Пример на Python**: `Workshop/samples/session01/chat_bootstrap.py` - включает потоковые ответы и обработку ошибок
> - **Jupyter Notebook**: `Workshop/notebooks/session01_chat_bootstrap.ipynb` - интерактивная версия с подробными объяснениями

#### Тестирование вашего чат-приложения

```powershell
# No need to manually start models - FoundryLocalManager handles this!
# Just run your chat application
python my_first_chat.py
```

Альтернатива: Используйте предоставленные примеры напрямую

```powershell
# Try the complete sample with streaming support
cd Workshop/samples
python -m session01.chat_bootstrap "Your question here"
```

Или откройте интерактивный ноутбук
Откройте Workshop/notebooks/session01_chat_bootstrap.ipynb в VS Code

Попробуйте следующие примеры диалогов:

- "Что такое Microsoft Foundry Local?"
- "Назовите 3 преимущества запуска моделей ИИ локально"
- "Помогите мне понять edge AI"

## Чего вы достигли

Поздравляем! Вы успешно:

1. ✅ **Установили Foundry Local** и убедились, что он работает
2. ✅ **Запустили свою первую модель ИИ** (phi-4-mini) локально
3. ✅ **Протестировали разные модели** через командную строку
4. ✅ **Создали чат-приложение**, которое подключается к вашему локальному ИИ
5. ✅ **Оценили локальную обработку ИИ** без зависимости от облака

## Понимание произошедшего

### Локальная обработка ИИ

- Ваши модели ИИ работают полностью на вашем компьютере
- Данные не отправляются в облако
- Ответы генерируются локально с использованием вашего CPU/GPU
- Конфиденциальность и безопасность данных сохраняются

### Управление моделями

- `foundry model run` загружает и запускает модели
- **FoundryLocalManager SDK** автоматически управляет запуском службы и загрузкой моделей
- Модели кэшируются локально для будущего использования
- Можно загрузить несколько моделей, но обычно запускается одна за раз
- Служба автоматически управляет жизненным циклом модели

### Подходы SDK и CLI

- **CLI-подход**: Ручное управление моделями с помощью `foundry model run <model>`
- **SDK-подход**: Автоматическое управление службой и моделями с помощью `FoundryLocalManager(alias)`
- **Рекомендация**: Используйте SDK для приложений, CLI для тестирования и изучения

## Справочник по основным командам

### Основные команды CLI

```powershell
# Installation & Setup
foundry --version              # Check installation
foundry --help                 # View all commands

# Model Management
foundry model list             # List available models
foundry model run <model>      # Download and start a model
foundry model run <model> --prompt "text"  # One-shot prompt
foundry cache list             # Show downloaded models

# Service Management
foundry service status         # Check if service is running
foundry service start          # Start the service manually
foundry service stop           # Stop the service
```

### Рекомендации по моделям

- **phi-4-mini**: Лучшая модель для начала - быстрая, легковесная, хорошее качество
- **qwen2.5-0.5b**: Самая быстрая обработка, минимальное использование памяти
- **gpt-oss-20b**: Высокое качество ответов, требует больше ресурсов
- **deepseek-coder-1.3b**: Оптимизирована для программирования и задач с кодом

## Устранение неполадок

### "Команда Foundry не найдена"

**Решение:**

```powershell
# Restart your terminal after installation
# Or manually add to PATH (Windows)
$env:PATH += ";C:\Program Files\Microsoft\FoundryLocal"
```

### "Не удалось загрузить модель"

**Решение:**

```powershell
# Check available system memory
foundry service status

# Try a smaller model first
foundry model run phi-4-mini

# Check disk space for model downloads
# Models are stored in: %USERPROFILE%\.foundry\models (Windows)
```

### "Отказано в подключении к localhost"

**Решение:**

```powershell
# Check if service is running
foundry service status

# Start service if needed
foundry service start

# Verify the port (default is 5273)
# Check for port conflicts with: netstat -an | findstr 5273
```

## Следующие шаги

### Немедленные действия

1. **Экспериментируйте** с различными моделями и запросами
2. **Модифицируйте** ваше чат-приложение, чтобы попробовать разные модели
3. **Создайте** свои собственные запросы и протестируйте ответы
4. **Изучите** Сессию 2: Создание приложений RAG

### Расширенный путь обучения

1. **Сессия 2**: Создание решений ИИ с RAG (Retrieval-Augmented Generation)
2. **Сессия 3**: Сравнение различных моделей с открытым исходным кодом
3. **Сессия 4**: Работа с передовыми моделями
4. **Сессия 5**: Создание мультиагентных систем ИИ

## Переменные окружения (опционально)

Для более сложного использования вы можете настроить следующие переменные окружения:

| Переменная | Назначение | Пример |
|------------|------------|--------|
| `FOUNDRY_LOCAL_ALIAS` | Модель по умолчанию | `phi-4-mini` |
| `FOUNDRY_LOCAL_ENDPOINT` | Переопределение URL-адреса конечной точки | `http://localhost:5273/v1` |

Создайте файл `.env` в каталоге вашего проекта:
```
FOUNDRY_LOCAL_ALIAS=phi-4-mini
FOUNDRY_LOCAL_ENDPOINT=auto
```

## Дополнительные ресурсы

### Документация

- [Справочник по Foundry Local Python SDK](https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/reference/reference-sdk?pivots=programming-language-python)
- [Руководство по установке Foundry Local](https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/install)
- [Каталог моделей](https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/models)

### Примеры кода

- **Пример на Python для Session01**: `Workshop/samples/session01/chat_bootstrap.py` - Полное чат-приложение с потоковой передачей
- **Ноутбук для Session01**: `Workshop/notebooks/session01_chat_bootstrap.ipynb` - Интерактивный учебник  
- [Пример 01 модуля 08](../Module08/samples/01/README.md) - Быстрый старт REST Chat
- [Пример 02 модуля 08](../Module08/samples/02/README.md) - Интеграция OpenAI SDK
- [Пример 03 модуля 08](../Module08/samples/03/README.md) - Обнаружение и тестирование моделей

### Сообщество

- [Обсуждения на GitHub Foundry Local](https://github.com/microsoft/Foundry-Local/discussions)
- [Сообщество Azure AI](https://techcommunity.microsoft.com/category/artificialintelligence)

---

**Продолжительность сессии**: 30 минут практики + 15 минут вопросов и ответов  
**Уровень сложности**: Начальный  
**Предварительные требования**: Windows 11/macOS 11+, Python 3.10+, администраторский доступ

## Пример сценария семинара

### Контекст реального мира

**Сценарий**: Команда ИТ в компании должна оценить локальную обработку ИИ для обработки конфиденциальных отзывов сотрудников без отправки данных во внешние сервисы.

**Ваша цель**: Продемонстрировать, что локальные модели ИИ могут предоставлять качественные ответы с задержкой менее секунды, сохраняя полную конфиденциальность данных.

### Тестовые запросы

Используйте эти запросы для проверки вашей настройки:

```json
[
    "List two benefits of local inference.",
    "Summarize why keeping data on device improves privacy.",
    "Give one trade-off when choosing a small model over a large model."
]
```

### Критерии успеха

- ✅ Все запросы получают ответы менее чем за 2 секунды
- ✅ Данные не покидают ваш локальный компьютер
- ✅ Ответы релевантны и полезны
- ✅ Ваше чат-приложение работает без сбоев

Эта проверка гарантирует, что ваша настройка Foundry Local готова к продвинутым семинарам в Сессиях 2-6.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от ответственности**:  
Этот документ был переведен с использованием сервиса автоматического перевода [Co-op Translator](https://github.com/Azure/co-op-translator). Несмотря на наши усилия обеспечить точность, автоматические переводы могут содержать ошибки или неточности. Оригинальный документ на его родном языке следует считать авторитетным источником. Для получения критически важной информации рекомендуется профессиональный перевод человеком. Мы не несем ответственности за любые недоразумения или неправильные интерпретации, возникающие в результате использования данного перевода.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->