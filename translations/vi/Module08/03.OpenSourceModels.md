# Buổi 3: Khám Phá và Quản Lý Mô Hình Mã Nguồn Mở

## Tổng Quan

Buổi học này tập trung vào việc khám phá và quản lý mô hình thực tiễn với Foundry Local. Bạn sẽ học cách liệt kê các mô hình có sẵn, thử nghiệm các lựa chọn khác nhau, và hiểu các đặc tính hiệu suất cơ bản. Phương pháp nhấn mạnh trải nghiệm thực hành với CLI foundry để giúp bạn chọn lựa mô hình phù hợp cho các trường hợp sử dụng của mình.

## Mục Tiêu Học Tập

- Làm chủ các lệnh CLI foundry để khám phá và quản lý mô hình
- Hiểu cách thức lưu trữ cache mô hình và lưu trữ cục bộ
- Học cách thử nghiệm nhanh và so sánh các mô hình khác nhau
- Thiết lập quy trình làm việc thực tiễn cho lựa chọn và đánh giá mô hình
- Khám phá hệ sinh thái mô hình ngày càng phát triển có qua Foundry Local

## Điều Kiện Tiên Quyết

- Đã hoàn thành Buổi 1: Bắt đầu với Foundry Local
- Đã cài đặt và truy cập được CLI Foundry Local
- Có đủ dung lượng lưu trữ cho việc tải mô hình (mô hình có thể từ 1GB đến hơn 20GB)
- Hiểu biết cơ bản về loại mô hình và các trường hợp sử dụng - Mô Hình Nguồn Mở với Foundry Local

## Tổng Quan

Buổi học này khám phá cách mang mô hình mã nguồn mở vào Foundry L## Phần 6: Bài Tập Thực Hành

### Bài Tập: Khám Phá và So Sánh Mô Hình

Tạo tập lệnh đánh giá mô hình của riêng bạn dựa trên Mẫu 03:

```cmd
REM create_model_test.cmd
@echo off
echo Model Discovery and Testing Script
echo =====================================

echo.
echo Step 1: List available models
foundry model list

echo.
echo Step 2: Check what's cached
foundry cache list

echo.
echo Step 3: Start phi-4-mini for testing
foundry model run phi-4-mini --verbose

echo.
echo Step 4: Test with a simple prompt
curl -X POST http://localhost:8000/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"phi-4-mini\",\"messages\":[{\"role\":\"user\",\"content\":\"Hello, please introduce yourself.\"}],\"max_tokens\":100}"

echo.
echo Model test complete!
```

### Nhiệm Vụ Của Bạn

1. **Chạy tập lệnh Mẫu 03**: `samples\03\list_and_bench.cmd`
2. **Thử các mô hình khác nhau**: Thử ít nhất 3 mô hình khác nhau
3. **So sánh hiệu suất**: Ghi chú sự khác biệt về tốc độ và chất lượng phản hồi
4. **Ghi lại kết quả**: Tạo một bảng so sánh đơn giản

### Định Dạng So Sánh Ví Dụ

```
Model Comparison Results:
========================
phi-4-mini:        Fast (~2s), good for general chat
qwen2.5-7b:       Slower (~5s), better reasoning  
deepseek-r1:      Medium (~3s), excellent for code

Recommendation: Start with phi-4-mini for development, 
switch to qwen2.5-7b for production reasoning tasks.
```

## Phần 7: Xử Lý Sự Cố và Thực Tiễn Tốt Nhất

### Các Vấn Đề Thường Gặp và Giải Pháp

**Mô Hình Không Khởi Động:**
```cmd
REM Check service status
foundry service status

REM Restart service if needed
foundry service stop
foundry service start

REM Try with verbose output
foundry model run phi-4-mini --verbose
```

**Bộ Nhớ Không Đủ:**
- Bắt đầu với mô hình nhỏ hơn (`phi-4-mini`)
- Đóng các ứng dụng khác
- Nâng cấp RAM nếu thường xuyên đạt giới hạn

**Hiệu Suất Chậm:**
- Đảm bảo mô hình đã được tải đầy đủ (kiểm tra đầu ra verbose)
- Đóng các ứng dụng nền không cần thiết
- Cân nhắc sử dụng ổ SSD nhanh hơn

### Thực Tiễn Tốt Nhất

1. **Bắt Đầu Nhỏ**: Bắt đầu với `phi-4-mini` để xác nhận thiết lập
2. **Một Mô Hình Mỗi Lần**: Dừng mô hình trước khi bắt đầu mới
3. **Giám Sát Tài Nguyên**: Theo dõi sử dụng bộ nhớ
4. **Kiểm Tra Nhất Quán**: Sử dụng cùng câu hỏi để so sánh công bằng
5. **Ghi Chép Kết Quả**: Ghi chú hiệu suất mô hình cho trường hợp sử dụng

## Phần 8: Bước Tiếp Theo và Tham Khảo

### Chuẩn Bị cho Buổi 4

- **Trọng Tâm Buổi 4**: Công cụ và kỹ thuật tối ưu hóa
- **Điều Kiện Tiên Quyết**: Thành thạo chuyển đổi mô hình và kiểm tra hiệu suất cơ bản
- **Khuyến Nghị**: Xác định 2-3 mô hình yêu thích từ buổi này

### Tài Nguyên Bổ Sung

- **[Tài liệu Foundry Local](https://learn.microsoft.com/azure/ai-foundry/foundry-local/)**: Tài liệu chính thức
- **[Tham khảo CLI](https://learn.microsoft.com/azure/ai-foundry/foundry-local/reference/reference-cli)**: Tài liệu đầy đủ lệnh
- **[Model Mondays](https://aka.ms/model-mondays)**: Spotlight mô hình hàng tuần
- **[Foundry Local GitHub](https://github.com/microsoft/Foundry-Local)**: Cộng đồng và vấn đề
- **[Mẫu 03: Khám Phá Mô Hình](samples/03/README.md)**: Ví dụ tập lệnh thực hành

### Những Điểm Chính

✅ **Khám Phá Mô Hình**: Dùng `foundry model list` để khám phá các mô hình có sẵn  
✅ **Thử Nhanh**: Mẫu `list_and_bench.cmd` cho đánh giá nhanh  
✅ **Giám Sát Hiệu Suất**: Đo lường tài nguyên và thời gian phản hồi cơ bản  
✅ **Lựa Chọn Mô Hình**: Hướng dẫn thực tiễn chọn mô hình theo trường hợp sử dụng  
✅ **Quản Lý Cache**: Hiểu cách lưu trữ và dọn dẹp  

Bạn đã có kỹ năng thực tế để khám phá, thử nghiệm và chọn lựa mô hình phù hợp cho các ứng dụng AI của mình bằng cách sử dụng phương pháp CLI đơn giản của Foundry Local: lựa chọn mô hình cộng đồng, tích hợp nội dung Hugging Face, và áp dụng chiến lược “mang mô hình của riêng bạn” (BYOM). Bạn cũng sẽ khám phá chuỗi Model Mondays để học liên tục và khám phá mô hình.

Tài liệu tham khảo:
- Tài liệu Foundry Local: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- Cách biên dịch mô hình Hugging Face: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-compile-hugging-face-models
- Model Mondays: https://aka.ms/model-mondays
- Foundry Local GitHub: https://github.com/microsoft/Foundry-Local

## Mục Tiêu Học Tập
- Khám phá và đánh giá các mô hình mã nguồn mở cho suy luận cục bộ
- Biên dịch và chạy các mô hình Hugging Face được chọn trong Foundry Local
- Áp dụng chiến lược lựa chọn mô hình cho độ chính xác, độ trễ, và nhu cầu tài nguyên
- Quản lý mô hình cục bộ với cache và phiên bản

## Phần 1: Khám Phá Mô Hình với Foundry CLI

### Các Lệnh Quản Lý Mô Hình Cơ Bản

CLI foundry cung cấp các lệnh đơn giản để khám phá và quản lý mô hình:

```cmd
REM List all available models in the catalog
foundry model list

REM List cached (downloaded) models
foundry cache list

REM Check cache directory location
foundry cache ls
```

### Chạy Các Mô Hình Đầu Tiên của Bạn

Bắt đầu với các mô hình phổ biến, được kiểm thử tốt để hiểu đặc tính hiệu suất:

```cmd
REM Run Phi-4-Mini (lightweight, fast)
foundry model run phi-4-mini --verbose

REM Run Qwen 2.5 7B (larger, more capable)
foundry model run qwen2.5-7b --verbose

REM Run DeepSeek (specialized for coding)
foundry model run deepseek-r1-7b --verbose
```

**Lưu ý:** Cờ `--verbose` cung cấp thông tin chi tiết khi khởi động, bao gồm:
- Tiến trình tải mô hình (lần chạy đầu tiên)
- Chi tiết phân bổ bộ nhớ
- Thông tin liên kết dịch vụ
- Các chỉ số khởi tạo hiệu suất

### Hiểu Các Loại Mô Hình

**Mô Hình Ngôn Ngữ Nhỏ (SLMs):**
- `phi-4-mini`: Nhanh, hiệu quả, phù hợp chat thông thường
- `phi-4`: Phiên bản có khả năng tốt hơn với suy luận nâng cao

**Mô Hình Trung Bình:**
- `qwen2.5-7b`: Suy luận xuất sắc và ngữ cảnh dài hơn
- `deepseek-r1-7b`: Tối ưu cho sinh mã

**Mô Hình Lớn Hơn:**
- `llama-3.2`: Mô hình mã nguồn mở mới nhất của Meta
- `qwen2.5-14b`: Suy luận cấp doanh nghiệp

## Phần 2: Kiểm Tra và So Sánh Mô Hình Nhanh

### Cách Tiếp Cận Mẫu 03: Liệt Kê và Đánh Giá Đơn Giản

Dựa trên mẫu 03, đây là quy trình tối thiểu:

```cmd
@echo off
REM Sample 03 - List and bench pattern
echo Listing available models...
foundry model list

echo.
echo Checking cached models...
foundry cache list

echo.
echo Starting phi-4-mini with verbose output...
foundry model run phi-4-mini --verbose
```

### Kiểm Tra Hiệu Suất Mô Hình

Khi mô hình đang chạy, thử nghiệm với câu hỏi nhất quán:

```cmd
REM Test via curl (Windows Command Prompt)
curl -X POST http://localhost:8000/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"phi-4-mini\",\"messages\":[{\"role\":\"user\",\"content\":\"Explain edge AI in one sentence.\"}],\"max_tokens\":50}"
```

### Lựa Chọn Kiểm Tra PowerShell

```powershell
# Phương pháp PowerShell để thử nghiệm
$body = @{
    model = "phi-4-mini"
    messages = @(
        @{
            role = "user"
            content = "Explain edge AI in one sentence."
        }
    )
    max_tokens = 50
} | ConvertTo-Json -Depth 3

Invoke-RestMethod -Uri "http://localhost:8000/v1/chat/completions" -Method Post -Body $body -ContentType "application/json"
```

## Phần 3: Quản Lý Cache và Lưu Trữ Mô Hình

### Hiểu Cache Mô Hình

Foundry Local tự động quản lý việc tải và lưu cache mô hình:

```cmd
REM Check cache directory and contents
foundry cache ls

REM View cache location
foundry cache cd

REM Clean up unused models (if needed)
foundry cache clean
```

### Các Lưu Ý về Lưu Trữ Mô Hình

**Kích Thước Mô Hình Thông Thường:**
- `phi-4-mini`: ~2.5 GB
- `qwen2.5-7b`: ~4.1 GB  
- `deepseek-r1-7b`: ~4.3 GB
- `llama-3.2`: ~4.9 GB
- `qwen2.5-14b`: ~8.2 GB

**Thực Tiễn Tốt Nhất Về Lưu Trữ:**
- Giữ 2-3 mô hình trong cache để chuyển đổi nhanh
- Xóa mô hình không sử dụng để giải phóng không gian: `foundry cache clean`
- Giám sát sử dụng đĩa, đặc biệt trên SSD nhỏ
- Cân nhắc kích thước mô hình so với khả năng

### Giám Sát Hiệu Suất Mô Hình

Trong khi mô hình chạy, theo dõi tài nguyên hệ thống:

**Trình Quản Lý Tác Vụ Windows:**
- Theo dõi sử dụng bộ nhớ (mô hình được tải trong RAM)
- Giám sát sử dụng CPU trong quá trình suy luận
- Kiểm tra I/O đĩa khi tải mô hình ban đầu

**Giám Sát Qua Dòng Lệnh:**
```cmd
REM Check memory usage (PowerShell)
Get-Process | Where-Object {$_.ProcessName -like "*foundry*"} | Select-Object ProcessName, WorkingSet64

REM Monitor running models
foundry service ps
```

## Phần 4: Hướng Dẫn Lựa Chọn Mô Hình Thực Tiễn

### Lựa Chọn Mô Hình Theo Trường Hợp Sử Dụng

**Cho Trò Chuyện Chung và Hỏi Đáp:**
- Bắt đầu với: `phi-4-mini` (nhanh, hiệu quả)
- Nâng cấp lên: `phi-4` (suy luận tốt hơn)
- Nâng cao: `qwen2.5-7b` (ngữ cảnh dài hơn)

**Cho Sinh Mã:**
- Khuyến nghị: `deepseek-r1-7b`
- Thay thế: `qwen2.5-7b` (cũng tốt cho sinh mã)

**Cho Suy Luận Phức Tạp:**
- Tốt nhất: `qwen2.5-7b` hoặc `qwen2.5-14b`
- Lựa chọn tiết kiệm: `phi-4`

### Hướng Dẫn Yêu Cầu Phần Cứng

**Yêu Cầu Hệ Thống Tối Thiểu:**
```
phi-4-mini:     8GB RAM,  entry-level CPU
phi-4:         12GB RAM,  mid-range CPU
qwen2.5-7b:    16GB RAM,  mid-range CPU
deepseek-r1:   16GB RAM,  mid-range CPU
qwen2.5-14b:   24GB RAM,  high-end CPU
```

**Khuyến Nghị Để Hiệu Suất Tốt:**
- RAM 32GB+ để chuyển đổi đa mô hình thoải mái
- Ổ SSD cho tải mô hình nhanh hơn
- CPU hiện đại với hiệu năng đơn luồng tốt
- Hỗ trợ NPU (máy Windows 11 có Copilot+) để tăng tốc

### Quy Trình Chuyển Đổi Mô Hình

```cmd
REM Stop current model (if needed)
foundry service stop

REM Start different model
foundry model run qwen2.5-7b

REM Verify model is running
foundry service status
```

## Phần 5: Đánh Giá Hiệu Suất Mô Hình Đơn Giản

### Kiểm Tra Hiệu Suất Cơ Bản

Đây là phương pháp đơn giản để so sánh hiệu suất mô hình:

```python
# simple_bench.py - Dựa trên các mẫu Mẫu 03
import time
import requests
import json

def test_model_response(model_name, prompt="Explain edge AI in one sentence."):
    """Test a single model with a prompt and measure response time."""
    start_time = time.time()
    
    try:
        response = requests.post(
            "http://localhost:8000/v1/chat/completions",
            headers={"Content-Type": "application/json"},
            json={
                "model": model_name,
                "messages": [{"role": "user", "content": prompt}],
                "max_tokens": 64
            },
            timeout=30
        )
        
        elapsed = time.time() - start_time
        
        if response.status_code == 200:
            result = response.json()
            return {
                "model": model_name,
                "latency_sec": round(elapsed, 3),
                "response": result["choices"][0]["message"]["content"],
                "status": "success"
            }
        else:
            return {
                "model": model_name,
                "status": "error",
                "error": f"HTTP {response.status_code}"
            }
            
    except Exception as e:
        return {
            "model": model_name,
            "status": "error", 
            "error": str(e)
        }

# Kiểm tra mô hình đang chạy hiện tại
if __name__ == "__main__":
    # Kiểm tra với các mô hình khác nhau (khởi động từng mô hình trước)
    test_models = ["phi-4-mini", "qwen2.5-7b", "deepseek-r1-7b"]
    
    print("Model Performance Test")
    print("=" * 50)
    
    for model in test_models:
        print(f"\nTesting {model}...")
        print("Note: Make sure this model is running first with 'foundry model run {model}'")
        
        result = test_model_response(model)
        
        if result["status"] == "success":
            print(f"✅ {model}: {result['latency_sec']}s")
            print(f"   Response: {result['response'][:100]}...")
        else:
            print(f"❌ {model}: {result['error']}")
```

### Đánh Giá Chất Lượng Thủ Công

Với mỗi mô hình, thử nghiệm với các câu hỏi nhất quán và đánh giá thủ công:

**Các Câu Hỏi Kiểm Tra:**
1. "Giải thích điện toán lượng tử một cách đơn giản."
2. "Viết hàm Python để sắp xếp một danh sách."
3. "Ưu và nhược điểm của làm việc từ xa là gì?"
4. "Tóm tắt lợi ích của AI biên."

**Tiêu Chí Đánh Giá:**
- **Độ chính xác**: Thông tin có đúng không?
- **Sự rõ ràng**: Giải thích có dễ hiểu không?
- **Đầy đủ**: Có trả lời trọn vẹn câu hỏi không?
- **Tốc độ**: Phản hồi nhanh như thế nào?

### Giám Sát Sử Dụng Tài Nguyên

```cmd
REM Monitor while testing different models
REM Start model
foundry model run phi-4-mini

REM In another terminal, monitor resources
foundry service status
foundry service ps

REM Check system resources (PowerShell)
Get-Process | Where-Object ProcessName -Like "*foundry*" | Format-Table ProcessName, WorkingSet64, CPU
```

## Phần 6: Bước Tiếp Theo
- Đăng ký Model Mondays để nhận mô hình và mẹo mới: https://aka.ms/model-mondays
- Đóng góp kết quả phát hiện vào `models.json` của nhóm bạn
- Chuẩn bị cho Buổi 4: so sánh LLMs vs SLMs, suy luận cục bộ vs đám mây, và demo thực hành

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Tuyên bố miễn trừ trách nhiệm**:
Tài liệu này đã được dịch bằng dịch vụ dịch thuật AI [Co-op Translator](https://github.com/Azure/co-op-translator). Mặc dù chúng tôi cố gắng đảm bảo độ chính xác, xin lưu ý rằng bản dịch tự động có thể chứa lỗi hoặc sai sót. Tài liệu gốc bằng ngôn ngữ gốc nên được coi là nguồn tin chính thức. Đối với thông tin quan trọng, nên sử dụng dịch vụ dịch thuật chuyên nghiệp bởi con người. Chúng tôi không chịu trách nhiệm về bất kỳ hiểu lầm hoặc giải thích sai nào phát sinh từ việc sử dụng bản dịch này.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->