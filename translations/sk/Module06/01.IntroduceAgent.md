# AI agenti a malé jazykové modely: komplexný sprievodca

## Úvod

V tomto návode preskúmame AI agentov a malé jazykové modely (SLM) a ich pokročilé implementačné stratégie pre prostredia edge computingu. Pokryjeme základné koncepty agentickej AI, techniky optimalizácie SLM, praktické stratégie nasadenia pre zariadenia s obmedzenými zdrojmi a Microsoft Agent Framework na vytváranie produkčne pripravených agentných systémov.

Prostredie umelej inteligencie zažíva paradigmatický posun v roku 2025. Kým rok 2023 bol rokom chatbotov a 2024 znamenal rozmach copilotov, rok 2025 patrí AI agentom — inteligentným systémom, ktoré myslia, rozumujú, plánujú, používajú nástroje a vykonávajú úlohy s minimálnym ľudským zásahom, čoraz viac poháňané efektívnymi malými jazykovými modelmi. Microsoft Agent Framework sa ukazuje ako vedúce riešenie na budovanie týchto inteligentných systémov s offline schopnosťami založenými na edge.

## Ciele učenia

Na konci tohto návodu budete schopní:

- 🤖 Pochopiť základné koncepty AI agentov a agentických systémov
- 🔬 Identifikovať výhody malých jazykových modelov oproti veľkým jazykovým modelom v agentických aplikáciách
- 🚀 Naučiť sa pokročilé stratégie nasadenia SLM pre prostredia edge computingu
- 📱 Implementovať praktických agentov poháňaných SLM pre reálne aplikácie
- 🏗️ Budovať produkčne pripravených agentov pomocou Microsoft Agent Framework
- 🌐 Nasadiť offline edge-based agentov s lokálnou integráciou LLM a SLM
- 🔧 Integrovať Microsoft Agent Framework s Foundry Local pre edge nasadenie

## Pochopenie AI agentov: základy a klasifikácia

### Definícia a základné koncepty

AI agent označuje systém alebo program schopný autonómne vykonávať úlohy v mene používateľa alebo iného systému navrhovaním svojho pracovného toku a využívaním dostupných nástrojov. Na rozdiel od tradičnej AI, ktorá iba odpovedá na vaše otázky, agent môže samostatne konať, aby dosiahol ciele.

### Rámec klasifikácie agentov

Pochopenie hraníc agentov pomáha vybrať vhodné typy agentov pre rôzne výpočtové scenáre:

- **🔬 Jednoduchí reflexní agenti**: Pravidlami riadené systémy reagujúce na bezprostredné vnímanie (termostaty, základná automatizácia)
- **📱 Agentov prispôsobených modelu**: Systémy udržiavajúce vnútorný stav a pamäť (robotické vysávače, navigačné systémy)
- **⚖️ Agentov orientovaných na cieľ**: Systémy plánujúce a vykonávajúce sekvencie na dosiahnutie cieľov (plánovače trás, plánovače úloh)
- **🧠 Učiace sa agenti**: Adaptívne systémy, ktoré zlepšujú výkon v priebehu času (odporúčacie systémy, personalizovaní asistenti)

### Kľúčové výhody AI agentov

AI agenti ponúkajú viacero zásadných výhod, ktoré ich robia ideálnymi pre aplikácie edge computingu:

**Prevádzková autonómia**: Agenti poskytujú nezávislé vykonávanie úloh bez neustáleho dohľadu človeka, čo ich robí ideálnymi pre aplikácie v reálnom čase. Vyžadujú minimálny dohľad pri zachovaní adaptívneho správania, čo umožňuje nasadenie na zariadeniach s obmedzenými zdrojmi so zníženou prevádzkovou záťažou.

**Flexibilita nasadenia**: Tieto systémy umožňujú AI schopnosti priamo na zariadení bez potreby internetového pripojenia, zvyšujú súkromie a bezpečnosť pomocou lokálneho spracovania, môžu byť prispôsobené špecifickým doménam a sú vhodné pre rôzne prostredia edge computingu.

**Nákladová efektívnosť**: Agentné systémy ponúkajú nákladovo efektívne nasadenie v porovnaní s cloudovými riešeniami, so zníženými prevádzkovými nákladmi a nižšími požiadavkami na šírku pásma pre edge aplikácie.

## Pokročilé stratégie malých jazykových modelov

### Základy SLM (Malých jazykových modelov)

Malý jazykový model (SLM) je jazykový model, ktorý sa zmestí do bežného spotrebiteľského elektronického zariadenia a vykoná inferenciu s dostatočne nízkou latenciou, aby bol praktický pri obsluhovaní agentických požiadaviek jedného používateľa. V praktických pojmoch sú SLM zvyčajne modely s menej než 10 miliardami parametrov.

**Funkcie objavovania formátov**: SLM ponúkajú pokročilú podporu rôznych úrovní kvantizácie, multiplatformovú kompatibilitu, optimalizáciu výkonu v reálnom čase a možnosti nasadenia na edge. Používatelia môžu získať zvýšené súkromie prostredníctvom lokálneho spracovania a podporu WebGPU pre nasadenie v prehliadači.

**Kolekcie úrovní kvantizácie**: Medzi populárne formáty SLM patria Q4_K_M pre vyváženú kompresiu v mobilných aplikáciách, séria Q5_K_S pre kvalitatívne zamerané edge nasadenie, Q8_0 pre takmer originálnu presnosť na výkonných edge zariadeniach a experimentálne formáty ako Q2_K pre scenáre s ultra nízkymi zdrojmi.

### GGUF (General GGML Universal Format) pre nasadenie SLM

GGUF slúži ako primárny formát pre nasadenie kvantizovaných SLM na CPU a edge zariadeniach, špeciálne optimalizovaný pre agentické aplikácie:

**Funkcie optimalizované pre agentov**: Formát poskytuje komplexné zdroje pre konverziu a nasadenie SLM s rozšírenou podporou volania nástrojov, generovania štruktúrovaného výstupu a viackolových rozhovorov. Multiplatformová kompatibilita zabezpečuje konzistentné správanie agentov na rôznych edge zariadeniach.

**Optimalizácia výkonu**: GGUF umožňuje efektívne využitie pamäte pre pracovné toky agentov, podporuje dynamické načítanie modelov pre multi-agentné systémy a poskytuje optimalizovanú inferenciu pre interakcie agentov v reálnom čase.

### Frameworky SLM optimalizované pre edge

#### Optimalizácia Llama.cpp pre agentov

Llama.cpp poskytuje špičkové kvantizačné techniky špeciálne optimalizované pre agentické nasadenie SLM:

**Agentom špecifická kvantizácia**: Framework podporuje Q4_0 (optimálne pre mobilné nasadenie agentov so znížením veľkosti o 75 %), Q5_1 (vyvážená kvalita-kompresia pre stromové inferenčné agentov) a Q8_0 (takmer originálna kvalita pre produkčné agentné systémy). Pokročilé formáty umožňujú ultra-komprimovaných agentov pre extrémne edge scenáre.

**Výhody implementácie**: CPU-optimalizovaná inferencia s akceleráciou SIMD poskytuje pamäťovo efektívne vykonávanie agentov. Multiplatformová kompatibilita naprieč architektúrami x86, ARM a Apple Silicon umožňuje univerzálne nasadenie agentov.

#### Apple MLX Framework pre SLM agentov

Apple MLX poskytuje natívnu optimalizáciu špeciálne navrhnutú pre agentov poháňaných SLM na zariadeniach Apple Silicon:

**Optimalizácia agentov pre Apple Silicon**: Framework využíva jednotnú pamäťovú architektúru s integráciou Metal Performance Shaders, automatickú zmiešanú presnosť pre inferenciu agentov a optimalizovanú pamäťovú šírku pásma pre multi-agentné systémy. SLM agenti dosahujú výnimočný výkon na čipoch série M.

**Vývojové funkcie**: Podpora Python a Swift API s agentovými optimalizáciami, automatická diferenciácia pre učenie agentov a bezproblémová integrácia s Apple vývojovými nástrojmi poskytujú komplexné prostredie na vývoj agentov.

#### ONNX Runtime pre multiplatformových SLM agentov

ONNX Runtime poskytuje univerzálny inferenčný engine, ktorý umožňuje SLM agentom bežať konzistentne na rôznych hardvérových platformách a operačných systémoch:

**Univerzálne nasadenie**: ONNX Runtime zabezpečuje konzistentné správanie SLM agentov na platformách Windows, Linux, macOS, iOS a Android. Táto multiplatformová kompatibilita umožňuje vývojárom napísať raz a nasadiť všade, čo výrazne znižuje vývojovú a údržbovú záťaž pre multiplatformové aplikácie.

**Možnosti hardvérovej akcelerácie**: Framework poskytuje optimalizovaných poskytovateľov vykonávania pre rôzne hardvérové konfigurácie vrátane CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm) a špecializované akcelerátory (Intel VPU, Qualcomm NPU). SLM agenti môžu automaticky využiť najlepší dostupný hardvér bez zmien kódu.

**Funkcie pripravené na produkciu**: ONNX Runtime ponúka funkcie podnikovej úrovne nevyhnutné pre produkčné nasadenie agentov vrátane optimalizácie grafov pre rýchlejšiu inferenciu, správy pamäte pre prostredia s obmedzenými zdrojmi a komplexné nástroje na profilovanie výkonu. Framework podporuje rozhrania API pre Python aj C++ pre flexibilnú integráciu.


## SLM vs LLM v agentických systémoch: pokročilé porovnanie

### Výhody SLM v agentických aplikáciách

**Prevádzková efektívnosť**: SLM poskytujú 10-30× zníženie nákladov v porovnaní s LLM pre agentné úlohy, čo umožňuje agentické reakcie v reálnom čase vo veľkom meradle. Ponúkajú rýchlejšie inferenčné časy vďaka zníženej výpočtovej náročnosti, čo ich robí ideálnymi pre interaktívne agentné aplikácie.

**Možnosti nasadenia na edge**: SLM umožňujú vykonávanie agentov priamo na zariadení bez závislosti na internete, zvýšené súkromie cez lokálne spracovanie agentov a prispôsobenie pre doménovo špecifické agentné aplikácie vhodné pre rôzne prostredia edge computingu.

**Agentová optimalizácia**: SLM vynikajú pri volaní nástrojov, generovaní štruktúrovaných výstupov a rutinnej rozhodovacej logike, ktoré tvoria 70-80 % typických agentných úloh.

### Kedy použiť SLM vs LLM v agentných systémoch

**Ideálne pre SLM**:
- **Opakované agentné úlohy**: Zadávanie dát, vyplňovanie formulárov, rutinové volania API
- **Integrácia nástrojov**: Dopyty do databáz, operácie so súbormi, systémové interakcie
- **Štruktúrované pracovné toky**: Nasledovanie vopred definovaných agentných procesov
- **Doménovo špecifickí agenti**: Zákaznícky servis, plánovanie, základná analýza
- **Lokálne spracovanie**: Agentné operácie citlivé na súkromie

**Lepšie pre LLM**:
- **Komplexné rozumovanie**: Riešenie nových problémov, strategické plánovanie
- **Otvárané konverzácie**: Všeobecné rozhovory, tvorivé diskusie
- **Úlohy vyžadujúce široké znalosti**: Výskum vyžadujúci rozsiahle všeobecné znalosti
- **Nové situácie**: Riešenie úplne nových agentných scenárov

### Hybridná agentná architektúra

Optimálny prístup kombinuje SLM a LLM v heterogénnych agentických systémoch:

**Inteligentná orchestrácia agentov**:
1. **SLM ako primárny**: Lokálne spracovanie 70-80 % rutiných agentných úloh
2. **LLM pri potrebe**: Presmerovanie zložitých dotazov na cloudové väčšie modely
3. **Špecializované SLM**: Rôzne malé modely pre rôzne agentné domény
4. **Optimalizácia nákladov**: Minimalizácia nákladných volaní LLM inteligentným smerovaním

## Produkčné stratégie nasadenia SLM agentov

### Foundry Local: podnikově úroveň runtime pre edge AI

Foundry Local (https://github.com/microsoft/foundry-local) slúži ako vedúce riešenie Microsoftu na nasadenie malých jazykových modelov v produkčných edge prostrediach. Poskytuje kompletné runtime prostredie špeciálne navrhnuté pre agentov poháňaných SLM s funkciami na úrovni podniku a bezproblémovou integráciou.

**Jadrová architektúra a funkcie**:
- **API kompatibilné s OpenAI**: Plná kompatibilita s OpenAI SDK a integráciami Agent Frameworku
- **Automatická hardvérová optimalizácia**: Inteligentný výber variant modelov na základe dostupného hardvéru (CUDA GPU, Qualcomm NPU, CPU)
- **Správa modelov**: Automatizované sťahovanie, cachovanie a správa životného cyklu modelov SLM
- **Objavovanie služieb**: Detekcia služieb bez konfigurácie pre agentné frameworky
- **Optimalizácia zdrojov**: Inteligentná správa pamäte a energetická efektívnosť pre edge nasadenie

#### Inštalácia a nastavenie

**Multiplatformová inštalácia**:
```bash
# Windows (odporúčané)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# Linux (manuálna inštalácia)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**Rýchly štart pre vývoj agentov**:
```bash
# Spustiť službu s automatickým načítaním modelu
foundry model run phi-4-mini

# Overiť stav služby a koncový bod
foundry service status

# Zoznam dostupných modelov
foundry model ls

# Otestovať API koncový bod
curl http://localhost:<port>/v1/models
```

#### Integrácia Agent Frameworku

**Integrácia Foundry Local SDK**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Inicializujte Foundry Local s automatickým spravovaním služieb
manager = FoundryLocalManager("phi-4-mini")

# Nakonfigurujte klienta OpenAI pre lokálnu inferenciu
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # Automaticky vygenerované pre lokálne použitie
)

# Vytvorte agenta s backendom Foundry Local
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**Automatický výber modelu a hardvérová optimalizácia**:
```python
# Foundry Local automaticky vyberá optimálnu variantu modelu
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, ultra rýchly
    "general_conversation": "phi-4-mini",       # 2.4GB, vyvážený
    "complex_reasoning": "phi-4",               # 7GB, vysoko výkonný
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, optimalizovaný pre kód
}

# Foundry Local zabezpečuje detekciu hardvéru a kvantizáciu
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # Príklady výstupu:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (optimalizované pre CPU)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (akcelerované GPU)
```

#### Produkčné vzory nasadenia

**Nastavenie produkčného jedného agenta**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # Pridajte výrobné nástroje
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# Použitie vo výrobe
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**Orchestrácia multi-agentného produkčného systému**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # Ľahký smerovací agent
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # Agent zákazníckeho servisu
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # Agent technickej podpory
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # Presmerovať požiadavku na príslušného agenta
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # Určiť cieľového agenta na základe smerovania
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # Spracovať so špecializovaným agentom
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# Produkčné nasadenie
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### Podnikové funkcie a monitorovanie

**Monitorovanie zdravia a pozorovateľnosť**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# Nastavenie monitorovania výroby
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# Kontinuálne monitorovanie
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # Zber metrík každých 30 sekúnd
```

**Správa zdrojov a automatické škálovanie**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # Zmenšiť veľkosť, ak je vysoká spotreba pamäte
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # Zvýšiť veľkosť, ak je vysoká požiadavka a zdroje to umožňujú
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 minút nečinnosti
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### Pokročilé konfigurácie a optimalizácie

**Vlastná konfigurácia modelov**:
```python
# Pokročilá lokálna konfigurácia Foundry pre produkciu
from foundry_local import FoundryLocalManager, ModelConfig

# Vlastná konfigurácia pre špecifické prípady použitia
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # Konkrétna úroveň kvantizácie
    context_length=4096,    # Rozšírený kontext pre zložité agenty
    batch_size=1,          # Optimalizované pre agentov s jedným používateľom
    threads=4,             # Optimalizácia vlákien CPU
    gpu_layers=32,         # Vrstvy akcelerácie GPU
    memory_lock=True,      # Uzamknutie modelu v pamäti pre konzistentný výkon
    numa=True              # Optimalizácia NUMA pre viaczásuvkové systémy
)

manager = FoundryLocalManager(config=config)
```

**Kontrolný zoznam pre produkčné nasadenie**:

✅ **Konfigurácia služby**:
- Konfigurácia vhodných aliasov modelov pre použitie
- Nastavenie limitov zdrojov a prahov monitorovania
- Povolenie kontrol zdravia a zberu metrík
- Konfigurácia automatického reštartu a failoveru

✅ **Nastavenie zabezpečenia**:
- Povolenie len lokálneho API prístupu (bez externej dostupnosti)
- Konfigurácia správ správy API kľúčov
- Nastavenie auditného záznamu interakcií agentov
- Implementácia obmedzenia rýchlosti pre produkčné použitie

✅ **Optimalizácia výkonu**:
- Test výkonu modelov pri očakávanej záťaži
- Konfigurácia vhodných úrovní kvantizácie
- Nastavenie stratégií cachovania a zahrievania modelov
- Monitorovanie vzorov využitia pamäte a CPU

✅ **Testovanie integrácie**:
- Test integrácie agentného frameworku
- Overenie kapacít práce offline
- Test failover a režimov obnovy
- Overenie end-to-end pracovných tokov agentov

### Ollama: zjednodušené nasadenie SLM agentov

### Ollama: komunitné zameranie na nasadenie SLM agentov

Ollama poskytuje komunitne riadený prístup k nasadeniu SLM agentov so zameraním na jednoduchosť, rozsiahly ekosystém modelov a vývojársky priateľské pracovné toky. Kým Foundry Local sa sústreďuje na podnikové funkcie, Ollama vyniká v rýchlom prototypovaní, prístupe ku komunitným modelom a zjednodušených scenároch nasadenia.

**Jadrová architektúra a funkcie**:
- **API kompatibilné s OpenAI**: Plná kompatibilita s REST API pre bezproblémovú integráciu s agentnými frameworkmi
- **Rozsiahla knižnica modelov**: Prístup k stovkám modelov prispievateľov komunity aj oficiálnych modelov
- **Jednoduchá správa modelov**: Inštalácia a prepínanie modelov jedným príkazom
- **Podpora multiplatformy**: Natívna podpora Windows, macOS a Linuxu
- **Optimalizácia zdrojov**: Automatická kvantizácia a detekcia hardvéru

#### Inštalácia a nastavenie

**Multiplatformová inštalácia**:
```bash
# Windows
winget install Ollama.Ollama

# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Nasadenie Dockeru
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**Rýchly štart pre vývoj agentov**:
```bash
# Spustiť službu Ollama
ollama serve

# Stiahnuť a spustiť modely pre vývoj agenta
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# Otestovať dostupnosť modelu
ollama list

# Otestovať API koncový bod
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### Integrácia Agent Frameworku

**Ollama s Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama nevyžaduje skutočný API kľúč
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# Inicializujte Ollama pre vývoj agenta
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# Nakonfigurujte agenta s Ollama backendom
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Nastavenie multi-modelových agentov s Ollama**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # Použiť model Pull, ak nie je dostupný
            self._pull_model(model_name)
            
            # Vytvorte klienta OpenAI pre každý model
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# Príklad použitia
manager = OllamaMultiModelManager()

# Získajte vhodných agentov pre rôzne úlohy
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### Produkčné vzory nasadenia

**Produkčná služba s Ollama**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # Zabezpečte dostupnosť modelu
                self._ensure_model_ready(model_name)
                
                # Vytvorte produkčného agenta
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # Pridajte produkčné nástroje podľa typu agenta
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # Skontrolujte, či model existuje
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # Simulujte vyhľadávanie v databáze
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # Aktualizujte metriky
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # V produkcii by toto zapisovalo do správneho logovacieho systému
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # Skontrolujte stav služby Ollama
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # Skontrolujte dostupnosť modelu
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# Príklad produkčného nasadenia
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# Spracovanie požiadaviek
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### Podnikové funkcie a monitorovanie

**Monitorovanie a pozorovateľnosť Ollama**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # Skontrolovať stav služby
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # Získať informácie o modeli
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # Krátky súhrn
                    }
                
                # Otestovať výkonnosť inferencie
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # Uchovať len posledných 100 záznamov metrik
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # Odhadnúť tokeny (približný odhad)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # Získať informácie o načasovaní, ak sú dostupné
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # Prevod nanosekúnd na sekundy
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # Skontrolovať čas odpovede
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # Skontrolovať stav služby
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # Vypočítať priemery
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# Nastavenie monitorovania v produkcii
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # Zaznamenať súhrn výkonu každých 10 minút
            if int(time.time()) % 600 == 0:  # Každých 10 minút
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # Kontrola každých 30 sekúnd

# Spustiť monitorovanie
# asyncio.create_task(monitoring_loop())
```

#### Pokročilá konfigurácia a optimalizácia

**Vlastná správa modelov s Ollama**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # Ľahké modely pre rýchle odpovede
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # Vyvážené modely pre všeobecné použitie
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # Špecializované modely pre konkrétne úlohy
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # Modely s vysokou schopnosťou
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # Model typu pull
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # Toto by obvykle kontrolovalo skutočné špecifikácie hardvéru
        # Pre demonštračné účely budeme simulovať detekciu hardvéru
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# Nastavenie produkčného modelu
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Kontrolný zoznam pre produkčné nasadenie Ollama**:

✅ **Konfigurácia služby**:
- Inštalácia Ollama služby s riadnou systémovou integráciou
- Konfigurácia modelov pre konkrétne agentné prípady použitia
- Nastavenie správnych štartovacích skriptov a správy služieb
- Testovanie načítania modelov a dostupnosti API

✅ **Správa modelov**:
- Stiahnutie potrebných modelov a overenie integrity
- Nastavenie postupov aktualizácie a rotácie modelov
- Konfigurácia cachovania modelov a optimalizácie úložiska
- Testovanie výkonu modelov pri očakávanej záťaži

✅ **Nastavenie zabezpečenia**:
- Konfigurácia firewall pravidiel pre lokálny prístup
- Nastavenie kontrol prístupu API a obmedzenie rýchlosti
- Implementácia auditných záznamov interakcií agentov
- Konfigurácia bezpečného ukladania modelov a prístupov

✅ **Optimalizácia výkonu**:
- Benchmarkovanie modelov pre očakávané prípady použitia
- Konfigurácia vhodnej hardvérovej akcelerácie
- Nastavenie stratégií zahriatia a cachovania modelov
- Monitorovanie využitia zdrojov a výkonnostných metrík

✅ **Testovanie integrácie**:
- Testovanie integrácie Microsoft Agent Frameworku
- Overenie schopností práce offline

- Testovať scénare prepnutia pri zlyhaní a spracovanie chýb
- Overiť end-to-end pracovné postupy agenta

**Porovnanie s Foundry Local**:

| Funkcia | Foundry Local | Ollama |
|---------|---------------|--------|
| **Cieľové použitie** | Podniková produkcia | Vývoj a komunita |
| **Ekosystém modelov** | Spravovaný spoločnosťou Microsoft | Rozsiahla komunita |
| **Optimalizácia hardvéru** | Automatická (CUDA/NPU/CPU) | Manuálna konfigurácia |
| **Podnikové funkcie** | Vstavané monitorovanie, bezpečnosť | Komunitné nástroje |
| **Zložitosť nasadenia** | Jednoduché (winget inštalácia) | Jednoduché (curl inštalácia) |
| **API kompatibilita** | OpenAI + rozšírenia | OpenAI štandard |
| **Podpora** | Oficiálna od Microsoftu | Riadená komunitou |
| **Najvhodnejšie pre** | Produkčné agentov | Prototypovanie, výskum |

**Kedy vybrať Ollama**:
- **Vývoj a prototypovanie**: Rýchle experimentovanie s rôznymi modelmi
- **Modely komunity**: Prístup k najnovším modelom prispievateľov komunity
- **Vzdelávacie použitie**: Učenie a výučba vývoja AI agentov
- **Výskumné projekty**: Akademický výskum vyžadujúci rozmanitý prístup k modelom
- **Vlastné modely**: Budovanie a testovanie vlastných doladených modelov

### VLLM: Výkonný SLM agent inference

VLLM (inferencia veľmi veľkého jazykového modelu) poskytuje vysokopriepustný, pamäťovo efektívny inferenčný engine špeciálne optimalizovaný pre produkčné SLM nasadenia v meradle. Kým Foundry Local sa zameriava na jednoduchosť použitia a Ollama zdôrazňuje modely komunity, VLLM vyniká vo vysoko výkonných scenároch vyžadujúcich maximálny priepust a efektívne využívanie zdrojov.

**Jadrá architektúry a funkcie**:
- **PagedAttention**: Revolučné spravovanie pamäte pre efektívne výpočty pozornosti
- **Dynamické dávkovanie**: Inteligentné dávkovanie požiadaviek pre optimálny priepust
- **Optimalizácia GPU**: Pokročilé CUDA kernely a podpora paralelizmu tensorov
- **Kompatibilita OpenAI**: Plná API kompatibilita pre bezproblémovú integráciu
- **Špekulatívne dekódovanie**: Pokročilé techniky zrýchlenia inferencie
- **Podpora kvantizácie**: INT4, INT8 a FP16 kvantizácia pre efektívnosť pamäte

#### Inštalácia a nastavenie

**Možnosti inštalácie**:
```bash
# Štandardná inštalácia
pip install vllm

# S dodatočnými závislosťami pre agentové rámce
pip install vllm[agent] openai

# Docker nasadenie pre produkciu
docker pull vllm/vllm-openai:latest

# Zo zdroja pre najnovšie funkcie
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**Rýchly štart pre vývoj agenta**:
```bash
# Spustiť server VLLM s modelom SLM
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# Alternatíva: Spustiť s Qwen2.5 pre ľahké agenty
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# Otestovať API koncový bod
curl http://localhost:8000/v1/models

# Otestovať dokončenie chatu
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### Integrácia s agent frameworkom

**VLLM s Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # Znížiť zaznamenávanie pre agentov
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # Počkaj na spustenie servera
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# Inicializovať VLLM pre vysoko výkonných agentov
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # Nakonfigurovať agenta s backendom VLLM
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM nevyžaduje API kľúč
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**Nastavenie pre viacagentné vysoko priepustné prostredie**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # Spustiť server v vlákne, aby sa predišlo zablokovaniu
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # Vytvoriť agenta
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # Vytvoriť klientský bazén pre vysoký prietok
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # 5 klientov na model pre paralelizmus
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # Skontrolovať, či je preferovaný agent dostupný a nie je preťažený
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # Max 10 súbežných na agenta
            return preferred_category
        
        # Použiť záložného najmenej zaťaženého dostupného agenta
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # Predvolený záložný agent
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # Určiť optimálneho agenta
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # Vytvoriť úlohu spracovania
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # Spracovať všetky požiadavky paralelne
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Naformátovať výsledky
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# Príklad použitia s vysokým prietokom
throughput_manager = VLLMHighThroughputManager()

# Inicializovať všetky modely
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# Spracovať dávkové požiadavky
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### Produkčné vzory nasadenia

**Podniková produkčná služba VLLM**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # Spustiť spracovanie pracovníkov
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # Max 4 pracovníci
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # Pridať kvantizáciu, ak je špecifikovaná
        if config.quantization:
            # Toto by sa pridalo do spúšťacieho príkazu správcu
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # Vytvoriť produkčného agenta
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # Pridať produkčné nástroje
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # Vyhľadávanie v produkčnej databáze
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # Pridať požiadavku do fronty
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # Čakať na spracovanie (s časovým limitom)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # Získať požiadavku z fronty
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # Vybrať vhodného agenta
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # Spracovať požiadavku
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # Aktualizovať metriky
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # Toto je zjednodušené - v produkcii by ste sledovali futures správne
        await asyncio.sleep(0.1)  # Zástupný symbol
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # Jeden pracovník pre jednoduchosť
        )
        server = uvicorn.Server(config)
        await server.serve()

# Príklad produkčného nasadenia
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# Inicializovať a spustiť produkčnú službu
# await production_service.initialize_production_environment()
# await production_service.start_production_server()
```

#### Podnikové funkcie a monitorovanie

**Pokročilé monitorovanie výkonu VLLM**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # Inicializovať NVIDIA ML pre monitorovanie GPU
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # Vytvoriť metriky chýb
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # Získať metriky špecifické pre VLLM cez API
        vllm_stats = await self._get_vllm_stats(manager)
        
        # Získať systémové metriky
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # Získať metriky GPU, ak sú k dispozícii
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # Predpokladáme prvý GPU pre jednoduchosť
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # Testovať inferenciu na meranie latencie
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # Muselo by byť sprístupnené VLLM
                "active_requests": 1  # Aproximácia
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # Analyzovať každú inštanciu
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # Odporúčanie pri vysokej latencii
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # Odporúčanie pri vysokej vyťaženosti GPU
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # Odporúčanie pri nízkej vyťaženosti
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# Pokročilé nastavenie monitorovania
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # Pridať ďalších manažérov podľa potreby
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # Zhromaždiť metriky
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # Uchovávať iba posledných 1000 záznamov
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # Generovať odporúčania každých 5 minút
            if len(monitoring.metrics_history) % 10 == 0:  # Každá 10. kolekcia (5 minút pri zbere každých 30s)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # Generovať výkonnostnú správu každú hodinu
            if len(monitoring.metrics_history) % 120 == 0:  # Každá 120. kolekcia (1 hodina)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # Zhromažďovať metriky každých 30 sekúnd

# Spustiť pokročilé monitorovanie
# asyncio.create_task(advanced_monitoring_loop())
```

#### Pokročilá konfigurácia a optimalizácia

**Konfiguračné šablóny pre produkčný VLLM**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # Pridajte voliteľné parametre
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# Príklady použitia
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**Kontrolný zoznam pre produkčné nasadenie VLLM**:

✅ **Optimalizácia hardvéru**:
- Nastaviť paralelizmus tensorov pre multi-GPU nastavenia
- Povoliť kvantizáciu (AWQ/GPTQ) pre efektívnosť pamäte
- Nastaviť optimálne využitie GPU pamäte (85-95%)
- Konfigurovať vhodné veľkosti dávok pre priepust

✅ **Ladenie výkonu**:
- Povoliť prefix caching pre opakované dopyty
- Konfigurovať dávkovanie predvyplnenia pre dlhé sekvencie
- Nastaviť špekulatívne dekódovanie pre rýchlejšiu inferenciu
- Optimalizovať max_num_seqs na základe hardvéru

✅ **Produkčné funkcie**:
- Nastaviť monitorovanie stavu a zber metrík
- Konfigurovať automatický reštart a prepnutie pri zlyhaní
- Implementovať radovanie požiadaviek a vyváženie záťaže
- Nastaviť komplexné protokolovanie a upozorňovanie

✅ **Bezpečnosť a spoľahlivosť**:
- Konfigurovať pravidlá firewallu a prístupové kontroly
- Nastaviť limitovanie rýchlosti API a autentifikáciu
- Implementovať plynulé vypínanie a čistenie
- Konfigurovať zálohovanie a obnovu po havárii

✅ **Testovanie integrácie**:
- Testovať integráciu Microsoft Agent Framework
- Overiť scenáre vysokej priepustnosti
- Testovať postupy prepnutia pri zlyhaní a zotavenia
- Benchmarkovať výkon pri zaťažení

**Porovnanie s inými riešeniami**:

| Funkcia | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **Cieľové použitie** | Vysokopriepustná produkcia | Podniková jednoduchosť | Vývoj a komunita |
| **Výkon** | Maximálny priepust | Vyvážený | Dobrý |
| **Efektívnosť pamäte** | Optimalizácia PagedAttention | Automatická optimalizácia | Štandardná |
| **Zložitosť nastavenia** | Vysoká (mnoho parametrov) | Nízka (automatická) | Nízka (jednoduchá) |
| **Škálovateľnosť** | Vynikajúca (tensor/rúrkový paralel) | Dobrá | Obmedzená |
| **Kvantizácia** | Pokročilá (AWQ, GPTQ, FP8) | Automatická | Štandardný GGUF |
| **Podnikové funkcie** | Vyžaduje vlastnú implementáciu | Vstavané | Komunitné nástroje |
| **Najlepšie pre** | Produkčných agentov vo veľkom meradle | Podniková produkcia | Vývoj |

**Kedy vybrať VLLM**:
- **Požiadavky na vysoký priepust**: Spracovanie stoviek požiadaviek za sekundu
- **Veľkorozmerné nasadenia**: Multi-GPU, multi-uzlové nasadenia
- **Kritický výkon**: Podsekundové reakčné časy vo veľkom meradle
- **Pokročilá optimalizácia**: Potreba vlastnej kvantizácie a dávkovania
- **Efektívnosť zdrojov**: Maximálne využitie drahého GPU hardvéru


## Reálne aplikácie SLM agentov

### SLM agenti zákazníckej podpory
- **SLM schopnosti**: Vyhľadávanie účtov, resetovanie hesiel, kontrola stavu objednávky
- **Úspora nákladov**: 10-násobné zníženie nákladov na inferenciu v porovnaní s LLM agentmi
- **Výkon**: Rýchlejšie reakčné časy s konzistentnou kvalitou pre rutinné dopyty

### SLM agenti pre obchodné procesy
- **Agenti spracovania faktúr**: Extrakcia dát, overovanie informácií, smerovanie na schválenie
- **Agenti správy e-mailov**: Kategorizácia, priorizácia, automatické vytváranie odpovedí
- **Agenti plánovania**: Koordinovanie stretnutí, správa kalendárov, odosielanie pripomienok

### Osobní digitálni asistenti SLM
- **Agenti správy úloh**: Efektívne vytváranie, aktualizovanie a organizovanie zoznamov úloh
- **Agenti zhromažďovania informácií**: Lokálne štúdium tém, sumarizácia zistení
- **Agenti komunikácie**: Tvorba emailov, správ, príspevkov na sociálne siete v súkromí

### SLM agenti pre obchod a financie
- **Agenti sledovania trhu**: Sledovanie cien, identifikácia trendov v reálnom čase
- **Agenti tvorby správ**: Automatické vytváranie dennej/týždennej sumarizácie
- **Agenti hodnotenia rizika**: Vyhodnocovanie pozícií portfólia pomocou lokálnych dát

### SLM agenti pre zdravotnú starostlivosť
- **Agenti plánovania pacientov**: Koordinácia termínov, odosielanie automatických pripomienok
- **Agenti dokumentácie**: Lokálne generovanie lekárskych zhrnutí a správ
- **Agenti správy predpisov**: Sledovanie doplnkov, kontrola interakcií v súkromí

## Microsoft Agent Framework: Produkčný vývoj agentov

### Prehľad a architektúra

Microsoft Agent Framework poskytuje komplexnú platformu podnikovej úrovne na tvorbu, nasadzovanie a správu AI agentov, ktorí môžu pracovať v cloude aj v offline edge prostrediach. Framework je navrhnutý na bezproblémovú spoluprácu s malými jazykovými modelmi (SLM) a edge computingom, čo ho robí ideálnym pre nasadenia citlivé na súkromie a obmedzené zdroje.

**Základné komponenty frameworku**:
- **Behové prostredie agenta**: Ľahké prostredie optimalizované pre edge zariadenia
- **Systém integrácie nástrojov**: Rozšíriteľná architektúra pluginov na prepojenie externých služieb a API
- **Správa stavu**: Trvalá pamäť agenta a spracovanie kontextu naprieč reláciami
- **Bezpečnostná vrstva**: Vstavané bezpečnostné kontroly pre podnikové nasadenie
- **Orchestrace motor**: Koordinácia viacagentných systémov a manažment pracovných tokov

### Kľúčové funkcie pre edge nasadenie

**Offline-prvotná architektúra**: Microsoft Agent Framework je navrhnutý s princípom offline-prvého fungovania, umožňujúc agentom efektívne fungovať bez neustáleho internetového pripojenia. Zahŕňa lokálnu inferenciu modelov, cacheované znalostné bázy, offline vykonávanie nástrojov a plynulý pokles funkčnosti, keď cloudové služby nie sú dostupné.

**Optimalizácia zdrojov**: Framework poskytuje inteligentné riadenie zdrojov s automatickou optimalizáciou pamäte pre SLM, vyváženie záťaže CPU/GPU pre edge zariadenia, adaptívny výber modelov podľa dostupných zdrojov a energeticky úsporné vzory inferencie pre mobilné nasadenia.

**Bezpečnosť a súkromie**: Podnikové bezpečnostné funkcie zahŕňajú lokálne spracovanie dát na zachovanie súkromia, šifrované komunikačné kanály agentov, prístupové kontroly založené na rolách pre schopnosti agentov a auditné záznamy pre požiadavky na súlad.

### Integrácia s Foundry Local

Microsoft Agent Framework sa hladko integruje s Foundry Local, čím poskytuje kompletné riešenie edge AI:

**Automatické objavovanie modelov**: Framework automaticky deteguje a pripája sa k inštanciám Foundry Local, objavuje dostupné SLM modely a vyberá optimálne modely podľa požiadaviek agenta a hardvérovej kapacity.

**Dynamické načítanie modelov**: Agenti môžu dynamicky načítať rôzne SLM pre špecifické úlohy, čo umožňuje multi-modelové agentné systémy, kde rôzne modely spracúvajú rôzne typy požiadaviek, a automatické prepnutie medzi modelmi podľa dostupnosti a výkonu.

**Optimalizácia výkonu**: Integrované cache mechanizmy skracujú časy načítania modelu, zoskupovanie spojení optimalizuje volania API do Foundry Local a inteligentné dávkovanie zlepšuje priepust pre viac agentných požiadaviek.

### Budovanie agentov s Microsoft Agent Framework

#### Definícia a konfigurácia agenta

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# Nakonfigurujte agenta s integráciou Foundry Local
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# Inicializujte pripojenie Foundry Local
foundry = FoundryLocalManager("phi-4-mini")

# Vytvorte inštanciu agenta
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### Integrácia nástrojov pre edge scenáre

```python
# Definovať nástroje pre offline prevádzku
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # Dotaz do lokálnej databázy - funguje offline
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # Lokálne vytvorenie lístka so synchronizáciou pri pripojení online
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # Lokálne plánovanie s integráciou kalendára
    return local_calendar.schedule(customer_id, preferred_time)
```

#### Orchestrace viac agentov

```python
from microsoft_agent_framework import AgentOrchestrator

# Vytvorte špecializovaných agentov pre rôzne oblasti
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # Ľahká váha pre jednoduché úlohy
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # Vyspelejší pre zložité problémy
        specialized_for="technical_support"
    )
)

# Orchestruji viacero agentov
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# Smerujte požiadavky podľa zámeru
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### Pokročilé vzory edge nasadenia

#### Hierarchická architektúra agentov

**Lokálne agentné klastre**: Nasadiť viacero špecializovaných SLM agentov na edge zariadenia, každý optimalizovaný na konkrétne úlohy. Použiť ľahké modely ako Qwen2.5-0.5B pre jednoduché smerovanie a plánovanie, stredné modely ako Phi-4-Mini pre zákaznícku podporu a dokumentáciu a väčšie modely pre komplexné uvažovanie, keď to zdroje dovolia.

**Koordinácia edge k cloudu**: Implementovať inteligentné eskalačné vzory, kde lokálni agenti spracovávajú rutinné úlohy, cloud agenti poskytujú komplexné uvažovanie pri dostupnosti konektivity a plynulé odovzdanie medzi edge a cloud spracovaním zachováva kontinuitu.

#### Konfigurácie nasadenia

**Nasadenie na jedno zariadenie**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**Distribuované edge nasadenie**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### Optimalizácia výkonu pre edge agentov

#### Strategie výberu modelu

**Priraďovanie modelu na základe úlohy**: Microsoft Agent Framework umožňuje inteligentný výber modelu podľa zložitosti a požiadaviek úloh:

- **Jednoduché úlohy** (Q&A, smerovanie): Qwen2.5-0.5B (500MB, <100ms odozva)
- **Stredné úlohy** (zákaznícka podpora, plánovanie): Phi-4-Mini (2.4GB, 200-500ms odozva)
- **Komplexné úlohy** (technická analýza, plánovanie): Phi-4 (7GB, 1-3s odozva keď zdroje dovolia)

**Dynamické prepínanie modelu**: Agenti môžu prepínať medzi modelmi podľa aktuálneho zaťaženia systému, hodnotenia zložitosti úlohy, úrovní priority používateľa a dostupných hardvérových zdrojov.

#### Správa pamäte a zdrojov

```python
# Nakonfigurujte obmedzenia zdrojov pre okrajové nasadenie
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### Vzory podnikovej integrácie

#### Bezpečnosť a súlad s predpismi

**Lokálne spracovanie dát**: Všetko spracovanie agentov prebieha lokálne, čo zaručuje, že citlivé údaje nikdy neopustia edge zariadenie. Zahŕňa to ochranu informácií o zákazníkoch, súlad HIPAA pre zdravotníckych agentov, bezpečnosť finančných dát pre bankové agentov a GDPR súlad pre európske nasadenia.

**Prístupová kontrola**: Oprávnenia založené na rolách kontrolujú, ku ktorým nástrojom môžu agenti pristupovať, autentifikácia používateľov pre interakcie s agentmi a auditné záznamy o všetkých akciách a rozhodnutiach agentov.

#### Monitorovanie a pozorovateľnosť

```python
from microsoft_agent_framework import AgentMonitor

# Nastavte monitorovanie pre edge agentov
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # Uchovávajte metriky lokálne pre offline prevádzku
)

agent.add_monitor(monitor)
```

### Príklady implementácie v reálnom svete

#### Systém retail edge agentov

```python
# Predajný kiosk agent pre asistenciu zákazníkom v obchode
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### Agent pre podporu zdravotnej starostlivosti

```python
# Agent podpory pacientov v súlade s HIPAA
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # Zlepšené súkromie pre zdravotnú starostlivosť
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### Najlepšie praktiky pre Microsoft Agent Framework

#### Vývojové smernice

1. **Začať jednoducho**: Začať s jednagentovými scenármi pred budovaním komplexných viacagentných systémov
2. **Správna veľkosť modelu**: Vybrať najmenší model, ktorý spĺňa požiadavky na presnosť
3. **Navrhovanie nástrojov**: Vytvárať zamerané, jednofunkčné nástroje namiesto komplexných viacfunkčných
4. **Spracovanie chýb**: Implementovať plynulé degradácie pre offline scenáre a zlyhania modelov
5. **Testovanie**: Dôkladné testovanie agentov v offline podmienkach a prostrediach s obmedzenými zdrojmi

#### Najlepšie praktiky nasadenia

1. **Postupné zavádzanie**: Nasadiť najskôr malej skupine používateľov, starostlivo monitorovať výkonnostné metriky
2. **Monitorovanie zdrojov**: Nastaviť upozornenia na prahové hodnoty pamäte, CPU a odozvy
3. **Náhradné stratégie**: Mať vždy záložné plány na zlyhania modelov alebo vyčerpanie zdrojov
4. **Bezpečnosť na prvom mieste**: Bezpečnostné kontroly implementovať od začiatku, nie až dodatočne
5. **Dokumentácia**: Udržiavať jasnú dokumentáciu schopností a limitácií agentov

### Budúca cesta a integrácia

Microsoft Agent Framework sa naďalej vyvíja s vylepšenou optimalizáciou SLM, lepšími nástrojmi pre edge nasadenie, lepším riadením zdrojov pre obmedzené prostredia a rozšíreným ekosystémom nástrojov pre bežné podnikové scenáre.

**Nadchádzajúce funkcie**:
- **AutoML pre optimalizáciu agentov**: Automatické doladenie SLM pre špecifické úlohy agentov
- **Edge Mesh Networking**: Koordinácia medzi viacerými edge agentnými nasadeniami
- **Pokročilá telemetria**: Vylepšené monitorovanie a analytika výkonu agentov
- **Vizualny tvorca agentov**: Nástroje na vývoj agentov s minimom kódu alebo bez kódu

## Najlepšie praktiky pre implementáciu SLM agentov

### Smernice výberu SLM pre agentov

Pri výbere SLM pre nasadenie agentov zvážte nasledujúce faktory:

**Zváženie veľkosti modelu**: Vyberte ultra komprimované modely ako Q2_K pre extrémne mobilné agentné aplikácie, vyvážené modely ako Q4_K_M pre všeobecné agentné scenáre a modely s vyššou presnosťou ako Q8_0 pre aplikácie agentov, kde je kritická kvalita.

**Zladenie použitia agenta**: Prispôsobte schopnosti SLM konkrétnym požiadavkám agentov, zohľadňujúc faktory ako zachovanie presnosti pri rozhodovaní agenta, rýchlosť inferencie pre interakcie v reálnom čase, pamäťové obmedzenia pre edge nasadenie a požiadavky na offline fungovanie pre súkromie orientovaných agentov.

### Výber optimalizačnej stratégie pre SLM agentov

**Prístup ku kvantizácii pre agentov**: Vyberte vhodné úrovne kvantizácie podľa požiadaviek na kvalitu agenta a hardvérových obmedzení. Zvážte Q4_0 pre maximálnu kompresiu v mobilných agentoch, Q5_1 pre vyváženú kvalitu a kompresiu v bežných agentoch a Q8_0 pre takmer originálnu kvalitu v kritických aplikáciách agentov.


**Výber rámca pre nasadenie agentov**: Vyberte optimalizačné rámce na základe cieľového hardvéru a požiadaviek agentov. Použite Llama.cpp pre nasadenie agentov optimalizovaných pre CPU, Apple MLX pre aplikácie agentov na Apple Silicon a ONNX pre multiplatformovú kompatibilitu agentov.

## Praktická konverzia a použitie SLM agentov

### Reálne scenáre nasadenia agentov

**Mobilné agentové aplikácie**: Formáty Q4_K vynikajú v aplikáciách agentov na smartfónoch s minimálnou pamäťovou stopou, zatiaľ čo Q8_0 poskytuje vyvážený výkon pre agentové systémy na tabletoch. Formáty Q5_K ponúkajú vynikajúcu kvalitu pre mobilných agentov zameraných na produktivitu.

**Agentové výpočty na desktopoch a edge zariadeniach**: Q5_K zabezpečuje optimálny výkon pre desktopové aplikácie agentov, Q8_0 poskytuje vysokokvalitný inference pre pracoviskové agentové prostredia a Q4_K umožňuje efektívne spracovanie na edge zariadeniach.

**Výskumní a experimentálne agenti**: Pokročilé formáty kvantizácie umožňujú skúmanie ultra-nízkej presnosti inference agentov pre akademický výskum a konceptové aplikácie agentov vyžadujúce extrémne obmedzenia zdrojov.

### Výkonnostné benchmarky SLM agentov

**Rýchlosť inference agentov**: Q4_K dosahuje najrýchlejšie časy odozvy agentov na mobilných CPU, Q5_K poskytuje vyvážený pomer rýchlosti a kvality pre všeobecné agentové aplikácie, Q8_0 ponúka najvyššiu kvalitu pre zložité agentové úlohy a experimentálne formáty zabezpečujú maximálny priepustnosť pre špecializovaný agentový hardvér.

**Pamäťové požiadavky agentov**: Úrovne kvantizácie pre agentov sa pohybujú od Q2_K (pod 500MB pre malé agentové modely) po Q8_0 (približne 50 % pôvodnej veľkosti), pričom experimentálne konfigurácie dosahujú maximálnu kompresiu pre agentové prostredia s obmedzenými zdrojmi.

## Výzvy a úvahy pre SLM agentov

### Výkonnostné kompromisy v agentových systémoch

Nasadenie SLM agentov vyžaduje starostlivé zváženie kompromisov medzi veľkosťou modelu, rýchlosťou odozvy agentov a kvalitou výstupu. Kým Q4_K ponúka výnimočnú rýchlosť a efektivitu pre mobilných agentov, Q8_0 zabezpečuje špičkovú kvalitu pre zložité agentové úlohy. Q5_K predstavuje strednú cestu vhodnú pre väčšinu všeobecných agentových aplikácií.

### Kompatibilita hardvéru pre SLM agentov

Rôzne edge zariadenia majú rozličné schopnosti pre nasadenie SLM agentov. Q4_K beží efektívne na základných procesoroch pre jednoduchých agentov, Q5_K vyžaduje stredné výpočtové zdroje pre vyvážený výkon agentov a Q8_0 profitujú z výkonného hardvéru pre pokročilé agentové schopnosti.

### Bezpečnosť a ochrana súkromia v agentových systémoch SLM

Kým SLM agenti umožňujú lokálne spracovanie pre zvýšenie súkromia, je potrebné zaviesť správne bezpečnostné opatrenia na ochranu agentových modelov a dát v edge prostrediach. To platí najmä pri nasadení agentových formátov s vysokou presnosťou v podnikových prostrediach alebo komprimovaných formátov v aplikáciách pracujúcich s citlivými údajmi.

## Budúce trendy vo vývoji SLM agentov

Prostredie pre SLM agentov sa neustále vyvíja vďaka pokrokom v kompresných technikách, optimalizačných metódach a stratégiách nasadenia na edge. Budúce inovácie zahŕňajú efektívnejšie kvantizačné algoritmy pre agentové modely, vylepšené metódy kompresie pre pracovné procesy agentov a lepšiu integráciu s hardvérovými akcelerátormi na edge pre spracovanie agentov.

**Predpovede trhu pre SLM agentov**: Podľa nedávnych štúdií môže automatizácia poháňaná agentmi odstrániť 40–60 % opakujúcich sa kognitívnych úloh v podnikových procesoch do roku 2027, pričom SLM vedú túto transformáciu vďaka svojej nákladovej efektívnosti a flexibilite nasadenia.

**Technologické trendy v SLM agentoch**:
- **Špecializované SLM agenti**: Doménovo špecifické modely trénované pre určité agentové úlohy a odvetvia
- **Výpočty na edge zariadeniach**: Vylepšené schopnosti agentov priamo na zariadeniach so zlepšeným súkromím a zníženou latenciou
- **Orchestrácia agentov**: Lepšia koordinácia medzi viacerými SLM agentmi s dynamickým smerovaním a vyvažovaním záťaže
- **Demokratizácia**: Flexibilita SLM umožňuje širšiu účasť na vývoji agentov v rámci organizácií

## Začiatky so SLM agentmi

### Krok 1: Nastavenie prostredia Microsoft Agent Framework

**Inštalácia závislostí**:
```bash
# Nainštalujte Microsoft Agent Framework
pip install microsoft-agent-framework

# Nainštalujte Foundry Local SDK pre nasadenie na okraji
pip install foundry-local-sdk

# Nainštalujte ďalšie závislosti pre agentov na okraji
pip install openai asyncio
```

**Inicializácia Foundry Local**:
```bash
# Spustiť službu Foundry Local
foundry service start

# Načítať predvolený model pre vývoj agenta
foundry model run phi-4-mini
```

### Krok 2: Výber vášho SLM pre agentové aplikácie
Populárne možnosti pre Microsoft Agent Framework:
- **Microsoft Phi-4 Mini (3.8B)**: Výborný pre všeobecné agentové úlohy s vyváženým výkonom
- **Qwen2.5-0.5B (0.5B)**: Ultraefektívny pre jednoduchých agentov na smerovanie a klasifikáciu
- **Qwen2.5-Coder-0.5B (0.5B)**: Špecializovaný pre agentové úlohy súvisiace s kódom
- **Phi-4 (7B)**: Pokročilé uvažovanie pre zložité edge scenáre, keď to dovoľujú zdroje

### Krok 3: Vytvorte svojho prvého agenta s Microsoft Agent Framework

**Základné nastavenie agenta**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# Inicializujte lokálne pripojenie Foundry
foundry = FoundryLocalManager("phi-4-mini")

# Vytvorte konfiguráciu agenta
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# Vytvorte a nakonfigurujte agenta
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# Definujte jednoduchý nástroj
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# Otestujte agenta
response = agent.chat("What time is it?")
print(response)
```

### Krok 4: Definujte rozsah a požiadavky agenta
Začnite s cieľovými a jasne definovanými agentovými aplikáciami pomocou Microsoft Agent Framework:
- **Agent s jednou doménou**: zákaznícky servis ALEBO plánovanie ALEBO výskum
- **Jasné ciele agentov**: konkrétne, merateľné cieľe výkonu agenta
- **Obmedzená integrácia nástrojov**: max. 3-5 nástrojov pre počiatočné nasadenie agenta
- **Definované hranice agenta**: jasné eskalačné cesty pre zložité scenáre
- **Edge-first dizajn**: uprednostnite offline funkčnosť a lokálne spracovanie

### Krok 5: Implementujte edge nasadenie s Microsoft Agent Framework

**Konfigurácia zdrojov**:
```python
from microsoft_agent_framework import ResourceConfig

# Konfigurovať pre nasadenie na okraji
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**Nasadenie bezpečnostných opatrení pre agentov na edge**:
- **Lokálna validácia vstupov**: kontrola požiadaviek bez závislosti od cloudu
- **Offline filtrovanie výstupov**: zabezpečiť, že odpovede spĺňajú lokálne kvalitatívne štandardy
- **Kontroly bezpečnosti na edge**: implementácia bezpečnosti bez nutnosti internetového pripojenia
- **Lokálny monitoring**: sledovanie výkonu a označovanie problémov pomocou telemetrie na edge

### Krok 6: Meranie a optimalizácia výkonu agentov na edge
- **Miera dokončenia agentových úloh**: sledovanie úspešnosti v offline scenároch
- **Časy odozvy agentov**: zaistenie odozvy pod jednu sekundu pri edge nasadení
- **Využitie zdrojov**: sledovanie pamäte, CPU a spotreby batérie na edge zariadeniach
- **Nákladová efektívnosť**: porovnanie nákladov nasadenia na edge s cloudovými alternatívami
- **Spoľahlivosť offline**: meranie výkonu agentov počas výpadkov siete

## Kľúčové poznatky pre implementáciu SLM agentov

1. **SLM sú postačujúce pre agentov**: pre väčšinu agentových úloh malé modely dosahujú rovnaké výsledky ako veľké, pričom ponúkajú významné výhody
2. **Nákladová efektívnosť agentov**: prevádzka SLM agentov je 10-30x lacnejšia, čo ich robí ekonomicky životaschopnými pre široké nasadenie
3. **Špecializácia funguje pre agentov**: jemne doladené SLM často prevyšujú všeobecné LLM v konkrétnych agentových aplikáciách
4. **Hybridná agentová architektúra**: použite SLM pre rutinné agentové úlohy a LLM pre zložité uvažovanie, keď je to potrebné
5. **Microsoft Agent Framework umožňuje výrobnú prevádzku**: poskytuje podnikové nástroje na tvorbu, nasadenie a správu agentov na edge
6. **Princípy edge-first dizajnu**: offline schopní agenti s lokálnym spracovaním zaisťujú súkromie a spoľahlivosť
7. **Integrácia Foundry Local**: bezproblémové prepojenie medzi Microsoft Agent Framework a lokálnym inference modelov
8. **Budúcnosť sú SLM agenti**: malé jazykové modely s výrobnými rámcami sú budúcnosťou agentnej AI, umožňujúc demokratizované a efektívne nasadenie agentov

## Referencie a ďalšie čítanie

### Kľúčové výskumné práce a publikácie

#### AI agenti a agentové systémy
- **"Language Agents as Optimizable Graphs"** (2024) - Základný výskum architektúry agentov a optimalizácie
  - Autori: Wenyue Hua, Lishan Yang, a kol.
  - Odkaz: https://arxiv.org/abs/2402.16823
  - Kľúčové poznatky: Dizajn agentov založený na grafoch a stratégie optimalizácie

- **"The Rise and Potential of Large Language Model Based Agents"** (2023)
  - Autori: Zhiheng Xi, Wenxiang Chen, a kol.
  - Odkaz: https://arxiv.org/abs/2309.07864
  - Kľúčové poznatky: Komplexný prehľad schopností a aplikácií agentov založených na LLM

- **"Cognitive Architectures for Language Agents"** (2024)
  - Autori: Theodore Sumers, Shunyu Yao, a kol.
  - Odkaz: https://arxiv.org/abs/2309.02427
  - Kľúčové poznatky: Kognitívne rámce pre dizajn inteligentných agentov

#### Malé jazykové modely a optimalizácia
- **"Phi-3 Technical Report: A Highly Capable Language Model Locally on Your Phone"** (2024)
  - Autori: Výskumný tím Microsoft
  - Odkaz: https://arxiv.org/abs/2404.14219
  - Kľúčové poznatky: Princípy dizajnu SLM a stratégie mobilného nasadenia

- **"Qwen2.5 Technical Report"** (2024)
  - Autori: Alibaba Cloud tím
  - Odkaz: https://arxiv.org/abs/2407.10671
  - Kľúčové poznatky: Pokročilé techniky tréningu SLM a optimalizácia výkonu

- **"TinyLlama: An Open-Source Small Language Model"** (2024)
  - Autori: Peiyuan Zhang, Guangtao Zeng, a kol.
  - Odkaz: https://arxiv.org/abs/2401.02385
  - Kľúčové poznatky: Ultra-kompaktný dizajn modelu a efektívnosť tréningu

### Oficiálna dokumentácia a rámce

#### Microsoft Agent Framework
- **Oficiálna dokumentácia**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHub repozitár**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **Hlavný repozitár**: https://github.com/microsoft/foundry-local
- **Dokumentácia**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **Hlavný repozitár**: https://github.com/vllm-project/vllm
- **Dokumentácia**: https://docs.vllm.ai/


#### Ollama
- **Oficiálna stránka**: https://ollama.ai/
- **GitHub repozitár**: https://github.com/ollama/ollama

### Rámce pre optimalizáciu modelov

#### Llama.cpp
- **Repozitár**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **Dokumentácia**: https://microsoft.github.io/Olive/
- **GitHub repozitár**: https://github.com/microsoft/Olive

#### OpenVINO
- **Oficiálna stránka**: https://docs.openvino.ai/

#### Apple MLX
- **Repozitár**: https://github.com/ml-explore/mlx

### Priemyselné správy a analýzy trhu

#### Výskum trhu AI agentov
- **"The State of AI Agents 2025"** - McKinsey Global Institute
  - Odkaz: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - Kľúčové poznatky: Trendy na trhu a vzory adopcie v podnikoch

#### Technické benchmarky

- **"Edge AI Inference Benchmarks"** - MLPerf
  - Odkaz: https://mlcommons.org/en/inference-edge/
  - Kľúčové poznatky: Štandardizované výkonové metriky pre nasadenie na edge

### Štandardy a špecifikácie

#### Formáty a štandardy modelov
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - Multiplatformový formát modelov pre interoperabilitu
- **Špecifikácia GGUF**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - Kvantizovaný formát modelov pre CPU inference
- **Špecifikácia OpenAI API**: https://platform.openai.com/docs/api-reference
  - Štandardný API formát pre integráciu jazykových modelov

#### Bezpečnosť a súlad
- **NIST AI Risk Management Framework**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - AI Systémy**: Rámec pre bezpečné AI systémy
- **IEEE štandardy pre AI**: https://standards.ieee.org/industry-connections/ai/

Prechod na agentov poháňaných SLM predstavuje zásadnú zmenu v prístupe k nasadeniu AI. Microsoft Agent Framework, v kombinácii s lokálnymi platformami a efektívnymi malými jazykovými modelmi, poskytuje komplexné riešenie pre vytváranie výrobných agentov, ktorí efektívne fungujú v edge prostrediach. Zameraním sa na efektivitu, špecializáciu a praktickú použiteľnosť tento technologický stack sprístupňuje AI agentov, robí ich cenovo dostupnejšími a efektívnejšími pre reálne aplikácie naprieč všetkými odvetviami a edge computing prostrediami.

Ako postupujeme do roku 2025, kombinácia čoraz schopnejších malých modelov, sofistikovaných agentových rámcov ako Microsoft Agent Framework a robustných platforiem pre edge nasadenie odomkne nové možnosti pre autonómne systémy, ktoré môžu efektívne fungovať na edge zariadeniach pri zachovaní súkromia, znížení nákladov a poskytovaní výnimočných používateľských skúseností.

**Ďalšie kroky pre implementáciu**:
1. **Preskúmajte volanie funkcií**: Naučte sa, ako SLM zvládajú integráciu nástrojov a štruktúrované výstupy
2. **Ovládnite Model Context Protocol (MCP)**: Porozumejte pokročilým vzorom komunikácie agentov
3. **Budujte produkčné agenti**: Použite Microsoft Agent Framework pre podnikové nasadenia
4. **Optimalizujte pre edge**: Aplikujte pokročilé optimalizačné techniky pre prostredia s obmedzenými zdrojmi


## ➡️ Čo bude ďalej

- [02: Volanie funkcií v malých jazykových modeloch (SLMs)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Vyhlásenie o zodpovednosti**:
Tento dokument bol preložený pomocou AI prekladateľskej služby [Co-op Translator](https://github.com/Azure/co-op-translator). Hoci sa snažíme o presnosť, vezmite prosím na vedomie, že automatické preklady môžu obsahovať chyby alebo nepresnosti. Pôvodný dokument v jeho natívnom jazyku by mal byť považovaný za autoritatívny zdroj. Pre kritické informácie sa odporúča profesionálny ľudský preklad. Nie sme zodpovední za žiadne nedorozumenia alebo nesprávne interpretácie vyplývajúce z použitia tohto prekladu.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->