# AI Agenti a Malé Jazykové Modely: Komplexní průvodce

## Úvod

V tomto tutoriálu prozkoumáme AI agenty a malé jazykové modely (SLM) a jejich pokročilé implementační strategie pro edge computing prostředí. Pokryjeme základní koncepty agentní AI, optimalizační techniky SLM, praktické nasazovací strategie pro zařízení s omezenými zdroji a Microsoft Agent Framework pro tvorbu produkčně připravených agentních systémů.

Oblast umělé inteligence zažívá paradigmatický posun v roce 2025. Zatímco rok 2023 byl rokem chatbotů a rok 2024 přinesl boom v asistentech copilotech, rok 2025 patří AI agentům — inteligentním systémům, které myslí, uvažují, plánují, používají nástroje a vykonávají úkoly s minimálním lidským zásahem, poháněné stále více efektivními malými jazykovými modely. Microsoft Agent Framework se stává předním řešením pro stavbu těchto inteligentních systémů s offline edge schopnostmi.

## Výukové cíle

Na konci tohoto tutoriálu budete schopni:

- 🤖 Porozumět základním konceptům AI agentů a agentních systémů
- 🔬 Identifikovat výhody malých jazykových modelů oproti velkým jazykovým modelům v agentních aplikacích
- 🚀 Naučit se pokročilé strategie nasazení SLM pro edge computing prostředí
- 📱 Implementovat praktické agenty poháněné SLM pro aplikace v reálném světě
- 🏗️ Vytvářet produkčně připravené agenty pomocí Microsoft Agent Framework
- 🌐 Nasadit offline edge-based agenty s lokální integrací LLM a SLM
- 🔧 Integrovat Microsoft Agent Framework s Foundry Local pro edge nasazení

## Pochopení AI Agentů: Základy a klasifikace

### Definice a základní koncepty

Umělý inteligentní (AI) agent je systém nebo program schopný autonomně vykonávat úkoly za uživatele nebo jiný systém tím, že navrhuje svůj pracovní postup a využívá dostupné nástroje. Na rozdíl od tradiční AI, která pouze odpovídá na vaše otázky, agent může jednat nezávisle za účelem dosažení cílů.

### Rámec klasifikace agentů

Pochopení hranic agentů pomáhá vybrat vhodné typy agentů pro různé scénáře výpočetní techniky:

- **🔬 Jednoduchí reflexivní agenti**: Systémy založené na pravidlech reagující na okamžité vjemy (termostaty, základní automatizace)
- **📱 Agentní model založený na vnitřním stavu**: Systémy udržující interní stav a paměť (robotické vysavače, navigační systémy)
- **⚖️ Agentní cílově orientovaní**: Systémy plánující a vykonávající sekvence k dosažení cílů (plánovači tras, plánovači úkolů)
- **🧠 Učící se agenti**: Adaptivní systémy zlepšující výkon v průběhu času (doporučovací systémy, personalizovaní asistenti)

### Klíčové výhody AI agentů

AI agenti nabízejí několik zásadních výhod, díky nimž jsou ideální pro edge computing aplikace:

**Provozní autonomie**: Agenti poskytují nezávislé vykonávání úkolů bez neustálého lidského dohledu, což je činí ideálními pro aplikace v reálném čase. Vyžadují minimální dohled, přitom udržují adaptivní chování, což umožňuje nasazení na zařízeních s omezenými zdroji a snižuje provozní zatížení.

**Flexibilita nasazení**: Tyto systémy umožňují AI schopnosti přímo na zařízení bez nutnosti internetového připojení, zvyšují soukromí a bezpečnost díky místnímu zpracování, mohou být přizpůsobeny pro specifické domény a jsou vhodné pro různá edge computing prostředí.

**Nákladová efektivita**: Agenti nabízejí nákladově efektivní nasazení ve srovnání s cloudovými řešeními, s nižšími provozními náklady a menšími nároky na šířku pásma v edge aplikacích.

## Pokročilé Strategie Malých Jazykových Modelů

### Základy SLM (Small Language Model)

Malý jazykový model (SLM) je jazykový model, který se vejde na běžné spotřebitelské elektronické zařízení a dokáže vykonávat inference s latencí dostatečně nízkou, aby byl praktický při obsluze agentních požadavků jednoho uživatele. V praktických termínech jsou SLM obvykle modely s méně než 10 miliardami parametrů.

**Funkce pro objevování formátu**: SLM nabízejí pokročilou podporu různých úrovní kvantizace, multiplatformní kompatibilitu, optimalizaci výkonu v reálném čase a schopnosti edge nasazení. Uživatelé mohou získat vyšší soukromí díky lokálnímu zpracování a podpoře WebGPU pro nasazení v prohlížeči.

**Sady úrovní kvantizace**: Oblíbené formáty SLM zahrnují Q4_K_M pro vyváženou kompresi v mobilních aplikacích, sérii Q5_K_S pro kvalitní edge nasazení, Q8_0 pro téměř originální přesnost na výkonných edge zařízeních a experimentální formáty jako Q2_K pro scénáře s extrémně nízkými zdroji.

### GGUF (General GGML Universal Format) pro nasazení SLM

GGUF slouží jako primární formát pro nasazení kvantovaných SLM na CPU a edge zařízeních, specificky optimalizovaný pro agentní aplikace:

**Funkce optimalizované pro agenty**: Formát poskytuje komplexní zdroje pro konverzi a nasazení SLM s rozšířenou podporou volání nástrojů, strukturované generování výstupu a vícetahové konverzace. Multiplatformní kompatibilita zajišťuje konzistentní chování agentů na různých edge zařízeních.

**Optimalizace výkonu**: GGUF umožňuje efektivní využití paměti pro agentní pracovní toky, podporuje dynamické načítání modelů pro multiagentní systémy a poskytuje optimalizovaný inference pro interakce agentů v reálném čase.

### Edge-optimalizované rámce SLM

#### Optimalizace Llama.cpp pro agenty

Llama.cpp nabízí špičkové kvantizační techniky specificky optimalizované pro agentní nasazení SLM:

**Agent-specifická kvantizace**: Rámec podporuje Q4_0 (optimální pro mobilní nasazení agentů s 75% redukcí velikosti), Q5_1 (vyvážená kvalita a komprese pro edge inferenční agenty) a Q8_0 (téměř originální kvalita pro produkční agentní systémy). Pokročilé formáty umožňují ultra komprimované agenty pro extrémní edge scénáře.

**Výhody implementace**: CPU-optimalizované inference s SIMD akcelerací poskytují paměťově efektivní výkon agentů. Multiplatformní kompatibilita napříč architekturami x86, ARM a Apple Silicon umožňuje univerzální nasazovací schopnosti agentů.

#### Apple MLX Framework pro SLM agenty

Apple MLX poskytuje nativní optimalizaci navrženou přímo pro agenty poháněné SLM na zařízeních Apple Silicon:

**Optimalizace pro Apple Silicon agenty**: Rámec využívá jednotnou paměťovou architekturu s integrací Metal Performance Shaders, automatickou smíšenou přesnost pro inference agentů a optimalizovanou propustnost paměti pro multiagentní systémy. Agenti SLM vykazují výjimečný výkon na čipech série M.

**Vývojové funkce**: Podpora API v Pythonu a Swiftu s optimalizacemi specifickými pro agenty, automatická diferenciace pro učení agentů a bezproblémová integrace s Apple vývojovými nástroji poskytují komplexní vývojové prostředí agentů.

#### ONNX Runtime pro multiplatformní SLM agenty

ONNX Runtime poskytuje univerzální inference engine umožňující agenty SLM spouštět konzistentně na různých hardwarových platformách a operačních systémech:

**Univerzální nasazení**: ONNX Runtime zajišťuje konzistentní chování SLM agentů napříč platformami Windows, Linux, macOS, iOS a Android. Tato multiplatformní kompatibilita umožňuje vývojářům napsat jednou a nasadit kdekoliv, výrazně snižující vývojové a údržbové náklady pro multiplatformní aplikace.

**Možnosti hardwarové akcelerace**: Rámec poskytuje optimalizované execution providery pro různé hardwarové konfigurace včetně CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm) a specializovaných akcelerátorů (Intel VPU, Qualcomm NPU). Agenti SLM mohou automaticky využívat nejlepší dostupný hardware bez změn kódu.

**Produkčně připravené funkce**: ONNX Runtime nabízí podnikové funkce nezbytné pro produkční nasazení agentů, včetně optimalizace grafu pro rychlejší inference, správy paměti pro prostředí s omezenými zdroji a komplexních nástrojů pro profilování výkonu. Rámec podporuje API v Pythonu i C++ pro flexibilní integraci.


## SLM vs LLM v agentních systémech: pokročilé srovnání

### Výhody SLM v agentních aplikacích

**Provozní efektivita**: SLM poskytují 10-30× snížení nákladů oproti LLM pro agentní úkoly, což umožňuje reálné časové agentní odpovědi ve velkém měřítku. Nabízejí rychlejší inference díky nižší výpočetní složitosti, což je činí ideálními pro interaktivní agentní aplikace.

**Schopnosti edge nasazení**: SLM umožňují lokální výkon agentů bez závislosti na internetu, zvyšují soukromí díky místnímu zpracování agentů a umožňují přizpůsobení pro doménově specifické agentní aplikace vhodné pro různá edge computing prostředí.

**Agentně specifická optimalizace**: SLM vynikají ve volání nástrojů, generování strukturovaných výstupů a rutinních rozhodovacích pracovních postupech, které tvoří 70-80 % typických agentních úloh.

### Kdy použít SLM vs LLM v agentních systémech

**Ideální pro SLM**:
- **Opakující se agentní úkoly**: Zadávání dat, vyplňování formulářů, rutinní volání API
- **Integrace nástrojů**: Dotazy do databází, operace se soubory, systémové interakce
- **Strukturované pracovní postupy**: Dodržování předdefinovaných agentních procesů
- **Doménově specifické agenti**: Zákaznický servis, plánování, základní analýzy
- **Lokální zpracování**: Agentní operace citlivé na soukromí

**Lépe pro LLM**:
- **Komplexní uvažování**: Řešení nových problémů, strategické plánování
- **Otevřené konverzace**: Obecné chaty, kreativní diskuse
- **Úkoly vyžadující rozsáhlé znalosti**: Výzkum vyžadující rozsáhlé obecné znalosti
- **Nové situace**: Řízení zcela nových agentních scénářů

### Hybridní agentní architektura

Optimální přístup kombinuje SLM a LLM v heterogenních agentních systémech:

**Chytrá orchestraci agentů**:
1. **SLM jako primární**: Lokálně zvládnout 70-80 % rutinních agentních úloh
2. **LLM v případě potřeby**: Směrovat komplexní dotazy na cloudové větší modely
3. **Specializované SLM**: Různé malé modely pro různé agentní domény
4. **Optimalizace nákladů**: Minimalizovat drahé volání LLM pomocí inteligentního směrování

## Produkční strategie nasazení SLM agentů

### Foundry Local: Podnikové edge AI runtime

Foundry Local (https://github.com/microsoft/foundry-local) slouží jako hlavní řešení Microsoftu pro nasazení malých jazykových modelů v produkčních edge prostředích. Poskytuje kompletní runtime prostředí speciálně navržené pro agenty poháněné SLM s funkcemi na podnikové úrovni a bezproblémovou integrací.

**Jádrová architektura a funkce**:
- **OpenAI-kompatibilní API**: Plná kompatibilita se SDK OpenAI a integracemi Agent Framework
- **Automatická hardwarová optimalizace**: Inteligentní výběr variant modelů na základě dostupného hardwaru (CUDA GPU, Qualcomm NPU, CPU)
- **Správa modelů**: Automatizované stahování, kešování a správa životního cyklu SLM modelů
- **Objevování služeb**: Bezkoncepční detekce služeb pro agentní rámce
- **Optimalizace zdrojů**: Inteligentní správa paměti a energetická efektivita pro edge nasazení

#### Instalace a nastavení

**Multiplatformní instalace**:
```bash
# Windows (doporučeno)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# Linux (ruční instalace)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**Rychlý start pro vývoj agentů**:
```bash
# Spusťte službu s automatickým načítáním modelu
foundry model run phi-4-mini

# Ověřte stav služby a koncový bod
foundry service status

# Vypsat dostupné modely
foundry model ls

# Otestujte API koncový bod
curl http://localhost:<port>/v1/models
```

#### Integrace Agent Framework

**Integrace Foundry Local SDK**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Inicializujte Foundry Local s automatickou správou služeb
manager = FoundryLocalManager("phi-4-mini")

# Nakonfigurujte klienta OpenAI pro lokální inferenci
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # Automaticky generováno pro lokální použití
)

# Vytvořte agenta s backendem Foundry Local
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**Automatický výběr modelů a hardwarová optimalizace**:
```python
# Foundry Local automaticky vybírá optimální variantu modelu
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, ultra rychlý
    "general_conversation": "phi-4-mini",       # 2.4GB, vyvážený
    "complex_reasoning": "phi-4",               # 7GB, vysoká kapacita
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, optimalizovaný pro kód
}

# Foundry Local zajišťuje detekci hardwaru a kvantizaci
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # Příklady výstupů:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (optimalizováno pro CPU)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (zrychleno GPU)
```

#### Vzory produkčního nasazení

**Produkční nastavení jednoho agenta**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # Přidat výrobní nástroje
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# Výrobní použití
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**Orchestrace produkce vícero agentů**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # Lehký směrovací agent
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # Agent zákaznického servisu
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # Agent technické podpory
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # Přesměrovat požadavek na příslušného agenta
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # Určit cílového agenta na základě směrování
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # Zpracovat se specializovaným agentem
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# Nasazení do produkce
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### Podnikové funkce a monitorování

**Monitorování stavu a dohledatelnost**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# Nastavení monitorování výroby
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# Nepřetržité monitorování
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # Sbírat metriky každých 30 sekund
```

**Správa zdrojů a automatické škálování**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # Zmenšit, pokud je využití paměti vysoké
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # Zvýšit, pokud je poptávka vysoká a zdroje to dovolují
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 minut nečinnosti
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### Pokročilá konfigurace a optimalizace

**Vlastní konfigurace modelů**:
```python
# Pokročilé místní nastavení Foundry pro produkci
from foundry_local import FoundryLocalManager, ModelConfig

# Vlastní konfigurace pro specifické případy použití
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # Specifická úroveň kvantizace
    context_length=4096,    # Rozšířený kontext pro složité agenty
    batch_size=1,          # Optimalizováno pro jednoho uživatele agenty
    threads=4,             # Optimalizace vláken CPU
    gpu_layers=32,         # Vrstev GPU akcelerace
    memory_lock=True,      # Zamknout model v paměti pro konzistentní výkon
    numa=True              # NUMA optimalizace pro vícesocketové systémy
)

manager = FoundryLocalManager(config=config)
```

**Kontrolní seznam pro produkční nasazení**:

✅ **Konfigurace služby**:
- Nakonfigurujte vhodné aliasy modelů pro konkrétní použití
- Nastavte limity zdrojů a prahy monitorování
- Povolit kontroly zdravotního stavu a sběr metrik
- Nastavit automatický restart a failover

✅ **Nastavení zabezpečení**:
- Povolit přístup k API pouze lokálně (bez externí expozice)
- Nastavit správu API klíčů
- Nastavit auditní záznamy interakcí agentů
- Implementovat omezení rychlosti pro produkční použití

✅ **Optimalizace výkonu**:
- Testovat výkon modelů při očekávané zátěži
- Nastavit vhodné úrovně kvantizace
- Nastavit strategie kešování a zahřívání modelu
- Monitorovat použití paměti a CPU

✅ **Testování integrace**:
- Testovat integraci agentního rámce
- Ověřit schopnosti offline provozu
- Testovat scénáře failover a zotavení
- Validovat end-to-end agentní pracovní toky

### Ollama: Zjednodušené nasazení SLM agentů

### Ollama: Komunitně orientované nasazení SLM agentů

Ollama nabízí komunitně řízený přístup k nasazení SLM agentů s důrazem na jednoduchost, rozsáhlý ekosystém modelů a přívětivé pracovní postupy pro vývojáře. Zatímco Foundry Local se zaměřuje na podnikové funkce, Ollama vyniká rychlým prototypováním, přístupem ke komunitním modelům a zjednodušenými scénáři nasazení.

**Jádrová architektura a funkce**:
- **OpenAI-kompatibilní API**: Plná kompatibilita REST API pro bezproblémovou integraci agentního rámce
- **Široká knihovna modelů**: Přístup ke stovkám komunitních i oficiálních modelů
- **Jednoduchá správa modelů**: Instalace a přepínání modelů jedním příkazem
- **Podpora multiplatformnosti**: Nativní podpora pro Windows, macOS a Linux
- **Optimalizace zdrojů**: Automatická kvantizace a detekce hardwaru

#### Instalace a nastavení

**Multiplatformní instalace**:
```bash
# Windows
winget install Ollama.Ollama

# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Nasazení Dockeru
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**Rychlý start pro vývoj agentů**:
```bash
# Spustit službu Ollama
ollama serve

# Stáhnout a spustit modely pro vývoj agenta
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# Otestovat dostupnost modelu
ollama list

# Otestovat API koncový bod
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### Integrace Agent Framework

**Ollama s Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama nevyžaduje skutečný API klíč
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# Inicializujte Ollama pro vývoj agenta
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# Nakonfigurujte agenta s backendem Ollama
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Nastavení multi-modelových agentů s Ollama**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # Použij model Pull, pokud není k dispozici
            self._pull_model(model_name)
            
            # Vytvoř klienta OpenAI pro každý model
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# Příklad použití
manager = OllamaMultiModelManager()

# Získej vhodné agenty pro různé úkoly
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### Vzory produkčního nasazení

**Produkční služba s Ollama**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # Zajistěte dostupnost modelu
                self._ensure_model_ready(model_name)
                
                # Vytvořit produkční agenta
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # Přidat produkční nástroje podle typu agenta
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # Zkontrolujte, zda model existuje
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # Simulovat vyhledávání v databázi
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # Aktualizovat metriky
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # Ve výrobě by toto zapisovalo do řádného systému protokolování
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # Zkontrolujte stav služby Ollama
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # Zkontrolujte dostupnost modelu
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# Příklad produkčního nasazení
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# Zpracovat požadavky
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### Podnikové funkce a monitorování

**Monitorování a dohledatelnost Ollama**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # Zkontrolovat stav služby
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # Získat informace o modelu
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # Krátký souhrn
                    }
                
                # Otestovat výkon inferencí
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # Uchovat pouze posledních 100 záznamů metrik
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # Odhad tokenů (hrubý odhad)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # Získat časové informace, pokud jsou k dispozici
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # Převést nanosekundy na sekundy
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # Zkontrolovat dobu odezvy
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # Zkontrolovat stav služby
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # Vypočítat průměry
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# Nastavení produkčního monitoringu
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # Logovat souhrn výkonu každých 10 minut
            if int(time.time()) % 600 == 0:  # Každých 10 minut
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # Kontrolovat každých 30 sekund

# Spustit monitoring
# asyncio.create_task(monitoring_loop())
```

#### Pokročilá konfigurace a optimalizace

**Vlastní správa modelů s Ollama**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # Lehká modely pro rychlé reakce
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # Vyvážené modely pro běžné použití
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # Specializované modely pro konkrétní úkoly
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # Vysoce výkonné modely
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # Model na vyžádání
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # Toto by obvykle kontrolovalo skutečné specifikace hardwaru
        # Pro demo účely budeme simulovat detekci hardwaru
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# Nastavení výrobního modelu
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Kontrolní seznam produkčního nasazení pro Ollama**:

✅ **Konfigurace služby**:
- Nainstalovat službu Ollama s vhodnou systémovou integrací
- Nakonfigurovat modely pro specifické agentní případy použití
- Nastavit správné startovní skripty a správu služby
- Testovat načítání modelů a dostupnost API

✅ **Správa modelů**:
- Stáhnout požadované modely a ověřit integritu
- Nastavit postupy aktualizace a rotace modelů
- Nakonfigurovat kešování modelů a optimalizaci ukládání
- Testovat výkon modelů při očekávané zátěži

✅ **Nastavení zabezpečení**:
- Nakonfigurovat pravidla firewallu pro lokální přístup
- Nastavit kontroly přístupu k API a omezení rychlosti
- Implementovat auditní záznamy interakcí agentů
- Konfigurovat zabezpečené ukládání modelů a přístup

✅ **Optimalizace výkonu**:
- Provádět benchmark modelů pro očekávané případy použití
- Nastavit vhodnou hardwarovou akceleraci
- Nastavit strategie zahřívání a kešování modelů
- Monitorovat využití zdrojů a výkonové metriky

✅ **Testování integrace**:
- Testovat integraci Microsoft Agent Framework
- Ověřit schopnosti offline provozu

- Testovat scénáře přepnutí při selhání a zpracování chyb
- Ověřit end-to-end pracovní postupy agentů

**Porovnání s Foundry Local**:

| Funkce | Foundry Local | Ollama |
|---------|---------------|--------|
| **Cílový případ použití** | Podniková produkce | Vývoj & komunita |
| **Ekosystém modelů** | Microsoft-kurátorovaný | Rozsáhlá komunita |
| **Optimalizace hardwaru** | Automatická (CUDA/NPU/CPU) | Manuální konfigurace |
| **Funkce pro podniky** | Vestavěný monitoring, bezpečnost | Nástroje komunity |
| **Složitost nasazení** | Jednoduché (winget instalace) | Jednoduché (curl instalace) |
| **Kompatibilita API** | OpenAI + rozšíření | Standard OpenAI |
| **Podpora** | Oficiální Microsoft | Komunitní podpora |
| **Nejvhodnější pro** | Produkční agenty | Prototypování, výzkum |

**Kdy zvolit Ollama**:
- **Vývoj a prototypování**: Rychlé experimentování s různými modely
- **Modely komunity**: Přístup k nejnovějším modelům přispěným komunitou
- **Vzdělávací využití**: Výuka a učení se vývoji AI agentů
- **Výzkumné projekty**: Akademický výzkum vyžadující přístup k různorodým modelům
- **Vlastní modely**: Stavba a testování vlastních doladěných modelů

### VLLM: Výkonný inference engine pro SLM agenty

VLLM (inference velmi velkého jazykového modelu) poskytuje vysoce výkonný, paměťově efektivní inference engine speciálně optimalizovaný pro produkční nasazení SLM ve velkém měřítku. Zatímco Foundry Local se zaměřuje na snadné použití a Ollama zdůrazňuje komunitní modely, VLLM vyniká ve vysoce výkonných scénářích vyžadujících maximální propustnost a efektivní využití zdrojů.

**Základní architektura a funkce**:
- **PagedAttention**: Revoluční správa paměti pro efektivní výpočet pozornosti
- **Dynamické dávkování**: Inteligentní dávkování požadavků pro optimální propustnost
- **Optimalizace GPU**: Pokročilé CUDA jádra a podpora tensorového paralelismu
- **Kompatibilita s OpenAI**: Plná kompatibilita API pro bezproblémovou integraci
- **Spekulativní dekódování**: Pokročilé techniky zrychlení inference
- **Podpora kvantizace**: INT4, INT8 a FP16 kvantizace pro úsporu paměti

#### Instalace a nastavení

**Možnosti instalace**:
```bash
# Standardní instalace
pip install vllm

# S dalšími závislostmi pro agentní rámce
pip install vllm[agent] openai

# Nasazení pomocí Dockeru pro produkci
docker pull vllm/vllm-openai:latest

# Ze zdroje pro nejnovější funkce
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**Rychlý start pro vývoj agentů**:
```bash
# Spustit VLLM server s modelem SLM
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# Alternativa: Spustit s Qwen2.5 pro lehké agenty
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# Otestovat API endpoint
curl http://localhost:8000/v1/models

# Otestovat dokončení chatu
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### Integrace agentního frameworku

**VLLM s Microsoft Agent Framework**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # Snižte zaznamenávání pro agenty
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # Počkejte na spuštění serveru
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# Inicializujte VLLM pro vysoce výkonné agenty
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # Nakonfigurujte agenta s VLLM backendem
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM nevyžaduje API klíč
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**Nastavení víceagentního vysokopropustného režimu**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # Spusťte server v vlákně, aby nedocházelo k blokování
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # Vytvořit agenta
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # Vytvořit klientský pool pro vysoký průchod
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # 5 klientů na model pro paralelismus
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # Zkontrolovat, zda je preferovaný agent dostupný a nepřetížený
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # Max 10 současně na agenta
            return preferred_category
        
        # Náhradní varianta na nejméně zatíženého dostupného agenta
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # Výchozí náhradní varianta
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # Určit optimálního agenta
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # Vytvořit úlohu zpracování
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # Zpracovat všechny požadavky paralelně
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Naformátovat výsledky
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# Příklad použití s vysokým průtokem
throughput_manager = VLLMHighThroughputManager()

# Inicializovat všechny modely
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# Zpracovat dávkové požadavky
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### Produkční vzory nasazení

**Podniková VLLM produkční služba**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # Zahájit zpracování pracovníků
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # Maximálně 4 pracovníci
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # Přidat kvantizaci, pokud je určena
        if config.quantization:
            # Toto by bylo přidáno ke startovacímu příkazu správce
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # Vytvořit produkční agenta
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # Přidat produkční nástroje
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # Vyhledávání v produkční databázi
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # Přidat požadavek do fronty
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # Čekat na zpracování (s časovým limitem)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # Získat požadavek z fronty
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # Vybrat vhodného agenta
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # Zpracovat požadavek
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # Aktualizovat metriky
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # Toto je zjednodušené - v produkci byste správně sledovali futures
        await asyncio.sleep(0.1)  # Zástupný text
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # Jeden pracovník pro jednoduchost
        )
        server = uvicorn.Server(config)
        await server.serve()

# Příklad produkčního nasazení
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# Inicializovat a spustit produkční službu
# await production_service.initialize_production_environment()
# await production_service.start_production_server()
```

#### Podnikové funkce a monitoring

**Pokročilý monitoring výkonu VLLM**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # Inicializovat NVIDIA ML pro monitorování GPU
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # Vytvořit metriky chyb
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # Získat metriky specifické pro VLLM přes API
        vllm_stats = await self._get_vllm_stats(manager)
        
        # Získat systémové metriky
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # Získat metriky GPU, pokud jsou dostupné
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # Pro jednoduchost předpokládat první GPU
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # Testovat inference pro měření latence
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # Muselo by být zpřístupněno VLLM
                "active_requests": 1  # Aproximace
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # Analyzovat každou instanci
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # Doporučení pro vysokou latenci
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # Doporučení pro vysoké využití GPU
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # Doporučení pro nízké využití
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# Pokročilé nastavení monitorování
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # Přidat další manažery podle potřeby
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # Sbírat metriky
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # Uchovávat pouze posledních 1000 záznamů
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # Generovat doporučení každých 5 minut
            if len(monitoring.metrics_history) % 10 == 0:  # Každý 10. sběr (5 minut při sběru každých 30 s)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # Generovat výkonovou zprávu každou hodinu
            if len(monitoring.metrics_history) % 120 == 0:  # Každý 120. sběr (1 hodina)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # Sbírat metriky každých 30 sekund

# Spustit pokročilé monitorování
# asyncio.create_task(advanced_monitoring_loop())
```

#### Pokročilá konfigurace a optimalizace

**Šablony konfigurace pro produkční VLLM**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # Přidat volitelné parametry
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# Příklady použití
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**Kontrolní seznam produkčního nasazení VLLM**:

✅ **Optimalizace hardwaru**:
- Konfigurace tensorového paralelismu pro multi-GPU konfigurace
- Povolení kvantizace (AWQ/GPTQ) pro efektivitu paměti
- Nastavení optimálního využití paměti GPU (85-95 %)
- Konfigurace vhodných velikostí dávky pro propustnost

✅ **Ladění výkonu**:
- Povolit prefix caching pro opakované dotazy
- Konfigurace chunked prefill pro dlouhé sekvence
- Nastavení spekulativního dekódování pro rychlejší inference
- Optimalizace max_num_seqs podle hardwaru

✅ **Produkční funkce**:
- Nastavení sledování stavu a sběru metrik
- Konfigurace automatického restartu a přepnutí při selhání
- Implementace fronty požadavků a vyvažování zátěže
- Nastavení detailního logování a upozornění

✅ **Bezpečnost a spolehlivost**:
- Konfigurace pravidel firewallu a přístupových kontrol
- Nastavení limitování API a autentizace
- Implementace postupného vypnutí a úklidu
- Konfigurace zálohování a obnovy po havárii

✅ **Testování integrace**:
- Test integrace s Microsoft Agent Framework
- Validace scénářů vysokopropustnosti
- Testování postupů přepnutí a obnovení
- Benchmark výkonu pod zátěží

**Porovnání s ostatními řešeními**:

| Funkce | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **Cílový případ použití** | Výkonnostní produkce | Podnikově snadné použití | Vývoj & komunita |
| **Výkon** | Maximální propustnost | Vyvážený | Dobrý |
| **Efektivita paměti** | Optimalizace PagedAttention | Automatická optimalizace | Standardní |
| **Složitost nastavení** | Vysoká (mnoho parametrů) | Nízká (automatická) | Nízká (jednoduchá) |
| **Škálovatelnost** | Výborná (tensor/pipeline paralelismus) | Dobrá | Omezená |
| **Kvantizace** | Pokročilá (AWQ, GPTQ, FP8) | Automatická | Standard GGUF |
| **Podnikové funkce** | Vyžaduje vlastní implementaci | Vestavěné | Nástroje komunity |
| **Nejvhodnější pro** | Vysoce škálovatelné produkční agenty | Podnikové produkce | Vývoj |

**Kdy zvolit VLLM**:
- **Požadavky na vysokou propustnost**: Zpracování stovek požadavků za sekundu
- **Velká nasazení**: Multi-GPU, víceuzlová nasazení
- **Výkon kritický**: Odezvy pod sekundu ve velkém měřítku
- **Pokročilá optimalizace**: Potřeba vlastní kvantizace a dávkování
- **Efektivita zdrojů**: Maximální využití drahého GPU hardwaru


## Reálné aplikace SLM agentů

### SLM agenti zákaznické podpory
- **Schopnosti SLM**: Kontrola účtů, resetování hesel, kontrola stavu objednávek
- **Nákladové výhody**: 10x snížení nákladů na inference oproti LLM agentům
- **Výkon**: Rychlejší odezvy s konzistentní kvalitou pro rutinní dotazy

### SLM agenti pro obchodní procesy
- **Agent pro zpracování faktur**: Extrakce dat, validace informací, směrování ke schválení
- **Agent pro správu emailů**: Kategorizace, priorizace, automatické vytvoření odpovědí
- **Agent pro plánování**: Koordinace schůzek, správa kalendářů, odesílání připomenutí

### Osobní digitální asistenti SLM
- **Agent pro správu úkolů**: Efektivní vytváření, aktualizace, organizace seznamů úkolů
- **Agent pro sběr informací**: Výzkum témat, lokální shrnutí nálezů
- **Agent pro komunikaci**: Soukromé návrhy emailů, zpráv a příspěvků na sociální sítě

### Obchodní a finanční SLM agenti
- **Agent pro monitorování trhu**: Sledování cen, identifikace trendů v reálném čase
- **Agent pro generování zpráv**: Automatické tvorba denních/týdenních shrnutí
- **Agent pro hodnocení rizik**: Vyhodnocování portfoliových pozic pomocí lokálních dat

### SLM agenti pro zdravotní péči
- **Agent pro plánování pacientů**: Koordinace schůzek, odesílání automatických připomenutí
- **Agent pro dokumentaci**: Generování lékařských shrnutí a zpráv lokálně
- **Agent pro správu léků**: Sledování doplňování předpisů, kontrola interakcí soukromě

## Microsoft Agent Framework: Produkčně připravený vývoj agentů

### Přehled a architektura

Microsoft Agent Framework poskytuje komplexní, podnikové platformu pro vytváření, nasazení a správu AI agentů, kteří mohou fungovat jak v cloudových, tak offline edge prostředích. Framework je speciálně navržen pro bezproblémovou práci s malými jazykovými modely (SLM) a edge computing scénáři, což ho činí ideálním pro nasazení citlivá na soukromí a omezená zdroji.

**Jádrové komponenty frameworku**:
- **Runtime agenta**: Lehký běhový prostředí optimalizované pro edge zařízení
- **Systém integrace nástrojů**: Rozšiřitelná architektura pluginů pro připojení externích služeb a API
- **Správa stavu**: Přetrvávající paměť agenta a správa kontextu přes relace
- **Bezpečnostní vrstva**: Vestavěné bezpečnostní kontroly pro podnikovou implementaci
- **Orchestrace**: Koordinace více agentů a řízení workflow

### Klíčové funkce pro edge nasazení

**Offline-first architektura**: Microsoft Agent Framework je navržen s principy offline-first, umožňující agenty efektivně fungovat bez trvalého internetového připojení. To zahrnuje lokální inference modelu, kešované znalostní databáze, offline vykonávání nástrojů a elegantní degradaci, když cloudové služby nejsou dostupné.

**Optimalizace zdrojů**: Framework poskytuje inteligentní správu zdrojů s automatickou optimalizací paměti pro SLM, vyvažování zátěže CPU/GPU pro edge zařízení, adaptivní výběr modelů na základě dostupných zdrojů a energeticky úsporné vzory inference pro mobilní nasazení.

**Bezpečnost a soukromí**: Podnikové bezpečnostní funkce zahrnují lokální zpracování dat pro zachování soukromí, šifrované kanály komunikace agentů, přístupové kontroly založené na rolích pro schopnosti agentů a auditní záznamy pro potřeby souladu s předpisy.

### Integrace s Foundry Local

Microsoft Agent Framework se hladce integruje s Foundry Local, aby poskytl kompletní edge AI řešení:

**Automatické objevování modelů**: Framework automaticky detekuje a připojuje se k instancím Foundry Local, objevuje dostupné SLM modely a vybírá optimální modely na základě požadavků agentů a hardwarových možností.

**Dynamické načítání modelů**: Agent může dynamicky načítat různé SLM pro konkrétní úkoly, což umožňuje multi-modelové agentní systémy, kde různé modely zpracovávají různé typy požadavků, a automatické přepínání mezi modely podle dostupnosti a výkonu.

**Optimalizace výkonu**: Integrované mezipaměťní mechanismy zkracují dobu načítání modelů, pooling připojení optimalizuje volání API na Foundry Local a inteligentní dávkování zlepšuje propustnost pro více požadavků agentů.

### Vytváření agentů s Microsoft Agent Framework

#### Definice a konfigurace agenta

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# Nakonfigurujte agenta s integrací Foundry Local
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# Inicializujte připojení Foundry Local
foundry = FoundryLocalManager("phi-4-mini")

# Vytvořte instanci agenta
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### Integrace nástrojů pro edge scénáře

```python
# Definujte nástroje pro offline provoz
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # Dotaz do lokální databáze - funguje offline
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # Vytvoření lokálního tiketu s synchronizací při online
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # Lokální plánování s integrací kalendáře
    return local_calendar.schedule(customer_id, preferred_time)
```

#### Orchestrace více agentů

```python
from microsoft_agent_framework import AgentOrchestrator

# Vytvořit specializované agenty pro různé oblasti
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # Lehký pro jednoduché úkoly
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # Schopnější pro složité problémy
        specialized_for="technical_support"
    )
)

# Orchestrace více agentů
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# Směrovat požadavky na základě záměru
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### Pokročilé vzory nasazení na edge

#### Hierarchická architektura agentů

**Místní agentní clustery**: Nasadit více specializovaných SLM agentů na edge zařízeních, každý optimalizovaný pro specifické úkoly. Použít lehké modely jako Qwen2.5-0.5B pro jednoduché směrování a plánování, střední modely jako Phi-4-Mini pro zákaznickou podporu a dokumentaci a větší modely pro složité úlohy, když to zdroje dovolí.

**Koordinace edge-cloud**: Implementovat inteligentní vzory eskalace, kdy místní agenti řeší rutinní úkoly, cloud agenti poskytují složitější rozhodování pokud je dostupné připojení, a bezproblémové předávání mezi edge a cloud zpracováním pro zachování kontinuity.

#### Konfigurace nasazení

**Nasazení na jedno zařízení**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**Distribuované edge nasazení**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### Optimalizace výkonu pro edge agenty

#### Strategie výběru modelů

**Přiřazení modelu podle úkolu**: Microsoft Agent Framework umožňuje inteligentní výběr modelu na základě složitosti a požadavků úkolu:

- **Jednoduché úkoly** (Q&A, směrování): Qwen2.5-0.5B (500MB, <100ms odezva)
- **Střední úkoly** (zákaznická služba, plánování): Phi-4-Mini (2.4GB, 200-500ms odezva)
- **Složitější úkoly** (technická analýza, plánování): Phi-4 (7GB, 1-3s odezva pokud zdroje dovolí)

**Dynamické přepínání modelů**: Agent může přepínat mezi modely na základě aktuální zátěže systému, hodnocení složitosti úkolu, priorit uživatele a dostupných hardwarových zdrojů.

#### Správa paměti a zdrojů

```python
# Nakonfigurujte omezení prostředků pro nasazení na okraji
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### Vzory integrace pro podniky

#### Bezpečnost a soulad s předpisy

**Lokální zpracování dat**: Veškeré zpracování agentů probíhá lokálně, které zajišťuje, že citlivá data nikdy neopustí edge zařízení. To zahrnuje ochranu zákaznických informací, soulad s HIPAA pro zdravotnické agenty, zabezpečení finančních dat pro bankovní agenty a soulad s GDPR pro evropská nasazení.

**Kontrola přístupu**: Řízení přístupů založené na rolích pro nástroje, autentizace uživatelů pro interakce s agenty a auditní záznamy všech akcí a rozhodnutí agentů.

#### Monitoring a observabilita

```python
from microsoft_agent_framework import AgentMonitor

# Nastavit monitorování pro edge agenty
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # Ukládat metriky lokálně pro provoz offline
)

agent.add_monitor(monitor)
```

### Reálné implementační příklady

#### Retailový edge agentní systém

```python
# Prodejní kioskový agent pro pomoc zákazníkům v obchodě
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### Agent pro podporu zdravotní péče

```python
# Agent podpory pacientů v souladu s HIPAA
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # Zvýšené soukromí pro zdravotní péči
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### Nejlepší postupy pro Microsoft Agent Framework

#### Pokyny pro vývoj

1. **Začněte jednoduše**: Začněte se scénáři s jedním agentem před vytvářením složitých multi-agentních systémů
2. **Správná velikost modelu**: Vyberte nejmenší model, který splňuje požadavky na přesnost
3. **Návrh nástrojů**: Vytvářejte zaměřené, jednoúčelové nástroje místo složitých multifunkčních nástrojů
4. **Zpracování chyb**: Implementujte elegantní degradaci pro offline scénáře a selhání modelů
5. **Testování**: Důkladně testujte agenty v offline podmínkách a prostředích s omezenými zdroji

#### Nejlepší postupy nasazení

1. **Postupné zavádění**: Nejprve nasazujte do malých skupin uživatelů a pečlivě sledujte metriky výkonu
2. **Monitoring zdrojů**: Nastavte upozornění na prahové hodnoty paměti, CPU a doby odezvy
3. **Fallback strategie**: Mějte vždy záložní plány pro selhání modelů nebo vyčerpání zdrojů
4. **Bezpečnost na prvním místě**: Implementujte bezpečnostní opatření od začátku, ne dodatečně
5. **Dokumentace**: Udržujte jasnou dokumentaci schopností a omezení agentů

### Budoucí plán a integrace

Microsoft Agent Framework se nadále vyvíjí s vylepšenou optimalizací SLM, lepšími nástroji pro edge nasazení, zlepšenou správou zdrojů pro omezená prostředí a rozšířeným ekosystémem nástrojů pro běžné podnikové scénáře.

**Nadcházející funkce**:
- **AutoML pro optimalizaci agentů**: Automatické doladění SLM pro specifické agentní úkoly
- **Edge mesh síťování**: Koordinace mezi více edge nasazeními agentů
- **Pokročilá telemetrie**: Rozšířený monitoring a analytika výkonu agentů
- **Vizualní builder agentů**: Nástroje pro vývoj agentů s nízkým/no-code rozhraním

## Nejlepší postupy pro implementaci SLM agentů

### Pokyny pro výběr SLM pro agenty

Při výběru SLM pro nasazení agentů zvažte následující faktory:

**Úvahy o velikosti modelu**: Vyberte ultra-komprimované modely jako Q2_K pro extrémní mobilní aplikace agentů, vyvážené modely jako Q4_K_M pro běžné agentní scénáře a modely vyšší přesnosti jako Q8_0 pro aplikace agentů kritických na kvalitu.

**Soulad s případem použití agenta**: Přizpůsobte schopnosti SLM specifickým požadavkům agentů, s ohledem na zachování přesnosti rozhodnutí agenta, rychlost inference pro interakce v reálném čase, omezení paměti pro edge nasazení a požadavky na offline provoz pro agenty zaměřené na soukromí.

### Výběr strategie optimalizace pro SLM agenty

**Přístup ke kvantizaci pro agenty**: Vyberte vhodnou úroveň kvantizace na základě požadavků na kvalitu agenta a hardwarových omezení. Zvažte Q4_0 pro maximální kompresi u mobilních agentů, Q5_1 pro vyváženou kvalitu a kompresi u obecně použitelných agentů a Q8_0 pro kvalitu blízkou originálu u kritických aplikací agentů.


**Výběr frameworku pro nasazení agentů**: Vyberte optimalizační frameworky na základě cílového hardwaru a požadavků agentů. Použijte Llama.cpp pro nasazení agentů optimalizovaných pro CPU, Apple MLX pro aplikace agentů na Apple Silicon a ONNX pro multiplatformní kompatibilitu agentů.

## Praktická konverze SLM agentů a případové studie

### Scénáře nasazení agentů v reálném světě

**Mobilní aplikace agentů**: Formáty Q4_K excelují v aplikacích agentů pro chytré telefony s minimální paměťovou stopou, zatímco Q8_0 poskytuje vyvážený výkon pro systémy agentů na tabletech. Formáty Q5_K nabízejí vynikající kvalitu pro mobilní produktivní agenty.

**Desktopové a edge výpočty agentů**: Q5_K zajišťuje optimální výkon pro desktopové aplikace agentů, Q8_0 poskytuje vysoce kvalitní inferenci pro pracovní stanice agentů a Q4_K umožňuje efektivní zpracování na edge zařízeních agentů.

**Výzkumní a experimentální agenti**: Pokročilé formáty kvantizace umožňují průzkum ultra-nízkopřesné inference agentů pro akademický výzkum a proof-of-concept aplikace agentů vyžadující extrémní omezení zdrojů.

### Výkonové benchmarky SLM agentů

**Rychlost inference agentů**: Q4_K dosahuje nejrychlejších dob odezvy agentů na mobilních CPU, Q5_K poskytuje vyvážený poměr rychlosti a kvality pro obecné aplikace agentů, Q8_0 nabízí vyšší kvalitu pro složité úkoly agentů a experimentální formáty dosažení maximální propustnosti pro specializovaný hardware agentů.

**Paměťové požadavky agentů**: Úrovně kvantizace agentů sahají od Q2_K (pod 500MB pro malé modely agentů) po Q8_0 (přibližně 50 % původní velikosti), přičemž experimentální konfigurace dosahují maximální komprese pro zdrojově omezené agentní prostředí.

## Výzvy a úvahy pro SLM agenty

### Výkonnostní kompromisy v systémech agentů

Nasazení SLM agentů vyžaduje pečlivé zvážení kompromisů mezi velikostí modelu, rychlostí odezvy agenta a kvalitou výstupu. Zatímco Q4_K nabízí výjimečnou rychlost a efektivitu pro mobilní agenty, Q8_0 poskytuje vyšší kvalitu pro složité úkoly agentů. Q5_K představuje kompromis vhodný pro většinu obecných aplikací agentů.

### Hardwarová kompatibilita pro SLM agenty

Různá edge zařízení mají různou schopnost nasazení SLM agentů. Q4_K běží efektivně na základních procesorech pro jednoduché agenty, Q5_K vyžaduje střední výpočetní zdroje pro vyvážený výkon agentů a Q8_0 těží z výkonnějšího hardwaru pro pokročilé schopnosti agentů.

### Bezpečnost a soukromí v systémech SLM agentů

Zatímco SLM agenti umožňují lokální zpracování pro zvýšené soukromí, musí být implementována odpovídající bezpečnostní opatření na ochranu modelů agentů a dat v edge prostředích. To je zvláště důležité při nasazování agentů s vysokou přesností v podnikových prostředích nebo komprimovaných formátů agentů v aplikacích pracujících s citlivými daty.

## Budoucí trendy ve vývoji SLM agentů

Oblast SLM agentů se neustále vyvíjí s pokroky v kompresních technikách, optimalizačních metodách a strategiích nasazení na edge. Mezi budoucí vývoj patří účinnější kvantizační algoritmy pro modely agentů, vylepšené metody komprese pro agentní pracovní toky a lepší integrace s hardwarem urychlovačů na edge pro zpracování agentů.

**Tržní předpovědi pro SLM agenty**: Podle nedávného výzkumu by automatizace řízená agenty mohla do roku 2027 eliminovat 40–60 % opakujících se kognitivních úkolů v podnikových pracovních postupech, přičemž SLM budou tyto změny vést díky své nákladové efektivitě a flexibilitě nasazení.

**Technologické trendy ve SLM agentech**:
- **Specializovaní SLM agenti**: Doménově specifické modely trénované pro konkrétní úkoly agentů a odvětví
- **Edge výpočty agentů**: Vylepšené schopnosti agentů přímo na zařízení s vylepšeným soukromím a sníženou latencí
- **Orchestrace agentů**: Lepší koordinace mezi více SLM agenty s dynamickým směrováním a vyvažováním zátěže
- **Demokratizace**: Flexibilita SLM umožňuje širší zapojení do vývoje agentů v rámci organizací

## Začínáme se SLM agenty

### Krok 1: Nastavte prostředí Microsoft Agent Framework

**Nainstalujte závislosti**:
```bash
# Nainstalujte Microsoft Agent Framework
pip install microsoft-agent-framework

# Nainstalujte Foundry Local SDK pro nasazení na okraji
pip install foundry-local-sdk

# Nainstalujte další závislosti pro edge agenty
pip install openai asyncio
```

**Inicializujte Foundry Local**:
```bash
# Spustit službu Foundry Local
foundry service start

# Načíst výchozí model pro vývoj agenta
foundry model run phi-4-mini
```

### Krok 2: Vyberte svůj SLM pro aplikace agentů
Oblíbené možnosti pro Microsoft Agent Framework:
- **Microsoft Phi-4 Mini (3.8B)**: Výborný pro obecné úkoly agentů s vyváženým výkonem
- **Qwen2.5-0.5B (0.5B)**: Ultraefektivní pro jednoduché směrování a klasifikační agenty
- **Qwen2.5-Coder-0.5B (0.5B)**: Specializovaný na úkoly agentů související s kódem
- **Phi-4 (7B)**: Pokročilé uvažování pro složité edge scénáře, pokud to zdroje dovolí

### Krok 3: Vytvořte svého prvního agenta pomocí Microsoft Agent Framework

**Základní nastavení agenta**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# Inicializovat lokální připojení Foundry
foundry = FoundryLocalManager("phi-4-mini")

# Vytvořit konfiguraci agenta
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# Vytvořit a nakonfigurovat agenta
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# Definovat jednoduchý nástroj
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# Otestovat agenta
response = agent.chat("What time is it?")
print(response)
```

### Krok 4: Definujte rozsah a požadavky agenta
Začněte s koncentrovanými, jasně definovanými aplikacemi agentů pomocí Microsoft Agent Framework:
- **Agenti v jedné doméně**: zákaznický servis NEBO plánování NEBO výzkum
- **Jasné cíle agentů**: Specifické, měřitelné cíle výkonu agenta
- **Omezená integrace nástrojů**: maximálně 3-5 nástrojů pro počáteční nasazení agenta
- **Definované hranice agentů**: Jasné cesty eskalace pro složité scénáře
- **Edge-first design**: Prioritizace offline funkčnosti a lokálního zpracování

### Krok 5: Implementujte edge nasazení s Microsoft Agent Framework

**Konfigurace zdrojů**:
```python
from microsoft_agent_framework import ResourceConfig

# Konfigurace pro nasazení na okraji
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**Nasazení bezpečnostních opatření pro edge agenty**:
- **Lokální validace vstupů**: Kontrola požadavků bez závislosti na cloudu
- **Offline filtrování výstupů**: Zajištění kvality odpovědí přímo lokálně
- **Edge bezpečnostní kontroly**: Implementace bezpečnosti bez nutnosti internetového připojení
- **Lokální monitorování**: Sledování výkonu a označování problémů pomocí telemetry na edge

### Krok 6: Měřte a optimalizujte výkon edge agentů
- **Míry dokončení úkolů agentů**: Monitorujte úspěšnost v offline scénářích
- **Doby odezvy agentů**: Zajistěte odezvy do jedné sekundy pro edge nasazení
- **Využití zdrojů**: Sledujte paměť, CPU a spotřebu baterie na edge zařízeních
- **Nákladová efektivita**: Porovnejte náklady edge nasazení s cloudovými alternativami
- **Offline spolehlivost**: Měřte výkon agentů během výpadků sítě

## Klíčové závěry pro implementaci SLM agentů

1. **SLM jsou dostatečné pro agenty**: Pro většinu úkolů agentů malé modely podávají výkon srovnatelný s velkými a nabízejí významné výhody
2. **Nákladová efektivita u agentů**: Provoz SLM agentů je 10-30x levnější, což je činí ekonomicky životaschopnými pro široké nasazení
3. **Specializace funguje u agentů**: Jemně dolaďované SLM často překonávají obecné LLM v konkrétních aplikacích agentů
4. **Hybridní architektura agentů**: Používejte SLM pro rutinní úkoly agentů a LLM pro složité uvažování, pokud je to nutné
5. **Microsoft Agent Framework umožňuje produkční nasazení**: Nabízí podnikové nástroje pro budování, nasazení a správu edge agentů
6. **Principy edge-first designu**: Offline schopní agenti s lokálním zpracováním zajišťují soukromí a spolehlivost
7. **Integrace Foundry Local**: Plynulé propojení mezi Microsoft Agent Framework a lokální inferencí modelu
8. **Budoucnost jsou SLM agenti**: Malé jazykové modely s produkčními frameworky představují budoucnost agentní AI a umožňují demokratizované a efektivní nasazení agentů

## Reference a další čtení

### Základní výzkumné články a publikace

#### AI agenti a agentní systémy
- **"Language Agents as Optimizable Graphs"** (2024) - Základní výzkum architektury agentů a optimalizace
  - Autoři: Wenyue Hua, Lishan Yang a další
  - Odkaz: https://arxiv.org/abs/2402.16823
  - Klíčové poznatky: Grafové návrhy agentů a optimalizační strategie

- **"The Rise and Potential of Large Language Model Based Agents"** (2023)
  - Autoři: Zhiheng Xi, Wenxiang Chen a další
  - Odkaz: https://arxiv.org/abs/2309.07864
  - Klíčové poznatky: Komplexní přehled schopností a aplikací agentů založených na LLM

- **"Cognitive Architectures for Language Agents"** (2024)
  - Autoři: Theodore Sumers, Shunyu Yao a další
  - Odkaz: https://arxiv.org/abs/2309.02427
  - Klíčové poznatky: Kognitivní frameworky pro návrh inteligentních agentů

#### Malé jazykové modely a optimalizace
- **"Phi-3 Technical Report: A Highly Capable Language Model Locally on Your Phone"** (2024)
  - Autoři: Tým Microsoft Research
  - Odkaz: https://arxiv.org/abs/2404.14219
  - Klíčové poznatky: Principy návrhu SLM a strategie mobilního nasazení

- **"Qwen2.5 Technical Report"** (2024)
  - Autoři: Tým Alibaba Cloud
  - Odkaz: https://arxiv.org/abs/2407.10671
  - Klíčové poznatky: Pokročilé techniky tréninku SLM a optimalizace výkonu

- **"TinyLlama: An Open-Source Small Language Model"** (2024)
  - Autoři: Peiyuan Zhang, Guangtao Zeng a další
  - Odkaz: https://arxiv.org/abs/2401.02385
  - Klíčové poznatky: Ultrakompaktní návrh modelu a efektivita tréninku

### Oficiální dokumentace a frameworky

#### Microsoft Agent Framework
- **Oficiální dokumentace**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHub repozitář**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **Hlavní repozitář**: https://github.com/microsoft/foundry-local
- **Dokumentace**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **Hlavní repozitář**: https://github.com/vllm-project/vllm
- **Dokumentace**: https://docs.vllm.ai/


#### Ollama
- **Oficiální web**: https://ollama.ai/
- **GitHub repozitář**: https://github.com/ollama/ollama

### Frameworky pro optimalizaci modelů

#### Llama.cpp
- **Repozitář**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **Dokumentace**: https://microsoft.github.io/Olive/
- **GitHub repozitář**: https://github.com/microsoft/Olive

#### OpenVINO
- **Oficiální stránka**: https://docs.openvino.ai/

#### Apple MLX
- **Repozitář**: https://github.com/ml-explore/mlx

### Průmyslové zprávy a tržní analýzy

#### Výzkum trhu AI agentů
- **"The State of AI Agents 2025"** - McKinsey Global Institute
  - Odkaz: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - Klíčové poznatky: Tržní trendy a vzorce přijetí v podnicích

#### Technické benchmarky

- **"Edge AI Inference Benchmarks"** - MLPerf
  - Odkaz: https://mlcommons.org/en/inference-edge/
  - Klíčové poznatky: Standardizované metriky výkonu pro edge nasazení

### Standardy a specifikace

#### Formáty modelů a standardy
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - Multiplatformní formát modelů pro interoperabilitu
- **Specifikace GGUF**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - Kvantizovaný formát modelu pro inferenci na CPU
- **Specifikace OpenAI API**: https://platform.openai.com/docs/api-reference
  - Standardní API formát pro integraci jazykových modelů

#### Bezpečnost a shoda
- **NIST AI Risk Management Framework**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 - AI systémy**: Framework pro systémy AI a bezpečnost
- **IEEE standardy pro AI**: https://standards.ieee.org/industry-connections/ai/

Přechod k agentům poháněným SLM představuje zásadní změnu v přístupu k nasazení AI. Microsoft Agent Framework ve spojení s lokálními platformami a efektivními malými jazykovými modely poskytuje kompletní řešení pro budování produkčně připravených agentů, kteří efektivně fungují v edge prostředích. Díky zaměření na efektivitu, specializaci a praktickou užitečnost činí tento technologický stack AI agenty dostupnější, cenově výhodnější a účinnější pro reálné aplikace napříč všemi průmysly a edge computing prostředími.

Jak budeme postupovat do roku 2025, kombinace stále schopnějších malých modelů, sofistikovaných agentních frameworků jako Microsoft Agent Framework a robustních edge nasazovacích platforem otevře nové možnosti pro autonomní systémy, které mohou efektivně fungovat na edge zařízeních při zajištění soukromí, snížení nákladů a poskytování výjimečných uživatelských zkušeností.

**Další kroky k implementaci**:
1. **Prozkoumejte funkci Function Calling**: Naučte se, jak SLM zvládají integraci nástrojů a strukturované výstupy
2. **Ovládněte Model Context Protocol (MCP)**: Pochopte pokročilé vzory komunikace agentů
3. **Budujte produkční agenty**: Použijte Microsoft Agent Framework pro podnikové nasazení
4. **Optimalizujte pro edge**: Aplikujte pokročilé optimalizační techniky pro zdrojově omezená prostředí


## ➡️ Co dál

- [02: Function Calling in Small Language Models (SLMs)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Prohlášení o omezení odpovědnosti**:
Tento dokument byl přeložen pomocí AI překladatelské služby [Co-op Translator](https://github.com/Azure/co-op-translator). Přestože usilujeme o co největší přesnost, mějte prosím na paměti, že automatizované překlady mohou obsahovat chyby nebo nepřesnosti. Originální dokument v jeho mateřském jazyce by měl být považován za autoritativní zdroj. Pro kritické informace se doporučuje profesionální lidský překlad. Nejsme odpovědní za jakékoli nedorozumění nebo nesprávné interpretace vzniklé použitím tohoto překladu.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->