# الجلسة 3: اكتشاف وإدارة النماذج مفتوحة المصدر

## نظرة عامة

تركز هذه الجلسة على اكتشاف النماذج وإدارتها بشكل عملي باستخدام Foundry Local. ستتعلم كيفية سرد النماذج المتاحة، اختبار الخيارات المختلفة، وفهم الخصائص الأساسية للأداء. يركز النهج على الاستكشاف العملي باستخدام واجهة Foundry CLI لمساعدتك في اختيار النماذج المناسبة لحالات استخدامك.

## أهداف التعلم

- إتقان أوامر foundry CLI لاكتشاف وإدارة النماذج
- فهم نماذج التخزين المؤقت والتخزين المحلي للنماذج
- تعلم كيفية اختبار ومقارنة النماذج المختلفة بسرعة
- تأسيس سير عمل عملي لاختيار النماذج وقياس الأداء
- استكشاف النظام البيئي المتنامي للنماذج المتاحة من خلال Foundry Local

## المتطلبات السابقة

- إتمام الجلسة 1: البدء مع Foundry Local
- تثبيت واجهة Foundry Local CLI والوصول إليها
- توفر مساحة تخزين كافية لتحميل النماذج (تتراوح أحجام النماذج من 1 جيجابايت إلى أكثر من 20 جيجابايت)
- فهم أساسي لأنواع النماذج وحالات الاستخدام

## نظرة عامة

تستكشف هذه الجلسة كيفية جلب النماذج مفتوحة المصدر إلى Foundry L## الجزء 6: تمرين عملي

### تمرين: اكتشاف النماذج والمقارنة

أنشئ نص تقييم النموذج الخاص بك استنادًا إلى Sample 03:

```cmd
REM create_model_test.cmd
@echo off
echo Model Discovery and Testing Script
echo =====================================

echo.
echo Step 1: List available models
foundry model list

echo.
echo Step 2: Check what's cached
foundry cache list

echo.
echo Step 3: Start phi-4-mini for testing
foundry model run phi-4-mini --verbose

echo.
echo Step 4: Test with a simple prompt
curl -X POST http://localhost:8000/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"phi-4-mini\",\"messages\":[{\"role\":\"user\",\"content\":\"Hello, please introduce yourself.\"}],\"max_tokens\":100}"

echo.
echo Model test complete!
```

### مهمتك

1. **شغل نص Sample 03**: `samples\03\list_and_bench.cmd`
2. **جرب نماذج مختلفة**: اختبر 3 نماذج مختلفة على الأقل
3. **قارن الأداء**: لاحظ الاختلافات في السرعة وجودة الاستجابة
4. **وثق النتائج**: أنشئ جدول مقارنة بسيط

### صيغة مقارنة نموذجية

```
Model Comparison Results:
========================
phi-4-mini:        Fast (~2s), good for general chat
qwen2.5-7b:       Slower (~5s), better reasoning  
deepseek-r1:      Medium (~3s), excellent for code

Recommendation: Start with phi-4-mini for development, 
switch to qwen2.5-7b for production reasoning tasks.
```

## الجزء 7: استكشاف الأخطاء والممارسات الأفضل

### المشكلات الشائعة والحلول

**النموذج لا يبدأ:**
```cmd
REM Check service status
foundry service status

REM Restart service if needed
foundry service stop
foundry service start

REM Try with verbose output
foundry model run phi-4-mini --verbose
```

**ذاكرة غير كافية:**
- ابدأ بنماذج أصغر (`phi-4-mini`)
- أغلق التطبيقات الأخرى
- قم بترقية الذاكرة العشوائية إذا كانت الحدود تُصل إليها بشكل متكرر

**أداء بطيء:**
- تأكد من تحميل النموذج بالكامل (تحقق من الإخراج التفصيلي)
- أغلق التطبيقات الخلفية غير الضرورية
- فكر في استخدام تخزين أسرع (SSD)

### الممارسات الأفضل

1. **ابدأ صغيرًا**: ابدأ بـ `phi-4-mini` للتحقق من الإعداد
2. **نموذج واحد في كل مرة**: أوقف النماذج السابقة قبل بدء جديدة
3. **راقب الموارد**: تابع استخدام الذاكرة
4. **اختبر باستمرار**: استخدم نفس التنبيهات للمقارنات العادلة
5. **وثق النتائج**: قم بتسجيل ملاحظات حول أداء النموذج لحالات الاستخدام الخاصة بك

## الجزء 8: الخطوات التالية والمراجع

### التحضير للجلسة 4

- **تركيز الجلسة 4**: أدوات وتقنيات التحسين
- **المتطلبات السابقة**: إتقان تبديل النماذج والاختبار الأساسي للأداء
- **موصى به**: تحديد 2-3 نماذج مفضلة من هذه الجلسة

### الموارد الإضافية

- **[وثائق Foundry Local](https://learn.microsoft.com/azure/ai-foundry/foundry-local/)**: الوثائق الرسمية
- **[مرجع CLI](https://learn.microsoft.com/azure/ai-foundry/foundry-local/reference/reference-cli)**: مرجع الأوامر الكامل
- **[Model Mondays](https://aka.ms/model-mondays)**: تسليط الضوء الأسبوعي على النماذج
- **[Foundry Local GitHub](https://github.com/microsoft/Foundry-Local)**: المجتمع والمشكلات
- **[Sample 03: اكتشاف النموذج](samples/03/README.md)**: نص عملي نموذجي

### النقاط الرئيسية

✅ **اكتشاف النموذج**: استخدم `foundry model list` لاستكشاف النماذج المتاحة  
✅ **اختبار سريع**: نمط `list_and_bench.cmd` للتقييم السريع  
✅ **مراقبة الأداء**: قياس استخدام الموارد الأساسي ووقت الاستجابة  
✅ **اختيار النموذج**: إرشادات عملية لاختيار النماذج حسب حالة الاستخدام  
✅ **إدارة التخزين المؤقت**: فهم إجراءات التخزين والتنظيف  

لديك الآن المهارات العملية لاكتشاف، اختبار، واختيار النماذج المناسبة لتطبيقات الذكاء الاصطناعي الخاصة بك باستخدام نهج CLI البسيط لـ Foundry Local: اختيار نماذج المجتمع، دمج محتوى Hugging Face، وتبني استراتيجيات "أحضر نموذجك الخاص" (BYOM). ستكتشف أيضًا سلسلة Model Mondays للتعلم المستمر واكتشاف النماذج.

المراجع:
- وثائق Foundry Local: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- تجميع نماذج Hugging Face: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-compile-hugging-face-models
- Model Mondays: https://aka.ms/model-mondays
- Foundry Local GitHub: https://github.com/microsoft/Foundry-Local

## أهداف التعلم
- اكتشاف وتقييم النماذج مفتوحة المصدر للمحاكاة المحلية
- تجميع وتشغيل نماذج Hugging Face مختارة داخل Foundry Local
- تطبيق استراتيجيات اختيار النماذج للدقة، الكمون، واحتياجات الموارد
- إدارة النماذج محليًا مع التخزين المؤقت وإصدار النسخ

## الجزء 1: اكتشاف النماذج باستخدام Foundry CLI

### أوامر إدارة النماذج الأساسية

تقدم Foundry CLI أوامر بسيطة لاكتشاف وإدارة النماذج:

```cmd
REM List all available models in the catalog
foundry model list

REM List cached (downloaded) models
foundry cache list

REM Check cache directory location
foundry cache ls
```

### تشغيل نماذجك الأولى

ابدأ بالنماذج الشائعة والمجربة لفهم خصائص الأداء:

```cmd
REM Run Phi-4-Mini (lightweight, fast)
foundry model run phi-4-mini --verbose

REM Run Qwen 2.5 7B (larger, more capable)
foundry model run qwen2.5-7b --verbose

REM Run DeepSeek (specialized for coding)
foundry model run deepseek-r1-7b --verbose
```

**ملاحظة:** يوفر المعامل `--verbose` معلومات تفصيلية عن بدء التشغيل، بما في ذلك:
- تقدم تحميل النموذج (في التشغيل الأول)
- تفاصيل تخصيص الذاكرة
- معلومات ربط الخدمة
- مقاييس تهيئة الأداء

### فهم فئات النماذج

**نماذج اللغة الصغيرة (SLMs):**
- `phi-4-mini`: سريع، فعال، رائع للدردشة العامة
- `phi-4`: نسخة أكثر قدرة مع تفكير أفضل

**النماذج المتوسطة:**
- `qwen2.5-7b`: تفكير ممتاز وسياق أطول
- `deepseek-r1-7b`: محسّن لتوليد الكود

**النماذج الأكبر:**
- `llama-3.2`: أحدث نموذج مفتوح المصدر من Meta
- `qwen2.5-14b`: تفكير بمستوى المؤسسات

## الجزء 2: اختبار النماذج السريع والمقارنة

### أسلوب Sample 03: قائمة بسيطة وقياس أداء

استنادًا إلى نمط Sample 03، إليك سير العمل الأدنى:

```cmd
@echo off
REM Sample 03 - List and bench pattern
echo Listing available models...
foundry model list

echo.
echo Checking cached models...
foundry cache list

echo.
echo Starting phi-4-mini with verbose output...
foundry model run phi-4-mini --verbose
```

### اختبار أداء النموذج

بمجرد تشغيل نموذج، اختبره بتنبيهات متسقة:

```cmd
REM Test via curl (Windows Command Prompt)
curl -X POST http://localhost:8000/v1/chat/completions ^
  -H "Content-Type: application/json" ^
  -d "{\"model\":\"phi-4-mini\",\"messages\":[{\"role\":\"user\",\"content\":\"Explain edge AI in one sentence.\"}],\"max_tokens\":50}"
```

### بديل اختبار PowerShell

```powershell
# نهج PowerShell للاختبار
$body = @{
    model = "phi-4-mini"
    messages = @(
        @{
            role = "user"
            content = "Explain edge AI in one sentence."
        }
    )
    max_tokens = 50
} | ConvertTo-Json -Depth 3

Invoke-RestMethod -Uri "http://localhost:8000/v1/chat/completions" -Method Post -Body $body -ContentType "application/json"
```

## الجزء 3: إدارة تخزين النماذج والتخزين المؤقت

### فهم التخزين المؤقت للنموذج

يدير Foundry Local تلقائيًا تنزيلات النماذج والتخزين المؤقت:

```cmd
REM Check cache directory and contents
foundry cache ls

REM View cache location
foundry cache cd

REM Clean up unused models (if needed)
foundry cache clean
```

### اعتبارات تخزين النموذج

**أحجام النماذج النموذجية:**
- `phi-4-mini`: ~2.5 جيجابايت
- `qwen2.5-7b`: ~4.1 جيجابايت  
- `deepseek-r1-7b`: ~4.3 جيجابايت
- `llama-3.2`: ~4.9 جيجابايت
- `qwen2.5-14b`: ~8.2 جيجابايت

**أفضل ممارسات التخزين:**
- احتفظ بذاكرة تخزين مؤقت لـ 2-3 نماذج للتبديل السريع
- أزل النماذج غير المستخدمة لتحرير المساحة: `foundry cache clean`
- راقب استخدام القرص، خاصةً على SSDs الأصغر
- فكر في مقايضات حجم النموذج مقابل القدرة

### مراقبة أداء النموذج

أثناء تشغيل النماذج، راقب موارد النظام:

**مدير مهام ويندوز:**
- راقب استخدام الذاكرة (تبقى النماذج محملة في الذاكرة العشوائية)
- راقب استخدام وحدة المعالجة المركزية أثناء الاستدلال
- تحقق من نشاط الإدخال/الإخراج للقرص عند تحميل النموذج أولًا

**مراقبة سطر الأوامر:**
```cmd
REM Check memory usage (PowerShell)
Get-Process | Where-Object {$_.ProcessName -like "*foundry*"} | Select-Object ProcessName, WorkingSet64

REM Monitor running models
foundry service ps
```

## الجزء 4: إرشادات اختيار النماذج العملية

### اختيار النماذج حسب حالة الاستخدام

**للدردشة العامة والأسئلة والأجوبة:**
- ابدأ بـ: `phi-4-mini` (سريع، فعال)
- ترقية إلى: `phi-4` (تفكير أفضل)
- متقدم: `qwen2.5-7b` (سياق أطول)

**لتوليد الكود:**
- موصى به: `deepseek-r1-7b`
- بديل: `qwen2.5-7b` (جيد أيضًا للكود)

**للتفكير المعقد:**
- الأفضل: `qwen2.5-7b` أو `qwen2.5-14b`
- خيار الميزانية: `phi-4`

### دليل متطلبات الأجهزة

**المتطلبات الدنيا للنظام:**
```
phi-4-mini:     8GB RAM,  entry-level CPU
phi-4:         12GB RAM,  mid-range CPU
qwen2.5-7b:    16GB RAM,  mid-range CPU
deepseek-r1:   16GB RAM,  mid-range CPU
qwen2.5-14b:   24GB RAM,  high-end CPU
```

**موصى به لأفضل أداء:**
- 32 جيجابايت+ ذاكرة وصول عشوائي لتبديل النماذج مريح متعدد النماذج
- تخزين SSD لتحميل النموذج بشكل أسرع
- وحدة معالجة مركزية حديثة مع أداء جيد للخيط الواحد
- دعم NPU (أجهزة الكمبيوتر مع Windows 11 Copilot+) للتسريع

### سير عمل تبديل النماذج

```cmd
REM Stop current model (if needed)
foundry service stop

REM Start different model
foundry model run qwen2.5-7b

REM Verify model is running
foundry service status
```

## الجزء 5: قياس أداء النموذج البسيط

### اختبار الأداء الأساسي

إليك نهج مباشر لمقارنة أداء النماذج:

```python
# simple_bench.py - بناءً على أنماط النموذج 03
import time
import requests
import json

def test_model_response(model_name, prompt="Explain edge AI in one sentence."):
    """Test a single model with a prompt and measure response time."""
    start_time = time.time()
    
    try:
        response = requests.post(
            "http://localhost:8000/v1/chat/completions",
            headers={"Content-Type": "application/json"},
            json={
                "model": model_name,
                "messages": [{"role": "user", "content": prompt}],
                "max_tokens": 64
            },
            timeout=30
        )
        
        elapsed = time.time() - start_time
        
        if response.status_code == 200:
            result = response.json()
            return {
                "model": model_name,
                "latency_sec": round(elapsed, 3),
                "response": result["choices"][0]["message"]["content"],
                "status": "success"
            }
        else:
            return {
                "model": model_name,
                "status": "error",
                "error": f"HTTP {response.status_code}"
            }
            
    except Exception as e:
        return {
            "model": model_name,
            "status": "error", 
            "error": str(e)
        }

# اختبار النموذج الجاري تشغيله حالياً
if __name__ == "__main__":
    # اختبار مع نماذج مختلفة (ابدأ كل نموذج أولاً)
    test_models = ["phi-4-mini", "qwen2.5-7b", "deepseek-r1-7b"]
    
    print("Model Performance Test")
    print("=" * 50)
    
    for model in test_models:
        print(f"\nTesting {model}...")
        print("Note: Make sure this model is running first with 'foundry model run {model}'")
        
        result = test_model_response(model)
        
        if result["status"] == "success":
            print(f"✅ {model}: {result['latency_sec']}s")
            print(f"   Response: {result['response'][:100]}...")
        else:
            print(f"❌ {model}: {result['error']}")
```

### تقييم الجودة اليدوي

لكل نموذج، اختبر باستخدام تنبيهات متسقة وقم بالتقييم يدويًا:

**تنبيهات الاختبار:**
1. "اشرح الحوسبة الكمومية بمصطلحات بسيطة."
2. "اكتب دالة بايثون لترتيب قائمة."
3. "ما هي مزايا وعيوب العمل عن بُعد؟"
4. "لخص فوائد الذكاء الاصطناعي الحصي."

**معايير التقييم:**
- **الدقة**: هل المعلومات صحيحة؟
- **الوضوح**: هل الشرح سهل الفهم؟
- **الشمولية**: هل يعالج السؤال بالكامل؟
- **السرعة**: ما مدى سرعة الاستجابة؟

### مراقبة استخدام الموارد

```cmd
REM Monitor while testing different models
REM Start model
foundry model run phi-4-mini

REM In another terminal, monitor resources
foundry service status
foundry service ps

REM Check system resources (PowerShell)
Get-Process | Where-Object ProcessName -Like "*foundry*" | Format-Table ProcessName, WorkingSet64, CPU
```

## الجزء 6: الخطوات التالية
- اشترك في Model Mondays للنماذج الجديدة والنصائح: https://aka.ms/model-mondays
- ساهم بالنتائج في `models.json` لفريقك
- استعد للجلسة 4: مقارنة بين LLMs وSLMs، المحاكاة المحلية مقابل السحابية، وعروض عملية

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**تنويه**:
تمت ترجمة هذا المستند باستخدام خدمة الترجمة بالذكاء الاصطناعي [Co-op Translator](https://github.com/Azure/co-op-translator). بينما نسعى للدقة، يرجى العلم أن الترجمات الآلية قد تحتوي على أخطاء أو عدم دقة. يجب اعتبار المستند الأصلي بلغته الأصلية المصدر الرسمي والمعتمد. للمعلومات الهامة، يُنصح بالاستعانة بترجمة بشرية محترفة. نحن غير مسؤولين عن أي سوء فهم أو تفسير ناتج عن استخدام هذه الترجمة.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->