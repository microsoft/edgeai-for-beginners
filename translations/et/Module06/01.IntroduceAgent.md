# Tehisintellekti agendid ja väikesed keelemudelid: põhjalik juhend

## Sissejuhatus

Selles juhendis uurime tehisintellekti agente ja väikseid keelemudeleid (SLM-e) ning nende edasijõudnud rakendusstrateegiaid servaarvutuse keskkondades. Käsitleme agendipõhise tehisintellekti põhikontseptsioone, SLM-ide optimeerimistehnikaid, praktilisi juurutusstrateegiaid ressurssidega piiratud seadmetele ning Microsofti Agent Frameworki tootmiskõlbulike agendisüsteemide loomiseks.

Tehisintellekti maastik kogeb 2025. aastal paradigmanihket. Kui 2023 oli juturobotite aasta ja 2024 nägi kaaslaste äriedu, kuulub 2025 tehisintellekti agentidele — intelligentsetele süsteemidele, mis mõtlevad, mõistavad, planeerivad, kasutavad tööriistu ja täidavad ülesandeid minimaalsete inimsekkumisega, üha enam tõhusate väikeste keelemudelite abil. Microsoft Agent Framework kerkib esile juhtiva lahendusena nende intelligentsete süsteemide ehitamiseks koos võrguühenduseta servapõhiste võimekustega.

## Õpieesmärgid

Selle juhendi lõpuks oskate:

- 🤖 Mõista tehisintellekti agentide ja agentipõhiste süsteemide põhikontseptsioone
- 🔬 Tuvastada väikeste keelemudelite eelised suurte keelemudelite ees agentipõhistes rakendustes
- 🚀 Õppida edasijõudnud SLM-de juurutusstrateegiaid servaarvutuse keskkondades
- 📱 Rakendada praktilisi SLM-põhiseid agente reaalses maailmas
- 🏗️ Luua tootmiskõlbulikke agente Microsoft Agent Frameworki abil
- 🌐 Juurutada võrguühenduseta servapõhiseid agente kohaliku LLM- ja SLM-integratsiooniga
- 🔧 Integreerida Microsoft Agent Framework Foundry Localiga servajuurutuseks

## Tehisintellekti agentide mõistmine: alused ja klassifikatsioonid

### Määratlus ja põhikontseptsioonid

Tehisintellekti (AI) agent viitab süsteemile või programmile, mis suudab iseseisvalt täita kasutaja või teise süsteemi eest ülesandeid, kujundades oma töövoo ja kasutades olemasolevaid tööriistu. Traditsioonilisest AI-st erinevalt, mis lihtsalt vastab teie küsimustele, võib agent tegutseda iseseisvalt eesmärkide saavutamiseks.

### Agendi klassifitseerimise raamistik

Agendi piire mõistes saab valida sobivaid agenditüüpe erinevate arvutuskeskkondade jaoks:

- **🔬 Lihtsad refleksagendid**: Reeglipõhised süsteemid, mis reageerivad otsestele tajudele (termostaadid, põhiautomaadid)
- **📱 Mudelipõhised agendid**: Süsteemid, mis säilitavad sisemise oleku ja mälu (robottolmud, navigatsioonisüsteemid)
- **⚖️ Eesmärgipõhised agendid**: Süsteemid, mis planeerivad ja täidavad tegevustesarju eesmärkide saavutamiseks (marsruudiplaanijad, ülesannete ajastajad)
- **🧠 Õppivad agendid**: Kohanduvad süsteemid, mis parandavad jõudlust aja jooksul (soovitussüsteemid, personaalsed assistendid)

### AI agentide peamised eelised

AI agendid pakuvad mitmeid põhieeldusi, mis muudavad nad ideaalseks servaarvutuse rakendusteks:

**Tööautonoomia**: Agendid täidavad iseseisvalt ülesandeid ilma pideva inimjärelevalveta, sobides ideaalselt reaalajas rakendusteks. Nad vajavad minimaalset järelevalvet, säilitades samal ajal kohanduva käitumise, võimaldades juurutamist ressurssidega piiratud seadmetel vähendatud töökuluga.

**Juurutuspaindlikkus**: Need süsteemid võimaldavad seadmel tehisintellekti võimekust ilma internetiühenduseta, suurendavad privaatsust ja turvalisust kohaliku töötlemise kaudu, neid saab kohandada domeenipõhiste rakenduste jaoks ja need sobivad erinevatesse servaarvutuse keskkondadesse.

**Kulukuse efektiivsus**: Agendisüsteemid pakuvad kulutõhusat juurutust võrreldes pilvelahendustega, vähendades tööjõukulusid ja nõudes servarakendustel väiksemat ribalaiust.

## Edasijõudnud väikeste keelemudelite strateegiad

### SLM (väike keelemudel) alused

Väike keelemudel (SLM) on keelemudel, mis mahub tavapärasele tarbijaelektroonikaseadmele ja suudab teha järeldusi piisavalt madala latentsusega, et olla praktiline ühe kasutaja agentipõhiste päringute teenindamisel. Praktiliselt on SLM-id tavaliselt mudelid, millel on alla 10 miljardi parameetri.

**Vormindusfunktsioonide avastamine**: SLM-id toetavad arenenud taseme kvantiseerimist, platvormideülest ühilduvust, reaalajas jõudluse optimeerimist ja servajuurutuse võimekust. Kasutajad saavad nautida täiustatud privaatsust kohaliku töötlemise ja WebGPU toe kaudu brauseripõhiseks juurutuseks.

**Kvantiseerimistaseme kogumikud**: Populaarsed SLM-vormingud hõlmavad Q4_K_M tasakaalustatud tihenduseks mobiilirakendustes, Q5_K_S seeria kvaliteedikeskseks servajuurutuseks, Q8_0 võimsatel servaseadmetel originaalile lähedase täpsusega ning katsealused vormingud nagu Q2_K ülivaeste ressursside jaoks.

### GGUF (üldine GGML universaalvorming) SLM-de juurutamiseks

GGUF on peaformaadiks kvantiseeritud SLM-ide juurutamiseks CPU-l ja servaseadmetel, kohandatud agentipõhiste rakenduste jaoks:

**Agendispetsiifilised funktsioonid**: Vorming võimaldab SLM-de teisendamist ja juurutust koos täiustatud tööriistakõnede, struktureeritud väljundi loomise ja mitmekordsete vestluste toega. Platvormideülene ühilduvus tagab ühtlase agendikäitumise eri servaseadmetel.

**Jõudluse optimeerimine**: GGUF võimaldab agentide töövoogude mälu tõhusat kasutust, toetab dünaamilist mudelite laadimist mitme agent süsteemides ning pakub optimeeritud järelduste tegemist reaalajas agentide vahelistes suhtlustes.

### Servale optimeeritud SLM raamistikud

#### Llama.cpp optimeerimine agentidele

Llama.cpp pakub tipptasemel kvantiseerimistehnikaid, mis on spetsiaalselt optimeeritud agendipõhiseks SLM juurutuseks:

**Agendispetsiifiline kvantiseerimine**: Raamistik toetab Q4_0 (mobiilsed agendid 75% suuruse vähendusega), Q5_1 (tasakaalustatud kvaliteet-tihendus servajärelduste agentidele) ja Q8_0 (originaalile lähedane kvaliteet tootmisagentide süsteemidele). Täiustatud vormingud võimaldavad ülitihedalt surutud agente äärmuslikeks servastsenaariumiteks.

**Rakenduslike eeliste toetamine**: CPU optimeeritud järeldused koos SIMD kiirendusega pakuvad mälu efektiivset agendieksekutsiooni. Platvormideülene ühilduvus x86, ARM ja Apple Siliconi arhitektuuride vahel võimaldab universaalset agendi juurutusvõimekust.

#### Apple MLX raamistik SLM agentidele

Apple MLX pakub natiivset optimeerimist, mis on mõeldud SLM-jõulistele agentidele Apple Silicon seadmetel:

**Apple Siliconi agentide optimeerimine**: Raamistik kasutab ühtset mäluarhitektuuri koos Metal Performance Shaders integratsiooniga, automaatset segaprecisiooni agentide järeldusteks ning optimeeritud mälukaugu ribalaiust mitme agendi süsteemide jaoks. SLM agendid näitavad erakordset jõudlust M-seeria kiipidel.

**Arendusfunktsioonid**: Python ja Swift API tugi agentide spetsiifiliste optimeerimistega, automaatne diferentseerimine agentide õppimiseks ja sujuv integratsioon Apple'i arendustööriistadega pakuvad terviklikku agendi arenduskeskkonda.

#### ONNX Runtime platvormideülesteks SLM agentideks

ONNX Runtime pakub universaalset järeldusmootorit, mis võimaldab SLM agentidel töötada ühtselt eri riistvaraplatvormidel ja operatsioonisüsteemidel:

**Universaalne juurutus**: ONNX Runtime tagab SLM agentide ühtlase käitumise Windows, Linux, macOS, iOS ja Android platvormidel. Platvormeülene ühilduvus võimaldab arendajatel kirjutada üks kord ja juurutada kõikjal, vähendades märkimisväärselt mitmeplatvormilise arenduse ja hoolduse ülekoormust.

**Riistvarakiirenduse valikud**: Raamistik pakub optimeeritud täitjapakkujaid erinevate riistvarakonfiguratsioonide jaoks, sealhulgas CPU (Intel, AMD, ARM), GPU (NVIDIA CUDA, AMD ROCm) ja spetsiaalsed kiirendid (Intel VPU, Qualcomm NPU). SLM agendid saavad automaatselt kasutada parimat olemasolevat riistvara ilma koodimuudatusteta.

**Tootmiskõlbulikud funktsioonid**: ONNX Runtime pakub äriklassi funktsioone, mis on olulised tootmiseks juurutamiseks, sh graafikute optimeerimine kiiremate järelduste jaoks, mälu haldamine ressurssidega piiratud keskkondades ja täiustatud jõudluse analüüsi tööriistad. Raamistik toetab nii Python kui ka C++ API-sid paindlikuks integratsiooniks.


## SLM ja LLM agentipõhistes süsteemides: edasijõudnud võrdlus

### SLM eelised agentirakendustes

**Tööefektiivsus**: SLM-id vähendavad agentide ülesannete kulu 10–30 korda võrreldes LLM-idega, võimaldades reaalajas agentikõnet suures mahus. Nad pakuvad kiiremaid järeldusaegu tänu väiksemale arvutuslikule keerukusele, olles ideaalsed interaktiivseteks agentirakendusteks.

**Servajuurutusvõimekus**: SLM-id võimaldavad agentide täitmist seadmel ilma internetiühenduseta, tõstavad privaatsust kohaliku töötlemise kaudu ning pakuvad kohandamist domeenipõhiste agentirakenduste jaoks, sobides erinevatesse servaarvutuse keskkondadesse.

**Agendispetsiifiline optimeerimine**: SLM-id on edukad tööriistakõnedes, struktureeritud väljundi loomises ja rutiinsetes otsustusprotsessides, mis moodustavad 70–80% tüüpilistest agentide ülesannetest.

### Millal kasutada SLM-e vs LLM-e agentisüsteemides

**Sobib ideaalselt SLM-ile**:
- **Korduvad agentide ülesanded**: andmesisestus, vormide täitmine, rutiinsed API päringud
- **Tööriistade integratsioon**: andmebaasi päringud, failitoimingud, süsteemitöötlus
- **Struktureeritud töövood**: eelmääratletud agentiprotsesside järgimine
- **Domeenispetsiifilised agendid**: klienditeenindus, ajastamine, põhianalüüs
- **Kohalik töötlemine**: privaatsusnõudlikud agentide toimingud

**Parem LLM-ide puhul**:
- **Keerukas mõtlemine**: uudsed probleemilahendused, strateegiline planeerimine
- **Avatud lõppega vestlused**: üldine vestlus, loomingulised arutelud
- **Laiad teadmised**: uurimistöö, mis nõuab ulatuslikku üldteadmiste baasi
- **Uued olukorrad**: täiesti uute agentstsenaariumide käsitlemine

### Hübriidne agendiarhitektuur

Optimaalne lähenemisviis ühendab SLM-id ja LLM-id heterogeensetes agentipõhistes süsteemides:

**Tark agentide orkestreerimine**:
1. **SLM põhirollis**: käsitleda 70–80% rutiinsetest agenditöödest kohapeal
2. **LLM vajadusel**: suunata keerukad päringud pilvepõhistele suurematele mudelitele
3. **Spetsialiseeritud SLM-id**: erinevad väiksemad mudelid eri agendivaldkondade jaoks
4. **Kulukuse optimeerimine**: minimeerida kuluka LLM-kõnede arvu nutika marsruutimise kaudu

## Tootmiskõlbulike SLM agentide juurutusstrateegiad

### Foundry Local: äriklassi serva AI käitusaeg

Foundry Local (https://github.com/microsoft/foundry-local) on Microsofti lipulaevlahendus väikeste keelemudelite juurutamiseks tootmise servakeskkondades. See pakub täielikku käitusaega, mis on spetsiaalselt loodud SLM-põhiste agentide jaoks äriklassi funktsioonide ja sujuva integratsiooniga.

**Tuumikarhitektuur ja funktsioonid**:
- **OpenAI-ühilduv API**: täisühilduvus OpenAI SDK ja Agent Frameworki integratsioonidega
- **Automaatne riistvara optimeerimine**: nutikas mudelivariantide valik vastavalt olemasolevale riistvarale (CUDA GPU, Qualcomm NPU, CPU)
- **Mudelite haldus**: automatiseeritud SLM mudelite allalaadimine, vahemällu salvestamine ja elutsükli haldus
- **Teenuse avastamine**: nullkonfiguratsiooniga teenuse tuvastus agentide raamistikule
- **Ressursside optimeerimine**: nutikas mäluhaldussüsteem ja energiatõhusus servajuurutusteks

#### Paigaldus ja seadistamine

**Platvormideülene paigaldus**:
```bash
# Windows (soovitatav)
winget install Microsoft.FoundryLocal

# macOS
brew tap microsoft/foundrylocal
brew install foundrylocal

# Linux (käsitsi paigaldamine)
wget https://github.com/microsoft/foundry-local/releases/latest/download/foundry-local-linux.tar.gz
tar -xzf foundry-local-linux.tar.gz
sudo mv foundry-local /usr/local/bin/
```

**Kiire algus agendi arendamiseks**:
```bash
# Käivita teenus automaatse mudeli laadimisega
foundry model run phi-4-mini

# Kontrolli teenuse olekut ja lõpp-punkti
foundry service status

# Kuva saadaolevad mudelid
foundry model ls

# Testi API lõpp-punkti
curl http://localhost:<port>/v1/models
```

#### Agendi raamistikuga integratsioon

**Foundry Local SDK integratsioon**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config
import openai

# Initsialiseeri Foundry Local automaatse teenusehaldusega
manager = FoundryLocalManager("phi-4-mini")

# Konfigureeri OpenAI klient lokaalse inferentsi jaoks
client = openai.OpenAI(
    base_url=manager.endpoint,
    api_key=manager.api_key  # Automaatselt genereeritud lokaalseks kasutamiseks
)

# Loo agent Foundry Local taustsüsteemiga
agent_config = Config(
    name="production-agent",
    model_provider="foundry-local",
    model_id=manager.get_model_info("phi-4-mini").id,
    endpoint=manager.endpoint,
    api_key=manager.api_key
)

agent = Agent(config=agent_config)
```

**Automaatne mudelivalik ja riistvara optimeerimine**:
```python
# Foundry Local valib automaatselt optimaalse mudelivariandi
models_by_use_case = {
    "lightweight_routing": "qwen2.5-0.5b",      # 500MB, ülikiire
    "general_conversation": "phi-4-mini",       # 2.4GB, tasakaalustatud
    "complex_reasoning": "phi-4",               # 7GB, kõrge võimekus
    "code_assistance": "qwen2.5-coder-0.5b"    # 500MB, koodi optimeeritud
}

# Foundry Local haldab riistvara tuvastamist ja kvantimist
for use_case, model_alias in models_by_use_case.items():
    manager = FoundryLocalManager(model_alias)
    print(f"{use_case}: {manager.get_model_info(model_alias).variant_selected}")
    # Väljundi näited:
    # lightweight_routing: qwen2.5-0.5b-instruct-q4_k_m.gguf (CPU optimeeritud)
    # general_conversation: phi-4-mini-instruct-cuda-q5_k_m.gguf (GPU kiirendatud)
```

#### Tootmise juurutuse mustrid

**Ühekordne agendi tootmiskonfiguratsioon**:
```python
import asyncio
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import Agent, Config, Tool

class ProductionAgentService:
    def __init__(self, model_alias="phi-4-mini"):
        self.foundry = FoundryLocalManager(model_alias)
        self.agent = self._create_agent()
        
    def _create_agent(self):
        config = Config(
            name="production-customer-service",
            model_provider="foundry-local",
            model_id=self.foundry.get_model_info().id,
            endpoint=self.foundry.endpoint,
            api_key=self.foundry.api_key,
            max_tokens=512,
            temperature=0.1,
            timeout=30.0
        )
        
        agent = Agent(config=config)
        
        # Lisa tootmistööriistad
        @agent.tool
        def lookup_customer(customer_id: str) -> dict:
            """Look up customer information from local database."""
            return self.local_db.get_customer(customer_id)
            
        @agent.tool
        def create_ticket(issue: str, priority: str = "medium") -> str:
            """Create a support ticket."""
            ticket_id = self.ticketing_system.create(issue, priority)
            return f"Created ticket {ticket_id}"
            
        return agent
    
    async def process_request(self, user_input: str) -> str:
        """Process user request with error handling and monitoring."""
        try:
            response = await self.agent.chat_async(user_input)
            self.log_interaction(user_input, response, "success")
            return response
        except Exception as e:
            self.log_interaction(user_input, str(e), "error")
            return "I'm experiencing technical difficulties. Please try again."
    
    def health_check(self) -> dict:
        """Check service health for monitoring."""
        return {
            "foundry_status": self.foundry.health_check(),
            "model_loaded": self.foundry.is_model_loaded(),
            "endpoint": self.foundry.endpoint,
            "memory_usage": self.foundry.get_memory_usage()
        }

# Tootmise kasutus
service = ProductionAgentService("phi-4-mini")
response = await service.process_request("I need help with my order #12345")
```

**Mitme agendi tootmise orkestreerimine**:
```python
from foundry_local import FoundryLocalManager
from microsoft_agent_framework import AgentOrchestrator, Agent, Config

class MultiAgentProductionSystem:
    def __init__(self):
        self.agents = self._initialize_agents()
        self.orchestrator = AgentOrchestrator(list(self.agents.values()))
        
    def _initialize_agents(self):
        agents = {}
        
        # Kergekaaluline marsruutimise agent
        routing_foundry = FoundryLocalManager("qwen2.5-0.5b")
        agents["router"] = Agent(Config(
            name="request-router",
            model_provider="foundry-local",
            endpoint=routing_foundry.endpoint,
            api_key=routing_foundry.api_key,
            role="Route user requests to appropriate specialized agents"
        ))
        
        # Klienditeeninduse agent
        service_foundry = FoundryLocalManager("phi-4-mini")
        agents["customer_service"] = Agent(Config(
            name="customer-service",
            model_provider="foundry-local",
            endpoint=service_foundry.endpoint,
            api_key=service_foundry.api_key,
            role="Handle customer service inquiries and support requests"
        ))
        
        # Tehnilise toe agent
        tech_foundry = FoundryLocalManager("qwen2.5-coder-0.5b")
        agents["technical"] = Agent(Config(
            name="technical-support",
            model_provider="foundry-local",
            endpoint=tech_foundry.endpoint,
            api_key=tech_foundry.api_key,
            role="Provide technical assistance and troubleshooting"
        ))
        
        return agents
    
    async def process_request(self, user_input: str) -> str:
        """Route and process user requests through appropriate agents."""
        # Suuna päring sobivale agendile
        routing_result = await self.agents["router"].chat_async(
            f"Classify this request and route to customer_service or technical: {user_input}"
        )
        
        # Määrake sihtagent marsruutimise põhjal
        target_agent = "customer_service" if "customer" in routing_result.lower() else "technical"
        
        # Töötle spetsialiseeritud agendiga
        response = await self.agents[target_agent].chat_async(user_input)
        
        return response

# Tootmisesse juurutamine
system = MultiAgentProductionSystem()
response = await system.process_request("My application keeps crashing")
```

#### Ettevõtte funktsioonid ja jälgimine

**Tervise jälgimine ja nähtavus**:
```python
from foundry_local import FoundryLocalManager
import asyncio
import logging

class FoundryMonitoringService:
    def __init__(self):
        self.managers = {}
        self.metrics = []
        
    def add_model(self, alias: str) -> FoundryLocalManager:
        """Add a model to monitoring."""
        manager = FoundryLocalManager(alias)
        self.managers[alias] = manager
        return manager
    
    async def collect_metrics(self):
        """Collect performance metrics from all Foundry Local instances."""
        metrics = {
            "timestamp": time.time(),
            "models": {}
        }
        
        for alias, manager in self.managers.items():
            try:
                model_metrics = {
                    "status": "healthy" if manager.health_check() else "unhealthy",
                    "memory_usage": manager.get_memory_usage(),
                    "inference_count": manager.get_inference_count(),
                    "average_latency": manager.get_average_latency(),
                    "error_rate": manager.get_error_rate()
                }
                metrics["models"][alias] = model_metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {alias}: {e}")
                metrics["models"][alias] = {"status": "error", "error": str(e)}
        
        self.metrics.append(metrics)
        return metrics
    
    def get_health_status(self) -> dict:
        """Get overall system health status."""
        healthy_models = 0
        total_models = len(self.managers)
        
        for alias, manager in self.managers.items():
            if manager.health_check():
                healthy_models += 1
        
        return {
            "overall_status": "healthy" if healthy_models == total_models else "degraded",
            "healthy_models": healthy_models,
            "total_models": total_models,
            "health_percentage": (healthy_models / total_models) * 100 if total_models > 0 else 0
        }

# Tootmise jälgimise seadistus
monitor = FoundryMonitoringService()
monitor.add_model("phi-4-mini")
monitor.add_model("qwen2.5-0.5b")

# Pidev jälgimine
async def monitoring_loop():
    while True:
        metrics = await monitor.collect_metrics()
        health = monitor.get_health_status()
        
        if health["health_percentage"] < 100:
            logging.warning(f"System health degraded: {health}")
        
        await asyncio.sleep(30)  # Koguge mõõdikud iga 30 sekundi järel
```

**Ressursside haldus ja automaatne skaleerimine**:
```python
class FoundryResourceManager:
    def __init__(self):
        self.model_instances = {}
        self.resource_limits = {
            "max_memory_gb": 8,
            "max_concurrent_models": 3,
            "cpu_threshold": 80
        }
    
    def auto_scale_models(self, demand_metrics: dict):
        """Automatically scale models based on demand."""
        current_memory = self.get_total_memory_usage()
        
        # Vähenda skaala, kui mälukasutus on kõrge
        if current_memory > self.resource_limits["max_memory_gb"] * 0.8:
            self.scale_down_idle_models()
        
        # Suurenda skaala, kui nõudlus on suur ja ressursid lubavad
        for model_alias, demand in demand_metrics.items():
            if demand > 0.8 and len(self.model_instances) < self.resource_limits["max_concurrent_models"]:
                self.load_model_instance(model_alias)
    
    def load_model_instance(self, alias: str) -> FoundryLocalManager:
        """Load a new model instance if resources allow."""
        if alias not in self.model_instances:
            try:
                manager = FoundryLocalManager(alias)
                self.model_instances[alias] = manager
                logging.info(f"Loaded model instance: {alias}")
                return manager
            except Exception as e:
                logging.error(f"Failed to load model {alias}: {e}")
                return None
        return self.model_instances[alias]
    
    def scale_down_idle_models(self):
        """Remove idle model instances to free resources."""
        idle_models = []
        
        for alias, manager in self.model_instances.items():
            if manager.get_idle_time() > 300:  # 5 minutit tühikäiku
                idle_models.append(alias)
        
        for alias in idle_models:
            self.model_instances[alias].shutdown()
            del self.model_instances[alias]
            logging.info(f"Scaled down idle model: {alias}")
```

#### Täiustatud konfiguratsioon ja optimeerimine

**Kohandatud mudeli konfiguratsioon**:
```python
# Täiustatud Foundry lokaalne konfiguratsioon tootmiseks
from foundry_local import FoundryLocalManager, ModelConfig

# Kohandatud konfiguratsioon konkreetseteks kasutusjuhtudeks
config = ModelConfig(
    alias="phi-4-mini",
    quantization="Q5_K_M",  # Konkreetne kvantiseerimise tase
    context_length=4096,    # Laiendatud kontekst keerukate agentide jaoks
    batch_size=1,          # Optimeeritud ühe kasutajaga agentidele
    threads=4,             # CPU lõime optimeerimine
    gpu_layers=32,         # GPU kiirenduse kihid
    memory_lock=True,      # Mudeli lukustamine mällu järjepideva jõudluse jaoks
    numa=True              # NUMA optimeerimine mitme pesa süsteemide jaoks
)

manager = FoundryLocalManager(config=config)
```

**Tootmise juurutuse kontrollnimekiri**:

✅ **Teenuse konfiguratsioon**:
- Määrata sobivad mudelinimed kasutusjuhtude jaoks
- Seada ressursipiirangud ja jälgimise läved
- Lubada tervisekontrollid ja mõõdikute kogumine
- Konfigureerida automaatne taaskäivitamine ja rikete haldus

✅ **Turvaseadistus**:
- Lubada ainult lokaalne API juurdepääs (väliste ühendusteta)
- Konfigureerida vastavad API võtmete haldus
- Seadistada auditeerimise logimine agendi suhtlusele
- Rakendada kasutuspiiranguid tootmises

✅ **Jõudluse optimeerimine**:
- Katsetada mudeli jõudlust oodatud koormuse all
- Seadistada sobivad kvantiseerimistasemed
- Korraldada mudelite vahemällu salvestamise ja eelsoojenduse strateegiad
- Jälgida mälu ja CPU kasutusmustreid

✅ **Integratsioonitestimine**:
- Katsetada agentide raamistikuga integratsiooni
- Kontrollida võrguühenduseta töövõimekust
- Testida rikete halduse ja taastumise stsenaariume
- Kinnitada lõpp-lõpuni agenditöövood

### Ollama: lihtsustatud SLM agendi juurutus

### Ollama: kogukonnakeskne SLM agendi juurutus

Ollama pakub kogukonnajuhtimist SLM ide juurutusprotsessile, rõhutades lihtsust, laia mudeliekosüsteemi ja arendajasõbralikke töövooge. Kuigi Foundry Local keskendub äriklassi funktsioonidele, paistab Ollama silma kiire prototüüpimise, kogukonna mudelite ligipääsu ja lihtsustatud juurutusstsenaariumidega.

**Tuumikarhitektuur ja funktsioonid**:
- **OpenAI-ühilduv API**: täis REST API ühilduvus sujuvaks agentide raamistikuga integratsiooniks
- **Extensive Model Library**: ligipääs sadadele kogukonna ja ametlikele mudelitele
- **Lihtne mudelite haldus**: ühe käsu mudeli install ja vahetus
- **Platvormideülene tugi**: natiivne tugi Windowsile, macOS-ile ja Linuxile
- **Ressursside optimeerimine**: automaatne kvantiseerimine ja riistvara tuvastus

#### Paigaldus ja seadistamine

**Platvormideülene paigaldus**:
```bash
# Windows
winget install Ollama.Ollama

# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Dockeri juurutamine
docker run -d -v ollama:/root/.ollama -p 11434:11434 --name ollama ollama/ollama
```

**Kiire algus agendi arendamiseks**:
```bash
# Käivita Ollama teenus
ollama serve

# Tõmba ja käivita mudeleid agendi arendamiseks
ollama pull phi3.5:3.8b-mini-instruct-q4_K_M    # Microsoft Phi-3.5 Mini
ollama pull qwen2.5:0.5b-instruct-q4_K_M        # Qwen2.5 0.5B
ollama pull llama3.2:1b-instruct-q4_K_M         # Llama 3.2 1B

# Testi mudeli kättesaadavust
ollama list

# Testi API lõpp-punkti
curl http://localhost:11434/api/generate -d '{
  "model": "phi3.5:3.8b-mini-instruct-q4_K_M",
  "prompt": "Hello, how can I help you today?"
}'
```

#### Agendi raamistikuga integratsioon

**Ollama Microsoft Agent Frameworkiga**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import requests
import json

class OllamaManager:
    def __init__(self, model_name: str, base_url: str = "http://localhost:11434"):
        self.model_name = model_name
        self.base_url = base_url
        self.api_url = f"{base_url}/api"
        self.openai_url = f"{base_url}/v1"
        
    def ensure_model_available(self) -> bool:
        """Ensure the model is pulled and available."""
        try:
            response = requests.post(f"{self.api_url}/pull", 
                json={"name": self.model_name})
            return response.status_code == 200
        except Exception as e:
            print(f"Failed to pull model {self.model_name}: {e}")
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for Ollama."""
        return openai.OpenAI(
            base_url=self.openai_url,
            api_key="ollama",  # Ollama ei nõua tegelikku API võtit
        )
    
    def health_check(self) -> bool:
        """Check if Ollama service is running."""
        try:
            response = requests.get(f"{self.base_url}/api/tags")
            return response.status_code == 200
        except:
            return False

# Algatage Ollama agendi arendamiseks
ollama_manager = OllamaManager("phi3.5:3.8b-mini-instruct-q4_K_M")
ollama_manager.ensure_model_available()

# Konfigureerige agent Ollama taustsüsteemiga
agent_config = Config(
    name="ollama-agent",
    model_provider="ollama",
    model_id="phi3.5:3.8b-mini-instruct-q4_K_M",
    endpoint=ollama_manager.openai_url,
    api_key="ollama"
)

agent = Agent(config=agent_config)
```

**Mitme mudeli agendi seadistus Ollamaga**:
```python
class OllamaMultiModelManager:
    def __init__(self):
        self.models = {
            "lightweight": "qwen2.5:0.5b-instruct-q4_K_M",      # 350MB
            "balanced": "phi3.5:3.8b-mini-instruct-q4_K_M",     # 2.3GB
            "capable": "llama3.2:3b-instruct-q4_K_M",           # 1.9GB
            "coding": "codellama:7b-code-q4_K_M"                # 4.1GB
        }
        self.base_url = "http://localhost:11434"
        self.clients = {}
        self._initialize_models()
    
    def _initialize_models(self):
        """Pull all required models and create clients."""
        for category, model_name in self.models.items():
            # Kui mudel pole saadaval, tõmba see
            self._pull_model(model_name)
            
            # Loo OpenAI klient iga mudeli jaoks
            self.clients[category] = openai.OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key="ollama"
            )
    
    def _pull_model(self, model_name: str):
        """Pull model if not already available."""
        try:
            response = requests.post(f"{self.base_url}/api/pull", 
                json={"name": model_name})
            if response.status_code == 200:
                print(f"Model {model_name} ready")
        except Exception as e:
            print(f"Failed to pull {model_name}: {e}")
    
    def get_agent_for_task(self, task_type: str) -> Agent:
        """Get appropriate agent based on task complexity."""
        model_category = self._classify_task(task_type)
        model_name = self.models[model_category]
        
        config = Config(
            name=f"ollama-{model_category}-agent",
            model_provider="ollama",
            model_id=model_name,
            endpoint=f"{self.base_url}/v1",
            api_key="ollama"
        )
        
        return Agent(config=config)
    
    def _classify_task(self, task_type: str) -> str:
        """Classify task to appropriate model category."""
        if any(keyword in task_type.lower() for keyword in ["simple", "route", "classify"]):
            return "lightweight"
        elif any(keyword in task_type.lower() for keyword in ["code", "programming", "debug"]):
            return "coding"
        elif any(keyword in task_type.lower() for keyword in ["complex", "analysis", "research"]):
            return "capable"
        else:
            return "balanced"

# Kasutusnäide
manager = OllamaMultiModelManager()

# Saa sobivad agendid erinevate ülesannete jaoks
routing_agent = manager.get_agent_for_task("simple routing")
coding_agent = manager.get_agent_for_task("code debugging")
analysis_agent = manager.get_agent_for_task("complex analysis")
```

#### Tootmise juurutuse mustrid

**Tootmisteenus Ollama abil**:
```python
import asyncio
import logging
from typing import Dict, Optional
from microsoft_agent_framework import Agent, Config
import requests
import openai

class OllamaProductionService:
    def __init__(self, models_config: Dict[str, str]):
        self.models_config = models_config
        self.base_url = "http://localhost:11434"
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "errors": 0,
            "model_usage": {model: 0 for model in models_config.keys()}
        }
        self._initialize_production_agents()
    
    def _initialize_production_agents(self):
        """Initialize production agents with health checks."""
        for agent_type, model_name in self.models_config.items():
            try:
                # Veendu, et mudel on saadaval
                self._ensure_model_ready(model_name)
                
                # Loo tootmises kasutatav agent
                config = Config(
                    name=f"production-{agent_type}",
                    model_provider="ollama",
                    model_id=model_name,
                    endpoint=f"{self.base_url}/v1",
                    api_key="ollama",
                    max_tokens=512,
                    temperature=0.1,
                    timeout=30.0
                )
                
                agent = Agent(config=config)
                
                # Lisa tootmisseadmed vastavalt agendi tüübile
                self._add_production_tools(agent, agent_type)
                
                self.agents[agent_type] = agent
                logging.info(f"Initialized {agent_type} agent with model {model_name}")
                
            except Exception as e:
                logging.error(f"Failed to initialize {agent_type} agent: {e}")
    
    def _ensure_model_ready(self, model_name: str):
        """Ensure model is pulled and ready for use."""
        try:
            # Kontrolli, kas mudel eksisteerib
            response = requests.get(f"{self.base_url}/api/tags")
            models = response.json().get('models', [])
            
            model_exists = any(model['name'] == model_name for model in models)
            
            if not model_exists:
                logging.info(f"Pulling model {model_name}...")
                pull_response = requests.post(f"{self.base_url}/api/pull", 
                    json={"name": model_name})
                
                if pull_response.status_code != 200:
                    raise Exception(f"Failed to pull model {model_name}")
                    
        except Exception as e:
            raise Exception(f"Model setup failed for {model_name}: {e}")
    
    def _add_production_tools(self, agent: Agent, agent_type: str):
        """Add tools based on agent type."""
        if agent_type == "customer_service":
            @agent.tool
            def lookup_customer(customer_id: str) -> dict:
                """Look up customer information."""
                # Simuleeri andmebaasi otsingut
                return {"customer_id": customer_id, "status": "active", "tier": "premium"}
            
            @agent.tool
            def create_support_ticket(issue: str, priority: str = "medium") -> str:
                """Create a support ticket."""
                ticket_id = f"TICK-{hash(issue) % 10000:04d}"
                return f"Created ticket {ticket_id} with priority {priority}"
        
        elif agent_type == "technical_support":
            @agent.tool
            def run_diagnostics(system_info: str) -> dict:
                """Run system diagnostics."""
                return {"status": "healthy", "issues": [], "recommendations": []}
            
            @agent.tool
            def access_knowledge_base(query: str) -> str:
                """Search technical knowledge base."""
                return f"Knowledge base results for: {query}"
    
    async def process_request(self, request: str, agent_type: str = "customer_service") -> dict:
        """Process user request with monitoring and error handling."""
        start_time = time.time()
        
        try:
            if agent_type not in self.agents:
                raise ValueError(f"Agent type {agent_type} not available")
            
            agent = self.agents[agent_type]
            response = await agent.chat_async(request)
            
            # Uuenda mõõdikuid
            self.metrics["requests_processed"] += 1
            self.metrics["model_usage"][agent_type] += 1
            
            processing_time = time.time() - start_time
            
            self._log_interaction(request, response, "success", processing_time, agent_type)
            
            return {
                "response": response,
                "status": "success",
                "processing_time": processing_time,
                "agent_type": agent_type
            }
            
        except Exception as e:
            self.metrics["errors"] += 1
            processing_time = time.time() - start_time
            
            self._log_interaction(request, str(e), "error", processing_time, agent_type)
            
            return {
                "response": "I'm experiencing technical difficulties. Please try again.",
                "status": "error",
                "error": str(e),
                "processing_time": processing_time
            }
    
    def _log_interaction(self, request: str, response: str, status: str, 
                        processing_time: float, agent_type: str):
        """Log interaction for monitoring and analysis."""
        logging.info(f"Agent: {agent_type}, Status: {status}, Time: {processing_time:.2f}s")
        
        # Tootmises kirjutataks see korralikku logimissüsteemi
        log_entry = {
            "timestamp": time.time(),
            "agent_type": agent_type,
            "request_length": len(request),
            "response_length": len(response),
            "status": status,
            "processing_time": processing_time
        }
    
    def get_health_status(self) -> dict:
        """Get service health status."""
        try:
            # Kontrolli Ollama teenuse tervist
            response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            ollama_healthy = response.status_code == 200
            
            # Kontrolli mudeli kättesaadavust
            available_models = []
            if ollama_healthy:
                models = response.json().get('models', [])
                available_models = [model['name'] for model in models]
            
            return {
                "service_status": "healthy" if ollama_healthy else "unhealthy",
                "ollama_endpoint": self.base_url,
                "available_models": available_models,
                "active_agents": list(self.agents.keys()),
                "metrics": self.metrics,
                "timestamp": time.time()
            }
            
        except Exception as e:
            return {
                "service_status": "error",
                "error": str(e),
                "timestamp": time.time()
            }

# Tootmisse juurutamise näide
production_models = {
    "customer_service": "phi3.5:3.8b-mini-instruct-q4_K_M",
    "technical_support": "llama3.2:3b-instruct-q4_K_M",
    "routing": "qwen2.5:0.5b-instruct-q4_K_M"
}

service = OllamaProductionService(production_models)

# Töötle päringuid
result = await service.process_request(
    "I need help with my account settings", 
    "customer_service"
)
print(result)
```

#### Ettevõtte funktsioonid ja jälgimine

**Ollama jälgimine ja nähtavus**:
```python
import time
import asyncio
import requests
from typing import Dict, List

class OllamaMonitoringService:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.metrics_history = []
        self.alert_thresholds = {
            "response_time_ms": 2000,
            "error_rate_percent": 5,
            "memory_usage_percent": 85
        }
    
    async def collect_metrics(self) -> dict:
        """Collect comprehensive metrics from Ollama service."""
        metrics = {
            "timestamp": time.time(),
            "service_status": "unknown",
            "models": {},
            "performance": {},
            "resources": {}
        }
        
        try:
            # Teenuse oleku kontrollimine
            health_response = requests.get(f"{self.base_url}/api/tags", timeout=5)
            metrics["service_status"] = "healthy" if health_response.status_code == 200 else "unhealthy"
            
            if metrics["service_status"] == "healthy":
                # Saada mudeli info
                models_data = health_response.json().get('models', [])
                for model in models_data:
                    model_name = model['name']
                    metrics["models"][model_name] = {
                        "size_gb": model.get('size', 0) / (1024**3),
                        "modified": model.get('modified_at', ''),
                        "digest": model.get('digest', '')[:12]  # Lühike kokkuvõte
                    }
                
                # Katse ennustustulemuste jõudlus
                start_time = time.time()
                test_response = requests.post(f"{self.base_url}/api/generate", 
                    json={
                        "model": list(metrics["models"].keys())[0] if metrics["models"] else "",
                        "prompt": "Hello",
                        "stream": False
                    }, timeout=10)
                
                if test_response.status_code == 200:
                    inference_time = (time.time() - start_time) * 1000
                    metrics["performance"] = {
                        "inference_time_ms": inference_time,
                        "tokens_per_second": self._calculate_tokens_per_second(test_response.json()),
                        "last_successful_inference": time.time()
                    }
            
        except Exception as e:
            metrics["service_status"] = "error"
            metrics["error"] = str(e)
        
        self.metrics_history.append(metrics)
        
        # Hoia alles ainult viimased 100 mõõdikukirjet
        if len(self.metrics_history) > 100:
            self.metrics_history = self.metrics_history[-100:]
        
        return metrics
    
    def _calculate_tokens_per_second(self, response_data: dict) -> float:
        """Calculate approximate tokens per second from response."""
        try:
            # Hinda toksoneid (ligikaudne hinnang)
            response_text = response_data.get('response', '')
            estimated_tokens = len(response_text.split())
            
            # Saada ajainfo, kui see on olemas
            eval_duration = response_data.get('eval_duration', 0)
            if eval_duration > 0:
                # Muuda nanosekundid sekunditeks
                duration_seconds = eval_duration / 1e9
                return estimated_tokens / duration_seconds if duration_seconds > 0 else 0
        except:
            pass
        return 0
    
    def check_alerts(self, current_metrics: dict) -> List[dict]:
        """Check current metrics against alert thresholds."""
        alerts = []
        
        # Kontrolli reageerimisaega
        if current_metrics.get('performance', {}).get('inference_time_ms', 0) > self.alert_thresholds['response_time_ms']:
            alerts.append({
                "type": "performance",
                "message": f"High response time: {current_metrics['performance']['inference_time_ms']:.0f}ms",
                "severity": "warning"
            })
        
        # Kontrolli teenuse olekut
        if current_metrics.get('service_status') != 'healthy':
            alerts.append({
                "type": "availability",
                "message": f"Service unhealthy: {current_metrics.get('error', 'Unknown error')}",
                "severity": "critical"
            })
        
        return alerts
    
    def get_performance_summary(self, minutes: int = 60) -> dict:
        """Get performance summary for the last N minutes."""
        cutoff_time = time.time() - (minutes * 60)
        recent_metrics = [m for m in self.metrics_history if m['timestamp'] > cutoff_time]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        # Arvuta keskmised
        response_times = [m.get('performance', {}).get('inference_time_ms', 0) 
                         for m in recent_metrics if m.get('performance')]
        
        healthy_checks = sum(1 for m in recent_metrics if m.get('service_status') == 'healthy')
        uptime_percent = (healthy_checks / len(recent_metrics)) * 100 if recent_metrics else 0
        
        return {
            "period_minutes": minutes,
            "total_checks": len(recent_metrics),
            "uptime_percent": uptime_percent,
            "avg_response_time_ms": sum(response_times) / len(response_times) if response_times else 0,
            "max_response_time_ms": max(response_times) if response_times else 0,
            "min_response_time_ms": min(response_times) if response_times else 0
        }

# Tootmise jälgimise seadistus
monitor = OllamaMonitoringService()

async def monitoring_loop():
    """Continuous monitoring loop."""
    while True:
        try:
            metrics = await monitor.collect_metrics()
            alerts = monitor.check_alerts(metrics)
            
            if alerts:
                for alert in alerts:
                    logging.warning(f"ALERT: {alert['message']} (Severity: {alert['severity']})")
            
            # Logi jõudluse kokkuvõte iga 10 minuti järel
            if int(time.time()) % 600 == 0:  # Iga 10 minuti tagant
                summary = monitor.get_performance_summary(10)
                logging.info(f"Performance Summary: {summary}")
            
        except Exception as e:
            logging.error(f"Monitoring error: {e}")
        
        await asyncio.sleep(30)  # Kontrolli iga 30 sekundi järel

# Alusta jälgimist
# asyncio.create_task(monitoring_loop())
```

#### Täiustatud konfiguratsioon ja optimeerimine

**Kohandatud mudelite haldus Ollamas**:
```python
class OllamaModelManager:
    def __init__(self, base_url: str = "http://localhost:11434"):
        self.base_url = base_url
        self.model_catalog = {
            # Kerged mudelid kiirete vastuste jaoks
            "ultra_light": [
                "qwen2.5:0.5b-instruct-q4_K_M",
                "tinyllama:1.1b-chat-q4_K_M"
            ],
            # Üldotstarbelised tasakaalustatud mudelid
            "balanced": [
                "phi3.5:3.8b-mini-instruct-q4_K_M",
                "llama3.2:3b-instruct-q4_K_M"
            ],
            # Spetsialiseeritud mudelid konkreetsete ülesannete jaoks
            "code_specialist": [
                "codellama:7b-code-q4_K_M",
                "codegemma:7b-code-q4_K_M"
            ],
            # Kõrge võimekusega mudelid
            "high_capability": [
                "llama3.1:8b-instruct-q4_K_M",
                "qwen2.5:7b-instruct-q4_K_M"
            ]
        }
    
    def setup_production_models(self, categories: List[str]) -> dict:
        """Set up models for production use."""
        setup_results = {}
        
        for category in categories:
            if category not in self.model_catalog:
                setup_results[category] = {"status": "error", "message": "Unknown category"}
                continue
            
            models = self.model_catalog[category]
            category_results = []
            
            for model in models:
                try:
                    # Tõmme mudel
                    response = requests.post(f"{self.base_url}/api/pull", 
                        json={"name": model})
                    
                    if response.status_code == 200:
                        category_results.append({"model": model, "status": "ready"})
                    else:
                        category_results.append({"model": model, "status": "failed"})
                        
                except Exception as e:
                    category_results.append({"model": model, "status": "error", "error": str(e)})
            
            setup_results[category] = category_results
        
        return setup_results
    
    def optimize_for_hardware(self) -> dict:
        """Recommend optimal models based on available hardware."""
        # Tavaliselt kontrollitakse tegelikke riistvara spetsifikatsioone
        # Demo eesmärgil simuleerime riistvara tuvastamist
        
        recommendations = {
            "low_resource": {
                "models": ["qwen2.5:0.5b-instruct-q4_K_M"],
                "max_concurrent": 1,
                "memory_usage": "< 1GB"
            },
            "medium_resource": {
                "models": ["phi3.5:3.8b-mini-instruct-q4_K_M", "llama3.2:3b-instruct-q4_K_M"],
                "max_concurrent": 2,
                "memory_usage": "2-4GB"
            },
            "high_resource": {
                "models": ["llama3.1:8b-instruct-q4_K_M", "codellama:7b-code-q4_K_M"],
                "max_concurrent": 3,
                "memory_usage": "6-12GB"
            }
        }
        
        return recommendations

# Tootmismudeli seadistamine
model_manager = OllamaModelManager()
setup_results = model_manager.setup_production_models(["balanced", "ultra_light"])
print(f"Model setup results: {setup_results}")
```

**Ollama tootmise juurutuse kontrollnimekiri**:

✅ **Teenuse konfiguratsioon**:
- Installida Ollama teenus korrektse süsteemiintegratsiooniga
- Konfigureerida mudelid konkreetsete agendikasutuste jaoks
- Seadistada korrektsed käivitusskriptid ja teenuse haldus
- Testida mudelite laadimist ja API kättesaadavust

✅ **Mudelite haldus**:
- Tõmmata vajalikud mudelid ja kontrollida terviklikkust
- Seadistada mudelite uuendamise ja ringlussevõtu protseduurid
- Konfigureerida mudelite vahemällu salvestus ja salvestusruumi optimeerimine
- Katsetada mudelite jõudlust oodatud koormuse all

✅ **Turvaseadistus**:
- Määrata tulemüürireeglid ainult kohaliku juurdepääsu jaoks
- Seadistada API juurdepääsukontrollid ja koormuse piirangud
- Rakendada agendi suhtluse auditilogimine
- Konfigureerida turvaline mudelite salvestus ja ligipääs

✅ **Jõudluse optimeerimine**:
- Võrdlusmõõdikute läbiviimine olenevalt kasutusjuhtudest
- Seadistada sobiv riistvarakiirendus
- Korraldada mudelite eelsoojenduse ja vahemällu salvestamise strateegiad
- Jälgida ressursside kasutust ja jõudlusmõõdikuid

✅ **Integratsioonitestimine**:
- Katsetada Microsoft Agent Frameworki integratsiooni
- Kontrollida võrguühenduseta töövõimekust

- Testige ülekatte stsenaariume ja veakäsitlust
- Kinnitage lõpp-lõpuni agendi töövood

**Võrdlus Foundry Localiga**:

| Funktsioon | Foundry Local | Ollama |
|---------|---------------|--------|
| **Eesmärgiks kasutusjuhtum** | Ettevõtte tootmine | Arendus ja kogukond |
| **Mudelite ökosüsteem** | Microsofti kureeritud | Ulatuslik kogukond |
| **Riistvara optimeerimine** | Automaatne (CUDA/NPU/CPU) | Käsitsi konfiguratsioon |
| **Ettevõtte funktsioonid** | Sisseehitatud järelevalve, turvalisus | Kogukonna tööriistad |
| **Lahenduse keerukus** | Lihtne (winget install) | Lihtne (curl install) |
| **API ühilduvus** | OpenAI + laiendused | OpenAI standard |
| **Tugi** | Microsofti ametlik | Kogukonna juhitud |
| **Parim valik** | Tootmise agendid | Prototüüpimine, uurimistöö |

**Millal valida Ollama**:
- **Arendus ja prototüüpimine**: Kiired eksperimendid erinevate mudelitega
- **Kogukonna mudelid**: Juurdepääs uusimatele kogukonna panustatud mudelitele
- **Hariduslik kasutus**: AI-agendi arenduse õppimine ja õpetamine
- **Uurimisprojektid**: Akadeemiline uurimistöö, mis vajab mitmekesist mudelite juurdepääsu
- **Kohandatud mudelid**: Kohandatud peenhäälestatud mudelite loomine ja testimine

### VLLM: Kõrgtasemeline SLM agentide järeldus

VLLM (Very Large Language Model inference) pakub suure läbilaskevõimega ja mälu tõhusat järeldusmootorit, mis on spetsiaalselt optimeeritud tootmise SLM-lahenduste jaoks suures mastaabis. Kui Foundry Local keskendub kasutusmugavusele ja Ollama rõhutab kogukonnamudeleid, siis VLLM paistab silma kõrgtasemelistes stsenaariumites, mis nõuavad maksimaalset läbilaskevõimet ja ressursitõhusust.

**Põhiarhitektuur ja funktsioonid**:
- **PagedAttention**: Revolutsiooniline mälu haldus efektiivseks tähelepanuarvutuseks
- **Dünaamiline partiide töötlemine**: Nutikas päringute pakkimine optimaalsete tulemuste jaoks
- **GPU optimeerimine**: Täiustatud CUDA tuumad ja teensorite paralleelsus
- **OpenAI ühilduvus**: Täielik API ühilduvus sujuvaks integreerimiseks
- **Spekulatiivne dekodeerimine**: Täiustatud järelduse kiirendusmeetodid
- **Kvantisatsiooni tugi**: INT4, INT8 ja FP16 kvantisatsioon mälu tõhususeks

#### Paigaldus ja seadistus

**Paigaldusvõimalused**:
```bash
# Standardne paigaldus
pip install vllm

# Agentide raamistike jaoks täiendavate sõltuvustega
pip install vllm[agent] openai

# Dockeri kasutuselevõtt tootmises
docker pull vllm/vllm-openai:latest

# Lõplikest funktsioonidest lähtekoodist
git clone https://github.com/vllm-project/vllm.git
cd vllm
pip install -e .
```

**Kiire algus agendi arendamiseks**:
```bash
# Käivita VLLM server SLM mudeliga
python -m vllm.entrypoints.openai.api_server \
    --model microsoft/Phi-3.5-mini-instruct \
    --trust-remote-code \
    --max-model-len 4096 \
    --gpu-memory-utilization 0.8

# Alternatiiv: Käivita Qwen2.5-iga kergete agentide jaoks
python -m vllm.entrypoints.openai.api_server \
    --model Qwen/Qwen2.5-0.5B-Instruct \
    --trust-remote-code \
    --max-model-len 2048 \
    --tensor-parallel-size 1

# Testi API lõpp-punkti
curl http://localhost:8000/v1/models

# Testi vestluse lõpetamist
curl http://localhost:8000/v1/chat/completions \
    -H "Content-Type: application/json" \
    -d '{
        "model": "microsoft/Phi-3.5-mini-instruct",
        "messages": [{"role": "user", "content": "Hello!"}]
    }'
```

#### Agendi raamistiku integratsioon

**VLLM koos Microsoft Agent Frameworkiga**:
```python
from microsoft_agent_framework import Agent, Config
import openai
import subprocess
import time
import requests
from typing import Optional, Dict, Any

class VLLMManager:
    def __init__(self, model_name: str, 
                 host: str = "localhost", 
                 port: int = 8000,
                 gpu_memory_utilization: float = 0.8,
                 max_model_len: int = 4096):
        self.model_name = model_name
        self.host = host
        self.port = port
        self.base_url = f"http://{host}:{port}"
        self.gpu_memory_utilization = gpu_memory_utilization
        self.max_model_len = max_model_len
        self.process = None
        self.client = None
        
    def start_server(self) -> bool:
        """Start VLLM server with optimized settings for agents."""
        try:
            cmd = [
                "python", "-m", "vllm.entrypoints.openai.api_server",
                "--model", self.model_name,
                "--host", self.host,
                "--port", str(self.port),
                "--gpu-memory-utilization", str(self.gpu_memory_utilization),
                "--max-model-len", str(self.max_model_len),
                "--trust-remote-code",
                "--disable-log-requests",  # Vähenda agentide logimist
                "--served-model-name", self.get_served_model_name()
            ]
            
            self.process = subprocess.Popen(cmd, 
                stdout=subprocess.PIPE, 
                stderr=subprocess.PIPE)
            
            # Oota serveri käivitumist
            max_retries = 30
            for _ in range(max_retries):
                if self.health_check():
                    self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
                    return True
                time.sleep(2)
                
            return False
            
        except Exception as e:
            print(f"Failed to start VLLM server: {e}")
            return False
    
    def get_served_model_name(self) -> str:
        """Get a clean model name for serving."""
        return self.model_name.replace("/", "--")
    
    def health_check(self) -> bool:
        """Check if VLLM server is healthy."""
        try:
            response = requests.get(f"{self.base_url}/health", timeout=5)
            return response.status_code == 200
        except:
            return False
    
    def get_openai_client(self) -> openai.OpenAI:
        """Get OpenAI-compatible client for VLLM."""
        if not self.client:
            self.client = openai.OpenAI(base_url=f"{self.base_url}/v1")
        return self.client
    
    def get_model_info(self) -> Dict[str, Any]:
        """Get model information and statistics."""
        try:
            response = requests.get(f"{self.base_url}/v1/models")
            if response.status_code == 200:
                return response.json()
        except:
            pass
        return {}
    
    def shutdown(self):
        """Shutdown VLLM server."""
        if self.process:
            self.process.terminate()
            self.process.wait()

# Algata VLLM kõrge jõudlusega agentide jaoks
vllm_manager = VLLMManager("microsoft/Phi-3.5-mini-instruct")
if vllm_manager.start_server():
    print("VLLM server started successfully")
    
    # Konfigureeri agent VLLM-i taustsüsteemiga
    agent_config = Config(
        name="vllm-performance-agent",
        model_provider="vllm",
        model_id=vllm_manager.get_served_model_name(),
        endpoint=f"{vllm_manager.base_url}/v1",
        api_key="none"  # VLLM ei vaja API-võtit
    )
    
    agent = Agent(config=agent_config)
else:
    print("Failed to start VLLM server")
```

**Kõrge läbilaskevõimega mitme agendi seade**:
```python
import asyncio
from concurrent.futures import ThreadPoolExecutor
from microsoft_agent_framework import Agent, Config
import openai

class VLLMHighThroughputManager:
    def __init__(self):
        self.model_configs = {
            "lightweight": {
                "model": "Qwen/Qwen2.5-0.5B-Instruct",
                "port": 8000,
                "max_model_len": 2048,
                "gpu_memory_utilization": 0.3
            },
            "balanced": {
                "model": "microsoft/Phi-3.5-mini-instruct",
                "port": 8001,
                "max_model_len": 4096,
                "gpu_memory_utilization": 0.5
            },
            "capable": {
                "model": "meta-llama/Llama-3.2-3B-Instruct",
                "port": 8002,
                "max_model_len": 8192,
                "gpu_memory_utilization": 0.7
            }
        }
        self.managers = {}
        self.agents = {}
        self.client_pool = {}
        
    async def initialize_all_models(self):
        """Initialize all VLLM models in parallel."""
        initialization_tasks = []
        
        for category, config in self.model_configs.items():
            task = self._initialize_model(category, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_inits = 0
        for i, result in enumerate(results):
            category = list(self.model_configs.keys())[i]
            if isinstance(result, Exception):
                print(f"Failed to initialize {category}: {result}")
            else:
                successful_inits += 1
                print(f"Successfully initialized {category} model")
        
        return successful_inits
    
    async def _initialize_model(self, category: str, config: Dict[str, Any]):
        """Initialize a single VLLM model instance."""
        manager = VLLMManager(
            model_name=config["model"],
            port=config["port"],
            max_model_len=config["max_model_len"],
            gpu_memory_utilization=config["gpu_memory_utilization"]
        )
        
        # Käivita server niidis, et vältida blokeerimist
        loop = asyncio.get_event_loop()
        with ThreadPoolExecutor() as executor:
            success = await loop.run_in_executor(executor, manager.start_server)
        
        if success:
            self.managers[category] = manager
            
            # Loo agent
            agent_config = Config(
                name=f"vllm-{category}-agent",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none"
            )
            
            self.agents[category] = Agent(config=agent_config)
            
            # Loo kliendipool kõrge läbilaskevõime jaoks
            self.client_pool[category] = [
                openai.OpenAI(base_url=f"{manager.base_url}/v1")
                for _ in range(5)  # 5 klienti iga mudeli kohta paralleelsuse tagamiseks
            ]
            
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {category}")
    
    def get_optimal_agent(self, request_complexity: str, current_load: Dict[str, int]) -> str:
        """Select optimal agent based on request complexity and current load."""
        complexity_mapping = {
            "simple": "lightweight",
            "moderate": "balanced", 
            "complex": "capable"
        }
        
        preferred_category = complexity_mapping.get(request_complexity, "balanced")
        
        # Kontrolli, kas eelistatud agent on vaba ja mitte ülekoormatud
        if (preferred_category in self.agents and 
            current_load.get(preferred_category, 0) < 10):  # Maksimaalselt 10 samaaegset päringut agendi kohta
            return preferred_category
        
        # Kasuta varukohtadena vähimalt koormatud vaba agenti
        available_agents = [(cat, load) for cat, load in current_load.items() 
                          if cat in self.agents and load < 10]
        
        if available_agents:
            return min(available_agents, key=lambda x: x[1])[0]
        
        return "balanced"  # Vaikimisi varukoht
    
    async def process_batch_requests(self, requests: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
        """Process multiple requests in parallel for maximum throughput."""
        current_load = {cat: 0 for cat in self.agents.keys()}
        tasks = []
        
        for request in requests:
            # Määra optimaalne agent
            complexity = request.get("complexity", "moderate")
            agent_category = self.get_optimal_agent(complexity, current_load)
            current_load[agent_category] += 1
            
            # Loo töötlemise ülesanne
            task = self._process_single_request(request, agent_category)
            tasks.append(task)
        
        # Töötle kõik päringud paralleelselt
        results = await asyncio.gather(*tasks, return_exceptions=True)
        
        # Vorminda tulemused
        formatted_results = []
        for i, result in enumerate(results):
            if isinstance(result, Exception):
                formatted_results.append({
                    "request_id": requests[i].get("id", i),
                    "status": "error",
                    "error": str(result)
                })
            else:
                formatted_results.append(result)
        
        return formatted_results
    
    async def _process_single_request(self, request: Dict[str, Any], agent_category: str) -> Dict[str, Any]:
        """Process a single request with the specified agent."""
        start_time = time.time()
        
        try:
            agent = self.agents[agent_category]
            response = await agent.chat_async(request["message"])
            
            processing_time = time.time() - start_time
            
            return {
                "request_id": request.get("id"),
                "status": "success",
                "response": response,
                "agent_used": agent_category,
                "processing_time": processing_time
            }
            
        except Exception as e:
            return {
                "request_id": request.get("id"),
                "status": "error",
                "error": str(e),
                "agent_used": agent_category,
                "processing_time": time.time() - start_time
            }

# Kõrge läbilaskevõime kasutamise näide
throughput_manager = VLLMHighThroughputManager()

# Algatusta kõik mudelid
initialized_count = await throughput_manager.initialize_all_models()
print(f"Initialized {initialized_count} models")

# Töötle partii päringud
batch_requests = [
    {"id": 1, "message": "Simple question", "complexity": "simple"},
    {"id": 2, "message": "Complex analysis needed", "complexity": "complex"},
    {"id": 3, "message": "Moderate difficulty task", "complexity": "moderate"}
]

results = await throughput_manager.process_batch_requests(batch_requests)
for result in results:
    print(f"Request {result['request_id']}: {result['status']} in {result.get('processing_time', 0):.2f}s")
```

#### Tootmispaigalduse mustrid

**Ettevõtte VLLM tootmisteenus**:
```python
import asyncio
import logging
import time
from typing import Dict, List, Optional
from dataclasses import dataclass
from microsoft_agent_framework import Agent, Config
import uvicorn
from fastapi import FastAPI, HTTPException, BackgroundTasks
from pydantic import BaseModel

@dataclass
class VLLMServerConfig:
    model_name: str
    port: int
    gpu_memory_utilization: float
    max_model_len: int
    tensor_parallel_size: int = 1
    quantization: Optional[str] = None

class AgentRequest(BaseModel):
    message: str
    agent_type: str = "general"
    priority: str = "normal"
    timeout: int = 30

class VLLMProductionService:
    def __init__(self, server_configs: Dict[str, VLLMServerConfig]):
        self.server_configs = server_configs
        self.managers = {}
        self.agents = {}
        self.metrics = {
            "requests_processed": 0,
            "requests_failed": 0,
            "total_processing_time": 0,
            "agent_usage": {name: 0 for name in server_configs.keys()},
            "throughput_per_minute": 0
        }
        self.request_queue = asyncio.Queue(maxsize=1000)
        self.processing_workers = []
        self.app = FastAPI(title="VLLM Agent Service")
        self._setup_routes()
        
    async def initialize_production_environment(self):
        """Initialize all VLLM servers for production."""
        logging.info("Initializing VLLM production environment...")
        
        initialization_tasks = []
        for name, config in self.server_configs.items():
            task = self._initialize_server(name, config)
            initialization_tasks.append(task)
        
        results = await asyncio.gather(*initialization_tasks, return_exceptions=True)
        
        successful_servers = 0
        for i, result in enumerate(results):
            server_name = list(self.server_configs.keys())[i]
            if isinstance(result, Exception):
                logging.error(f"Failed to initialize {server_name}: {result}")
            else:
                successful_servers += 1
                logging.info(f"Successfully initialized {server_name}")
        
        if successful_servers == 0:
            raise Exception("No VLLM servers could be initialized")
        
        # Töötlejate käivitamine
        self.processing_workers = [
            asyncio.create_task(self._processing_worker(i))
            for i in range(min(4, successful_servers))  # Maksimaalselt 4 töölist
        ]
        
        logging.info(f"Production environment ready with {successful_servers} servers")
        return successful_servers
    
    async def _initialize_server(self, name: str, config: VLLMServerConfig):
        """Initialize a single VLLM server."""
        manager = VLLMManager(
            model_name=config.model_name,
            port=config.port,
            gpu_memory_utilization=config.gpu_memory_utilization,
            max_model_len=config.max_model_len
        )
        
        # Lisa kvantimine, kui on määratud
        if config.quantization:
            # See lisatakse juhi käivitusmärguandele
            pass
        
        success = manager.start_server()
        if success:
            self.managers[name] = manager
            
            # Loo tootmisagent
            agent_config = Config(
                name=f"vllm-production-{name}",
                model_provider="vllm",
                model_id=manager.get_served_model_name(),
                endpoint=f"{manager.base_url}/v1",
                api_key="none",
                timeout=30.0
            )
            
            agent = Agent(config=agent_config)
            
            # Lisa tootmistööriistad
            self._add_production_tools(agent, name)
            
            self.agents[name] = agent
            return True
        else:
            raise Exception(f"Failed to start VLLM server for {name}")
    
    def _add_production_tools(self, agent: Agent, server_type: str):
        """Add production tools based on server type."""
        if server_type == "customer_service":
            @agent.tool
            def escalate_to_human(issue: str, customer_id: str) -> str:
                """Escalate complex issues to human agents."""
                return f"Escalated issue for customer {customer_id}: {issue}"
            
            @agent.tool
            def lookup_order_status(order_id: str) -> dict:
                """Look up order status from production database."""
                # Tootmisandmebaasi päring
                return {"order_id": order_id, "status": "shipped", "eta": "2 days"}
        
        elif server_type == "technical_support":
            @agent.tool
            def run_system_diagnostics(system_id: str) -> dict:
                """Run comprehensive system diagnostics."""
                return {"system_id": system_id, "status": "healthy", "issues": []}
            
            @agent.tool
            def create_incident_report(description: str, severity: str) -> str:
                """Create incident report in production system."""
                incident_id = f"INC-{hash(description) % 100000:05d}"
                return f"Created incident {incident_id} with severity {severity}"
    
    def _setup_routes(self):
        """Set up FastAPI routes for production service."""
        @self.app.post("/chat")
        async def chat_endpoint(request: AgentRequest, background_tasks: BackgroundTasks):
            try:
                # Lisa päring järjekorda
                await self.request_queue.put({
                    "request": request,
                    "timestamp": time.time(),
                    "future": asyncio.Future()
                })
                
                # Oota töötlemist (tähtajaga)
                result = await asyncio.wait_for(
                    self._wait_for_result(request),
                    timeout=request.timeout
                )
                
                return result
                
            except asyncio.TimeoutError:
                raise HTTPException(status_code=408, detail="Request timeout")
            except Exception as e:
                raise HTTPException(status_code=500, detail=str(e))
        
        @self.app.get("/health")
        async def health_endpoint():
            return await self.get_health_status()
        
        @self.app.get("/metrics")
        async def metrics_endpoint():
            return self.get_production_metrics()
    
    async def _processing_worker(self, worker_id: int):
        """Background worker for processing agent requests."""
        logging.info(f"Starting processing worker {worker_id}")
        
        while True:
            try:
                # Võta päring järjekorrast
                queue_item = await self.request_queue.get()
                request_data = queue_item["request"]
                request_future = queue_item["future"]
                
                # Vali sobiv agent
                agent_name = self._select_agent(request_data.agent_type)
                
                if agent_name not in self.agents:
                    request_future.set_exception(Exception(f"Agent {agent_name} not available"))
                    continue
                
                # Töötle päringut
                start_time = time.time()
                try:
                    agent = self.agents[agent_name]
                    response = await agent.chat_async(request_data.message)
                    
                    processing_time = time.time() - start_time
                    
                    # Uuenda mõõdikuid
                    self.metrics["requests_processed"] += 1
                    self.metrics["total_processing_time"] += processing_time
                    self.metrics["agent_usage"][agent_name] += 1
                    
                    result = {
                        "response": response,
                        "agent_used": agent_name,
                        "processing_time": processing_time,
                        "worker_id": worker_id
                    }
                    
                    request_future.set_result(result)
                    
                except Exception as e:
                    self.metrics["requests_failed"] += 1
                    request_future.set_exception(e)
                
                finally:
                    self.request_queue.task_done()
                    
            except Exception as e:
                logging.error(f"Worker {worker_id} error: {e}")
                await asyncio.sleep(1)
    
    def _select_agent(self, agent_type: str) -> str:
        """Select appropriate agent based on request type."""
        agent_mapping = {
            "customer_service": "customer_service",
            "technical": "technical_support",
            "general": "general_purpose"
        }
        
        return agent_mapping.get(agent_type, "general_purpose")
    
    async def _wait_for_result(self, request: AgentRequest):
        """Wait for request processing to complete."""
        # See on lihtsustatud - tootmises jälgitakse tulevikke õigesti
        await asyncio.sleep(0.1)  # Asetäitja
        return {"response": "Processed", "status": "success"}
    
    async def get_health_status(self) -> dict:
        """Get comprehensive health status of all services."""
        health_status = {
            "overall_status": "healthy",
            "servers": {},
            "queue_size": self.request_queue.qsize(),
            "active_workers": len([w for w in self.processing_workers if not w.done()]),
            "timestamp": time.time()
        }
        
        unhealthy_servers = 0
        for name, manager in self.managers.items():
            try:
                is_healthy = manager.health_check()
                health_status["servers"][name] = {
                    "status": "healthy" if is_healthy else "unhealthy",
                    "endpoint": manager.base_url,
                    "model": manager.model_name
                }
                if not is_healthy:
                    unhealthy_servers += 1
            except Exception as e:
                health_status["servers"][name] = {
                    "status": "error",
                    "error": str(e)
                }
                unhealthy_servers += 1
        
        if unhealthy_servers > 0:
            health_status["overall_status"] = "degraded" if unhealthy_servers < len(self.managers) else "unhealthy"
        
        return health_status
    
    def get_production_metrics(self) -> dict:
        """Get production performance metrics."""
        total_requests = self.metrics["requests_processed"] + self.metrics["requests_failed"]
        avg_processing_time = (
            self.metrics["total_processing_time"] / self.metrics["requests_processed"]
            if self.metrics["requests_processed"] > 0 else 0
        )
        
        success_rate = (
            self.metrics["requests_processed"] / total_requests * 100
            if total_requests > 0 else 0
        )
        
        return {
            "total_requests": total_requests,
            "successful_requests": self.metrics["requests_processed"],
            "failed_requests": self.metrics["requests_failed"],
            "success_rate_percent": success_rate,
            "average_processing_time_seconds": avg_processing_time,
            "agent_usage_distribution": self.metrics["agent_usage"],
            "queue_size": self.request_queue.qsize()
        }
    
    async def start_production_server(self, host: str = "0.0.0.0", port: int = 8080):
        """Start the production FastAPI server."""
        config = uvicorn.Config(
            self.app,
            host=host,
            port=port,
            log_level="info",
            workers=1  # Üks töötaja lihtsuse huvides
        )
        server = uvicorn.Server(config)
        await server.serve()

# Tootmise juurutamise näide
production_configs = {
    "customer_service": VLLMServerConfig(
        model_name="microsoft/Phi-3.5-mini-instruct",
        port=8000,
        gpu_memory_utilization=0.4,
        max_model_len=4096
    ),
    "technical_support": VLLMServerConfig(
        model_name="meta-llama/Llama-3.2-3B-Instruct",
        port=8001,
        gpu_memory_utilization=0.6,
        max_model_len=8192
    ),
    "general_purpose": VLLMServerConfig(
        model_name="Qwen/Qwen2.5-1.5B-Instruct",
        port=8002,
        gpu_memory_utilization=0.3,
        max_model_len=2048
    )
}

production_service = VLLMProductionService(production_configs)

# Algata ja käivita tootmisteenus
# oota tootmisteenus.initialize_production_environment()
# oota tootmisteenus.start_production_server()
```

#### Ettevõtte funktsioonid ja järelevalve

**Täiustatud VLLM jõudluse jälgimine**:
```python
import psutil
import nvidia_ml_py3 as nvml
from dataclasses import dataclass
from typing import List, Dict, Optional
import json
import asyncio

@dataclass
class PerformanceMetrics:
    timestamp: float
    requests_per_second: float
    average_latency_ms: float
    gpu_utilization_percent: float
    gpu_memory_used_gb: float
    cpu_utilization_percent: float
    memory_used_gb: float
    queue_length: int
    active_requests: int

class VLLMAdvancedMonitoring:
    def __init__(self, vllm_managers: Dict[str, VLLMManager]):
        self.managers = vllm_managers
        self.metrics_history = []
        self.alert_thresholds = {
            "gpu_utilization_max": 95,
            "gpu_memory_max_gb": 10,
            "latency_max_ms": 3000,
            "queue_length_max": 50,
            "error_rate_max_percent": 10
        }
        
        # Algata NVIDIA ML GPU jälgimiseks
        try:
            nvml.nvmlInit()
            self.gpu_monitoring_available = True
            self.gpu_count = nvml.nvmlDeviceGetCount()
        except:
            self.gpu_monitoring_available = False
            self.gpu_count = 0
    
    async def collect_comprehensive_metrics(self) -> Dict[str, PerformanceMetrics]:
        """Collect detailed performance metrics for all VLLM instances."""
        all_metrics = {}
        
        for name, manager in self.managers.items():
            try:
                metrics = await self._collect_single_instance_metrics(name, manager)
                all_metrics[name] = metrics
            except Exception as e:
                logging.error(f"Failed to collect metrics for {name}: {e}")
                # Loo veamõõdikud
                all_metrics[name] = PerformanceMetrics(
                    timestamp=time.time(),
                    requests_per_second=0,
                    average_latency_ms=0,
                    gpu_utilization_percent=0,
                    gpu_memory_used_gb=0,
                    cpu_utilization_percent=0,
                    memory_used_gb=0,
                    queue_length=0,
                    active_requests=0
                )
        
        return all_metrics
    
    async def _collect_single_instance_metrics(self, name: str, manager: VLLMManager) -> PerformanceMetrics:
        """Collect metrics for a single VLLM instance."""
        timestamp = time.time()
        
        # Saa VLLM-spetsiifilisi mõõdikuid API kaudu
        vllm_stats = await self._get_vllm_stats(manager)
        
        # Saa süsteemi mõõdikud
        cpu_percent = psutil.cpu_percent(interval=0.1)
        memory_info = psutil.virtual_memory()
        memory_used_gb = memory_info.used / (1024**3)
        
        # Saa GPU mõõdikud, kui need on saadaval
        gpu_utilization = 0
        gpu_memory_used = 0
        
        if self.gpu_monitoring_available and self.gpu_count > 0:
            try:
                # Lihtsuse huvides eeldades esimest GPU-d
                handle = nvml.nvmlDeviceGetHandleByIndex(0)
                gpu_util = nvml.nvmlDeviceGetUtilizationRates(handle)
                gpu_utilization = gpu_util.gpu
                
                gpu_mem = nvml.nvmlDeviceGetMemoryInfo(handle)
                gpu_memory_used = gpu_mem.used / (1024**3)
                
            except Exception as e:
                logging.warning(f"GPU monitoring failed: {e}")
        
        return PerformanceMetrics(
            timestamp=timestamp,
            requests_per_second=vllm_stats.get("requests_per_second", 0),
            average_latency_ms=vllm_stats.get("average_latency_ms", 0),
            gpu_utilization_percent=gpu_utilization,
            gpu_memory_used_gb=gpu_memory_used,
            cpu_utilization_percent=cpu_percent,
            memory_used_gb=memory_used_gb,
            queue_length=vllm_stats.get("queue_length", 0),
            active_requests=vllm_stats.get("active_requests", 0)
        )
    
    async def _get_vllm_stats(self, manager: VLLMManager) -> dict:
        """Get VLLM-specific statistics via API calls."""
        try:
            # Testi järeldamist latentsuse mõõtmiseks
            start_time = time.time()
            client = manager.get_openai_client()
            
            response = await asyncio.wait_for(
                asyncio.to_thread(
                    client.chat.completions.create,
                    model=manager.get_served_model_name(),
                    messages=[{"role": "user", "content": "ping"}],
                    max_tokens=1
                ),
                timeout=5.0
            )
            
            latency_ms = (time.time() - start_time) * 1000
            
            return {
                "average_latency_ms": latency_ms,
                "requests_per_second": 1000 / latency_ms if latency_ms > 0 else 0,
                "queue_length": 0,  # Peaks olema VLLMi poolt eksponeeritud
                "active_requests": 1  # Hinnanguline väärtus
            }
            
        except Exception as e:
            logging.warning(f"Failed to get VLLM stats: {e}")
            return {
                "average_latency_ms": 0,
                "requests_per_second": 0,
                "queue_length": 0,
                "active_requests": 0
            }
    
    def generate_performance_report(self, time_window_minutes: int = 60) -> dict:
        """Generate comprehensive performance report."""
        cutoff_time = time.time() - (time_window_minutes * 60)
        recent_metrics = [
            metrics for metrics in self.metrics_history
            if any(m.timestamp > cutoff_time for m in metrics.values())
        ]
        
        if not recent_metrics:
            return {"error": "No recent metrics available"}
        
        report = {
            "time_window_minutes": time_window_minutes,
            "total_samples": len(recent_metrics),
            "instances": {}
        }
        
        # Analüüsi iga eksemplari
        for instance_name in self.managers.keys():
            instance_metrics = [
                metrics[instance_name] for metrics in recent_metrics
                if instance_name in metrics
            ]
            
            if instance_metrics:
                report["instances"][instance_name] = {
                    "avg_latency_ms": sum(m.average_latency_ms for m in instance_metrics) / len(instance_metrics),
                    "max_latency_ms": max(m.average_latency_ms for m in instance_metrics),
                    "avg_gpu_utilization": sum(m.gpu_utilization_percent for m in instance_metrics) / len(instance_metrics),
                    "avg_requests_per_second": sum(m.requests_per_second for m in instance_metrics) / len(instance_metrics),
                    "max_queue_length": max(m.queue_length for m in instance_metrics),
                    "availability_percent": (len(instance_metrics) / len(recent_metrics)) * 100
                }
        
        return report
    
    async def auto_scaling_recommendations(self) -> List[dict]:
        """Generate auto-scaling recommendations based on performance metrics."""
        recommendations = []
        
        if not self.metrics_history:
            return recommendations
        
        latest_metrics = self.metrics_history[-1]
        
        for instance_name, metrics in latest_metrics.items():
            # Soovitus kõrge latentsuse korral
            if metrics.average_latency_ms > self.alert_thresholds["latency_max_ms"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_up",
                    "reason": f"High latency: {metrics.average_latency_ms:.0f}ms",
                    "suggestion": "Consider adding tensor parallelism or increasing GPU memory"
                })
            
            # Soovitus kõrge GPU kasutuse korral
            if metrics.gpu_utilization_percent > self.alert_thresholds["gpu_utilization_max"]:
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_out",
                    "reason": f"High GPU utilization: {metrics.gpu_utilization_percent:.1f}%",
                    "suggestion": "Consider adding additional GPU instances"
                })
            
            # Soovitus madala kasutuse korral
            if (metrics.gpu_utilization_percent < 20 and 
                metrics.requests_per_second < 1):
                recommendations.append({
                    "instance": instance_name,
                    "type": "scale_down",
                    "reason": f"Low utilization: {metrics.gpu_utilization_percent:.1f}% GPU, {metrics.requests_per_second:.1f} RPS",
                    "suggestion": "Consider consolidating workloads or reducing resources"
                })
        
        return recommendations

# Täiustatud jälgimise seadistus
monitoring = VLLMAdvancedMonitoring({
    "customer_service": vllm_manager,
    # Lisa vajadusel teisi haldureid
})

async def advanced_monitoring_loop():
    """Advanced monitoring with auto-scaling recommendations."""
    while True:
        try:
            # Kogu mõõdikuid
            metrics = await monitoring.collect_comprehensive_metrics()
            monitoring.metrics_history.append(metrics)
            
            # Hoia alles vaid viimased 1000 kirjet
            if len(monitoring.metrics_history) > 1000:
                monitoring.metrics_history = monitoring.metrics_history[-1000:]
            
            # Genereeri soovitused iga 5 minuti järel
            if len(monitoring.metrics_history) % 10 == 0:  # Iga 10. kogumine (5 minutit, kui kogumine toimub iga 30 sekundi järel)
                recommendations = await monitoring.auto_scaling_recommendations()
                
                if recommendations:
                    logging.info(f"Auto-scaling recommendations: {recommendations}")
            
            # Genereeri jõudlusaruanne iga tunni järel
            if len(monitoring.metrics_history) % 120 == 0:  # Iga 120. kogumine (1 tund)
                report = monitoring.generate_performance_report(60)
                logging.info(f"Performance report: {json.dumps(report, indent=2)}")
            
        except Exception as e:
            logging.error(f"Advanced monitoring error: {e}")
        
        await asyncio.sleep(30)  # Kogu mõõdikuid iga 30 sekundi järel

# Alusta täiustatud jälgimist
# asyncio.create_task(advanced_monitoring_loop())
```

#### Täiustatud konfiguratsioon ja optimeerimine

**Tootmise VLLM konfiguratsiooni mallid**:
```python
from enum import Enum
from typing import Dict, Any

class DeploymentScenario(Enum):
    DEVELOPMENT = "development"
    STAGING = "staging"
    PRODUCTION_LOW = "production_low"
    PRODUCTION_HIGH = "production_high"
    ENTERPRISE = "enterprise"

class VLLMConfigTemplates:
    """Production-ready VLLM configuration templates."""
    
    @staticmethod
    def get_config_template(scenario: DeploymentScenario) -> Dict[str, Any]:
        """Get optimized configuration for deployment scenario."""
        
        templates = {
            DeploymentScenario.DEVELOPMENT: {
                "gpu_memory_utilization": 0.6,
                "max_model_len": 2048,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": None,
                "enable_prefix_caching": False,
                "max_num_seqs": 32,
                "max_num_batched_tokens": 2048
            },
            
            DeploymentScenario.STAGING: {
                "gpu_memory_utilization": 0.8,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 64,
                "max_num_batched_tokens": 4096
            },
            
            DeploymentScenario.PRODUCTION_LOW: {
                "gpu_memory_utilization": 0.85,
                "max_model_len": 4096,
                "tensor_parallel_size": 1,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 128,
                "max_num_batched_tokens": 8192,
                "enable_chunked_prefill": True
            },
            
            DeploymentScenario.PRODUCTION_HIGH: {
                "gpu_memory_utilization": 0.9,
                "max_model_len": 8192,
                "tensor_parallel_size": 2,
                "pipeline_parallel_size": 1,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 256,
                "max_num_batched_tokens": 16384,
                "enable_chunked_prefill": True,
                "speculative_model": "small_draft_model"
            },
            
            DeploymentScenario.ENTERPRISE: {
                "gpu_memory_utilization": 0.95,
                "max_model_len": 16384,
                "tensor_parallel_size": 4,
                "pipeline_parallel_size": 2,
                "quantization": "awq",
                "enable_prefix_caching": True,
                "max_num_seqs": 512,
                "max_num_batched_tokens": 32768,
                "enable_chunked_prefill": True,
                "speculative_model": "optimized_draft_model",
                "guided_decoding_backend": "outlines"
            }
        }
        
        return templates[scenario]
    
    @staticmethod
    def generate_vllm_command(model_name: str, 
                             scenario: DeploymentScenario,
                             port: int = 8000,
                             host: str = "0.0.0.0") -> List[str]:
        """Generate optimized VLLM command for deployment scenario."""
        
        config = VLLMConfigTemplates.get_config_template(scenario)
        
        cmd = [
            "python", "-m", "vllm.entrypoints.openai.api_server",
            "--model", model_name,
            "--host", host,
            "--port", str(port),
            "--gpu-memory-utilization", str(config["gpu_memory_utilization"]),
            "--max-model-len", str(config["max_model_len"]),
            "--tensor-parallel-size", str(config["tensor_parallel_size"]),
            "--max-num-seqs", str(config["max_num_seqs"]),
            "--max-num-batched-tokens", str(config["max_num_batched_tokens"]),
            "--trust-remote-code",
            "--disable-log-requests"
        ]
        
        # Lisa valikulised parameetrid
        if config.get("quantization"):
            cmd.extend(["--quantization", config["quantization"]])
        
        if config.get("enable_prefix_caching"):
            cmd.append("--enable-prefix-caching")
        
        if config.get("enable_chunked_prefill"):
            cmd.append("--enable-chunked-prefill")
        
        if config.get("pipeline_parallel_size", 1) > 1:
            cmd.extend(["--pipeline-parallel-size", str(config["pipeline_parallel_size"])])
        
        if config.get("speculative_model"):
            cmd.extend(["--speculative-model", config["speculative_model"]])
        
        return cmd

# Kasutuse näited
dev_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.DEVELOPMENT,
    port=8000
)

prod_cmd = VLLMConfigTemplates.generate_vllm_command(
    "microsoft/Phi-3.5-mini-instruct",
    DeploymentScenario.PRODUCTION_HIGH,
    port=8001
)

print(f"Development command: {' '.join(dev_cmd)}")
print(f"Production command: {' '.join(prod_cmd)}")
```

**Tootmise paigalduse kontrollnimekiri VLLM-i jaoks**:

✅ **Riistvara optimeerimine**:
- Seadista teensoriparalleelsus mitme GPU jaoks
- Luba kvantisatsioon (AWQ/GPTQ) mälu tõhususeks
- Seo optimaalne GPU mälu kasutus (85-95%)
- Määra sobivad partiide suurused läbilaskevõime jaoks

✅ **Jõudluse häälestus**:
- Luba eelfikseerimine korduvate päringute jaoks
- Konfigureeri tükeldatud täitumine pikkade järjekordade jaoks
- Seadista spekulatiivne dekodeerimine kiiremaks järelduseks
- Optimeeri max_num_seqs riistvara põhjal

✅ **Tootmise funktsioonid**:
- Seadista tervise jälgimine ja mõõdikute kogumine
- Konfigureeri automaatne taaskäivitamine ja ülekatte (failover) mehhanism
- Rakenda päringute järjekorraldamine ja koormuse tasakaalustamine
- Määra ulatuslik logimine ja hoiatussüsteem

✅ **Turvalisus ja usaldusväärsus**:
- Konfigureeri tulemüüri reeglid ja juurdepääsukontrollid
- Seadista API päringute kiiruse piiramine ja autentimine
- Rakenda korrapärast sulgemist ja puhastamist
- Määra varundamine ja katastroofideks taastumine

✅ **Integratsiooni testimine**:
- Testi Microsoft Agent Frameworki integratsiooni
- Kinnita suure mahuga stsenaariumid
- Testi ülekatte ja taastumise protseduurid
- Võrdle jõudlust koormuse all

**Võrdlus teiste lahendustega**:

| Funktsioon | VLLM | Foundry Local | Ollama |
|---------|------|---------------|--------|
| **Eesmärk kasutamiseks** | Suure läbilaskevõimega tootmine | Ettevõtte kasutusmugavus | Arendus ja kogukond |
| **Jõudlus** | Maksimaalne läbilaskevõime | Tasakaalustatud | Hea |
| **Mälu tõhusus** | PagedAttention optimeerimine | Automaatne optimeerimine | Standard |
| **Seadistuse keerukus** | Kõrge (palju parameetreid) | Madal (automaatne) | Madal (lihtne) |
| **Skaalautuvus** | Suurepärane (teensor/pipeline paralleelsus) | Hea | Piiratud |
| **Kvantisatsioon** | Täiustatud (AWQ, GPTQ, FP8) | Automaatne | Standard GGUF |
| **Ettevõtte funktsioonid** | Vajalik kohandatud lahendus | Sisseehitatud | Kogukonna tööriistad |
| **Parim valik** | Suuremastaabilised tootmise agendid | Ettevõtte tootmine | Arendus |

**Millal valida VLLM**:
- **Suure läbilaskevõime nõuded**: Sajad päringud sekundis
- **Suumölulised paigaldised**: Mitme GPU ja sõlmega lahendused
- **Jõudlusoluline**: All sekundiline vastusajakohandu
- **Täiustatud optimeerimine**: Vajadus kohandatud kvantisatsiooni ja partiide töötlemise järele
- **Ressursitõhusus**: Maksimaalne kallite GPU-de kasutamine


## Tõelised SLM agentide rakendused

### Klienditeeninduse SLM agendid
- **SLM võimed**: Konto vaatamised, parooli lähtestamine, tellimuse staatuse kontroll
- **Kulude kokkuhoid**: 10x madalam järelduskulu võrreldes LLM agentidega
- **Jõudlus**: Kiirem vastus koos järjekindla kvaliteediga rutiinsete päringute korral

### Äriprotsesside SLM agendid
- **Arve töötlemise agendid**: Andmete väljavõtmine, info valideerimine, suunamine kinnitamiseks
- **E-posti haldamise agendid**: Kategooriate loomine, prioriseerimine, automaatsete vastuste koostamine
- **Ajakava agendid**: Koordineerib koosolekuid, haldab kalendreid, saadab meeldetuletusi

### Isiklikud SLM digitaalassistentid
- **Ülesannete haldamise agendid**: Loob, uuendab, organiseerib tegevuste nimekirju tõhusalt
- **Info kogumise agendid**: Tehke kohalikke uurimusi ja kokkuvõtteid teemadel
- **Kommunikatsiooni agendid**: Koostab privaatseid e-kirju, sõnumeid, sotsiaalmeedia postitusi

### Kauplemise ja finantsagendid SLM
- **Turujälgimise agendid**: Jälgib hindu ja trende reaalajas
- **Aruannete loomise agendid**: Loob automaatselt päevaste/nädalaste kokkuvõtete
- **Riskihindamise agendid**: Hinnang portfelli positsioonidele kohalike andmete põhjal

### Tervishoiutoe SLM agendid
- **Patsiendi ajakava agendid**: Koordineerib kohtumisi, saadab automaatseid meeldetuletusi
- **Dokumentatsiooni agendid**: Koostab meditsiinilisi kokkuvõtteid ja raporteid kohapeal
- **Retseptide haldamise agendid**: Jälgib uuendusi, kontrollib koostoimeid privaatselt

## Microsoft Agent Framework: tootmisvalmis agendi arendus

### Ülevaade ja arhitektuur

Microsoft Agent Framework pakub terviklikku, ettevõtte tasemel platvormi AI agentide loomisel, juurutamisel ja haldamisel, mis suudavad töötada nii pilves kui võrguvälises ääres (edge). Raamistik on spetsiaalselt loodud tõhusaks tööks väikeste keeltemudelite ja äärearvutuse stsenaariumitega, sobides ideaalselt privaatsusele tundlike ja piiratud ressurssidega juurutustega.

**Põhiraki komponendid**:
- **Agendi ümbritsev keskkond**: Kergekaaluline täitmise keskkond, optimeeritud ääreseadmeid silmas pidades
- **Tööriistade integratsioonisüsteem**: Laiendatav pistikprogrammi arhitektuur väliste teenuste ja API-de ühendamiseks
- **Oleku haldus**: Püsiv agendi mälu ja konteksti haldus sessioonide vahel
- **Turvakihid**: Sisseehitatud turvakontrollid ettevõtte paigalduste jaoks
- **Orkestreerimismootor**: Mitme agendi koordineerimine ja töövoo haldus

### Äärepaigalduse peamised omadused

**Võrguta-töö peamine arhitektuur**: Microsoft Agent Framework on loodud võrguvaba töö esimese põhimõttega, võimaldades agentidel töötada tõhusalt ilma pideva internetiühenduseta. See hõlmab kohaliku mudelite järeldamist, vahemällu salvestatud teadmiste baase, võrguväliseid tööriistade täitmisi ning sujuvat delegeerimist, kui pilveteenused pole saadaval.

**Ressursside optimeerimine**: Raamistik pakub nutikat ressursside haldust, automaatset mälu optimeerimist SLM-idel, CPU/GPU koormuse tasakaalustamist ääreseadmetel, kohanduvat mudelite valikut vastavalt saadavusele ning energiatõhusaid järeldusmustreid mobiilseadmetel.

**Turvalisus ja privaatsus**: Ettevõtte tasemel turvafunktsioonid hõlmavad kohalikku andmetöötlust privaatsuse tagamiseks, krüpteeritud agendisuhtlust, rollipõhist juurdepääsukontrolli agentide funktsioonidele ja audiitorajastust vastavusnõuete täitmiseks.

### Integratsioon Foundry Localiga

Microsoft Agent Framework integreerub sujuvalt Foundry Localiga, pakkudes täieliku ääre AI lahendust:

**Automaatne mudelite avastamine**: Raamistik tuvastab automaatselt Foundry Locali instantsid, avastab saadavalolevad SLM mudelid ja valib optimaalsed mudelid vastavalt agendi nõuetele ning riistvara võimekustele.

**Dünaamiline mudelite laadimine**: Agendid saavad dünaamiliselt laadida erinevaid SLM-e konkreetsete ülesannete jaoks, võimaldades mitmemudelist agentide süsteeme, kus erinevad mudelid tegelevad erinevat tüüpi päringutega ja automaatse ülekattega mudelite vahel vastavalt saadavusele ja jõudlusele.

**Jõudluse optimeerimine**: Integreeritud vahemälud vähendavad mudelite laadimisaegu, ühenduste haldamine optimeerib API kõnesid Foundry Localile ja nutikas partiide töötlemine parandab paljude agentide päringute läbilaskevõimet.

### Agentide loomine Microsoft Agent Frameworkiga

#### Agendi määratlus ja konfiguratsioon

```python
from microsoft_agent_framework import Agent, Tool, Config
from foundry_local import FoundryLocalManager

# Seadista agent Foundry Local integreerimisega
config = Config(
    name="customer-service-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    max_tokens=512,
    temperature=0.1,
    offline_mode=True
)

# Algata Foundry Local ühendus
foundry = FoundryLocalManager("phi-4-mini")

# Loo agendi eksemplar
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)
```

#### Tööriistade integratsioon ääre stsenaariumides

```python
# Määratle tööriistad võrguühenduseta tööks
@agent.tool
def lookup_customer_info(customer_id: str) -> dict:
    """Look up customer information from local database."""
    # Kohalik andmebaasi päring - töötab võrguühenduseta
    return local_db.get_customer(customer_id)

@agent.tool
def create_support_ticket(issue: str, priority: str) -> str:
    """Create a support ticket in local system."""
    # Kohalik piletite loomine koos sünkroonimisega võrku jõudes
    ticket_id = local_system.create_ticket(issue, priority)
    return f"Ticket {ticket_id} created successfully"

@agent.tool
def schedule_callback(customer_id: str, preferred_time: str) -> str:
    """Schedule a callback for the customer."""
    # Kohalik ajastamine koos kalendri integratsiooniga
    return local_calendar.schedule(customer_id, preferred_time)
```

#### Mitme agendi orkestreerimine

```python
from microsoft_agent_framework import AgentOrchestrator

# Loo spetsialiseerunud agendid erinevates valdkondades
scheduling_agent = Agent(
    config=Config(
        name="scheduling-agent",
        model_alias="qwen2.5-0.5b",  # Kergekaaluline lihtsate ülesannete jaoks
        specialized_for="scheduling"
    )
)

technical_support_agent = Agent(
    config=Config(
        name="technical-agent",
        model_alias="phi-4-mini",  # Võimsam keerukamate probleemide korral
        specialized_for="technical_support"
    )
)

# Koordineeri mitut agenti
orchestrator = AgentOrchestrator([
    scheduling_agent,
    technical_support_agent
])

# Suuna päringud kavatsuse põhjal
result = orchestrator.process_request(
    "I need to schedule a callback for a technical issue",
    routing_strategy="intent-based"
)
```

### Täiustatud ääre paigalduse mustrid

#### Hierarhiline agendi arhitektuur

**Kohalikud agendi klastrid**: Paigaldage mitu spetsialiseerunud SLM agenti ääreseadmetele, igaüks optimeeritud konkreetsete ülesannete jaoks. Kasutage kergekaalulisi mudeleid nagu Qwen2.5-0.5B lihtsaks marsruutimiseks ja ajakava haldamiseks, keskmisi mudeleid nagu Phi-4-Mini klienditeeninduseks ja dokumenteerimiseks ning suuremaid mudeleid keerukaks mõtlemiseks, kui ressursid lubavad.

**Äär-pilve koordineerimine**: Rakendage nutikaid eskaleerimismustreid, kus kohalikud agendid tegelevad rutiinsete ülesannetega, pilveagendid pakuvad keerukat mõtlemist, kui ühendus on võimalik ning sujuv ülekanne säilitab töö järjepidevuse.

#### Paigalduse konfiguratsioonid

**Ühe seadme paigaldus**:
```yaml
deployment:
  type: single-device
  hardware: edge-device
  models:
    - alias: "phi-4-mini"
      primary: true
      tasks: ["conversation", "reasoning"]
    - alias: "qwen2.5-0.5b"
      secondary: true
      tasks: ["routing", "classification"]
  agents:
    - name: "primary-agent"
      model: "phi-4-mini"
      tools: ["database", "calendar", "email"]
```

**Jaotatud ääre paigaldus**:
```yaml
deployment:
  type: distributed-edge
  nodes:
    - id: "edge-1"
      agents: ["customer-service", "scheduling"]
      models: ["phi-4-mini"]
    - id: "edge-2"
      agents: ["technical-support", "documentation"]
      models: ["qwen2.5-coder-0.5b"]
  coordination:
    load_balancing: true
    failover: automatic
```

### Jõudluse optimeerimine ääre agentide jaoks

#### Mudeli valiku strateegiad

**Ülesandel põhinev mudeli määramine**: Microsoft Agent Framework võimaldab intelligentset mudeli valikut vastavalt ülesande keerukusele ja nõudmistele:

- **Lihtsad ülesanded** (küsimused-vastused, marsruutimine): Qwen2.5-0.5B (500MB, <100ms vastus)
- **Keskmised ülesanded** (klienditeenindus, ajastamine): Phi-4-Mini (2.4GB, 200-500ms vastus)
- **Keerukad ülesanded** (tehniline analüüs, planeerimine): Phi-4 (7GB, 1-3s vastus kui ressursid lubavad)

**Dünaamiline mudelivahetus**: Agendid saavad vahetada mudeleid sõltuvalt süsteemi koormusest, ülesande keerukuse hindamisest, kasutaja prioriteedist ja riistvara saadavusest.

#### Mälu ja ressursside haldus

```python
# Konfigureeri ressursipiirangud servale juurutamiseks
resource_config = ResourceConfig(
    max_memory_usage="4GB",
    max_concurrent_agents=3,
    model_cache_size="2GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

### Ettevõtte integratsiooni mustrid

#### Turvalisus ja nõuetele vastavus

**Kohalik andmetöötlus**: Kõik agendi protsessid toimuvad kohapeal, tagades, et tundlikud andmed ei lahku ääreseadmest. See hõlmab kliendiinfo kaitset, HIPAA nõuetele vastamist tervishoiu agentide jaoks, finantsandmete turvalisust panganduse jaoks ja GDPR nõuete täitmist Euroopa juurutuste puhul.

**Juurdepääsukontroll**: Rollipõhised õigused kontrollivad, millistele tööriistadele agendid ligi pääsevad, kasutajate autentimine agendi interaktsioonides ja auditilogid kõigi agendi tegevuste ja otsuste kohta.

#### Jälgimine ja nähtavus

```python
from microsoft_agent_framework import AgentMonitor

# Häälestage servaagentide jälgimine
monitor = AgentMonitor(
    metrics=["response_time", "success_rate", "resource_usage"],
    alerts=[
        {"metric": "response_time", "threshold": "2s", "action": "scale_down_model"},
        {"metric": "memory_usage", "threshold": "80%", "action": "unload_idle_agents"}
    ],
    local_storage=True  # Salvestage mõõdikud kohapeal võrguvabas režiimis töötamiseks
)

agent.add_monitor(monitor)
```

### Tõelised rakenduse näited

#### Jaemüügi ääre agendi süsteem

```python
# Jaekioski esindaja poe sees klienditoeks
retail_agent = Agent(
    config=Config(
        name="retail-assistant",
        model_alias="phi-4-mini",
        context="You are a helpful retail assistant in an electronics store."
    )
)

@retail_agent.tool
def check_inventory(product_sku: str) -> dict:
    """Check local inventory for a product."""
    return local_inventory.lookup(product_sku)

@retail_agent.tool
def find_alternatives(product_category: str) -> list:
    """Find alternative products in the same category."""
    return local_catalog.find_similar(product_category)

@retail_agent.tool
def create_price_quote(items: list) -> dict:
    """Generate a price quote for multiple items."""
    return pricing_engine.calculate_quote(items)
```

#### Tervishoiutoe agent

```python
# HIPAA-nõuetele vastav patsienditoe agent
healthcare_agent = Agent(
    config=Config(
        name="patient-support",
        model_alias="phi-4-mini",
        privacy_mode=True,  # Täiustatud privaatsus tervishoius
        compliance=["HIPAA"]
    )
)

@healthcare_agent.tool
def check_appointment_availability(provider_id: str, date_range: str) -> list:
    """Check appointment slots with healthcare provider."""
    return local_scheduling.get_availability(provider_id, date_range)

@healthcare_agent.tool
def access_patient_portal(patient_id: str, auth_token: str) -> dict:
    """Secure access to patient information."""
    if security.validate_token(auth_token):
        return patient_portal.get_summary(patient_id)
    return {"error": "Authentication failed"}
```

### Parimad praktikad Microsoft Agent Frameworki jaoks

#### Arendusjuhendid

1. **Alusta lihtsalt**: Alusta ühe agendi stsenaariumitest enne keerukate mitme agendi süsteemide loomist
2. **Sobita mudeli suurus**: Vali väikseim mudel, mis rahuldab täpsusnõudeid
3. **Tööriistade kujundus**: Loo fookustatud, üheotstarbelised tööriistad, mitte keerulised multifunktsionaalsed tööriistad
4. **Veakäsitlus**: Rakenda võrguvabade stsenaariumite ja mudeliriketega sujuv degratsioon
5. **Testimine**: Testi agente põhjalikult võrguvälises ja piiratud ressurssidega keskkonnas

#### Paigalduse parimad tavad

1. **Järk-järguline juurutus**: Paigalda esmalt väikestele kasutajagruppidele ning jälgi jõudlusmärke tähelepanelikult
2. **Ressursside jälgimine**: Sea häired mälu, CPU ja vastuseaja künniste jaoks
3. **Varuplaanid**: Oma alati varuplaanid mudeliriketeks või ressursside ammendumiseks
4. **Turvalisus esikohal**: Rakenda turvameetmed algusest peale, mitte hiljem lisatult
5. **Dokumentatsioon**: Säilita selge dokumentatsioon agendi võimetest ja piirangutest

### Tulevikuplaan ja integratsioon

Microsoft Agent Framework areneb jätkuvalt, pakkudes paremat SLM optimeerimist, täiustatud äärealade tööriistu, paremat ressursside haldust piiratud keskkondades ja laiendatud tööriistade ökosüsteemi tavaliste ettevõtte stsenaariumide jaoks.

**Tulevased funktsioonid**:
- **AutoML agendi optimeerimiseks**: SLM-de automaatne peenhäälestus konkreetseteks agendi ülesanneteks
- **Ääre võrguvõrgu tihendamine**: Koordineerimine mitme äär-agendi paigalduse vahel
- **Täiustatud telemeetria**: Paranenud jälgimine ja analüütika agendi jõudluse jaoks
- **Visuaalne agendi ehitaja**: Madalama koodi / koodivabad agendi arendusvahendid

## Parimad praktikad SLM agendi rakendamisel

### SLM valimise juhised agentide jaoks

SLM-e valides arvestage järgmisi tegureid:

**Mudeli suuruse kaalutlused**: Valige äärmiselt tihendatud mudeleid nagu Q2_K mobiilsete agendi rakenduste jaoks, tasakaalustatud mudeleid nagu Q4_K_M üldiste agentide stsenaariumides ning kõrgema täpsusega mudeleid nagu Q8_0 kvaliteedikriitiliste agendite rakendusteks.

**Agendi kasutusjuhtumi kooskõla**: Sobitage SLM võimed konkreetsete agendi nõuetega, arvestades õigsuse säilimist agentide otsustes, järelduskiirust reaalajas interaktsioonide jaoks, mälu piiratust ääre agentide juurutamisel ning võrguvälise töö nõudeid privaatsuspõhiste agentide puhul.

### Optimeerimisstrateegia valik SLM agentidele

**Kvantisatsioon lähenemine agentidele**: Valige sobivad kvantisatsiooni tasemed vastavalt agendi kvaliteedinõuetele ja riistvara piirangutele. Arvestage Q4_0 maksimaalse tihenduse puhul mobiiliagentidele, Q5_1 tasakaalustatud kvaliteedi ja tihenduse jaoks üldagentidel ning Q8_0 peaaegu originaalse kvaliteedi jaoks kriitilistes agendi rakendustes.


**Raamistiku valik agendi juurutamiseks**: vali optimeerimisraamistikke sihtseadme riistvara ja agendi nõuete põhjal. Kasuta Llama.cpp CPU-optimeeritud agendi juurutamiseks, Apple MLX Apple Silicon agendi rakenduste jaoks ja ONNX-i platvormideüleseks agendi ühilduvuseks.

## Praktiline SLM-agendi teisendus ja kasutusjuhud

### Reaalsed agendi juurutamise stsenaariumid

**Mobiilsed agendi rakendused**: Q4_K formaadid paistavad silma nutitelefoni agendi rakendustes madala mälusarvestusega, Q8_0 pakub tasakaalustatud jõudlust tahvelarvutitele mõeldud agendi süsteemides. Q5_K formaadid annavad mobiilsetele tootlikkuse agentidele parem kvaliteedi.

**Lauaarvuti ja ääreagendi arvutus**: Q5_K tagab optimaalse jõudluse lauaarvuti agendi rakendustes, Q8_0 pakub kõrgekvaliteedilist järeldamist tööjaamade jaoks, ja Q4_K võimaldab efektiivset töötlemist ääreagendi seadmetel.

**Uurimis- ja eksperimentaalsed agendid**: Täiustatud kvantimisformaadid võimaldavad uurida ülivaikseid täpsustega agendi järeldamist akadeemilises uurimuses ja tõestuskonseptsiooni rakendustes, kus on äärmiselt piiratud ressursid.

### SLM-agendi jõudlusmõõdikud

**Agendi järeldamise kiirus**: Q4_K saavutab mobiilsetel CPU-del kõige kiiremad agendi vastusajad, Q5_K pakub tasakaalustatud kiiruse ja kvaliteedi suhet üldistele agendi rakendustele, Q8_0 tagab tipptasemel kvaliteedi keerukate ülesannete puhul ning eksperimentaalsed formaadid annavad maksimaalse läbilaske spetsiaalse riistvara jaoks.

**Agendi mälunõuded**: Kvantimistasemed agentidele ulatuvad Q2_K-st (alla 500 MB väikeste mudelite jaoks) kuni Q8_0-ni (umbes 50% algsuurusest), eksperimentaalsed konfiguratsioonid saavutavad maksimaalse kokkusurumise ressursipiiratud agentide keskkondades.

## Väljakutsed ja kaalutlused SLM-agentide puhul

### Jõudluse kompromissid agendi süsteemides

SLM-agendi juurutamine nõuab hoolikat kaalumist mudeli suuruse, agendi vastuskiiruse ja väljundi kvaliteedi vahel. Q4_K pakub erakordset kiirust ja efektiivsust mobiilsetel agentidel, samas kui Q8_0 annab parema kvaliteedi keerukamate ülesannete jaoks. Q5_K asetub vahepeale, olles sobiv enamiku üldiste rakenduste jaoks.

### Riistvaraline ühilduvus SLM-agentide jaoks

Erinevatel ääre seadmetel on erisugused võimekused SLM-agendi juurutamiseks. Q4_K töötab efektiivselt baastasemel protsessoritel lihtsate agentide jaoks, Q5_K vajab mõõdukat arvutusvõimsust tasakaalustatud jõudluseks ja Q8_0 eelistab tippklassi riistvara täiustatud agendi funktsioonide jaoks.

### Turvalisus ja privaatsus SLM-agentide süsteemides

Kuigi SLM-agendid võimaldavad kohaliku töötlemise privaatsuse parandamiseks, tuleb äärmiselt rakendada turvameetmeid agendi mudelite ja andmete kaitsmiseks ääre keskkondades. Eriti oluline on see kõrge täpsusega agentide formaadide juurutamisel ettevõtete keskkondades või komprimeeritud formaatide kasutamisel tundlike andmete töötlemisel.

## Tulevikutrendid SLM-agentide arenduses

SLM-agendi maastik areneb pidevalt edasi kokkusurumistehnikate, optimeerimismeetodite ja äärejuurutusstrateegiate kaudu. Tulevikus on oodata efektiivsemaid kvantimisalgoritme mudelite jaoks, täiustatud kokkusurumismeetodeid agentide töövoogudele ja paremat integreerimist ääri riistvarakiirendajatega.

**Turuprognoosid SLM-agentide jaoks**: Hiljutise uurimuse kohaselt võib agent-põhine automatiseerimine kõrvaldada 40–60% korduvatest kognitiivsetest ülesannetest ettevõtete töövoogudes aastaks 2027, kusjuures SLM-id juhivad seda muutust tänu oma kuluefektiivsusele ja juurutamise paindlikkusele.

**Tehnoloogilised trendid SLM-agentide puhul**:
- **Spetsialiseeritud SLM-agendid**: valdkonnapõhised mudelid, mis on koolitatud kindlatele ülesannetele ja tööstusharudele
- **Ääreagendi arvutus**: täiustatud seadmel töötamise võimekused koos parema privaatsuse ja vähendatud latentsusega
- **Agentide korraldamine**: parem koordineerimine mitme SLM-agendi vahel dünaamilise marsruutimise ja koormuse tasakaalustamise abil
- **Demokraatlikkus**: SLM-i paindlikkus võimaldab laiemat osalust agendi arendamises eri organisatsioonides

## Alustamine SLM-agentidega

### Samm 1: Microsoft Agent Frameworki keskkonna ülesseadmine

**Sõltuvuste installimine**:
```bash
# Installi Microsoft Agent Framework
pip install microsoft-agent-framework

# Installi Foundry Local SDK äärmise juurutuse jaoks
pip install foundry-local-sdk

# Installi täiendavad sõltuvused äärmise agentide jaoks
pip install openai asyncio
```

**Foundry Local initsialiseerimine**:
```bash
# Käivita Foundry Local teenus
foundry service start

# Laadi vaikemudel agendi arendamiseks
foundry model run phi-4-mini
```

### Samm 2: Vali oma SLM agendi rakendustele
Populaarsed valikud Microsoft Agent Frameworkis:
- **Microsoft Phi-4 Mini (3.8B)**: suurepärane üldiste agendi ülesannete jaoks, tasakaalustatud jõudlusega
- **Qwen2.5-0.5B (0.5B)**: üliefektiivne lihtsate marsruudi ja klassifikatsiooni agentide jaoks
- **Qwen2.5-Coder-0.5B (0.5B)**: spetsialiseeritud koodiga seotud agentide ülesannetele
- **Phi-4 (7B)**: keerukamate ääre stsenaariumite jaoks, kui ressursid lubavad

### Samm 3: Loo oma esimene agent Microsoft Agent Frameworki abil

**Põhiline agendi seadistamine**:
```python
from microsoft_agent_framework import Agent, Config
from foundry_local import FoundryLocalManager

# Algata Foundry Local ühendus
foundry = FoundryLocalManager("phi-4-mini")

# Loo agendi konfiguratsioon
config = Config(
    name="my-first-agent",
    model_provider="foundry-local",
    model_alias="phi-4-mini",
    offline_mode=True
)

# Loo ja konfigureeri agent
agent = Agent(
    config=config,
    model_endpoint=foundry.endpoint,
    api_key=foundry.api_key
)

# Määra lihtne tööriist
@agent.tool
def get_current_time() -> str:
    """Get the current time."""
    from datetime import datetime
    return datetime.now().strftime("%Y-%m-%d %H:%M:%S")

# Testi agenti
response = agent.chat("What time is it?")
print(response)
```

### Samm 4: Määra agendi ulatus ja nõuded
Alusta fookustatud, hästi määratletud agentide rakendustega Microsoft Agent Frameworkis:
- **Ühe valdkonna agendid**: klienditeenindus VÕI ajakava koostamine VÕI uuring
- **Selged agendi eesmärgid**: spetsiifilised, mõõdetavad soorituse eesmärgid
- **Piiratud tööriistade integratsioon**: alguseks maksimaalselt 3-5 tööriista agendi juurutamiseks
- **Määratletud agendi piirid**: selged eskaleerimisteed keerulistele stsenaariumitele
- **Ääre-esmase disainiga**: pane esikohale võimekus töötada võrguühenduseta ja kohalik töötlemine

### Samm 5: Rakenda äärejuurutus Microsoft Agent Frameworkiga

**Ressursside konfiguratsioon**:
```python
from microsoft_agent_framework import ResourceConfig

# Konfigureeri serva juurutamiseks
resource_config = ResourceConfig(
    max_memory_usage="2GB",
    max_concurrent_agents=2,
    model_cache_size="1GB",
    auto_unload_idle_models=True,
    power_management=True
)

agent = Agent(
    config=config,
    resource_limits=resource_config
)
```

**Turvalahenduste rakendamine ääreagentidele**:
- **Kohalik sisendi valideerimine**: kontrolli päringuid ilma pilve sõltuvusteta
- **Võrguühenduseta väljundite filtreerimine**: tagada vastuste kvaliteet kohapeal
- **Ääre turvakontrollid**: rakenda turvalisus ilma internetiühenduseta
- **Kohalik jälgimine**: jälgi sooritust ja märgi probleemid ääretelemetrisüsteemi abil

### Samm 6: Mõõda ja optimeeri ääreagentide jõudlust
- **Agendi ülesannete lõpetamise määrad**: jälgi edukaid lõpetamisi võrguühenduseta stsenaariumites
- **Agendi vastusajad**: tagada sekundilisekennane aeg äärejuurutuses
- **Ressursside kasutamine**: jälgi mälu, CPU ja aku kasutust ääreseadmetes
- **Kuluefektiivsus**: võrdle äärejuurutuse kulusid pilvepõhiste alternatiividega
- **Võrguühenduseta töökindlus**: mõõda agendi jõudlust võrgu katkestuste ajal

## Olulised järeldused SLM-agendi rakendamisel

1. **SLMid on agendile piisavad**: enamiku agentide ülesannete puhul toimivad väikesed mudelid sama hästi kui suured, pakkudes märkimisväärseid eeliseid
2. **Kulutõhusus agentides**: SLM-agentide käitamine on 10–30 korda odavam, muutes need majanduslikult elujõuliseks laialdaseks juurutamiseks
3. **Spetsialiseerumine töötab agentide puhul**: peenhäälestatud SLM-id ületavad sageli üldotstarbelisi LLM-e spetsiifilistes rakendustes
4. **Hübriidne agentide arhitektuur**: kasuta SLM-e igapäevaste ülesannete jaoks ja LLM-e keeruka järeldamise puhul
5. **Microsoft Agent Framework toetab tootmisjuurutust**: pakub äriklassi tööriistu ääreagentide ehitamiseks, juurutamiseks ja haldamiseks
6. **Ääre-esmane disaini põhimõte**: võrguühenduseta töötavad agendid kohaliku töötlemisega tagavad privaatsuse ja töökindluse
7. **Foundry Local integratsioon**: sujuv ühendus Microsoft Agent Frameworki ja kohaliku mudelijäreldamise vahel
8. **Tulevik on SLM-agentidel**: väikesed keelemudelid koos tootmisraamistikega on agendi AI tulevik, võimaldades demokraatlikku ja tõhusat juurutust

## Viited ja lisalugemine

### Põhjalikud uurimused ja väljaanded

#### AI agendid ja agentse süsteemid
- **"Keel-agentid optimeeritavate graafikutena"** (2024) - Põhjalik uurimus agendi arhitektuuri ja optimeerimise kohta
  - Autorid: Wenyue Hua, Lishan Yang jt.
  - Link: https://arxiv.org/abs/2402.16823
  - Põhiteadmised: graafipõhine agendi disain ja optimeerimisstrateegiad

- **"Suurte keelemudelitepõhiste agentide tõus ja potentsiaal"** (2023)
  - Autorid: Zhiheng Xi, Wenxiang Chen jt.
  - Link: https://arxiv.org/abs/2309.07864
  - Põhiteadmised: põhjalik ülevaade LLM-põhistest agentide võimetest ja rakendustest

- **"Kognitiivsed arhitektuurid keel-agentide jaoks"** (2024)
  - Autorid: Theodore Sumers, Shunyu Yao jt.
  - Link: https://arxiv.org/abs/2309.02427
  - Põhiteadmised: kognitiivsed raamistikud intelligentsete agentide disainiks

#### Väikesed keelemudelid ja optimeerimine
- **"Phi-3 tehniline aruanne: võimekas keelemudel kohapeal sinu telefonis"** (2024)
  - Autorid: Microsoft Researchi meeskond
  - Link: https://arxiv.org/abs/2404.14219
  - Põhiteadmised: SLM-i disaini põhimõtted ja mobiilse juurutuse strateegiad

- **"Qwen2.5 tehniline aruanne"** (2024)
  - Autorid: Alibaba Cloudi meeskond
  - Link: https://arxiv.org/abs/2407.10671
  - Põhiteadmised: täiustatud SLM-i treeningtehnikad ja jõudluse optimeerimine

- **"TinyLlama: avatud lähtekoodiga väike keelemudel"** (2024)
  - Autorid: Peiyuan Zhang, Guangtao Zeng jt.
  - Link: https://arxiv.org/abs/2401.02385
  - Põhiteadmised: ülitihe mudeli disain ja koolitus tõhusus

### Ametlik dokumentatsioon ja raamistikud

#### Microsoft Agent Framework
- **Ametlik dokumentatsioon**: https://docs.microsoft.com/en-us/azure/ai-services/agents/
- **GitHubi hoidla**: https://github.com/microsoft/agent-framework

#### Foundry Local
- **Põhihaldus**: https://github.com/microsoft/foundry-local
- **Dokumentatsioon**: https://github.com/microsoft/foundry-local/blob/main/docs/README.md


#### VLLM
- **Põhihaldus**: https://github.com/vllm-project/vllm
- **Dokumentatsioon**: https://docs.vllm.ai/


#### Ollama
- **Ametlik veebisait**: https://ollama.ai/
- **GitHubi hoidla**: https://github.com/ollama/ollama

### Mudeli optimeerimise raamistikud

#### Llama.cpp
- **Hoidla**: https://github.com/ggml-org/llama.cpp


#### Microsoft Olive
- **Dokumentatsioon**: https://microsoft.github.io/Olive/
- **GitHubi hoidla**: https://github.com/microsoft/Olive

#### OpenVINO
- **Ametlik sait**: https://docs.openvino.ai/

#### Apple MLX
- **Hoidla**: https://github.com/ml-explore/mlx

### Tööstusaruanded ja turuanalüüs

#### AI agendi turu-uuringud
- **"AI agentide seisund 2025"** - McKinsey Global Institute
  - Link: https://www.mckinsey.com/capabilities/mckinsey-digital/our-insights/ai-agents-2025
  - Põhiteadmised: turusuundumused ja ettevõtete võtmismustrid

#### Tehnilised mõõdikud

- **"Edge AI järeldamise mõõdikud"** - MLPerf
  - Link: https://mlcommons.org/en/inference-edge/
  - Põhiteadmised: standardiseeritud jõudluse mõõdikud äärejuurutuseks

### Standardid ja spetsifikatsioonid

#### Mudeli formaadid ja standardid
- **ONNX (Open Neural Network Exchange)**: https://onnx.ai/
  - Platvormideülene mudeli formaat ühilduvuseks
- **GGUF spetsifikatsioon**: https://github.com/ggerganov/ggml/blob/master/docs/gguf.md
  - Kvantitud mudeli formaat CPU järeldamiseks
- **OpenAI API spetsifikatsioon**: https://platform.openai.com/docs/api-reference
  - Keelemudeli integreerimise standard API formaat

#### Turvalisus ja nõuetele vastavus
- **NIST AI riskijuhtimise raamistik**: https://www.nist.gov/itl/ai-risk-management-framework
- **ISO/IEC 23053:2022 – AI süsteemid**: raamistik AI süsteemide ja ohutuse jaoks
- **IEEE AI standardid**: https://standards.ieee.org/industry-connections/ai/

Liikumine SLM-põhiste agentide suunas tähistab fundamentaalset muutust AI juurutamise lähenemises. Microsoft Agent Framework koos kohalike platvormide ja tõhusate väikeste keelemudelitega pakub terviklikku lahendust tootmisvalmis agentide ehitamiseks, mis toimivad efektiivselt ääreseadmetes. Keskendudes efektiivsusele, spetsialiseerumisele ja praktilisele kasulikkusele, teeb see tehnoloogia AI agentide juurutamise kättesaadavamaks, odavamaks ja tõhusamaks reaalse maailma rakendustes igas tööstusharus ja ääre arvutuskeskkonnas.

Kuni aastani 2025 avavad üha võimekamate väiksemate mudelite, keerukate agentide raamistikude nagu Microsoft Agent Framework ja vastupidavate äärejuurutusplatvormide kombinatsioon uued võimalused autonoomsetele süsteemidele, mis töötavad efektiivselt ääre seadmetel säilitades privaatsuse, vähendades kulusid ja pakkudes erakordset kasutajakogemust.

**Järgmised sammud rakenduseks**:
1. **Uuri funktsioonikõne**: õpi, kuidas SLM-id töötlevad tööriistaintegratsiooni ja struktureeritud väljundeid
2. **Valda mudelikonteksti protokolli (MCP)**: mõista agendi suhtlusmustreid edasijõudnutele
3. **Ehita tootmisagentide**: kasuta Microsoft Agent Frameworki ettevõttekvaliteediga juurutuseks
4. **Optimeeri ääre jaoks**: rakenda arenenud optimeerimistehnikaid ressursipiiratud keskkondades


## ➡️ Järgmine samm

- [02: Funktsioonikõne väikestes keelemudelites (SLMid)](./02.FunctionCalling.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Lahtiütlus**:
See dokument on tõlgitud kasutades AI tõlketeenust [Co-op Translator](https://github.com/Azure/co-op-translator). Kuigi me püüdleme täpsuse poole, palun pange tähele, et automatiseeritud tõlgetes võib esineda vigu või ebatäpsusi. Originaaldokument selle emakeeles tuleks pidada autoriteetseks allikaks. Olulise teabe puhul soovitatakse kasutada professionaalset inimtõlget. Me ei vastuta selle tõlkega seotud eksimustest või valesti mõistmistest.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->