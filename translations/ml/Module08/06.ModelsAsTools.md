# ## അവലോകനം

Foundry Local ഉപയോഗിച്ച് AI മോഡലുകളെ മോഡുലാർ, ഇഷ്ടാനുസൃതമാക്കാവുന്ന ഉപകരണങ്ങളായി പരിഗണിക്കുക, അവ നേരിട്ട് ഉപകരണത്തിൽ പ്രവർത്തിക്കുന്നു. ഈ സെഷൻ സ്വകാര്യത സംരക്ഷിക്കുന്ന, കുറഞ്ഞ വൈകിയുള്ള ഇൻഫറൻസ് എന്നിവയ്ക്കുള്ള പ്രായോഗിക പ്രവൃത്തിപദ്ധതികളെയും SDKകൾ, APIകൾ, CLI എന്നിവ വഴി ഈ ഉപകരണങ്ങൾ എങ്ങനെ സംയോജിപ്പിക്കാമെന്നും ഊന്നിപ്പറയുന്നു. ആവശ്യമായപ്പോൾ Azure AI Foundry-ലേക്ക് എങ്ങനെ സ്കെയിൽ ചെയ്യാമെന്നും നിങ്ങൾക്ക് പഠിക്കാം.

> **🔄 ആധുനിക SDK-ക്കായി അപ്ഡേറ്റ് ചെയ്തു**: ഈ മോഡ്യൂൾ ഏറ്റവും പുതിയ Microsoft Foundry-Local റിപോസിറ്ററി പാറ്റേണുകളുമായി പൊരുത്തപ്പെടുന്നു, കൂടാതെ `samples/06/`-ൽ ഉള്ള ബുദ്ധിമുട്ടുള്ള റൂട്ടിംഗ് നടപ്പിലാക്കലിനൊപ്പം പൊരുത്തപ്പെടുന്നു. ഉദാഹരണങ്ങൾ ഇപ്പോൾ ആധുനിക `foundry-local-sdk`യും പുരോഗമന മോഡൽ തിരഞ്ഞെടുപ്പ് തന്ത്രങ്ങളും ഉപയോഗിക്കുന്നു.

**🏗️ ആർക്കിടെക്ചർ ഹൈലൈറ്റുകൾ:**
- **ബുദ്ധിമുട്ടുള്ള മോഡൽ റൂട്ടിംഗ്**: പൊതുവായ, തർക്കം, കോഡ്, സൃഷ്ടിപരമായ മോഡലുകൾക്കിടയിൽ കീവേഡ് അടിസ്ഥാനത്തിലുള്ള തിരഞ്ഞെടുപ്പ്
- **ആധുനിക SDK സംയോജനം**: സ്വയം സേവന കണ്ടെത്തലോടുകൂടിയ `FoundryLocalManager` ഉപയോഗിക്കുന്നു
- **പരിസ്ഥിതി കോൺഫിഗറേഷൻ**: പരിസ്ഥിതി വേരിയബിളുകൾ വഴി ഫ്ലെക്സിബിൾ മോഡൽ നിയോഗം
- **ആരോഗ്യ നിരീക്ഷണം**: സേവന പരിശോധനയും മോഡൽ ലഭ്യത പരിശോധിക്കുകയും ചെയ്യുന്നു
- **പ്രൊഡക്ഷൻ റെഡി**: സമഗ്രമായ പിശക് കൈകാര്യം ചെയ്യലും ഫാൾബാക്ക് മെക്കാനിസങ്ങളും

**📁 ലോക്കൽ നടപ്പാക്കൽ:**
- `samples/06/router.py` - കീവേഡ് അടിസ്ഥാനത്തിലുള്ള ബുദ്ധിമുട്ടുള്ള മോഡൽ റൂട്ടർ
- `samples/06/model_router.ipynb` - ഇന്ററാക്ടീവ് ഉദാഹരണങ്ങളും ബെഞ്ച്മാർക്കുകളും
- `samples/06/README.md` - കോൺഫിഗറേഷൻ, ഉപയോഗ നിർദ്ദേശങ്ങൾ

റഫറൻസുകൾ:
- Foundry Local ഡോക്സ്: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- ഇൻഫറൻസ് SDKകളുമായി സംയോജിപ്പിക്കുക: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-integrate-with-inference-sdks
- Hugging Face മോഡലുകൾ കമ്പൈൽ ചെയ്യുക: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-compile-hugging-face-models6: Foundry Local – മോഡലുകൾ ഉപകരണങ്ങളായി

## അവലോകനം

Foundry Local ഉപയോഗിച്ച് AI മോഡലുകളെ മോഡുലാർ, ഇഷ്ടാനുസൃതമാക്കാവുന്ന ഉപകരണങ്ങളായി പരിഗണിക്കുക, അവ നേരിട്ട് ഉപകരണത്തിൽ പ്രവർത്തിക്കുന്നു. ഈ സെഷൻ സ്വകാര്യത സംരക്ഷിക്കുന്ന, കുറഞ്ഞ വൈകിയുള്ള ഇൻഫറൻസ് എന്നിവയ്ക്കുള്ള പ്രായോഗിക പ്രവൃത്തിപദ്ധതികളെയും SDKകൾ, APIകൾ, CLI എന്നിവ വഴി ഈ ഉപകരണങ്ങൾ എങ്ങനെ സംയോജിപ്പിക്കാമെന്നും ഊന്നിപ്പറയുന്നു. ആവശ്യമായപ്പോൾ Azure AI Foundry-ലേക്ക് എങ്ങനെ സ്കെയിൽ ചെയ്യാമെന്നും നിങ്ങൾക്ക് പഠിക്കാം.

റഫറൻസുകൾ:
- Foundry Local ഡോക്സ്: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/
- ഇൻഫറൻസ് SDKകളുമായി സംയോജിപ്പിക്കുക: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-integrate-with-inference-sdks
- Hugging Face മോഡലുകൾ കമ്പൈൽ ചെയ്യുക: https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-compile-hugging-face-models

## പഠന ലക്ഷ്യങ്ങൾ
- ഉപകരണത്തിൽ മോഡൽ-ആസ്-ടൂൾ പാറ്റേണുകൾ രൂപകൽപ്പന ചെയ്യുക
- OpenAI-സമാനമായ REST API അല്ലെങ്കിൽ SDKകൾ വഴി സംയോജിപ്പിക്കുക
- ഡൊമെയ്ൻ-നിർദ്ദിഷ്ട ഉപയോഗകേസുകൾക്കായി മോഡലുകൾ ഇഷ്ടാനുസൃതമാക്കുക
- Azure AI Foundry-യിലേക്ക് ഹൈബ്രിഡ് സ്കെയിലിംഗിനായി പദ്ധതി തയ്യാറാക്കുക

## ഭാഗം 1: ബുദ്ധിമുട്ടുള്ള മോഡൽ റൂട്ടർ (ആധുനിക നടപ്പാക്കൽ)

ലക്ഷ്യം: ക്വറി ഉള്ളടക്കത്തിന്റെ അടിസ്ഥാനത്തിൽ സ്വയം റൂട്ടിംഗ് ഉപയോഗിച്ച് ബുദ്ധിമുട്ടുള്ള മോഡൽ തിരഞ്ഞെടുപ്പ് നടപ്പിലാക്കുക.

> **📋 കുറിപ്പ്**: ഈ നടപ്പാക്കൽ `samples/06/router.py`-ൽ ഉപയോഗിക്കുന്ന പാറ്റേണുകളുമായി പൊരുത്തപ്പെടുന്നു, പുരോഗമന കീവേഡ് അടിസ്ഥാനത്തിലുള്ള മോഡൽ തിരഞ്ഞെടുപ്പോടുകൂടി.

പടി 1) FoundryLocalManager ഉപയോഗിച്ച് ആധുനിക മോഡൽ റൂട്ടർ നിർവചിക്കുക  
```python
# റൂട്ടർ/ഇന്റലിജന്റ്_റൂട്ടർ.py
from foundry_local import FoundryLocalManager
from openai import OpenAI
from typing import Dict, Any, Optional
import os
import json

class ModelRouter:
    """Intelligent model router that selects appropriate models for different task types."""
    
    def __init__(self):
        self.client = None
        self.base_url = None
        self.tools = self._load_tool_registry()
        self._initialize_client()
    
    def _load_tool_registry(self) -> Dict[str, Dict[str, Any]]:
        """Load tool registry from environment or use defaults."""
        default_tools = {
            "general": {
                "model": os.environ.get("GENERAL_MODEL", "phi-4-mini"),
                "notes": "Fast general-purpose chat and Q&A",
                "temperature": 0.7
            },
            "reasoning": {
                "model": os.environ.get("REASONING_MODEL", "deepseek-r1-7b"),
                "notes": "Step-by-step analysis and logical reasoning",
                "temperature": 0.3
            },
            "code": {
                "model": os.environ.get("CODE_MODEL", "qwen2.5-7b"),
                "notes": "Code generation, debugging, and technical tasks",
                "temperature": 0.2
            },
            "creative": {
                "model": os.environ.get("CREATIVE_MODEL", "phi-4-mini"),
                "notes": "Creative writing and storytelling",
                "temperature": 0.9
            }
        }
        
        # പരിസ്ഥിതി ഓവർറൈഡ് പരിശോധിക്കുക
        tools_env = os.environ.get("TOOL_REGISTRY")
        if tools_env:
            try:
                return json.loads(tools_env)
            except json.JSONDecodeError:
                print("Warning: Invalid TOOL_REGISTRY JSON, using defaults")
        
        return default_tools
```
  
പടി 2) ആധുനിക SDKയും സേവന കണ്ടെത്തലും ഉപയോഗിച്ച് ക്ലയന്റ് ആരംഭിക്കുക  
```python
    def _initialize_client(self):
        """Initialize OpenAI client with Foundry Local or fallback configuration."""
        try:
            from foundry_local import FoundryLocalManager
            # ക്ലയന്റ് ഇൻഷിയലൈസേഷനായി ലഭ്യമായ ഏതെങ്കിലും മോഡൽ ഉപയോഗിക്കാൻ ശ്രമിക്കുക
            first_model = next(iter(self.tools.values()))["model"]
            manager = FoundryLocalManager(first_model)
            
            self.client = OpenAI(
                base_url=manager.endpoint,
                api_key=manager.api_key
            )
            self.base_url = manager.endpoint
            print(f"✅ Foundry Local SDK initialized")
        except Exception as e:
            print(f"Warning: Could not use Foundry SDK ({e}), falling back to manual configuration")
            # മാനുവൽ കോൺഫിഗറേഷനിലേക്ക് തിരിച്ചുപോകുക
            self.base_url = os.environ.get("BASE_URL", "http://localhost:8000")
            api_key = os.environ.get("API_KEY", "")
            
            self.client = OpenAI(
                base_url=f"{self.base_url}/v1",
                api_key=api_key
            )
            print(f"Initialized manual configuration at {self.base_url}")
    
    def select_tool(self, user_query: str) -> str:
        """Select the most appropriate tool based on the user query."""
        query_lower = user_query.lower()
        
        # കോഡ്-സംബന്ധമായ കീവർഡുകൾ
        code_keywords = ["code", "python", "function", "class", "method", "bug", "debug", 
                        "programming", "script", "algorithm", "implementation", "refactor"]
        if any(keyword in query_lower for keyword in code_keywords):
            return "code"
        
        # കാരണവിവരണ കീവർഡുകൾ
        reasoning_keywords = ["why", "how", "explain", "step-by-step", "reason", "analyze", 
                             "think", "logic", "because", "cause", "compare", "evaluate"]
        if any(keyword in query_lower for keyword in reasoning_keywords):
            return "reasoning"
        
        # സൃഷ്ടിപരമായ കീവർഡുകൾ
        creative_keywords = ["story", "poem", "creative", "imagine", "write", "tale", 
                           "narrative", "fiction", "character", "plot"]
        if any(keyword in query_lower for keyword in creative_keywords):
            return "creative"
        
        # സാധാരണയായി ഡിഫോൾട്ട് ചെയ്യുക
        return "general"
    
    def chat(self, model: str, content: str, max_tokens: int = 300, temperature: Optional[float] = None) -> str:
        """Send chat completion request to the specified model."""
        try:
            params = {
                "model": model,
                "messages": [{"role": "user", "content": content}],
                "max_tokens": max_tokens
            }
            
            if temperature is not None:
                params["temperature"] = temperature
            
            response = self.client.chat.completions.create(**params)
            return response.choices[0].message.content
        except Exception as e:
            return f"Error generating response with model {model}: {str(e)}"
```
  
പടി 3) ബുദ്ധിമുട്ടുള്ള റൂട്ടിംഗ്, നിർവഹണം നടപ്പിലാക്കുക (`samples/06/router.py` കാണുക)  
```python
    def route_and_run(self, prompt: str) -> Dict[str, Any]:
        """Route the prompt to the appropriate model and generate response."""
        tool_key = self.select_tool(prompt)
        tool_config = self.tools[tool_key]
        model = tool_config["model"]
        temperature = tool_config.get("temperature", 0.7)
        
        print(f"🎯 Selected tool: {tool_key} (model: {model})")
        
        answer = self.chat(
            model=model, 
            content=prompt, 
            max_tokens=400, 
            temperature=temperature
        )
        
        return {
            "tool": tool_key,
            "model": model,
            "tool_description": tool_config["notes"],
            "temperature": temperature,
            "answer": answer
        }
    
    def check_service_health(self) -> Dict[str, Any]:
        """Check Foundry Local service health and available models."""
        try:
            models_response = self.client.models.list()
            available_models = [model.id for model in models_response.data]
            
            return {
                "status": "healthy",
                "base_url": self.base_url,
                "available_models": available_models,
                "tools_configured": list(self.tools.keys())
            }
        except Exception as e:
            return {
                "status": "error",
                "base_url": self.base_url,
                "error": str(e)
            }

if __name__ == "__main__":
    # ഉറപ്പാക്കുക: ഫൗണ്ട്രി മോഡൽ റൺ phi-4-mini
    router = ModelRouter()
    
    # ആരോഗ്യ പരിശോധന
    health = router.check_service_health()
    print(f"Service Health: {json.dumps(health, indent=2)}")
    
    # വ്യത്യസ്ത ക്വറി തരം പരീക്ഷിക്കുക
    queries = [
        "Write a Python function to calculate fibonacci numbers",  # -> കോഡ്
        "Explain step-by-step why the sky is blue",  # -> കാരണവിവരണം
        "Tell me a creative story about AI",  # -> സൃഷ്ടിപരമായ
        "What's the weather like today?"  # -> പൊതുവായ
    ]
    
    for query in queries:
        result = router.route_and_run(query)
        print(f"\nQuery: {query}")
        print(f"Selected: {result['tool']} -> {result['model']}")
        print(f"Answer: {result['answer'][:100]}...")
```
  
## ഭാഗം 2: ആധുനിക SDK സംയോജനം (പടി-പടി)

ലക്ഷ്യം: Foundry Local SDK OpenAI Python SDK-യുമായി ഉപയോഗിച്ച് സുതാര്യമായ സംയോജനം നടത്തുക.

പടി 1) ആശ്രിതത്വങ്ങൾ ഇൻസ്റ്റാൾ ചെയ്യുക  
```cmd
cd Module08
.\.venv\Scripts\activate
pip install foundry-local-sdk openai
```
  
പടി 2) പരിസ്ഥിതി കോൺഫിഗർ ചെയ്യുക (ഐച്ഛികം - `samples/06/README.md` കാണുക)  
```cmd
REM Override default models per tool
set GENERAL_MODEL=phi-4-mini
set REASONING_MODEL=deepseek-r1-7b
set CODE_MODEL=qwen2.5-7b
REM Or provide a full JSON registry
set TOOL_REGISTRY={"general":{"model":"phi-4-mini"},"reasoning":{"model":"deepseek-r1-7b"}}
```
  
പടി 3) ആധുനിക SDK സംയോജനം  
```python
# modern_sdk_demo.py
from foundry_local import FoundryLocalManager
from openai import OpenAI
import sys

def main():
    """Demonstrate modern SDK integration."""
    try:
        # FoundryLocalManager ഉപയോഗിച്ച് ആരംഭിക്കുക
        alias = "phi-4-mini"
        manager = FoundryLocalManager(alias)
        
        # Foundry Local എന്റ്പോയിന്റ് ഉപയോഗിച്ച് OpenAI ക്ലയന്റ് സൃഷ്ടിക്കുക
        client = OpenAI(
            base_url=manager.endpoint,
            api_key=manager.api_key
        )
        
        # മോഡൽ വിവരങ്ങൾ നേടുക
        model_info = manager.get_model_info(alias)
        print(f"Using model: {model_info.id}")
        
        # സ്ട്രീമിങ്ങ് ഉപയോഗിച്ച് അഭ്യർത്ഥന നടത്തുക
        stream = client.chat.completions.create(
            model=model_info.id,
            messages=[{"role": "user", "content": "Explain edge AI benefits in one paragraph."}],
            stream=True,
            max_tokens=200
        )
        
        print("Response: ", end="")
        for chunk in stream:
            if chunk.choices[0].delta.content:
                print(chunk.choices[0].delta.content, end="", flush=True)
        print()
        
    except Exception as e:
        print(f"Error: {e}")
        print("Ensure Foundry Local is running with: foundry model run phi-4-mini")
        sys.exit(1)

if __name__ == "__main__":
    main()
```
  
## ഭാഗം 3: ഡൊമെയ്ൻ ഇഷ്ടാനുസരണം (പടി-പടി)

ലക്ഷ്യം: പ്രോംപ്റ്റ് ടെംപ്ലേറ്റുകളും JSON സ്കീമയും ഉപയോഗിച്ച് ഡൊമെയ്ൻ-നിർദ്ദിഷ്ട ഔട്ട്പുട്ടുകൾ രൂപകൽപ്പന ചെയ്യുക.

പടി 1) ഡൊമെയ്ൻ പ്രോംപ്റ്റ് ടെംപ്ലേറ്റ് സൃഷ്ടിക്കുക  
```python
# ഡൊമെയ്ൻ/ടെംപ്ലേറ്റുകൾ.py
BUSINESS_ANALYST_SYSTEM = """
You are a senior business analyst. Provide:
1) Key insights
2) Risks
3) Next steps
Respond in valid JSON with fields: insights, risks, next_steps.
"""
```
  
പടി 2) JSON ഔട്ട്പുട്ട് നിർബന്ധിതമാക്കുക  
```python
# ഡൊമെയ്ൻ/അനലിസ്റ്റ്.py
import requests, os, json

BASE_URL = os.getenv("OPENAI_BASE_URL", "http://localhost:8000/v1")
API_KEY = os.getenv("OPENAI_API_KEY", "local-key")
HEADERS = {"Content-Type":"application/json","Authorization":f"Bearer {API_KEY}"}

from domain.templates import BUSINESS_ANALYST_SYSTEM

def analyze(text: str) -> dict:
    messages = [
        {"role":"system","content": BUSINESS_ANALYST_SYSTEM},
        {"role":"user","content": f"Analyze this business text:\n{text}"}
    ]
    r = requests.post(f"{BASE_URL}/chat/completions", json={
    "model":"phi-4-mini",
        "messages": messages,
        "response_format": {"type":"json_object"},
        "temperature": 0.3
    }, headers=HEADERS, timeout=60)
    r.raise_for_status()
    # JSON ഉള്ളടക്കം പാഴ്‌സ് ചെയ്യുക
    content = r.json()["choices"][0]["message"]["content"]
    return json.loads(content)

if __name__ == "__main__":
    print(analyze("Sales dipped 12% in Q3 due to supply constraints and marketing cuts."))
```
  
## ഭാഗം 4: ഓഫ്‌ലൈൻ, സുരക്ഷാ നില (പടി-പടി)

ലക്ഷ്യം: മോഡലുകൾ ഉപകരണങ്ങളിൽ ടൂളുകളായി പ്രവർത്തിക്കുമ്പോൾ സ്വകാര്യതയും പ്രതിരോധ ശേഷിയും ഉറപ്പാക്കുക.

പടി 1) പ്രീ-വോം, ലോക്കൽ എൻഡ്‌പോയിന്റ് പരിശോധന  
```cmd
foundry model run phi-4-mini
curl http://localhost:8000/v1/models
```
  
പടി 2) ഇൻപുട്ടുകൾ ശുദ്ധീകരിക്കുക  
```python
# സുരക്ഷ/സാനിറ്റൈസ്.py
import re
EMAIL_RE = re.compile(r"[\w\.-]+@[\w\.-]+")
PHONE_RE = re.compile(r"\+?\d[\d\s\-]{7,}\d")

def sanitize(text: str) -> str:
    text = EMAIL_RE.sub("[REDACTED_EMAIL]", text)
    text = PHONE_RE.sub("[REDACTED_PHONE]", text)
    return text
```
  
പടി 3) ലോക്കൽ-ഓൺലി ഫ്ലാഗും ലോഗിംഗും  
```python
# സുരക്ഷ/പ്രാദേശിക_മാത്രം.py
import os, json, time
LOG = os.getenv("MODELS_AS_TOOLS_LOG", "./tools_logs.jsonl")

def record(event: dict):
    with open(LOG, "a", encoding="utf-8") as f:
        f.write(json.dumps(event) + "\n")

# ഓരോ കോൾ ചെയ്യുന്നതിന് മുമ്പ് ഉപയോഗിക്കുക
def before_call(tool_name, payload):
    record({"ts": time.time(), "tool": tool_name, "event": "before_call"})

# ഓരോ കോൾ കഴിഞ്ഞ്
def after_call(tool_name, result):
    record({"ts": time.time(), "tool": tool_name, "event": "after_call"})
```
  
## ഭാഗം 5: പ്രൊഡക്ഷൻ വിന്യാസവും സ്കെയിലിംഗും

ലക്ഷ്യം: നിരീക്ഷണവും Azure AI Foundry സംയോജനവും ഉള്ള ബുദ്ധിമുട്ടുള്ള റൂട്ടർ വിന്യസിക്കുക.

> **📋 കുറിപ്പ്**: `samples/06/model_router.ipynb`-ൽ ഉള്ള ലോക്കൽ നടപ്പാക്കൽ പ്രൊഡക്ഷൻ വിന്യാസ പാറ്റേണുകളുടെ സമഗ്ര ഉദാഹരണങ്ങൾ ഉൾക്കൊള്ളുന്നു.

പടി 1) നിരീക്ഷണത്തോടെ പ്രൊഡക്ഷൻ റൂട്ടർ (`samples/06/router.py` കാണുക)  
```python
# പ്രൊഡക്ഷൻ/router.py
from router.intelligent_router import ModelRouter
import json
import time
import sys

class ProductionModelRouter(ModelRouter):
    """Production-ready model router with monitoring and logging."""
    
    def __init__(self):
        super().__init__()
        self.request_count = 0
        self.error_count = 0
        self.start_time = time.time()
    
    def route_and_run_with_monitoring(self, prompt: str) -> Dict[str, Any]:
        """Route with comprehensive monitoring and error handling."""
        start_time = time.time()
        self.request_count += 1
        
        try:
            result = self.route_and_run(prompt)
            processing_time = time.time() - start_time
            
            # വിജയകരമായ അഭ്യർത്ഥന ലോഗ് ചെയ്യുക
            self._log_request({
                "status": "success",
                "tool": result["tool"],
                "model": result["model"],
                "processing_time": processing_time,
                "timestamp": time.strftime("%Y-%m-%d %H:%M:%S")
            })
            
            result["processing_time"] = processing_time
            return result
            
        except Exception as e:
            self.error_count += 1
            error_result = {
                "status": "error",
                "error": str(e),
                "processing_time": time.time() - start_time,
                "timestamp": time.strftime("%Y-%m-%d %H:%M:%S")
            }
            
            self._log_request(error_result)
            return error_result
    
    def _log_request(self, data: Dict[str, Any]):
        """Log request data for monitoring."""
        print(f"📊 {json.dumps(data)}")
    
    def get_stats(self) -> Dict[str, Any]:
        """Get router statistics."""
        uptime = time.time() - self.start_time
        return {
            "uptime_seconds": uptime,
            "total_requests": self.request_count,
            "error_count": self.error_count,
            "success_rate": (self.request_count - self.error_count) / max(1, self.request_count),
            "requests_per_minute": self.request_count / max(1, uptime / 60)
        }

def main():
    """Production router demo."""
    router = ProductionModelRouter()
    
    # ആരോഗ്യ പരിശോധന
    health = router.check_service_health()
    if health["status"] == "error":
        print(f"❌ Service health check failed: {health['error']}")
        sys.exit(1)
    
    print(f"✅ Service healthy with {len(health['available_models'])} models")
    
    # ഉപയോക്തൃ ചോദ്യം പ്രോസസ്സ് ചെയ്യുക
    user_prompt = " ".join(sys.argv[1:]) or "Write three benefits of on-device AI in JSON format."
    print(f"\n🎯 Processing: {user_prompt}")
    
    result = router.route_and_run_with_monitoring(user_prompt)
    
    if result.get("status") == "error":
        print(f"❌ Error: {result['error']}")
    else:
        print(f"\n📋 Result:")
        print(f"Tool: {result['tool']} -> Model: {result['model']}")
        print(f"Processing Time: {result['processing_time']:.2f}s")
        print(f"Answer: {result['answer']}")
    
    # സ്ഥിതിവിവരക്കണക്കുകൾ കാണിക്കുക
    stats = router.get_stats()
    print(f"\n📊 Statistics: {json.dumps(stats, indent=2)}")

if __name__ == "__main__":
    main()
```
  
## ഹാൻഡ്‌സ്-ഓൺ ചെക്ക്ലിസ്റ്റ്
- [ ] കീവേഡ് അടിസ്ഥാനത്തിലുള്ള ബുദ്ധിമുട്ടുള്ള മോഡൽ റൂട്ടർ നടപ്പിലാക്കുക (`samples/06/router.py`)
- [ ] വിവിധ പ്രത്യേക മോഡലുകൾ കോൺഫിഗർ ചെയ്യുക (പൊതുവായ, തർക്കം, കോഡ്, സൃഷ്ടിപരമായ)
- [ ] ഇന്ററാക്ടീവ് Jupyter നോട്ട്‌ബുക്ക് പരീക്ഷിക്കുക (`samples/06/model_router.ipynb`)
- [ ] പരിസ്ഥിതി അടിസ്ഥാനമാക്കിയുള്ള മോഡൽ കോൺഫിഗറേഷൻ സജ്ജമാക്കുക
- [ ] സേവന ആരോഗ്യ നിരീക്ഷണവും പിശക് കൈകാര്യം ചെയ്യലും നടപ്പിലാക്കുക
- [ ] സമഗ്രമായ ലോഗിംഗോടുകൂടിയ പ്രൊഡക്ഷൻ റൂട്ടർ വിന്യസിക്കുക

## ലോക്കൽ സാമ്പിൾ സംയോജനം

പൂർണ്ണ നടപ്പാക്കൽ പ്രവർത്തിപ്പിക്കുക:  
```cmd
cd Module08
.\.venv\Scripts\activate

REM Start required models
foundry model run phi-4-mini
foundry model run qwen2.5-7b
foundry model run deepseek-r1-7b

REM Test the intelligent router
python samples\06\router.py "Write a Python function to sort a list"
python samples\06\router.py "Explain step-by-step how bubble sort works"
python samples\06\router.py "Tell me a creative story about robots"

REM Explore the interactive notebook
jupyter notebook samples/06/model_router.ipynb
```
  
## റഫറൻസുകളും അടുത്ത ഘട്ടങ്ങളും
- **ലോക്കൽ നടപ്പാക്കൽ**: `samples/06/` - ബുദ്ധിമുട്ടുള്ള റൂട്ടർ, നിരവധി മോഡൽ പിന്തുണയോടെ  
- **Microsoft സാമ്പിൾസ്**: [Hello Foundry Local](https://github.com/microsoft/Foundry-Local/tree/main/samples/python/hello-foundry-local)  
- **സംയോജനം ഡോക്സ്**: [Integrate with Inference SDKs](https://learn.microsoft.com/en-us/azure/ai-foundry/foundry-local/how-to/how-to-integrate-with-inference-sdks)  
- **പുരോഗമന പാറ്റേണുകൾ**: മോഡ്യൂൾ 5-ൽ ഫംഗ്ഷൻ കോളിംഗ്, മൾട്ടി-ഏജന്റ് ഓർക്കസ്ട്രേഷൻ എന്നിവ പരിശോധിക്കുക

## സമാപനം

Foundry Local മോഡലുകൾ ബുദ്ധിമുട്ടുള്ള, പ്രത്യേകതയുള്ള ഉപകരണങ്ങളായി മാറുന്ന ശക്തമായ ഉപകരണത്തിൽ AI സജ്ജമാക്കുന്നു. സ്വയം മോഡൽ തിരഞ്ഞെടുപ്പ്, സമഗ്ര നിരീക്ഷണം, പ്രൊഡക്ഷൻ റെഡി പാറ്റേണുകൾ എന്നിവയോടെ, ടീമുകൾ വ്യത്യസ്ത ടാസ്‌ക് തരം അനുസരിച്ച് അനുയോജ്യമായ, സ്വകാര്യതയും പ്രകടനവും നിലനിർത്തുന്ന സങ്കീർണ്ണ AI ആപ്ലിക്കേഷനുകൾ പുറത്തിറക്കാൻ കഴിയും. ഇവിടെ കാണിച്ച ബുദ്ധിമുട്ടുള്ള റൂട്ടർ പാറ്റേൺ ലോക്കൽ ഡെവലപ്പ്മെന്റിൽ നിന്ന് പ്രൊഡക്ഷൻ വിന്യാസത്തിലേക്ക് സ്കെയിൽ ചെയ്യാവുന്ന സങ്കീർണ്ണ AI സിസ്റ്റങ്ങൾ നിർമ്മിക്കാൻ ഒരു അടിസ്ഥാനം നൽകുന്നു.

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**അസൂയാപത്രം**:  
ഈ രേഖ AI വിവർത്തന സേവനം [Co-op Translator](https://github.com/Azure/co-op-translator) ഉപയോഗിച്ച് വിവർത്തനം ചെയ്തതാണ്. നാം കൃത്യതയ്ക്ക് ശ്രമിച്ചിട്ടുണ്ടെങ്കിലും, സ്വയം പ്രവർത്തിക്കുന്ന വിവർത്തനങ്ങളിൽ പിശകുകൾ അല്ലെങ്കിൽ തെറ്റുകൾ ഉണ്ടാകാമെന്ന് ദയവായി ശ്രദ്ധിക്കുക. അതിന്റെ മാതൃഭാഷയിലുള്ള യഥാർത്ഥ രേഖയാണ് പ്രാമാണികമായ ഉറവിടം എന്ന് പരിഗണിക്കേണ്ടതാണ്. നിർണായകമായ വിവരങ്ങൾക്ക്, പ്രൊഫഷണൽ മനുഷ്യ വിവർത്തനം ശുപാർശ ചെയ്യപ്പെടുന്നു. ഈ വിവർത്തനം ഉപയോഗിക്കുന്നതിൽ നിന്നുണ്ടാകുന്ന ഏതെങ്കിലും തെറ്റിദ്ധാരണകൾക്കോ തെറ്റായ വ്യാഖ്യാനങ്ങൾക്കോ ഞങ്ങൾ ഉത്തരവാദികളല്ല.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->